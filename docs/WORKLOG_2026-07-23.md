# 작업 일지 2026-07-23

## 오늘 완료한 작업 (커밋 순)

### 운영·인프라
- **프리미엄 PDF 자동 이행** (`a8a8435`): 리포트가 READY로 전환되면 주문 fulfillment를 COMPLETED로 자동 완료. 테스트·샘플 데이터는 오픈 시점 일괄 삭제 방침(백필 없음).
- **관리자 대시보드 개편** (`bae4f10`): 미처리 주문 큐·통계 키 분리.
- **스크립트 규칙 실호출 스윕** (`b92766e`): 비라틴 이름 스크립트 규칙 전건 검증 ALL PASS.
- **운영자 테스트 버튼** (`2dafe8b`): 글로벌 프리미엄 PDF를 결제 없이 관리자 인증으로 테스트 생성.
- **모바일 랜딩 넘침 수정** (`74684ed`): 긴 제목 로케일(en·de·es 등)에서 그리드 트랙 팽창(min-width:auto) 원인 — min-w-0 + 주소 줄바꿈 조건화. 전 로케일 헤드리스 측정으로 확인.

### 결제 준비 (다크 런치)
- **포트원 멀티채널** (`bd32b00`): 카카오페이(국내)+페이팔(해외) 채널 키 분리(`NEXT_PUBLIC_PORTONE_CHANNEL_KEY_KAKAOPAY`/`_PAYPAL`). 키 등록 전에는 모든 결제 버튼 "준비 중". 토스는 추후 직접 연동.

### 글로벌 프리미엄 상품
- **US$9.99 한글 이름 종합 리포트** (`b3f3476`→`71bad8e` 개편): 표지 + 후보(1~5)별 [선택 서체 아트 + 의미·추천 이유 2단 해설] + 종합·오행 사주. 가로(landscape).
- **US$2.99 발음 전환 아트** (`1f01d5c`→개편): 표기 후보(1~3)별 선택 서체 아트 + 발음 안내.
- **US$1.99 이름 아트 팩(신규)** (`71bad8e`): 후보 1개 × 서체 3개 아트. 재구매(서체 수집형) 수익 가설.
- 스크립트 커버리지: MixedText(문자 단위 스크립트 분리)로 23로케일 전부 지원, 기본 폰트 NotoSans.

### 서체 선택형 시스템 (오늘의 핵심)
- **서버 서체 레지스트리** (`5c2e684`): `report_fonts` 테이블 + 비공개 Storage — 코드·한/영 이름·저작권자·라이선스 유형·출처 URL·스토리(한국어 원문 + 22로케일 AI 번역)·sha256·인기 집계(pick_count).
- **관리자 조정형 상품 설정** (`78f7b9a`): `product_settings` — 가격·통화·적용 서체 수(font_count)·활성 여부를 관리자 화면(`/naming-artist/products`)에서 조정, 변경 이력 기록. 모든 주문 라우트가 이 설정만 읽음.
- **선택 UI** (`7549acc`·`9496131`): 모바일 우선 — 인기순 상위 4개 큰 칩 + "서체 더보기" 바텀시트(전체 선택), 서체 스토리 인라인+ⓘ팝업(큰 미리보기·전문·출처).
- **서체 18종 등록 완료** (`d1f412d`): 기존 6종에 더해 12종 신규 — 신라문화(B)·정선아리랑 3종·제주한라산·영월·솔뫼김대건·행복고흥(B)·헬스셋조릿대·도봉옛길·덕온공주·조선100년. 전부 공식 배포처 원본 + 라이선스 검증(`assets/fonts/LICENSES.md` 원장) + 22로케일 스토리 + 미리보기. 판매 PDF 하단에 서체명·저작권자·출처 자동 표기.
  - **확보 불가로 제외**: 문체부 훈민정음체(안심글꼴 369종 목록에 없음 → 임베딩 근거 미확인), 윤디자인 독립체·만세체(폰코자키 앱 전용 배포 전환), 대한민국독도·월인석보·밀양영남루(임베딩 불허). 보류: 청도읍성(링크 필요)·하늘내린인제(군청 확인 필요).
- **23로케일 상품 설명 갱신** (`7dabfc2`): 장수·서체 수 하드코딩 제거(관리자 조정과 충돌 방지), 실제 구성 반영. 46개 문자열.
- **업로드 시 미리보기 자동 생성** (`5d46f14`·`b492776`): `src/lib/font-preview.ts` — fontkit 글리프 아웃라인 → SVG(600×190, 기존 PNG와 동일 스타일). 로컬 스크립트 수동 실행 불필요. ※ fontkit ESM은 named export만 — default import는 터보팩 프로덕션 빌드 실패(`b492776`로 수정).

### 굿즈 (도장)
- **국내 도장 주문** (`4470336`·`ee9af4d`): `/stamp-order` ₩39,000, 모델 3종 동일가(원형 목도장/사각 목도장/흑단), 카카오페이(리디렉션 복구 포함), fulfillment 수동 전환.
- **글로벌 도장 주문** (`a58621d`): 로케일 분기 — ko는 국내, 그 외는 영문 UI·배송 국가 필수·페이팔 SPB·US$34.99(국제배송비 포함). 결과 페이지 굿즈 카드 23로케일 전부 활성 링크 + 도장 중심 문구로 재작성(92개 문자열).

### 배포
- 실패 3회(미커밋 모듈 2회 + fontkit default import 1회) 모두 당일 원인 규명·수정. 최종 프로덕션 **Ready** 확인.

## 진행 중 / 대기
- **포트원 채널 키 미등록** (사용자 액션): 카카오페이·페이팔 키 등록 시 전 결제 다크 런치 해제. 등록 후 실결제 검증 필요(전 상품: 일괄공개 990원/$1.99 · $9.99 · $2.99 · $1.99 · 도장 ₩39,000/$34.99).
- 아랍어(RTL) 혼합 어순 PDF 실렌더 미검증.

## 다음에 해야 할 작업
1. **약관·요금 문서에 신상품 반영**: $9.99/$2.99/$1.99 글로벌 상품 + 도장(국내·글로벌) — 요금안내·이용약관 4종 × 23로케일. 관리자 가격 변경 시에도 문서 갱신 경고 존재.
2. **실결제 검증**(채널 키 등록 후): 카카오페이·페이팔 전 플로우 + 웹훅 + 환불 경로.
3. 발음 표기 후보 다건(2~3개) 케이스 실검증(권장).
4. 서체 추가 검토: 청도읍성체(사용자 링크 제공 시)·하늘내린인제체(인제군청 확인 시).
5. 전체 CSP 적용(포트원 실결제 테스트 후).
6. 광고 잠금 서버화는 미저장 원칙과 충돌로 보류 유지.

## 참고
- 새 서체 등록: 관리자 화면 업로드(자동 번역·자동 미리보기) 또는 `scripts/seed-report-fonts.mjs`(일괄) + `scripts/generate-font-previews.tsx`(일괄 미리보기).
- 폰트 법적 근거: `assets/fonts/LICENSES.md` (OFL/KOGL-1/KOGL-3/FREE-EMBED 구분, 제외 목록 포함).
