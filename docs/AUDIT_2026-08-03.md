# 전수 감사 2026-08-03

> 이 문서는 **무엇을 어떻게 확인했는가**의 기록이다. "이상 없음"만 적으면 다음 사람이
> 그것이 안전해서인지 안 봐서인지 알 수 없다. 그래서 통과한 것도 어떤 방법으로 통과했는지
> 함께 적는다.

## 0. 한 줄 요약

**실제로 뚫려 있던 곳은 없었다.** 다만 이 저장소가 다른 모든 자리에서 지키는 규칙에서
**혼자만 빠져 있던** 자리가 셋 나왔고, 그중 둘은 권한, 하나는 검사기였다. 셋 다 고쳤다.

| # | 항목 | 등급 | 상태 |
| --- | --- | --- | --- |
| 1 | `admin_pii_access_logs` 권한 미회수 | 중 | **고침** |
| 2 | `increment_font_picks` anon 실행 권한 | 중 | **고침** |
| 3 | `sharp <0.35.0` CVE 4건 | 중 | 미조치(별도 판단 필요 → §5) |
| 4 | 한자 DB 검사기가 통과 불가 | 중 | **고침** |
| ~~5~~ | ~~inyeonlink 본문 크기 제한 부재~~ | — | **오탐. 철회** → §3-0 |
| 6 | inyeonlink 약관 모듈이 클라이언트 번들에 | 하 | **고침** |
| 7 | `X-Powered-By` 노출 | 하 | **고침** |
| 8 | `official_*` 닿지 않는 공개 정책 | 하 | **고침** |
| 9 | 테스트 데이터 잔존 | 하 | 오픈 시 정리(기존 목록) |

## 1. 감사 범위

| 대상 | 규모 |
| --- | --- |
| 검사 스크립트 | 32개(naminglink 19 · inyeonlink 8 · core 5) |
| API 라우트 | 46개(naminglink 36 · inyeonlink 10) |
| DB | 테이블 30 · SECURITY DEFINER 함수 7 · 정책 전수 |
| 빌드 산출물 | 브라우저 자산 79개(naminglink 49 · inyeonlink 30) |
| 라이브 | 보안 헤더 · robots · ads.txt · sitemap · 엔드포인트 응답 |
| 의존성 | `pnpm audit --prod` |

---

## 2. 고친 것

### 2-1. `admin_pii_access_logs` — 감사받는 쪽이 지울 수 있는 로그였다

```
admin_pii_access_logs   anon           DELETE,INSERT,TRUNCATE,SELECT,UPDATE,…
admin_pii_access_logs   authenticated  DELETE,INSERT,TRUNCATE,SELECT,UPDATE,…
```

이 표는 관리자가 배송지(개인정보)를 언제 열어 봤는지 남기는 **감사 기록**이다. 만든
마이그레이션(`20260727150000`)이 RLS만 켜고 `revoke`를 빠뜨렸다.

**지금 뚫려 있던 것은 아니다** — RLS가 켜져 있고 정책이 0개라 전부 거부된다. 문제는 정책이
하나라도 생기는 순간 열린다는 것이고, 하필 감사 로그다. 그리고 그 파일의 주석은 이미
"서비스 롤로만 읽고 쓴다"고 적어 두었는데 권한이 그것을 따르지 않았다.

`rate_limit_counters`·`unlock_tickets` 등 이 저장소의 다른 표는 전부 revoke가 붙어 있다.
그 규칙에 맞췄다.

### 2-2. `increment_font_picks` — 인증 없이 부를 수 있는 쓰기 함수였다

```sql
SECURITY DEFINER · anon=true · authenticated=true
update report_fonts set pick_count = pick_count + 1 where code = any(font_codes)
```

**anon 키는 클라이언트 번들에 들어 있는 공개값이다.** 누구나 이 RPC를 직접 불러 서체
인기순위를 무제한 올릴 수 있었고, DB 쓰기 부하도 만들 수 있었다.

부르는 곳은 `api/premium-reports/global-order`(service_role) **한 곳뿐**이라 이 권한 자체가
필요 없었다. Postgres가 함수에 PUBLIC EXECUTE를 기본으로 주는 것을 revoke하지 않은 것이다.
같은 저장소의 다른 SECURITY DEFINER 함수 6개는 전부 service_role 전용이었다 — 이것만
예외였다.

두 항목 모두 `supabase/migrations/20260803120000_tighten_grants.sql`로 적용·검증했다.

### 2-3. 한자 DB 검사기가 **절대 통과할 수 없는 상태**였다

```
Count mismatch before publish: {"hanjaEntries":9938, …}   ← 기대값 10380
```

`10380`은 최초 커밋(`8b3e9bf`)에 박힌 뒤 한 번도 갱신되지 않았다. **데이터는 정상이고
검사기가 낡은 것이었다.**

| 숫자 | 뜻 |
| --- | --- |
| 10,380 | **원본 추출 행 수** — 검사기가 멈춰 있던 값 |
| 9,938 | 현재 DB 행 수. 2026-07-20에 화면에 그릴 수 없는 코드(미할당 Plane 10 · PUA) **442행을 걷어낸** 결과 |
| 9,088 | 그중 후보로 낼 수 있는 고유 글자 |
| 488 | 음절 |

근거: `docs/WORK_STATUS_2026-07-20.md:58`(442건 삭제, 백업 `bad-hanja-backup.json`),
`docs/REFERENCE_DATA_UPDATES.md:13`("원본 추출 10,380행 · DB 9,938행").

**고치면서 두 가지가 더 드러났다.**

첫째, `10380`이 **세 군데**(59·179·182줄)에 있었다. 상수 하나로 모았다.

둘째, 그 뒤에 있던 **공개 조회 검사가 앱이 쓰지 않는 경로를 보고 있었다.** anon 키로
`official_hanja_entries`를 읽는지 확인했는데, 화면에 쓰이는 조회는 전부 서버에서
service_role로 돈다(`lib/hanja-guide-data.ts`, `lib/official-hanja-db.ts`,
`api/hanja/surname`). 안 쓰는 경로를 검사하면 **진짜 화면이 깨져도 통과하고**, 반대로 권한을
조이면 멀쩡한데도 실패한다 — 실제로 `permission denied`로 멈춰 있었다.

그래서 둘로 갈랐다.
- **데이터 검사**는 service_role로 옮겼다(남 31자 · 규 38자 실제 조회 확인).
- **권한 검사**는 뒤집었다 — anon 키로는 **읽히지 않아야 한다**. 이 표는 손질을 거친 자료이고
  화면에는 서버가 골라서 내보낸다. 나중에 누가 편의로 열어 두는 것을 여기서 잡는다.

개정이 오면 이 값을 함께 갱신해야 한다는 절차도 주석에 적었다(REVISIONS · EXPECTED · 문서 셋).

**최종 결과**
```json
{ "ok": true,
  "after": { "hanjaEntries": 9938, "hanjaProductionEntries": 9938,
             "pronunciationEntries": 2321, "namCandidates": 31,
             "gyuCandidates": 38, "anonReadBlocked": true } }
```

---

### 2-4. inyeonlink 약관 모듈이 클라이언트 번들에 실려 있었다

`payments-csp.ts`는 첫머리에 **"클라이언트 번들에는 들어가지 않는다"**고 적어 두었는데, 번들
검사가 그 단언이 깨져 있음을 잡았다. 브라우저 청크에 이런 것이 있었다.

```js
o.default.env.NEXT_PUBLIC_TOSS_CLIENT_KEY && o.default.env.TOSS_SECRET_KEY, …
```

**값은 새지 않는다** — Next는 `NEXT_PUBLIC_`만 인라인하고 이 표현식의 결과는 버려진다.
경로는 이랬다.

```
LegalModal.tsx("use client")  →  legal-content.ts  →  payments-csp.ts · ads.ts
        ↑ LEGAL_EFFECTIVE_DATE를 **값으로** 임포트한 것 하나 때문에
```

값 임포트가 하나라도 있으면 모듈 전체가 딸려 온다. 그래서 **23로케일 약관 원문까지** 브라우저로
나가고 있었다. 정작 모달은 문서를 `/api/legal`로 받아 오므로 그 원문이 필요 없었다.

고친 방법: 값(`LEGAL_EFFECTIVE_DATE`)은 원본인 `@/lib/company`에서 가져오고, `legal-content`는
**타입으로만** 가져온다. `LegalDocumentBody.tsx`도 같다.

결과: 브라우저 자산 **30개 → 27개**, `TOSS_SECRET` 흔적 사라짐.

naminglink는 같은 교훈을 이미 배워 약관 모듈을 클라이언트에서 임포트하지 않는다. 이 앱만
빠져 있었다.

### 2-5. `X-Powered-By: Next.js`

양쪽 앱에 `poweredByHeader: false`. 어떤 프레임워크인지 알릴 이유가 없다.

### 2-6. `official_*`의 닿지 않는 공개 읽기 정책

`"Anyone can read production …"` SELECT 정책이 `public` 역할로 걸려 있었지만 **그 역할에는
테이블 GRANT가 없었다.** RLS 정책은 GRANT 위에 얹히는 것이라, GRANT가 없으면 정책이 무엇을
허용하든 닿지 않는다 — 처음부터 아무 일도 하지 않고 있었다.

남겨 두면 이름이 거짓말을 한다. "Anyone can read"라고 적혀 있으니 다음 사람은 공개라고 읽고,
누가 편의로 GRANT 한 줄을 주면 **9,938행짜리 손질된 자료가 브라우저 키로 통째로 열린다.**
정책을 지우면 GRANT만으로는 아무것도 못 읽는다(RLS 켜짐 + 정책 없음 = 전부 거부).

적용 후 라이브 확인: `/ko/guide/hanja/ga` 한자 4,175자 정상, 성씨 조회 정상,
`verify-official-database.mjs` 통과. service_role은 RLS를 통과하므로 영향이 없다.

---

## 3. 남겨 둔 것

### 3-0. 철회한 지적 — inyeonlink 본문 크기 제한

**처음에 "제한이 없다"고 적었는데 틀렸다.** 다섯 라우트 전부 제한이 있다.

| 라우트 | 상한 |
| --- | --- |
| `api/day-master` | 1KB |
| `api/report/order` | 2KB |
| `api/match` · `api/affinity` | 4KB |
| `api/report/pdf` | 8KB |

오히려 naminglink(4KB~1MB)보다 빡빡하다. 공유 헬퍼(`readJsonBodyLimited`)가 아니라 각
라우트에 인라인으로 있어서 **헬퍼 이름으로 검색한 내 방법이 놓친 것**이다. 기능이 아니라
이름을 찾으면 이렇게 된다.

남는 것은 아주 작은 흠 하나 — `raw.length`는 **문자 수**이지 바이트가 아니다(상수 이름은
`MAX_BODY_BYTES`다). 한글은 문자당 3바이트라 실제 상한이 이름보다 최대 3배 느슨하다. 다만
상한 자체가 1~8KB로 작아 실질 위험이 없어 손대지 않았다.

### 3-1. `sharp <0.35.0` — high 등급 CVE 4건 · **지금 올리지 않기로(2026-08-03 사용자 결정)**

CVE-2026-33327 / 33328 / 35590 / 35591(libvips 상속, GIF·TIFF·VIPS 형식).

| 항목 | 값 |
| --- | --- |
| 설치된 버전 | `sharp@0.34.5` |
| 패치 버전 | `0.35.0` 이상(현재 최신 `0.35.3`, node ≥20.9 요구 — 이 저장소는 22) |
| **Next이 선언한 범위** | `optionalDependencies: { sharp: "^0.34.5" }` |
| 두 앱 `images` 설정 | **없음** → `remotePatterns` 없음 |

**`^0.34.5`가 판단의 중심이다.** 0.x에서 캐럿은 마이너를 올리지 않으므로 `>=0.34.5 <0.35.0`
이다. 즉 **패치 버전은 Next 16.2.11이 선언한 범위 밖**이고, 올리려면 `pnpm.overrides`로 Next의
선언을 강제로 무시해야 한다.

**성립 조건이 지금 구조에 없다.** 권고문 원문: *"Those processing untrusted input with versions
of sharp prior to 0.35.0 are affected."* — **공격자가 준 이미지를 처리해야** 성립한다(영향은
무결성·가용성이고 RCE가 아니다). 이 서비스가 이미지를 처리하는 자리는 셋뿐이다.

1. `/public`의 자체 자산 — 우리가 넣은 파일
2. 관리자가 올리는 리포트 배경(`report_backdrops`) — **관리자 인증 필요**
3. 서체 미리보기 — 서버가 생성

**일반 이용자가 이미지를 올리는 자리가 없고**, `remotePatterns`가 없어 `/_next/image`는 외부
URL을 최적화하지 않는다. 신뢰할 수 없는 이미지가 sharp에 닿는 경로가 **현재 없다.**

**확인하지 못한 것 하나**: Vercel 배포에서 `/_next/image`는 플랫폼의 이미지 최적화가 처리하고
번들된 sharp는 실행되지 않는 것으로 알려져 있으나, **이 배포에서 실증하지 못했다.** 사실이면
노출면은 0에 가깝고, 아니어도 위 세 경로뿐이라 결론은 같다.

**올리지 않는 쪽을 고른 이유**: ① 성립 조건이 없다 ② Next이 선언한 범위를 강제로 벗어나야
하는데 Next 16.2.11은 sharp 0.35와 함께 시험되지 않았다 — **이미지 최적화가 조용히 깨지면**
랜딩 첫 화면이 무거워진다(`BrandMark.tsx` 주석에 1024×1024 원본 964KB가 그대로 내려간 전례가
있다) ③ 실거래 0건이라 지금 감수할 이유가 없다.

**대신 조건을 걸었다** → `POST_LAUNCH_CHECKLIST.md` §6. 조건이 하나라도 성립하면 등급이
올라간다.

### 3-2. 테스트 데이터 잔존

orders 4건(전부 `is_test=true`·UNPAID), premium_analysis_sessions 3건(DELETED, 2026-07-27).
오픈 시 정리 목록(`POST_LAUNCH_CHECKLIST.md` §2)에 이미 있다.

---

## 4. 확인된 정상 — 어떻게 확인했는가

### 4-1. 서버 비밀이 브라우저로 나가지 않는다

**이름이 아니라 실제 값으로 검사했다.** 이름만 찾으면 값이 다른 경로로 흘러갔을 때 못 잡는다.
`.env.local`의 서버 전용 값 10종(service_role 키 · OpenAI · DB URL · `RESULT_SEAL_SECRET` ·
웹훅 시크릿 · `CRON_SECRET` · `ANALYTICS_HASH_SALT` 등)을 브라우저 자산 49개에서 전수 탐색 —
**하나도 없다**. 검사기는 `scripts/audit-bundle-secrets.mjs`로 남겼다.

### 4-2. 결제 승인 경로

`api/payments/toss/confirm`:
- 금액을 **주문에 저장된 값**으로 승인한다(돌아온 쿼리의 amount를 쓰지 않는다)
- 열린 리디렉트 차단(`/`로 시작하고 `//`가 아닌 경로만)
- `orderId`는 UUID 형식 검증
- 이미 PAID면 멱등하게 통과(새로고침·뒤로가기 대비)
- 승인 실패 시 결제도 성립하지 않는다(토스는 승인해야 결제가 된다)

### 4-3. 관리자 인증

`lib/admin-auth.ts` — Bearer 토큰 → `supabase.auth.getUser()` **서버 검증** →
**`app_metadata`** 의 역할 확인. 이용자가 고칠 수 있는 `user_metadata`가 아니다. 단수 `role`과
배열 `roles`를 모두 본다(한쪽만 보면 배열로 부여된 관리자가 회원 목록에 섞인다).
토큰 없음 401 · 세션 무효 401 · 권한 없음 403 · 설정 없음 503으로 전부 fail-closed.

### 4-4. DB 자세

- **테이블 30개 전부 RLS 활성**
- SECURITY DEFINER 함수 7개 전부 `search_path` 고정
- 조치 후 anon/authenticated 권한이 남은 표는 `naming_logs` 하나뿐이고, 그것은 의도된
  것이다(본인 기록 읽기·삭제, 정책 2개가 `auth.uid() = user_id`로 제한)
- `is_test` 안전망: `orders`·`premium_analysis_sessions` 양쪽 `NOT NULL` · 기본값 없음
  (빠뜨린 INSERT가 에러로 실패한다)

### 4-5. 라이브

| 항목 | naminglink | inyeonlink |
| --- | --- | --- |
| CSP | 있음(광고 도메인 허용) | 있음(더 좁음) |
| HSTS | `max-age=63072000; includeSubDomains; preload` | 동일 |
| X-Frame-Options | DENY | DENY |
| X-Content-Type-Options | nosniff | nosniff |
| Referrer-Policy | strict-origin-when-cross-origin | 동일 |
| Permissions-Policy | camera/microphone/geolocation 차단 | 동일 |
| robots | `/api/`·`/naming-artist`·`/admin` 차단 + sitemap | `Disallow: /`(배포 주소라 의도된 것) |
| sitemap | 624 URL | — |
| ads.txt | 퍼블리셔 ID 정상 | — |

### 4-6. 검사 스크립트

**32개 중 30개 통과.** 나머지 둘은 실행 조건 문제였다.

- `verify-official-database.mjs` — §2-3에서 고침, 이제 통과
- `validate-official-reference-data.mjs` — 원본 `docs/hanja.pdf`가 저장소에 없어 못 돈다
  (원본 대조용 스크립트라 정상)

**함정 하나**: 서버 전용 모듈을 부르는 검사는 `--tsconfig scripts/tsconfig.sweep.json`으로
돌려야 한다(`server-only` 스텁). 그냥 돌리면 4개가 실패하는데 **제품 결함이 아니라 실행법**
문제다. 감사 중 실제로 이것을 결함으로 오인할 뻔했다.

### 4-7. 거래 데이터

주문 4건이 **전부 `is_test=true` · UNPAID** — 실거래 0건이 데이터로 확인된다.

---

## 5. 감사 중 만든 것

| 스크립트 | 하는 일 |
| --- | --- |
| `apps/*/scripts/audit-bundle-secrets.mjs` | 빌드된 브라우저 자산에 서버 비밀이 **값으로** 들어갔는지 |
| `apps/naminglink/scripts/audit-db.mjs` | RLS · anon 권한 · SECURITY DEFINER · 잔존 데이터 한눈에 |

---

## 6. 이번 감사에서 배운 것

**안 도는 검사는 지키지 않는 검사다.** 이번에 실패한 6개 중 4개는 실행법 문제였고 2개는
진짜였는데, 겉보기로는 구별되지 않았다. 검사기가 조용히 죽어 있으면 "통과"와 "안 봄"이
같아 보인다.

**기대값을 현실에 맞추기 전에 현실이 맞는지 봐야 한다.** 한자 검사기의 10,380을 9,938로
바꾸는 것은 5초면 되지만, 그 감소가 의도된 정리인지 유실인지 모르면 검사를 무력화하는
것이 된다. 문서에서 근거(442행 삭제·백업 위치)를 찾은 뒤에 고쳤다.

**"이 자리만 예외"가 위험 신호다.** 이번에 나온 것은 전부 저장소의 다른 자리에서는 지켜지는
규칙이었다 — revoke 붙은 표 29개 중 하나만 빠짐, service_role 전용 함수 6개 중 하나만 열림,
갱신된 상수들 중 하나만 멈춤, 약관을 클라이언트에서 안 부르는 앱 둘 중 하나만 부름.

**기능이 아니라 이름을 찾으면 없는 결함을 만든다.** inyeonlink 본문 제한을 "없다"고 적었는데
(§3-0) 다섯 라우트 전부 있었다. `readJsonBodyLimited`라는 **헬퍼 이름**으로 찾았기 때문이고,
그 앱은 같은 일을 인라인으로 하고 있었다. 감사에서 "없다"고 말하려면 **하는 일**로 찾아야 한다.

**단언은 검사가 아니다.** `payments-csp.ts`는 "클라이언트 번들에 들어가지 않는다"고 주석에
적어 두었지만 실제로는 들어가 있었다(§2-4). 주석은 시간이 지나면 사실이 아니게 되고, 그것을
알려 주는 것은 주석이 아니라 **돌아가는 검사**다 — 이번에 그것을 잡은 것은
`audit-bundle-secrets.mjs`였다.
