# 인계 — 2026-09-04 (세션 재시작용)

**이 문서는 세션이 끊긴 자리에서 그대로 이어 가기 위한 것이다.** `docs/HANDOFF_2026-09-03.md`
를 대신한다(그 파일은 배치 123까지의 상태다 — 숫자는 이 파일이 최신). 오늘 하루의 배경과
실수 기록은 `docs/WORKLOG_2026-09-04.md`(배치 124부터 188까지 전부)와 `CLAUDE.md`에 있다.

**사용자 지시(2026-09-04 밤)로 이 지점에서 세션을 마친다.** "나머지 배치 전부 진행"
지시는 여전히 유효하다 — 막혀서 멈춘 게 아니라 사용자가 정지를 요청했다.

## 지금 어디까지 와 있나

```
배치 188(밀러 Poker~Pole-cat)까지 넣었다 — 5판 묶음(184~188)이 끝났고
전수 스윕·git push·배포 확인(curl로 404→200)까지 전부 끝났다.
다음 밀러 표제어는 `Police`부터다(그다음 — Polishing·Politician·Polka·Pomegranate…).
배치 파일(`mb189.json`)은 아직 안 만들었다.
```

## 해몽 사전 v2 — 지금 수치

```
상징 1,610 · 의미 5,463 · 인용 5,487
```

**남은 밀러 표제어를 원문에서 직접 세면 685개다**(전체 2,255개 중 1,570개 처리, 69.6%).
`grep -cE "^_[A-Za-z][A-Za-z' Œœ-]*_\.(\[[0-9]+\])?$" miller-1901-raw.txt` 로 표제어 줄을
직접 세는 방식이다 — 인계 문서에 손으로 적어 둔 수는 실측과 여러 번 어긋난 전례가 있다
(CLAUDE.md §4). **속도는 최근 50판(139~188) 기준 배치당 평균 4.1개 표제어** — 남은 배치는
대략 **170~250판**(밀도가 「새 상징만 세우는 구간」과 「기존 상징에 하나씩 겹쳐 붙이는
구간」 사이를 오가서 폭이 크다).

## 오늘(09-04) 낸 사고 하나 — 반드시 읽을 것

**배치 185에서 `build-dream-dictionary-v2.mjs` 를 `| tail`로 잘라 돌리다가, 기본값 플립을
`FALLBACK_FIRST`에 못 얼려 스크립트가 `exit 1`로 멈추고 파일을 안 쓰는 상태를 못 보고
여러 판을 헛돌았다.** `joy`에 붙인 세 번째 의미가 화면 데이터에서 통째로 사라진 채
`verify-dream-km`·`audit-km-dead-words`가 전부 초록불이었다(옛 파일을 검사했으니 당연했다).
재구현으로 원인을 30분 넘게 파다가 실제 스크립트 코드(1041~1061줄)를 읽고서야 잡았다.

→ **`build-dream-dictionary-v2.mjs`·`build-dream-match-tables-v2.mjs`를 `| tail`로 자르지
않는다.** `> 파일 2>&1; echo "EXIT=$?"`로 돌리거나, 파이프 없이 전체 출력을 보고 **마지막
줄에 「상징 N개 · 의미 M개 · 인용 K개」 요약이 실제로 찍혔는지**를 판정 기준으로 삼는다.
CLAUDE.md §10(2026-09-04)·메모리 `build-script-tail-hides-exit1`에 자세히 적어 뒀다.

## 09-04에 곁에서 찾아 고친 몇 주 묵은 상처들

전부 ⓪ 전수 grep이나 프로브 지킴 케이스로 찾았다 — 검사기 셋(verify-dream-km·
audit-km-dead-words·verify-dream-cite)은 전부 초록불이었던 자리다.

```
needle 의 영어 별칭 "pin"          → 새 상징 pins(옷핀)에 넘김(배치 179)
flute 의 bare 영어 별칭 "pipe"     → 새 상징 pipe(파이프)에 넘김(배치 180)
gun 의 판별어 "쐈다"가 형제와 겹침  → "여성이 총을 쐈다"가 몇 주째 남의 뜻으로 감(배치 181)
fork 의 별칭 "쇠스랑"              → 새 상징 pitchfork(쇠스랑)에 넘김(배치 182)
sword 의 별칭 "단검"·"dagger"      → 이미 있던 전용 상징 dagger(단도)에 넘김(배치 186)
lumber 의 별칭 "널빤지"·"planks"   → 새 상징 plank(널빤지)에 넘김(배치 184)
alabaster 의 bare 별칭 "석고"      → 새 상징 plaster(석고)에 넘김(배치 184)
```

## 09-04에 건너뛴(§31) 표제어와 이유

```
Pigeon 의 앞 두 문장(구구대는 소리·처녀의 이른 혼인)   dove(밀러 Doves)와 같은 그림(177)
Pistol 의 「hear the report」·「shooting off」          gun(밀러 Gun)의 같은 그림·같은 극성(181)
Pitcher 의 「A broken pitcher」                         jug(밀러 Jug)의 「깨진 주전자」와 같은 그림(182)
Poinard 의 「Dreaming of poinards, omens evil」          dagger(밀러 Dagger)의 「단도를 봄」과 같은 그림(186)
```

## 절차 — 아홉 단계 + 배치 관문

메모리 `dream-dictionary-v2-rebuild-in-progress`에 고정돼 있다. 요약:

```
⓪ 새 이름·term_en 을 기존 별칭 전부에서 grep(한국어 이름 + term_en 둘 다, .slice() 금지)
① mb<n>.json 으로 헤드워드를 쪼갠다
② m<n>.json 으로 원문에서 뽑는다 — cite.original 은 원문과 문자열이 같아야 한다
③ verify-dream-cite.mjs <그 파일>
④ 커버리지 대조(입력 헤드워드 ↔ cite.locator)
⑤ 인종·민감 표현 grep
⑥ kmm<n>.json(새 상징) 또는 patch-km-<n>.mjs(기존 상징 — 그 상징의 원본 kmm/km 파일을 고친다)
⑦ build-dream-dictionary-v2.mjs → build-dream-match-tables-v2.mjs  (반드시 둘 다,
   `| tail` 로 자르지 말고 EXIT 코드를 직접 확인한다 — 위 사고 참고)
⑧ verify-dream-km.mjs · audit-km-dead-words.mts · audit-cross-symbol-aliases.mjs
⑨ apps/dreamslink 에서 npx tsx scripts/probe-miller-batch.ts (CASES를 새 배치로 갈 것,
   건드린 기존 상징의 옛 답을 재는 지킴 케이스를 함께 넣을 것)
```

그 뒤 추가 배치 관문(모두 exit 0이어야 커밋): `verify-dream-match.ts` ·
`verify-dream-context-parity.ts` · `verify-conception-pick.ts` · `verify-guide-numbers.ts` ·
`npx tsc --noEmit` · `verify-dream-cite.mjs`(전수, r*·result*·m*·miller-pilot 전부).

**5판 묶음이 끝나면**: `audit-verifiers.mjs`(전수 스윕, 빨간불 4건은 상시 항목 — 상세는
CLAUDE.md 체크리스트) → `git push` → 새 배치 전용 주소로 배포 확인(푸시 전 404, 푸시 후
폴링으로 200) → 묶음 마감 워크로그 문단 → 별도 "인계" 커밋.

## 다음 세션이 먼저 읽을 것

1. `CLAUDE.md` 시작 전 체크리스트 — 특히 이번에 새로 추가된 `\| tail` 항목
2. 메모리 `dream-dictionary-v2-rebuild-in-progress` — 절차와 되풀이해 밟는 실수 전체 목록
3. `docs/WORKLOG_2026-09-04.md` — 오늘 배치 124~188의 전말(패치·판별어 충돌·건너뛴 이유
   전부 여기 있다)
4. `apps/dreamslink/scripts/probe-miller-batch.ts` — 지금 CASES가 배치 188 것으로 갈려
   있다. 다음 배치에서 반드시 다시 간다.
