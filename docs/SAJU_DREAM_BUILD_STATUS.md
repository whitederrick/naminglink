# SAJU · DREAM 빌드 상태 · 핸드오프

_as of 2026-08-06 · 이 문서 = 현재 상태 + 다음 작업 + 원칙 + 문서 인덱스의 단일 소스.
sajulink/dreamslink 작업 재개 시 먼저 읽는다._

---

## 0. 큰 그림 (Link 팩토리)

- 모노레포 `C:\myProjects\naminglink` (pnpm workspace).
  `apps/{naminglink, inyeonlink, sajulink, dreamslink}` + `packages/core`(사주엔진·pdf·env·apps.ts).
- **naminglink** — 라이브(`naming-link.com`). 애드센스 **심사 중**.
- **inyeonlink** — 라이브(`inyeon-link.com`). 궁합 + 인연의 결.
- **sajulink** — 라이브(`saju-link.com`, **2026-08-06 연결 완료**). 색인 개방·애드센스 신청 완료.
- **dreamslink** — 이미지 자산만 있고 **앱 없음**. 도메인 `dreams-link.com` 구매 예정.
- 공통 선행 블로커: 통신판매업 신고번호 · 결제 실키 · 애드센스 승인.
- 분업: 설계·리서치·콘텐츠 = Cowork(문서) / 레포 구현·빌드·git = VS Code Claude Code.

---

## 1. 사주링크 — 완료된 것

### ⑤ 리포트 장수를 구조로 고정 (커밋 `8138a90`)

고시의 5장·7장이 **선언된 `Page` 수가 아니라 글이 넘쳐서** 나오던 값이었다. 해설이 짧으면
4장·6장으로 나갔고, **팔던 5장 중 하나는 개인정보 안내 세 줄뿐인 빈 장**이었다.

→ 장 구성을 실제 내용으로 다시 짜서 **바닥은 구조(선언된 Page), 천장은 상한(`LIMITS`)**.
해설이 실패해도 엔진 서술이 같은 자리를 채운다.

### ⑥ 약관을 실제 상품과 맞춤 (커밋 `af120ab`)

약관 4종이 아직 **인연링크 궁합 서비스**를 팔고 있었다 — 서비스명이 「인연링크」, 「두 사람의
관계」·「매칭률」, 엔진에 없는 **대운·세운**. ko·en을 사주로 다시 쓰고 AI 고지를 넣었다.

**살아 있던 고지 위반 하나**: `getRequestLocale`이 접속 국가로 23개 로케일 아무거나 골라,
화면은 영어인데 **약관만 그 언어의 궁합 약관**이 나갔다. `translatedLocales`로 좁혔다.

### ⑦ 로케일 (커밋 `65fc18f`·`39e64e1`·`05123b7`)

**화면 사전 23개 · 약관 23개 완료.** 검사기 전부 통과.

핵심은 **재사용**이다 — 사주 잎 403개 중 **204개(51%)가 인연링크와 en 원문이 글자까지 같다**
(오행·띠·일간·십신 이름, footer 16개, 언어 선택기, 폼 라벨). 처음에는 그걸 전부 다시
번역시켜 호출이 두 배가 됐고, 한도에 걸려 **`id` 로케일이 141개나 영어로 남은 채 "성공"**했다.
`export-dictionaries.ts`(인연링크) → `translate-i18n.ts`가 **en이 같은 자리만** 물려받는다.

### 그 밖

- **로고** — 원형 로고를 세 앱의 머리글·PDF·OG 23장에 반영. 원본 2.5MB는 보관용, 코드는
  **256px·128색 사본(16KB)**을 쓴다(커밋 `264cb88`)
- **색** — 사주링크 브랜드 색이 **인연링크와 글자까지 같았다**(`#86335a` 자두). 로고에서 뽑아
  남색 `#16273f`·금색 `#976d26`·청록 `#456b74`로. 토큰 이름도 `brand-navy` 등으로(커밋 `29ce33e`)
- **naminglink** — 방침에 **제3자 쿠키 고지가 통째로 없었다**(광고 게재 중인 사이트인데).
  23로케일에 「쿠키와 광고」 신설(커밋 `2fc5424`). 약관 네 장의 꼬리글이 로케일을 못 받아
  22개 언어에서 한국어로 나가던 것도 고침(`b0ac0a8`)

---

## 2. 상품 개편 — 2026-08-05 결정

### 무엇을 왜 바꾸는가

- **사주와 오늘의 운세는 다른 것이다.** 사주는 평생, 오늘의 운세는 그 사주에 오늘 일진을 대 본
  하루짜리다. 그런데 「삶의 네 영역」이 **오늘 십신**으로 매긴 값이면서 "내 사주의 성향"으로
  읽혔고, 보관하는 문서의 40%가 하루짜리였다.
- **총운/프리미엄의 차이가 근거 숫자 두 장뿐**이라 팔 이유가 약했다.

### 확정된 것

| | |
| --- | --- |
| 상품 | **「평생 사주와 올해의 운세 리포트」 하나.** ₩990 / US$1.99 |
| 가격 근거 | 궁합은 상대가 바뀌면 재구매하지만 **사주는 1인 1회**. 무료로 주는 곳도 있어 기준선이 낮다 |
| PDF | **평생 + 올해 총운.** 오늘의 운세는 넣지 않는다 |
| 무료 화면 | 개략만 — 원국·일간 한 줄·오행 막대. **강약·용신·십신·영역은 유료** |
| 오늘의 운세 | 무료·화면 전용 + **형제 서비스 채널**(셀프 광고) |
| 해설 | 엔진 템플릿이 본편, **AI는 요약 한 문단**만 |
| 고지 | 참고자료 고지 + **AI 문단 고지**를 자리와 문장을 나눠서 |

### 왜 AI를 한 문단으로 좁혔나

넓게 두면 **이용자가 읽는 글이 사실상 전부 가변**이라, 처음 요구("매번 바뀌지 않게")가
해결된 게 아니라 면책된 것이 된다. 1회 구매·평생 보관·재발급 5회 상품에서는 다르다.

더 큰 이유는 **검증**이다 — 이 영역은 과장·단정이 정책 위반인데, 매번 새로 쓰는 글은 매번
새로 위반할 수 있고 23로케일을 사람이 다 읽어 볼 수 없다. **템플릿은 한 번 검토하면 끝난다.**

### 진행 상황

- **① 원국 기준 삶의 네 영역 엔진** ✅ `natal-outlook.ts` + 회귀 하니스(커밋 `b822988`)
- **②-1 뼈대 전환 · AI 한 문단** ✅ `MUTABLE_FIELDS` + 재현성 가드(커밋 `8b2f660`)
- **②-2 문안 확대 23로케일** ✅ 403 → 440개 문자열(커밋 `421a92e`)
- **③ 뼈대 빌더 배선** ✅ 십신 두터운 둘·없는 둘, 영역별 근거 둘, 용신 문단(커밋 `9092c6d`)
- **④ 화면 미끼화** ✅ 강약·용신 제거 + 일간 카드·오행 막대·리포트 자리(커밋 `45c74d6`)
- **⑤ 셀프 광고 이식** ✅ 다섯 중 둘 회전·명단은 코어·로고 포함(커밋 `b1da77e`)
- **⑤-2 사주/오늘 화면 분리** ✅ 계획서에 없던 일(사용자 지적). `/today/result`(커밋 `31e6992`)
- **⑥+⑦ 상품 하나로 · 리포트 9장 · 약관 23로케일** ✅ 둘을 함께 했다 — 장수를 하나로 정하려면
  상품이 하나여야 해서 따로 커밋하면 어느 쪽도 green이 아니다(커밋 `1db7b14`)
- **⑧ 검사기 재조준** ✅ ⑥에서 함께(장수 기준·상품 코드·셀프 광고 예외)

---

## 2.5 코드 리뷰 (2026-08-06) — 한 가지 병이 여덟 곳에서 나왔다

보안·개인정보·성능·품질·표준으로 세 앱을 훑었다. **잘 되어 있는 것**부터: 보안 헤더가 세
사이트 동일(CSP·HSTS preload·`X-Frame-Options: DENY`·nosniff), CSP가 기능이 켜질 때만 열리는
구조, 결제 금액은 DB가 정하고 결제는 결제사에 직접 확인, 주문 표에 생년월일 미저장, 로그에
PII 없음, 본문 받는 라우트 25개 전수 통과.

### 🔴 사주링크 운영에 Supabase 환경변수가 없었다

라이브가 `STORAGE_NOT_READY`를 냈다. 파급이 넷이었다:

1. **푸터가 폴백으로 그려졌다** — 상호 `Naming-Link`(실제 `(주)Platforest`), 등록번호 "준비 중".
   라이브 통신판매 사이트의 법적 고지가 사실과 달랐다(전자상거래법 제10조).
2. **레이트리밋이 전부 통과했다**(`!supabase → return true`).
3. 집계 유실 · **AI 원가 기록 0건**(§7에 "완료"로 적었지만 실제로는 안 돌고 있었다).
4. 상품을 켜면 주문 생성 503.

환경변수를 넣자 전부 복구됐다. **폴백이 셋 다 틀렸던 것**은 따로 고쳤다(아래).

### 왜 같은 패턴이 반복되는가

**앱 목록이 코드 곳곳에 손으로 박혀 있고, 앱을 늘리는 방법이 「복제」이기 때문이다.**

| 자리 | 증상 |
|---|---|
| `verify-route-guards` | 두 앱만 세고 **"ALL PASS"** |
| `verify-env-isolation` | 두 앱만 훑음 |
| `verify-app-split` | 두 앱만 더해 사주 방문 17건이 "새는 값"으로 |
| `api/admin/inyeon-status` | 콘솔이 한 앱만 물음 |
| `admin_analytics_snapshot` | 모르는 앱 → **조용히 naminglink 숫자** |
| 결제 복귀 | 앱마다 사본, 세대마다 하나씩 유실 |
| `company`·`request-guard`·`ops-alert` | 앱마다 거의 같은 사본 |
| Supabase 환경변수 | 새 앱에만 안 들어감 |

셋 다 구조적이다 — ① `packages/core`에 엔진·PDF·env만 있어 나머지는 앱별 사본, ② tsc는
*문자열 배열*과 *있어야 할 코드*를 못 본다, ③ **검사기가 자기 대상을 스스로 정한다.** 목록에서
빠진 앱은 통과한 것이 아니라 **검사받지 않는데** 화면에는 "ALL PASS"가 찍힌다.

### 고친 것

- **`scripts/verify-app-coverage.mjs` 신설**(대조군·주석 제외, 692파일) — 앱 목록을 손으로 적은
  자리를 찾아 실패시킨다. 목록의 원본은 `packages/core`의 `APP_KEYS`이고 `scripts/app-keys.mjs`가
  읽어 준다. **만들자마자 여섯을 잡았다**(위 표의 1~4 + 래퍼 허용 목록 + `verify-order-binding`이
  운영 orders에 행을 넣으면서 환경 가드가 없던 것)
- **콘솔 「서비스 오픈 상태」** — `inyeon-status` 폐지, `APP_KEYS`를 도는 `service-status`로.
  못 물어본 것과 꺼져 있는 것을 화면에서 가른다
- **지표 RPC 마이그레이션**(적용 완료) — 묻는 값을 그대로 쓴다. 모르는 값은 0으로 보인다.
  AI 집계는 `ai_usage_logs.app` 기준(사주가 유료 경로에서 AI를 쓰기 시작해 옛 전제가 깨졌다).
  검증: 방문 582+144+17 = 743 / 전체 743
- **사업자 정보를 `packages/core/src/company.ts`로** — 조회만 앱에 남겨 core가 Supabase를
  모르게 했다. `company-server`가 90줄 사본에서 조회 다섯 줄로 줄었고, **`197-86-02010`이
  저장소 전체에서 한 곳에만 있다**
- **레이트리밋 fail-open에 알림** — RPC 오류에는 알리면서 설정이 아예 없을 때는 알리지 않았다.
  가장 위험한 실패가 가장 조용했다
- **결제 복귀 두 구멍**(§7의 「결론 난 것」)

### 남은 것

- `request-guard`·`ops-alert`는 **일부러 안 옮겼다.** naminglink 것이 실제로 다르고(전역 AI
  상한은 fail-closed) 억지로 합치면 그 차이가 옵션 분기로 숨는다. dreamslink 때 다시 판단할 것
- `sharp` 런타임 취약점 1건 — 보류 조건([[sharp-cve-deferred]])은 아직 유효하나 분기마다 재확인
- 개발 의존성 취약점 6건(eslint 체인) — 런타임 영향 없음

---

## 3. 열린 항목

- **애드센스 EEA 동의 메시지** — naming-link.com은 **활성 확인 완료**(2026-08-05). 사이트를
  추가할 때 메시지 대상도 함께 넓힐 것 → [[eea-consent-cmp-pending]]
- **`verify-i18n`** — 로케일이 없을 때를 실패가 아니라 "검사할 것이 없는 상태"로 찍게 고침
- **브랜드 심볼** — `BrandMark`는 실제 로고로 바뀌었으나 OG의 도형 흔적은 정리 완료
- **`saju_report_structure.md`가 낡았다** — 총운 3장/프리미엄 5~6장으로 적혀 있다. 실제는
  **단일 상품 9장**이다(§7)
- **`premiumReport` 사전 절이 남아 있다** — 상품이 하나가 되며 쓰이지 않는다. 23로케일에서
  함께 지울 것

---

## 4. dreamslink (미착수)

- `apps/dreamslink`에 **이미지 7장뿐**. `src` 없음.
- 재료: 상징 사전 **209개**(118+91, dictVer 1.1.0) · `dreamslink_domain_logic.md` ·
  `IMPLEMENTATION_TICKET_saju_dream.md` §3 · 배경/로고 자산.
- **애드센스 승인 시점이 사실상 개발 마감**이다 — 승인 후 inyeon·saju·dream을 동시 신청하려면
  그때 앱이 서 있어야 한다.
- 만드는 길은 검증됐다(인연링크 복제 → 엔진 교체). 이번 세션에서 복제가 남기는 함정을 전부
  겪어 검사기로 박아 두었다.

---

## 5. 관통 원칙

1. **고시=실제.** 약관·상품 설명은 실제 제공물(장수·기능)과 일치. 문구로 부풀리지 않는다.
2. **분량으로 장수를 만들지 않는다.** 장수는 선언된 `Page`가 정한다. 다만 **글자 수 상한은
   지면이 아니다** — 라틴 451자와 한글 451자는 두 배 넘게 차이 난다. 상한은 사전이 소리 없이
   자라지 않게 하는 신호이고, 지면은 **로케일마다 실제로 렌더해 장을 세는 것**이 정한다(③).
3. **같은 입력이면 같은 문서.** 달라져도 되는 자리는 `MUTABLE_FIELDS`에만 있고, 코드·검사기·
   고지 세 곳이 같은 목록을 본다.
4. **무료=템플릿(무과금)·유료=엔진+AI 한 문단.** 고트래픽·SEO·애드센스 속도 보호.
5. **결정론.** 점수·간지는 엔진. 과잉약속 회피(운명·건강 단정 금지).
6. **복제 앱은 전수 grep — 라틴과 한글을 함께.** 2026-08-05에 한글 "인연"을 놓쳐 꼬리글이
   `Saju-Link ( 인연 링크 )`로 배포됐다.
7. **새로 만들기 전에 형제 앱을 먼저 볼 것.** 사전 잎의 51%가 이미 번역돼 있었다.
8. **검사기에는 대조군을 넣는다.** 늘 통과하는 검사기는 없는 것과 같다. 값으로 새는지 볼 때는
   **그 항목에만 있는 값**으로 본다 — 흔한 값(오행 이름 같은 것)을 기준 삼으면 멀쩡한 응답을
   잡는다(④).
9. **보내는 것과 그리는 것을 맞춘다.** 응답에 있고 화면에 없으면 새는 것이고, 화면에 있고
   응답에 없으면 화면이 깨진다. "화면이 안 그린다"는 방어가 아니다(④).
10. **green에서만 커밋.** 라이브 표는 후방호환 마이그레이션 + 두 앱 빌드 확인 후에만.
11. **앱 목록을 손으로 적지 않는다.** 원본은 `packages/core`의 `APP_KEYS`이고, `.mjs`는
    `scripts/app-keys.mjs`로 읽는다. `verify-app-coverage`가 어긴 자리를 실패시킨다.
    **검사기에서 이 병이 특히 나쁘다** — 빠진 앱은 통과한 것이 아니라 검사받지 않았는데
    화면에는 "ALL PASS"가 찍힌다(2026-08-06에 여섯 곳이 그랬다).
12. **모르는 값을 그럴듯한 기본값으로 바꾸지 않는다.** 지표 RPC가 모르는 앱을 naminglink로
    바꿔 남의 매출을 보여 주고 있었다. 0은 눈에 띄지만 남의 숫자는 그럴듯해서 안 띈다.
13. **폴백은 사실이어야 한다.** 폴백은 안 쓰이는 값이 아니라 **DB를 못 읽는 순간 법적 고지가
    되는 값**이다. 사업자 정보가 앱마다 따로 적혀 셋 다 틀린 채 라이브에 나갔다.

---

## 6. 문서 인덱스 (`naminglink/docs/`)

| 파일 | 용도 |
|---|---|
| `IMPLEMENTATION_TICKET_saju_dream.md` | 스캐폴딩 지시(복제→apps/*, 재활용맵) |
| `platforest_saju-link_spec_2026-08-04.md` | saju 상품·도메인 스펙 |
| `sajulink_domain_logic.md` | 오늘의운세 스코어링 규칙 + 해설 프롬프트 설계 |
| `sajulink_prompt_copy.md` | 해설 프롬프트 문자열·I/O·무료 템플릿 |
| `saju_scoring_defaults.json` | 스코어링 가중치·오행 행운맵 |
| `saju_report_structure.md` | PDF 구조안 — **낡았다**(단일 상품 9장으로 재작성 대상) |
| `dreamslink_domain_logic.md` | 해몽 상징사전·매칭·프롬프트 |
| `dream_symbols.seed.json` + `.batch2.json` | 해몽 상징 209개 시드 |
| `saju_dream_backgrounds.html` | 웹 배경 CSS/SVG |
| `image_prompts_saju_dream.md` | AI 이미지 프롬프트 |
| `operator_console_multiservice.md` | 멀티서비스 운영자 콘솔 설계 |
| `sajulink_dreamslink_acquisition_brief.md` | 애드센스 정책·경쟁·획득 전략 |
| `programmatic_seo_plan.md` | 시드→랜딩페이지 SEO 설계 |
| `multilingual_keyword_research.md` | ja/zh/vi/es 키워드·경쟁 |
| `launch_marketing_plan.md` | TikTok 공유카드·커뮤니티 시딩 |
| `WORKLOG_2026-08-04-2.md` / `-3.md` | Phase 1 · 리테마·리포트 경과 |
| **`SAJU_DREAM_BUILD_STATUS.md`** | **(본 문서) 현재 상태·핸드오프** |

---

## 7. 남은 일 (2026-08-06 기준)

**순서를 바꿀 수 없는 자리가 있다.** ⑥이 장수를 정해야 ⑦이 그 값으로 고시를 쓴다. ⑧은 ⑥·⑦이
만든 규칙을 세는 일이라 마지막이다. 그 밖은 언제 해도 된다.

### 끝난 것 (2026-08-06)

- [x] **⑤-2 사주/오늘 화면 분리** — `/today/result` 신설(커밋 `31e6992`)
- [x] **⑥+⑦ 상품 하나로 + 리포트 9장 + 약관 23로케일**(커밋 `1db7b14`)
- [x] **⑧ 검사기 재조준** — ⑥에서 함께. 장수 기준을 목차 줄 수 → `REPORT_PAGE_COUNT`,
      상품 코드, 셀프 광고 절 예외, `premiumReport` 검사 제외
- [x] **결제 패널 배선** — 결과 화면이 실제 패널을 부른다(커밋 `a01b14a`)
- [x] **마이그레이션 적용** — 주문 종류·상품 2행(`enabled=false`) · `ai_usage_logs.app`
- [x] **AI 원가 기록** — 사주링크가 `ai_usage_logs`에 한 줄도 안 쓰고 있었다
- [x] **레이트리밋 스코프** — `analytics` 하나를 세 앱이 공유하고 있었다
- [x] **리뷰 결함 셋**(커밋 `65fccad`) — 아래
- [x] **결제 복귀 두 구멍 + 검사기**(두 앱) — 아래 「결론 난 것」
- [x] **결제수단을 사기 전에 알림** — 화면 언어가 결제사를 정하므로(ko=토스 / 나머지=페이팔),
      영어로 보는 한국 이용자에게는 페이팔만 뜬다. 누르기 전에 보이게 버튼 아래 브랜드 이름을
      적었다. **설명 문구를 붙이지 않은 것은 고의다** — 붙이면 23로케일이 함께 걸리는데,
      필요한 정보는 "무엇으로 결제되는가" 하나뿐이고 브랜드 이름은 고유명사라 번역이 없다
- [x] **`regionForLocale` 두 벌에 상호 참조 주석** — 같은 규칙이 서버(`report-product.ts`)와
      패널에 따로 있다. 그 파일이 `server-only`라 브라우저가 부를 수 없어서인데, 한쪽만 고치면
      **화면이 보여 준 가격과 서버가 만드는 주문이 어긋나고 타입이 같아 컴파일러가 못 잡는다**

### 리뷰가 잡은 것 — 셋 다 조용한 결함이었다

1. **토스 승인 라우트가 없어진 주문 종류를 찾고 있었다**(`SAJU_CHONGUN_PDF`). 켰다면 결제는
   승인되고 주문은 못 찾는다. 타입이 그 파일 안에서 닫혀 있어 tsc도 검사기도 안 잡았다
   → [[file-local-types-hide-drift]]
2. **약관 원문에 두 상품 문장이 셋 남아** 있었고 21로케일이 그것을 충실히 옮겼다. 번역이
   아니라 원문이 틀렸다
3. **번역 캐시 키가 순번**이라 ko를 고쳐도 옛 번역이 나왔다(스크립트는 `ok`를 찍는다)
   → [[translation-cache-must-be-content-keyed]]

### 리포트 — 아홉 장

  1~3  표지·요약 / 성향 / 원국·오행
  4    일간의 강약과 지금 필요한 기운   ← 무료 화면에 없는 것이 여기서 시작된다
  5~6  왕상휴수사 + 십신 표 / 십신 심화(두터운 둘·없는 둘)
  7~9  삶의 네 영역(원국 기준) / 근거표·부록 / 올해 총운

장수는 `src/lib/report-pages.ts`의 `REPORT_PAGE_COUNT` 하나가 정하고 **렌더러·검사기·약관·
안내 문서가 모두 그 값을 본다.** 바꿀 때는 그 파일 주석의 네 단계를 따를 것.

### 지금 상태 — 이어받을 때 먼저 볼 것

- **push 완료**(2026-08-06, `0f56504`까지). 다른 기기에서 `git pull`로 그대로 이어받는다.
  sajulink는 Vercel에 연결돼 있어 **이 push가 곧 배포**다 — 상품은 `enabled=false`라 판매는
  안 열리지만 화면 구조·약관·PDF는 실제로 바뀌었다
- tsc 0(세 앱) · `next build` 성공 · 검사기 11종 통과 · 견본 63벌 전부 9장
- 운영 DB에 마이그레이션 2건 **적용 완료**. 상품 2행은 `enabled=false`(다크 런치)
- 중단해도 안전한 지점이다

### 결론 난 것 — 결제 복귀 (2026-08-06)

**의심은 사실이었고, 구멍이 하나가 아니라 둘이었다.**

1. **사주 — 되돌리는 코드가 아예 없었다.** `PENDING_KEY`를 쓰기만 하고 읽는 곳이 없어, 국내
   결제 후 결과 화면이 입력을 못 읽고 "결과를 읽을 수 없습니다"로 끝났다.
2. **사주·인연 둘 다 — 발급을 이어받는 자리가 없었다.** 승인 라우트가 `?payment=paid&orderId=`
   로 되돌려 보내는데 그것을 읽는 코드가 없어, **돈은 나가고 파일은 안 나온다.** 이용자가
   버튼을 다시 누르면 **새 주문이 하나 더** 생긴다(이중 결제).

고친 자리:

- 사주 `lib/pending-payment.ts` 신설 — 키·맡기기·되돌리기를 한 파일로. 되돌리기는
  `use-result-fragment`에 걸어 **두 결과 화면이 자동으로** 받는다(화면마다 두면 또 한쪽이 빠진다)
- 두 앱 패널에 복귀 처리 추가. 쿼리를 지울 때 **프래그먼트는 남긴다** — 입력값이 거기에만 있어
  함께 지우면 새로고침에 결과가 사라진다
- `download()`가 `Checkout` 전체 대신 `{orderId, paymentId}`만 받는다(복귀 자리엔 결제창 정보가 없다)

**왜 세 앱에서 반복되는가 — 구조는 같은데 코드가 공유되지 않는다.** `packages/core`에는 엔진·
PDF·env만 있고 **결제 흐름은 앱마다 사본**이라 고칠 자리가 늘 세 벌이다. 게다가 베낀 것이
원본이 아니라 이미 한 세대 열화된 사본이었다 — naminglink는 복귀 처리를 세 패널에 각각 손으로
적었고, 인연은 그중 어느 것도 옮기지 않고 새로 쓰며 되돌리기만 넣었고, 사주는 그 인연을 베끼며
되돌리기마저 흘렸다. **빠진 것이 타입이 아니라 "있어야 할 코드"라 tsc가 못 잡는다.**

→ `scripts/verify-payment-return.mjs` **신설**(대조군 내장). 토스를 여는 화면 5개 전수로
이어받기를 요구하고, 프래그먼트로 입력을 나르는 앱에는 맡기기·되돌리기가 **짝을 이루는지** 센다.

→ 같은 종류로 **`verify-route-guards.mjs`가 사주링크를 안 보고 있었다.** 그동안의 "ALL PASS"는
세 앱 중 둘에 대한 말이었다. 사주를 넣어도 통과해서(본문 라우트 19→25개) 그대로 넣었다.
**`verify-reachable-links.mjs`도 인자가 두 앱만 받는다** — 서버가 떠 있어야 해서 아직 못 고쳤다.

### 이어서 할 일

- [ ] **`saju_report_structure.md` 재작성** — 총운 3장/프리미엄 5~6장으로 적힌 낡은 문서
- [ ] **`premiumReport` 사전 절 제거** — 상품이 하나가 되며 쓰이지 않는다(23로케일).
      `i18n.ts`의 타입에서 지우면 21개 파일이 컴파일에서 걸리므로 그 목록대로 지우면 된다
- [x] **`saju-link.com` 연결 완료(2026-08-06)** — 아래 「도메인 연결」
- [ ] **상품 켜기** — 결제 실키 등록 후 관리자 화면에서 `enabled=true`
- [ ] **실결제 검증** — 토스·페이팔 각 1건. 특히 **토스 복귀 경로**를 실제로 밟아 볼 것.
      코드는 고쳤고 검사기도 붙었지만 **실제로 밟아 본 적은 없다**(화면 테스트로는 잘 안 밟힌다)
- [ ] **`verify-reachable-links.mjs`에 사주 더하기** — 인자가 두 앱만 받는다. 서버가 떠 있어야
      돌아가는 검사기라 이번에 못 했다(`verify-app-coverage`도 인자 목록은 못 잡는다)
- [ ] **`OPS_STATUS_TOKEN` 넣고 콘솔에서 확인** — 세 앱에 같은 값 + naminglink에
      `INYEONLINK_BASE_URL`. 사주용 주소 변수(`SAJULINK_BASE_URL`)도 이제 콘솔이 읽는다

### 도메인 연결 — `saju-link.com` (2026-08-06)

**한 번의 재배포로 끝난다.** `NEXT_PUBLIC_SITE_URL` **하나가 스위치**라(`lib/seo.ts`) 값이
`*.vercel.app`이면 `robots.ts`가 색인을 통째로 닫고, 실 도메인으로 바꾸면 robots·canonical·
hreflang·OG·sitemap이 **한꺼번에** 넘어간다. 따로 켜는 스위치를 안 만든 이유가 그것이다.

- DNS는 **A `@` → `216.198.79.1`**(가비아, TTL 3600). **naming-link 때의 `76.76.21.21`을 그대로
  쓰면 안 된다** — Vercel이 신규 도메인에 주는 값이 바뀌었다. 화면에 뜨는 값이 정답이다
- Vercel 「Add Domains」에서 **apex를 입력한다.** www를 넣으면 "A domain cannot redirect to
  itself"가 난다. **`Redirect apex domains to www`는 체크 해제** — 그 항목은 *www를 정본으로*
  삼겠다는 뜻이라, apex를 정본으로 쓰는 우리 설정(`NEXT_PUBLIC_SITE_URL`이 apex)과 어긋난다
- **www는 308(Permanent)로 걸 것.** 기본값이 307(Temporary)인데, 그러면 검색엔진이 www를 apex로
  합치지 않고 따로 색인할 수 있다. naming·inyeon 둘 다 308이라 대조로 잡았다
- 환경변수는 `NEXT_PUBLIC_SITE_URL`과 `NEXT_PUBLIC_ADSENSE_CLIENT`를 **한 번에** 넣고 재배포를
  한 번만 한다(둘 다 빌드 타임 인라인). **빌드 캐시를 끄지 않으면 값이 안 박힌다**
- 검증 결과 — robots `Disallow: /` → **개방**, canonical `saju-link.com`, sitemap **576 URL**
  (옛 주소 0건), ads.txt 200, 스크립트 전 페이지 `<head>`, **광고 유닛 0개**(다크 런치 유지),
  방침에 「쿠키와 광고」 절 켜짐
- 서치 콘솔은 **도메인 속성**(TXT `@`)으로 잡았다 — www·http·하위 경로가 한 번에 커버된다.
  A 레코드와 공존하므로 지우지 말 것. 사이트맵이 제출 직후 **「가져올 수 없음」**으로 뜨는 것은
  아직 한 번도 안 읽었다는 뜻이다(응답 200·`application/xml`·XML 파싱 OK로 확인). 재제출 금지

### 애드센스 (2026-08-06 정리)

- naming-link.com은 **코드 스니펫으로 소유권 확인 완료**(소유권 확인과 ads.txt 상태는 별개 지표다)
- ⚠️ **ads.txt 상태가 되돌아갔다.** 2026-07-30에 「승인됨」으로 확인됐던 것이
  (→ [[real-domain-and-adsense-live]]) 2026-08-06 현재 「찾을 수 없음 / 2026-07-29」로 보인다.
  **원인 미상.** 확인된 것은 이것뿐이다:

  - 라이브는 정상 — 200 · `text/plain` · 올바른 퍼블리셔 ID · AdsBot·Googlebot UA도 200 ·
    apex·www·http 모두 정규 주소로 200
  - `robots.txt`가 `/ads.txt`를 막지 않는다
  - 코드 쪽 원인 없음 — `adsEnabled`는 `NEXT_PUBLIC_ADSENSE_CLIENT` 하나만 보고 그 값은
    Vercel에 있다. 2026-08-06 push로 naminglink가 재배포됐으나 ads.txt·robots·미들웨어는
    건드리지 않았고, 지금 200이 나오는 것이 그 증거다

  콘솔 쪽 사정(사이트 재등록으로 행이 초기화됐다거나)일 가능성이 남아 있으나 **확인 못 했다.**
  광고 수익 배분에 관한 지표이고 사이트 심사와는 별개 절차다

  **단서 하나** — 콘솔 문구는 "사이트가 *마지막으로 크롤링되었을 때*"이고, 거기 찍힌 날짜가
  **2026-07-29**다. 「승인됨」이 뜬 07-30보다 하루 **앞선다.** 파일이 사라진 것이 아니라 그
  이전 크롤 결과를 다시 보여 주고 있는 것으로 보인다. 다만 이것도 추정이고, 확인된 것은
  "지금 라이브가 정상"뿐이다. **손댈 것은 없고 재크롤을 기다리는 자리다.**
- **사이트 확인은 코드 스니펫으로 한다.** ads.txt로 고르면 재크롤을 며칠 기다리게 된다

**세 사이트 모두 신청 완료 — 대기가 병렬로 돈다(2026-08-06).** 콘솔 상태는 셋 다 「준비 중」
(= 사이트 검토 대기, **이것이 진짜 관문**)이고, ads.txt 열은 셋 다 「찾을 수 없음」이다. 마지막
크롤 시각이 설명해 준다 — inyeon 08-06 07:24 · saju 08-06 07:57로, **둘 다 환경변수를 넣고
재배포하던 그 시간대**다. ads.txt가 살아나기 전에 찍힌 사진이다. naming은 07-29 그대로이고 그
뒤로 재크롤되지 않았다(7-30 「승인됨」과는 여전히 어긋나 **확인 못 한 채로 남긴다**).

같은 시각 AdsBot UA로 직접 재면 **세 사이트 다 200에 올바른 게시자 ID**다. 손댈 것이 없고,
재크롤을 기다리는 자리다. **사이트를 지웠다 다시 추가하지 말 것** — 검토가 처음부터 다시 돈다.
- **inyeon-link.com 준비 완료(2026-08-06).** `NEXT_PUBLIC_ADSENSE_CLIENT`를 넣고 재배포해
  ads.txt 200 · 스크립트 부착 · robots 개방을 확인했다. **지금 사이트 추가하면 된다.**
  - 부수 효과 둘을 함께 확인했다 — 방침에 「쿠키와 광고」·「Google AdSense」 절이 켜졌고
    (`legal-content.ts`가 `adsEnabled`를 따라간다), **광고 유닛은 여전히 0개**다(슬롯 ID를
    안 넣어 자리는 잠긴 채. 다크 런치가 유지된다)
- 게시자 ID는 계정 단위라 **세 사이트가 같은 값**을 쓴다(`ca-pub-5084236528895241`,
  ads.txt에서는 `ca-` 없이). 공개 값이라 `NEXT_PUBLIC_`이다. **슬롯 ID는 넣지 않는다**
- naming 승인을 기다리지 말고 **inyeon은 지금 신청**해도 된다(이미 라이브). 대기가 병렬로 돈다
  → [[sajulink-domain-and-adsense-plan]]

### 작업 방식 메모 (이번에 알게 된 것)

- **약관 번역은 로케일당 3~5분**이다. 배경으로 직접 띄운 작업은 자꾸 중단됐고, **전경에서
  시작해 자동으로 배경 전환된 것**은 완주했다. 2~4개씩 나눠 돌릴 것
- 캐시가 내용 기반이 된 뒤로는 **바뀐 문단만** 다시 부르므로 훨씬 빠르다
- ko 약관을 고쳤으면 `extract-ko-legal` → `translate-legal`(전 로케일) →
  `verify-legal-locales` 순서. 구조 검사기는 내용을 안 보므로 **옛 표현을 grep**할 것

### 그다음

- [ ] dreamslink 착수 — **애드센스 승인 시점이 사실상 개발 마감**이다(§4)
