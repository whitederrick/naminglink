# 2026-09-08 작업 기록

전날(09-07)에 이어 드림링크 밀러 배치 작업을 계속했다 — 20판 묶음(268~287)의
나머지(284~287)를 마치고 전수 스윕·push·배포 확인까지 끝냈다. 배치 268~283의
기록은 `docs/WORKLOG_2026-09-07.md`에 있다.

## 새 20판 묶음(268~287) 완료 — 배치 284~287(17~20/20)

- **284(United States Mail Box~Usurper)**: 새 상징 다섯(mailbox·
  urgent-petition·urinal·usurer·usurper), 기존 mystery(수수께끼)·
  urine(오줌)·jar(항아리)에 문맥을 나눠 붙였다. Unknown은 각주
  [234] See Mystery로 mystery에 합쳤다. Urn은 jar가 이미 영어 별칭
  "urn"을 쥐고 있어 합쳤는데, 둘째 문장(깨진 단지)은 기존 「깨진
  항아리를 봄」과 같은 그림이라 §31로 건너뜀.
  **프로브에서 넷 걸림** — mystery의 새 문맥 둘은 "낯선 이"를 가리키는
  별칭이 아예 없어(기존 별칭이 "수수께끼"·"신비" 계열뿐) 안 걸렸고,
  그중 하나는 discriminator도 실제 시험 문장에 없는 낱말("그림자")을
  써서 동점으로 엉뚱한 뜻으로 갔다. usurper의 두 새 문맥도 "찬탈자"라는
  낱말이 아예 안 나오는 자연스러운 문장이라 안 걸려 별칭·문장을 다시
  지었다. 지킴 케이스에서 jar의 "빈 항아리를 봄"도 걸렸는데, 1음절
  판별어("빈")의 구조적 한계(§3)라 데이터가 아니라 시험 문장을 기존
  "텅빈" 표기로 맞췄다. urine은 m284가 기존 오줌 출처(r3)보다 사전순으로
  앞서 기본값이 바뀔 뻔했으나, 서로 다른 문헌(주공해몽 길조 vs 밀러
  흉조)의 다른 판단이라 있던 답을 그대로 얼렸다.
  수치: 상징 1,941개 · 의미 6,692개 · 인용 6,727개.

- **285(Vaccinate~Varnishing)**: 새 상징 여섯(vaccinate·vagrant·
  valentine·valley·vapor-bath·varnishing). Vagrant는 기존 beggar(거지)가
  영어 별칭 "a vagrant"를 쥐고 있었으나 개념이 달라(구걸 vs 떠돎) 새
  상징을 세우고 그 별칭을 beggar에서 회수했다. beggar의 「거지에게
  적선함」(흉)과 Vagrant의 「부랑자에게 베풂」(길)이 같은 그림에 정반대
  극성인데, 서로 다른 헤드워드의 각자 판단이고 상징 이름도 달라
  discriminator 충돌이 없어 §31 건너뛰기 없이 둘 다 남겼다.
  **프로브 15건 전부 1차에서 통과** — 이 배치는 재수정 없이 깔끔했다.
  수치: 상징 1,947개 · 의미 6,707개 · 인용 6,742개.

- **286(Vase~Vehicle)**: 새 상징 셋(vase·vat·vatican), 기존 grave(무덤)·
  cash-box(금고)·vegetables(채소)·carriage(마차)에 문맥을 나눠 붙였다.
  밀러가 Vault 한 표제어에 두 뜻(납골 무덤 vs 귀중품 금고)을 섞어 써서
  첫 문장은 grave에, 나머지 둘은 cash-box에 나눴다 — 첫 문장을 cash-box에
  넣으면 기존 「금고를 봄」(안전, 길조)과 같은 그림에 정반대 극성이라
  grave 쪽으로 돌렸다. Vehicle의 첫 문장(탈것을 타고 있음)은 기존
  carriage 「마차를 타고 감」과 같은 그림이라 §31로 건너뜀.
  **프로브에서 하나 걸림** — carriage의 새 문맥 "부서진 마차를 봄"이
  기존 "마차를 봄"의 판별어 "보았"과 겹쳐 동점으로 밀렸다. "보았"은
  관찰형 문맥마다 재발할 흔한 낱말이라 기존 판별어에서 아예 빼고 "세워
  놓여"만 남겼다. vegetables는 m286이 기존 채소 출처(zhougong)보다
  사전순으로 앞서 기본값이 바뀔 뻔했으나 서로 다른 문헌의 다른 판단이라
  있던 답을 그대로 얼렸다.
  수치: 상징 1,950개 · 의미 6,723개 · 인용 6,758개.

- **287(Veil, 단독 — 20판 묶음 마지막 판)**: 새 상징 veil(베일) — 문맥
  아홉 개짜리 큰 표제어라 §33처럼 단독 배치로 뗌.
  **verify-dream-km이 처음으로 "동점 위험" 구조 위반을 그 자리에서
  잡았다** — 「신부의 베일을 봄」·「처녀가 신부 베일을 씀」 두 형제가
  둘 다 "신부"를 말해, 판별어를 "신부의"(관찰) 대 "썼다"(행동)로
  갈랐다가 프로브에서 걸렸고("스스로 베일을 썼다"가 "썼다"에 낚여
  신부 문맥으로 감), "처녀가 신부"(두 낱말)로 고치자 이번엔 검사기가
  "신부의"⊃"신부" 부분 문자열 겹침을 잡았다. 최종적으로 "처녀가"
  (형제 중 이 문맥만 갖는 낱말) 하나로 좁혀 프로브·검사기 둘 다
  통과시켰다.
  수치: 상징 1,951개 · 의미 6,732개 · 인용 6,767개.

## 20판 묶음(268~287) 마감 — 전수 스윕 · push · 배포 확인

- **전수 스윕**(`node scripts/audit-verifiers.mjs`, 포그라운드로 직접
  돌려 `\| tail` 없이 본문 143줄 전부 확인): 검사기 110개 · 실행 121회 ·
  통과 116 · 검사 안 됨 7 · **빨간불 5건**.
  - 4건은 이 작업과 무관한 기존 상시 항목 그대로다 —
    `audit-dream-ownterms-multiword.mjs`(폐기된 옛 사전을 읽는 늘-빨간불
    스크립트) · `verify-company-display.mjs`·
    `naminglink/verify-locale-manifest.ts`(통신판매업 번호 표기 기존
    사안) · `verify-rendered-locale.mjs`(naminglink vi/ar 로케일,
    dreamslink와 무관).
  - **새로 하나(`verify-declared-urls.mjs`)가 나왔다** — naminglink의
    `/guide/hanja/ma`·`/guide/hanja/pa` sitemap 항목이 404다. **이번
    20판 묶음이 건드린 파일은 전부 `apps/dreamslink/*`와 루트 스크립트
    하나(`scripts/build-dream-dictionary-v2.mjs`)뿐이라 이 hanja 가이드
    문제와는 무관** — naminglink 쪽에서 별도로 다뤄야 할 기존 결함으로
    본다.
- **git push**(`0fd1a883..7ddc5b59`) 완료 — 커밋 29개.
- **배포 확인**: `gh api .../commits/<sha>/status`로 네 앱 모두
  `success` 확인 후, **`npx vercel inspect <id> --logs`로 실제 로그를
  열어** naminglink·sajulink·inyeonlink 셋은 `Ignored Build Step` →
  Canceled(정상 스킵), dreamslink만 `Build Completed` → Ready(정상
  재빌드)를 확인했다. 새 상징 주소(veil·vaccinate·train·umbrella)로
  push 전 404 → push 후 200 확인. `truss`는 rupture 상징에 합쳐진
  것이라 애초에 자기 주소가 없는 것이 맞다 — 404가 정상이다.

## 20판 묶음(268~287) 최종 수치

상징 **1,951개** · 의미 **6,732개** · 인용 **6,767개**.

**다음 20판 묶음은 밀러 표제어 `Vein`부터 시작한다**(Veil 바로 다음
표제어). 절차·되풀이 실수 목록은 메모리
`dream-dictionary-v2-rebuild-in-progress`에 있다.

## 새 20판 묶음(288~307) 진행 중 — 배치 288(1/20)

- **288(Vein~Vessels)**: 새 상징 다섯(vein·velvet·veneer·ventriloquist·
  vertigo), 기존 balcony(발코니)·grasshopper(메뚜기)·boat(배)에 문맥을
  나눠 붙였다. Veranda는 이미 별칭 "베란다"가 있어 balcony에, Vermin은
  각주 [235] See Locust로 grasshopper에(이미 "locusts"를 담론), Vessels는
  각주 [236] See Ships로 boat에 합쳤다.
  **기본값 플립 둘을 받아들였다** — balcony·boat 둘 다 m288이 각자 기존
  출처보다 사전순으로 앞서 기본값이 바뀌었는데, 옛 답은 「연인·작별」·
  「맑은 물」처럼 조건이 붙은 그림이고 새로 들어온 「베란다에서 근심거리를
  성공으로 이끎」·「여러 배를 봄」은 조건 없이 더 막연한 그림이라 그쪽으로
  얼렸다(`FALLBACK_FIRST`에 이유와 함께 등록).
  **프로브에서 둘 걸림** — velvet의 "처녀가 벨벳 옷을 입음"이 "스스로
  벨벳을 입음"의 판별어 "입었"과 동점이 나서 "처녀가 옷을"(두 낱말)로
  좁혔다. grasshopper의 "해충을 없애지 못함"은 판별어 "못하는"(현재
  분사)이 시험 문장 "못했다"(과거)와 활용형이 안 맞아 안 걸려, 활용에
  안 갈리는 어간 "없애지"로 바꿨다.
  **verify-dream-km의 "동점 위험" 구조 검사**(287에서 처음 도입)는 이번
  배치에서는 위반 0건.
  수치: 상징 1,956개 · 의미 6,751개 · 인용 6,786개.
  커밋 `a301307a`.

- **289(Vexed~Vineyard, 2/20)**: 새 상징 여섯(vice·victim·victory·village·
  vinegar·vineyard). Vexed·Vicar·Vine 셋은 새 상징을 세우지 않고 기존
  annoy(성가심)·clergyman(목사)·grape(포도)에 문맥을 나눠 붙였다 — Vexed는
  annoy가 이미 영어 별칭 "vexation"·한국어 별칭 "짜증"을 쥐고 있었고,
  Vicar는 clergyman이 이미 "목사"라는 이름을 쥐고 있었고, Vine은 새
  상징을 세우면 term_ko "포도나무"가 PARTICLES의 "나" 때문에 grape와
  동시에 걸리는 품음 결함이 나서 셋 다 병합했다(Vicar의 혼인 문장은
  기존 "목사와 혼인함"과 같은 그림이지만 판단 초점이 달라 cite를 포개지
  않고 문맥 이름을 갈라 별개 의미로 남김).
  **기본값 플립 둘 중 하나만 받아들였다** — annoy는 있던 답을 그대로
  지켰고(신·구 둘 다 비슷하게 막연해 바꿀 이유 없음), grape는 m289가
  사전순으로 앞서 바뀐 「포도 덩굴 꿈을 꿈」을 받아들였다(옛 「포도를
  먹음」은 행동이 붙은 그림, 새 답은 조건 없이 그냥 꿈꾸는 그림).
  **프로브에서 넷 걸림** — "포도덩굴"(붙여쓰기)이 bare "포도" 뒤에 조사가
  아닌 "덩"이 와 `isStandalone`에 막혀 안 걸려 별칭 "포도덩굴"을
  추가했고, annoy의 옛 문맥("성가신 일을 겪는 꿈")이 term_ko "성가심"과
  형태가 달라(성가신 vs 성가심) **애초부터 안 걸리고 있던 것**을 별칭
  "성가신" 추가로 고쳤다(§29류 — 심어진 뒤로 한 번도 안 걸린 자리, 신·구
  문맥 모두 지킴 케이스로 확인). 죽은 낱말 둘(village의 "적", vinegar의
  "썼")도 1음절 필터에 걸려 고쳤다.
  수치: 상징 1,962개 · 의미 6,772개 · 인용 6,807개.
  커밋 `e29cab2f`.

- **290(Violence~Virgin, 3/20)**: 새 상징 셋(violence·violets·virgin).
  Violin은 기존 fiddle(바이올린, term_ko가 이미 같음)에, Viper는 기존
  adder(독사, 영어 별칭에 이미 "viper"가 있음 — snake·asp도 같은 별칭을
  쥠)에 문맥을 나눠 붙였다. Violin 첫 문장은 기존 fiddle의 「바이올린을
  봄」과 같은 그림이라 §31로 건너뜀.
  **기본값 플립 하나 거절** — fiddle은 m290이 사전순으로("m290" <
  "m34") 앞서 기본값이 바뀔 뻔했으나 옛 「바이올린을 봄」이 새로 들어온
  「젊은 여성이 바이올린을 연주함」보다 훨씬 막연해 그대로 지켰다.
  virgin의 term_ko는 "처녀"(이 사전 수백 문맥에 쓰이는 일반 낱말, 아무
  상징에도 등록 안 돼 있어 그대로 쓰면 무관한 문장에도 걸릴 위험) 대신
  "순결"로 세우고, "동정"은 "동정심"(공감)과 동형이의어라 별칭에서 뺐다.
  **프로브에서 셋 걸림** — virgin의 "순결한"·"순결하다고"가 이름 뒤에
  조사가 아닌 "한"·"하"가 와 안 걸려 두 활용형을 별칭으로 추가했다.
  수치: 상징 1,965개 · 의미 6,786개 · 인용 6,821개.
  커밋 `5b01a1f7`.

- **291(Visit~Voice, 4/20)**: 새 상징 셋(visit·vision·voice). Vitriol은
  기존 acid(산, 영어 별칭에 이미 "황산"이 있음)에 문맥 넷을 붙였다.
  vision의 term_ko는 "환상"으로 세웠다 — "환영"은 이미 entertainment
  (welcome)·phantom 둘이 나눠 쥔 동형이의어라 세 번째로 얹지 않았다.
  voice의 "목소리"는 called·bass-voice·dumb 셋이 이미 별칭으로 쥐고
  있지만 다들 다른 구체적 상황이라 함께 걸려도 무방하다고 봤다.
  **verify-dream-km이 새 결함 유형 둘을 잡았다** — visit의 en
  "pleasant"/"unpleasant" 부분 문자열 겹침, voice의 en 판별어가 상징
  자기 이름("voice")을 그대로 담고 있던 것(점수에서 자동으로 빠지는
  자리). 둘 다 다른 낱말로 고쳤다.
  **프로브에서 넷 걸림** — visit의 "방문"이 뒤에 조사가 아닌 "했"이
  와 안 걸려 "방문했다"·"방문했는데"를 별칭으로 추가했다.
  **verify-dream-cite가 둘 잡음** — voice 두 항목이 원래 세미콜론으로
  이어진 한 문장인데 둘로 나누며 구두점(.→;)·대문자(High→high)를
  바꿔 원문 불일치로 걸려, 원문 그대로(세미콜론·소문자) 되돌렸다.
  수치: 상징 1,968개 · 의미 6,806개 · 인용 6,841개.
  커밋 `92399ff5`.

- **292(Volcano~Vultures, 5/20 — 밀러 V 항목 완료)**: 새 상징 둘
  (volcano·vultures). 나머지 다섯(Vomit·Vote·Voucher·Vow·Voyage)은
  전부 기존 상징(vomiting·election·document·oath·long-journey)에
  합쳤다 — 다섯 다 term_ko나 영어 별칭이 이미 겹쳐 있었다. vultures는
  term_ko를 "대머리수리"로 세웠다 — 그대로 "독수리"를 쓰면 기존
  eagle과 완전히 겹친다.
  document·oath·election 셋은 원래 의미가 하나뿐이라 판별어 표가
  비어 있었는데, 의미를 더하며 그 빈자리를 채워야 했다(§30 곁가지).
  **기본값 플립 둘 중 하나만 받아들였다** — vomiting은 지키고(둘 다
  똑같이 막연해서), document는 받아들였다(새 답이 더 막연해서).
  **프로브에서 일곱 걸림** — vomiting("구토하는"·"토하는"·"토해내고"
  활용형 안 걸림, "토했다" 기존 별칭도 뒤에 "다"가 와 한 번도 안
  걸리고 있던 것 발견), election("투표했다" 활용형), oath("서약"
  동의어 안 걸림), long-journey("항해했다" 활용형). 고치는 과정에서
  "피를 토함"의 판별어가 방금 고친 "토했다" 별칭과 겹쳐 기본값을
  가로채, "갑작스레"로 다시 갈랐다.
  **이것으로 밀러 원문 V 항목이 전부 끝났다** — 다음은 W(Wading)부터.
  수치: 상징 1,970개 · 의미 6,825개 · 인용 6,860개.
  커밋 `dc52267c`.

- **293(Wading~Wages, 6/20 — 밀러 W 항목 시작)**: 새 상징 셋(wadding·
  wafer·wages). Wading은 각주 [237] See Bathing으로 기존 bathing(목욕)
  에, Wager는 기존 bet(내기, 영어 별칭에 이미 "placing a wager")에
  문맥을 붙였다. wages는 "임금"이 king·royal-court와 동형이의어라
  "급여"로 세웠다.
  Wading 넷은 "걸어 다님"으로 적어 기존 bathing의 "목욕함"류 문맥과
  그림을 갈랐고, 판별어도 기존 "차갑고 맑은 물"·"목욕물이 흐림"과
  겹치지 않는 새 낱말로 나눴다(en도 "muddy"·"cloudy" 중복 두 차례
  재조정).
  **프로브에서 latent 버그 둘을 새로 찾았다** — bathing의 term_ko
  "목욕"이 "물속을 걸어"류 문장에 아예 안 걸려 별칭을 추가했고, 기존
  "목욕물이 흐림"의 판별어("흐린·탁한·뿌연·흐려")가 자연스러운
  "흐렸다"(과거형)와 활용이 안 맞아 **심어진 뒤로 한 번도 안 걸리고
  있던 것**을 "흐렸다" 추가로 고쳤다.
  수치: 상징 1,973개 · 의미 6,841개 · 인용 6,876개.
  커밋 `090fb94c`.

- **294(Wagon, 7/20 — 단독 배치)**: 기존 wagon(짐수레) 상징(배치 32에서
  Driving 표제어의 「짐수레를 몲」 하나만으로 세워졌던 것)에 밀러 원
  Wagon 표제어의 문맥 아홉을 붙였다 — term_en이 똑같아 자연히
  합쳐졌다. 문맥이 아홉이라 이번 배치는 이 표제어 하나로 뗐다.
  죽은 낱말 하나(「위험한 둑」의 "둑" — 1음절이라 엔진이 안 셈)를
  "제방"으로 고쳤다. **프로브는 1차 시도에서 열 건 전부 통과** — 이번
  배치에서 새로 걸린 함정은 없었다.
  수치: 상징 1,973개 · 의미 6,850개 · 인용 6,885개.
  커밋 `628feb97`.

- **295(Wagtail~Wake, 8/20)**: 새 상징 다섯(wagtail·waif·waist·
  shirt-waist·waiter), 기존 weeping(울음)·condolence-visit(문상)에
  문맥을 나눠 붙였다. Wail은 각주 [238] See Weeping로 weeping에
  합쳤다. Wake는 처음엔 새 상징 "밤샘"으로 세우려다 ⓪ grep에서
  condolence-visit이 이미 영어 별칭 "wake"를 쥐고 있는 것을 찾아
  그쪽에 합쳤다 — term_ko를 "문상"에 맞췄다.
  **기본값 플립 하나 거절** — condolence-visit은 m295가 기존 문상
  출처(km4, 주공해몽)보다 사전순으로 앞서 기본값이 「상갓집에서 밤을
  지새움」(철야라는 조건이 붙은 그림)으로 바뀔 뻔했으나, 옛 「남에게
  문상을 감」이 조건 없이 더 막연해 FALLBACK_FIRST로 그대로 얼렸다.
  **프로브에서 하나 걸림** — weeping의 새 문맥 둘("꿈속에서 울부짖는
  소리를 들음"·"젊은 여성이 울부짖는 소리를 들음")이 판별어
  "울부짖는 소리"를 공유해 동점으로 앞의 것에 밀렸다. 겹치지 않는
  낱말("꿈속에서")로 갈라 고쳤다. 의미가 하나뿐이던 condolence-visit은
  셋으로 늘며 판별어 표를 새로 채웠다(§30 곁가지).
  수치: 상징 1,978개 · 의미 6,864개 · 인용 6,899개.
  커밋 `a690ce88`.

- **296(Walking~Wallet, 9/20)**: 새 상징 둘(walking·walking-stick),
  기존 pocketbook(지갑)에 문맥을 나눠 붙였다. Wallet은 pocketbook이
  이미 term_ko "지갑"을 쥐고 있어 합쳤다. Walking은 각주 [239] See
  Wading가 있었지만 실제 그림(가시밭길·밤길·빠른 걸음)이 배치 293의
  Wading(물속을 걺, bathing에 합침)과 전혀 달라 새 상징으로 세웠다.
  **프로브에서 처음엔 walking 네 건이 전부 안 걸렸다** — 별칭을
  "걸었다"(다까지 완전한 꼴)로만 적었는데 자연스러운 시험 문장은
  "걸었는데"(연결 어미)를 썼다. 정확히 이 배치에서 처음 겪은 게
  아니라 §29 곁가지 ①의 반복이다 — "걸었는데"·"걷고"(뒤가 공백이라
  자동으로 낱말 경계) 두 별칭을 더해 고쳤다. "걸었" 같은 어간만
  올리는 안은 "말을 걸었는데"(말을 걸다) 같은 동형이의어와 겹칠
  위험이 있어 피하고 완전한 활용형만 남겼다.
  수치: 상징 1,980개 · 의미 6,873개 · 인용 6,908개.
  커밋 `82b3d6b4`.

- **297(Walls, 10/20 — 단독 배치)**: 문맥 여덟 개짜리 큰 표제어라
  단독으로 뗐다. **⓪ grep을 term_en으로 안 돌려서 놓칠 뻔한 자리** —
  기존 zhougong `wall`(담, "담을 넘어 집으로 들어감" 등 셋)이 term_en
  "wall"로 밀러의 여덟 문맥과 자동으로 합쳐지며 term_ko가 다수결로
  "담" → "벽"으로 바뀌었다(3표 대 8표). 옛 "담" 활성화가 끊기지 않게
  "담이"·"담을"·"담에"·"담으로"·"담 아래"·"담 위" 별칭을 명시적으로
  더했다(term_ko가 더는 "담"이 아니라 자동으로 안 딸려 온다).
  **기본값 플립 수용** — 옛 「담을 넘어 집으로 들어감」은 두 동작+목적지가
  붙은 그림이고, 새 「벽이 앞길을 막고 있음을 봄」은 조건 없이 그냥
  보는 가장 막연한 그림이라 FALLBACK_FIRST로 얼렸다.
  **verify-dream-km이 판별어에 상징 이름("벽") 그대로 쓴 것 둘을 그
  자리에서 잡았다** — "위를"·"옆을"로 고쳤다. 프로브에 새 문맥 여덟과
  옛 zhougong 셋을 지킴 케이스로 함께 넣어 전부 통과 확인.
  수치: 상징 1,980개 · 의미 6,881개 · 인용 6,916개.
  커밋 `59112996`.
