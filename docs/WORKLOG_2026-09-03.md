# 작업 기록 — 2026-09-03

앞날의 기록은 `docs/WORKLOG_2026-09-02.md`(배치 21~54)에 있다. 이 파일은 **날이 바뀐 뒤의
작업**부터다(§0 — 작업이 있었던 날마다 무조건 쓴다).

## 1. 배치 55 — 밀러 `Hurricane`·`Husband`

표제어 둘 · 항목 16개. 새 상징 하나 — `hurricane`(태풍). `Husband` 열둘은 이미 있는
`husband`(남편)에 붙였다.

**`Husband` 하나가 열두 항목이라 두 표제어로 판을 끊었다**(배치 크기는 표제어 수가 아니라
일감으로 잡는다 — 배치 43·50과 같은 판단). `Hut` 부터는 다음 판이다.

### 별칭 「태풍」을 `wind` 에서 `hurricane` 으로 넘겼다

밀러가 `Wind` 와 `Hurricane` 을 따로 두고, 「태풍」은 태풍의 것이 더 정확하다
(§25 곁가지 — 「대문」 `door`→`gate` · 「말벌」 `bee`→`hornet` 과 같다).
**「회오리」·「강풍」·「돌풍」은 `wind` 가 그대로 갖는다.** 다른 상징의 판별어에 있는 「태풍」은
안 건드렸다 — 한국어 판별어는 별칭 임자와 무관하게 산다.

프로브에 `wind` 의 옛 답 둘을 지킴 케이스로 넣어 안 깨진 것을 확인했다.

### `husband` 는 **판별어 표가 비어 있었다**

의미가 하나(주공해몽 「남편을 품에 안음」)뿐이라 판별어가 없어도 됐다. **열둘을 붙이는
순간 모두 있어야 한다**(배치 35·38에서 배운 자리) — 기존 것도 함께 채웠다. 안 채웠으면
그 의미가 0점으로 남아 어떤 문장으로도 안 뽑힌다.

그리고 그것이 **이 상징의 기본값**이라 판별어를 「품에 안았」로 좁게 적었다 — 얼린 자리는
동점을 전부 가져간다(§30 곁가지). 기본값은 `FALLBACK_FIRST` 에 **있던 값 그대로** 얼렸다:
밀러 열둘이 전부 좁은 상황(떠남·죽음·창백함…)이라 기본값 자리가 아니다.

### 「여성이~」 갈래가 열둘인데도 부딪치지 않았다

배치 50~52에서 **한 상징에 「여성이~」 갈래는 하나뿐**이라는 것을 세 번 만났는데, `Husband`
표제어는 **열두 문장이 전부 아내가 남편을 보는 이야기**인데도 문제가 없었다.

→ **가르는 것이 「여성이냐」가 아니라 「남편이 무엇을 하느냐」이기 때문이다**(떠남·꾸짖음·
죽음·창백함·앓음·딴 여자…). 「여성이~」가 부딪치는 것은 **성별이 유일한 구분일 때**이지
그 갈래가 여럿일 때가 아니다 — 배치 50의 기록을 이렇게 좁혀 둔다.

### 영어 판별어가 또 글자로 물렸다

```
unfaithful ⊂ unfaithfulness    남편이 앓고 있음 ↔ 남편이 딴마음을 품었다고 꾸짖음
```

배치 50~52의 `horse` 와 같은 자리다. 둘 다 이번에 넣은 것이라 뒤엣것을 `ails` 로 갈았다.

### 커버리지 — 안 넣은 문장 셋은 **풀이의 뒷말**이다

```
"You will move and remove to distant places…"   ← 무너지는 집에서 끌어냄의 뒷말
"If disagreeable conclusions are avoided…"      ← 멀어지며 커 보임의 뒷말
"Unfavorable conditions follow this dream…"     ← 추문이 남의 뒷말
```

셋 다 **새 꿈 그림이 아니라 앞 항목 풀이의 이어지는 말**이라 각 항목의 `interpretation_ko`
에 녹였다. 커버리지 대조에서 비어 보이는 것이 옳고, 패치 머리말에 적었다(§24).

### 관문

```
verify-dream-cite (전수)   항목 3,894개 · 위반 0건
verify-dream-km            위반 0건 (**고치기 전 1건** — unfaithful ⊂ unfaithfulness)
audit-km-dead-words        죽은 낱말 0개
커버리지                    문장 19 중 16(안 넣은 셋은 위)
민감 표현 grep              걸린 것 없음
verify-dream-match / context-parity / conception-pick / guide-numbers /
check-guide-examples        전부 통과
probe-miller-batch  20건 · 안 걸림 0 · 다른 뜻 0 (**한 번에 통과**)
tsc  exit 0
전수 스윕  142줄 · 통과 117 / 검사 안 됨 7 / 빨간불 4 — **배치 49 때와 같은 넷**
```

## 2. 지금 사전

| | 배치 54 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,221 | **1,222** |
| 의미 | 3,860 | **3,876** |
| 인용 | 3,878 | **3,894** |
| 밀러 표제어 | 999 | **1,001** / 2,264 (남은 **1,263**) |

**다음은 밀러 `Hut` 부터.**

---

# 배치 56 — 밀러 `Hut`~`Hyssop` (07:40~)

## 1. 넣은 것

표제어 일곱 · 항목 열셋. **일곱 다 새 상징이고 기존 상징의 의미는 하나도 안 건드렸다.**

```
Hut(3) 오두막 · Hyacinth(1) 히아신스 · Hydrophobia(3) 공수병 · Hyena(2) 하이에나 ·
Hymns(1) 찬송가 · Hypocrite(2) 위선자 · Hyssop(1) 우슬초
```

`Hymns` 는 본문 한 문장 뒤에 `[97] See Singing.` 이 붙어 있다 — 참조 지시는 항목이 아니다.
`Hyena` 의 「If lovers have this dream…」은 **가르는 것이 꾸는 사람뿐**이라 넣지 않고
`interpretation_ko` 에 녹였다(§「여성이~」 갈래와 같은 자리, 09-03 배치 55에서 좁힌 규칙).
`Hyssop` 의 「if a woman…」도 같다.

## 2. ⓪ 전수 grep — **겹친 이름 아홉이 전부 안전했다**

새 이름과 별칭이 기존 이름을 품는 자리가 아홉인데, **전부 앞 글자가 한글이라 `isStandalone`
이 막는다.** 품음 관계를 찾은 자리에서 멈추지 않고 손으로 한 번 돌려 본 결과다(§25 곁가지).

```
광견병 ⊃ 광견(dog)      「병」이 조사가 아니다        안전
공수병 ⊃ 병(illness)     「수」가 한글이다            안전
초가집·오두막집 ⊃ 집     〃                          안전
히아신스꽃 ⊃ 꽃(flower)  〃                          안전
히솝풀 ⊃ 풀(grass)       〃                          안전
하이에나 ⊃ 이(teeth)     〃                          안전
히아신스 ⊃ 신(deity)     〃                          안전
```

**하나는 실제로 뺐다 — 「성가」.** `choir`(성가대)가 이미 그 자리를 쥐고 있고 뜻도 가깝다.
`hymn` 의 별칭은 「찬송」·「찬송가 부르는 소리」·「찬송가 소리」로 두었다.

## 3. 지킴 케이스가 **옛 상처 하나**를 찾았다 — 배치 56이 만든 것이 아니다

프로브에 매 판 넣는 「옛 답이 그대로인가」 여섯 중 하나가 빨간불이었다.

```
「미친개에게 물렸다」  →  바란 것: 미친개에게 물림   나온 것: 개에게 물림
```

`dog` 의 세 의미가 **「미친개」라는 말을 아무도 안 갖고 있었다**(배치 27a 이래).

```
개에게 물림       「물렸 깨물 물린」
미친개를 봄       「미쳐 날뛰 거품을」    ← 「미친개를 보았다」는 0점 → 기본값으로 떨어졌다
미친개에게 물림   「광견에게 발작 침을」  ← 「미친개에게 물렸다」는 0점 → 넓은 쪽에 뺏겼다
```

**어떤 관문도 못 본다** — 낱말이 겹치지도 죽지도 않았다(§30 곁가지). 셋 다 초록불이었다.

고침은 **낱말을 나눠 갖지 않고 차례로** 풀었다(§25 곁가지 3) — 좁은 쪽에 「미친개에게」·
「미친개를」·「미친개가」·「미친개한테」를 주고, `m27a.json` 에서 미친개 둘을 「개에게 물림」
**앞으로** 옮겼다. 「물렸」을 형제끼리 나눠 가지면 `verify-dream-km` 이 「동점 위험」으로 막는다.
**첫 의미(기본값 「사나운 개가 나타남」)는 안 건드렸다** — 패치가 되읽어 확인한다.

## 4. 관문

```
verify-dream-cite (밀러 전수)  항목 2,729개 · 위반 0건
verify-dream-km               위반 0건 · 판별어 빠진 의미 0개
audit-km-dead-words           죽은 낱말 0개 (판별어 낱말 24,477개)
커버리지                       입력 표제어 7 ↔ 결과 locator 7 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍 (참고 350건)
audit-unreachable-meanings    1건(`wife`) — 여러 판째 같다, 감사라 빨간불 아님
verify-dream-match / context-parity / conception-pick / guide-numbers  전부 통과
probe-miller-batch  19건 · 안 걸림 0 · 다른 뜻 0  (**고치기 전 1건** — 위 §3)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건 — 새 상징뿐이라 기존 기본값을 안 건드렸다
```

## 5. 지금 사전 — **남은 수를 다시 쟀다**

| | 배치 55 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,222 | **1,229** |
| 의미 | 3,876 | **3,889** |
| 인용 | 3,894 | **3,907** |
| 밀러 표제어 | 999 | **1,006** / 2,264 (남은 **1,258**) |

**배치 55 뒤의 수를 「1,001 / 남은 1,263」으로 적어 두었는데 실측은 999 / 1,265 였다.**
추출 파일 전부에서 `cite.work === "miller"` 인 `locator` 를 모아 센 것이고, 그 locator 가
실재하는 표제어인 것은 `verify-dream-cite` 가 이미 보증한다. 앞 기록의 둘은 어디서 온
수인지 모른다 — **세는 것을 짜서 낸 이 수를 쓴다**(§10 #40·41).

**다음은 밀러 `Ice` 부터**(11문장 — 한 판을 크게 먹는다). 그다음이 `Ice Cream`·`Icicles`.

---

# 배치 57 — 밀러 `Ice`·`Icicles`·`Ice Cream`

## 1. 넣은 것 — 표제어 셋, 상징 셋, 항목 열여덟

```
Ice(12문장) → 얼음(8) + 고드름(4)      ← 한 표제어를 두 상징으로 갈랐다
Icicles(1)  → 고드름(1)
Ice Cream(5) → 아이스크림(5)
```

**`Ice` 의 열두 문장 가운데 넷은 고드름 이야기다**(처마·울타리·나무·늘푸른나무). 이용자가
「고드름」이라고 쓰지 「얼음」이라고 쓰지 않으므로 `icicle` 상징으로 옮겼고, `Icicles` 표제어의
한 문장이 그 다섯째가 됐다. **인용의 `locator` 는 원문 그대로 남긴다**(넷은 `Ice`, 하나는
`Icicles`) — 커버리지 대조는 그 값으로 세므로 두 표제어가 다 채워진다.

「Eating ice… If you drink ice-water…」는 **한 문단에 든 두 그림**이라 항목 둘로 갈랐다.

## 2. 「처녀가 얼음 위를 걸음」을 넣을 수 있었던 까닭

배치 50~52에서는 「여자가·여성이·처녀가」 갈래가 **둘 이상**이라 문장을 뺐다. 여기는
그 갈래가 **하나**이고 짝이 되는 일반 문장(「얼음 위를 걸음」)이 따로 있다 —
`처녀 아가씨 처자` 대 `걸었 걸어가 밟고` 로 **1:1 동점**이 되고, 좁은 쪽을 파일에서
앞에 두어 풀었다(§25 곁가지 3). 배치 55에서 좁힌 규칙이 그대로 들어맞은 자리다.

같은 수를 고드름에도 썼다 — 「나무에서 떨어지는 것」과 「나무에 달린 것」이 「나무에」로
동점이 되므로 **떨어지는 쪽을 앞에** 두었다. 「늘푸른나무」와 「나무에」도 같다.

## 3. 대표(첫) 의미의 판별어를 좁게 적었다

`ice` 의 「얼음을 봄」과 `ice-cream` 의 「아이스크림을 먹음」이 기본값 자리다. 여기에
「보았」·「먹었」 같은 흔한 말을 주면 **구체적인 형제가 영영 안 뽑힌다**(§30 곁가지).
각각 「얼음덩이 커다란」·「맛있게 달콤」만 남겼다 — 아무것도 안 걸리면 어차피 이 자리로
떨어지므로 제 일을 한다. 실제로 「아이들이 아이스크림을 먹고 있었다」가 아이들 쪽으로 갔다.

## 4. 관문에서 하나 걸렸다 — 뜻이 아니라 **글자**가 물렸다

```
verify-dream-km  「thin」(처녀가 얼음 위를 걸음) ⊂ 「ba-thin-g」(얼음물에 몸을 담금)
```

의미가 여덟뿐인데도 났다 — §30 곁가지의 「스물이 넘으면」은 **잦아지는 조건**이지 문턱이
아니다. **새로 넣는 쪽만 갈았다**(「thin」을 빼고 「veil」로).

## 5. 관문

```
verify-dream-cite (밀러 전수)  항목 2,747개 · 위반 0건
verify-dream-km               위반 0건 (**고치기 전 1건** — 위 §4)
audit-km-dead-words           죽은 낱말 0개
커버리지                       입력 표제어 3 ↔ 결과 locator 3 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
audit-unreachable-meanings    1건(`wife`) — 여러 판째 같다
verify-dream-match / context-parity / conception-pick / guide-numbers  전부 통과
probe-miller-batch  20건 · 안 걸림 0 · 다른 뜻 0
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건 — 새 상징뿐이다
```

## 6. 지금 사전

| | 배치 56 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,229 | **1,232** |
| 의미 | 3,889 | **3,907** |
| 인용 | 3,907 | **3,925** |
| 밀러 표제어 | 1,006 | **1,009** / 2,264 (남은 **1,255**) |

## 7. 기록만 해 둔 것 — **앞 배치들이 건너뛴 표제어 다섯을 찾았다**

남은 목록을 차례대로 찍어 보니 알파벳 앞쪽에 아직 안 채워진 표제어가 있었다.
**이번 변경이 만든 것이 아니다** — 앞 배치들이 지나간 자리다.

```
Convicted        「[43] See Accuse.」   참조 지시뿐이다 — 비는 것이 옳다
Cords            「[44] See Rope.」     〃
Abortion         본문 두 문단           내용이 있다
Absalom          본문 한 문단           내용이 있다
Humidity         본문 한 문단           내용이 있다
```

뒤의 셋은 왜 건너뛰었는지 기록이 없다. `Gypsy`(§31)·`Hate`(같은 그림이 이미 있다)·
`Barrel`·`Embarrassment`(참조 지시뿐)처럼 **적어 둔 이유가 있는 자리와 구별된다.**

---

# 배치 58 — 밀러 `Absalom`~`Illness` (앞 배치들이 빠뜨린 셋을 함께 채웠다)

## 1. 넣은 것 — 표제어 일곱, 항목 열다섯

```
새 상징 여섯   압살롬 · 습기 · 이상형 · 어리석은 사람 · 게으름 · 우상
기존 상징 하나 병(illness) ← 밀러 `Illness` 한 문장
```

**앞쪽 알파벳에 남아 있던 셋(`Absalom`·`Humidity`·`Illness`)을 이 판에서 채웠다.**
배치 57 §7에 사실만 적어 둔 자리다. 같은 목록의 `Convicted`·`Cords` 는 참조 지시뿐이라
비는 것이 옳고, `Gypsy`(§31)·`Hate`(같은 그림이 이미 있다)·`Barrel`·`Embarrassment` 는
이미 이유가 적혀 있다.

**`Abortion` 은 넣지 않았다 — 이유를 여기 적는다.** 밀러의 두 문장은 낙태를 두고
「수치와 불행」·「직무 태만」을 점친다. 낱말이 비하인 자리(§31 앞갈래)도, 같은 그림이
이미 있는 자리도 아니다 — **오늘의 이용자에게 그대로 보여 주기에 해로운 판정**이라는
셋째 이유다. 23개 언어 화면에 뜨는 글이라 되돌리려면 이 줄을 지우고 다시 넣으면 된다.

## 2. `Illness` 를 기존 `병` 에 붙이면서 **기본값이 뒤집혔다**

조립기의 관문이 잡았다(§30).

```
illness (병)   전: 병들어 누웠는데 남이 부축해 줌   후: 여자가 제 병을 앓는 꿈을 꿈
```

`m58.json` 이 `r*.json` 보다 먼저 정렬되므로 밀러 의미가 첫째로 올라간 것이다.
**있던 답 그대로 얼렸다**(`FALLBACK_FIRST`) — 들어온 밀러 의미는 「꾸는 사람이 여자」라는
조건이 붙은 자리이지 아무것도 안 걸린 꿈이 떨어질 자리가 아니다.

가르는 것이 성별뿐인 자리인데도 넣을 수 있었던 까닭은 **그 갈래가 하나**이고 짝이 되는
것이 일반 문장(「자기가 병듦」)이라서다 — 배치 55에서 좁힌 규칙, 배치 57의 「처녀가 얼음
위를 걸음」과 같은 자리다.

## 3. 프로브가 다섯을 잡았다 — 셋은 내 데이터, 둘은 내 문장

```
안 걸림  어리숙해 보이는 아이들을 보았다   → 상징 이름이 문장에 없었다 (별칭 넷을 더했다)
안 걸림  내가 빈둥거리고 있었다            → 「빈둥거림」은 「빈둥거리고」에 안 걸린다
안 걸림  친구들이 빈둥거리는 것을 보았다    → 〃
안 걸림  병들어 누웠는데 …                → **내 문장이 틀렸다** (아래)
다른 뜻  게를 보았다 → 「게들을 봄」        → **판정할 수 없는 자리다** (아래)
```

**「빈둥거림」이 또 같은 병이다**(§29 곁가지 ① · 이번이 여덟 번째). 뒤에 오는 「고」·「는」·
「며」가 조사가 아니다 — 완전한 활용형 여섯을 별칭으로 올렸다.

**「병들어 누웠는데」는 내 문장이 틀렸다.** 「병」 뒤의 「들」이 조사가 아니라 `illness` 가 안
걸리고, 「병들어」는 **`disease`(병듦)가 이미 별칭으로 갖고 있다** — 남의 이름이라 뺏지
않는다(§28). 문장을 「병이 나서 누웠는데」로 고쳤다. 이용자가 실제로 쓰는 꼴이고 상징
이름이 그 안에 있다.

## 4. 기록만 해 둔 것 — `crab` 이 「의미가 중복인 자리」의 여덟 번째다

지킴 케이스 「게를 보았다」가 「게들을 봄」으로 갔다. 읽어 보니 **어느 쪽이 옳다고 말할 수
없는 자리**였다.

```
[0] 게들을 봄  (밀러 Crabs, 흉)   To dream of crabs, … many complicated affairs …
[2] 게를 봄    (주공해몽, 길)      螃蟹主百病消散
```

**둘 다 「게를 보았다」라는 같은 그림이고 길흉이 반대다.** 판별어로는 못 가른다 —
메모리에 적힌 「의미가 중복인 7곳」(`blood`·`shackles`·`bee`·`bathing`·`annoy`·`bride`·
`childbed`)과 같은 갈래이고, `crab` 이 여덟째다. **이번 변경이 만든 것이 아니다.**
지킴 케이스는 판정이 서는 「연인에게 게 꿈이 나타남」으로 바꿨다.

## 5. 관문

```
verify-dream-cite (밀러 전수)  항목 2,762개 · 위반 0건
verify-dream-km               위반 0건
audit-km-dead-words           죽은 낱말 0개
커버리지                       입력 표제어 7 ↔ 결과 locator 7 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
audit-unreachable-meanings    1건(`wife`) — 여러 판째 같다
verify-dream-match / context-parity / conception-pick / guide-numbers  전부 통과
probe-miller-batch  19건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 5건** — 위 §3)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  1건 — 얼려서 되돌림(위 §2)
```

## 6. 지금 사전

| | 배치 57 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,232 | **1,238** |
| 의미 | 3,907 | **3,922** |
| 인용 | 3,925 | **3,940** |
| 밀러 표제어 | 1,009 | **1,016** / 2,264 (남은 **1,248**) |

---

# 배치 59 — 밀러 `Illumination`~`Imps`

## 1. 넣은 것 — 표제어 다섯, 항목 열여섯

```
새 상징 넷    환한 빛(7) · 형상(3) · 흉내(2) · 연장(2)
기존 상징 하나 악귀(fiend) ← 밀러 `Imps` 둘
```

**`Imps` 는 새 상징을 세우지 않았다.** ⓪ grep 이 「악귀」를 EXACT 로 찍었다(`fiend`).
「작은 악귀」로 지으면 그 이름이 「악귀」를 품어 **둘 다 걸린다**(§25 곁가지) — 「마귀」는
`fiend`·`devil` 이 나눠 쥐었고 「도깨비」는 `ghost` 의 것이다. **임자에게 붙였다.**

## 2. 한 글자 이름을 조사째 별칭으로 살렸다 — 「빛」

`Illumination` 의 이용자 말은 「빛」인데 **한 글자 상징은 `verify-dream-km` 이 막는다.**
배치 41(`Gong`→「징소리」)에서 쓴 우회법을 그대로 썼다 — 이름은 「환한 빛」으로 두고
**조사를 붙인 두 글자 꼴**(「빛이」·「빛을」·「빛과」)을 별칭에 올렸다. 「햇빛이」·「달빛이」는
앞 글자가 한글이라 `isStandalone` 이 막으므로 다른 상징을 안 뺏는다.

**「불빛」은 일부러 뺐다** — `beacon-light` 가 「등대 불빛」으로 쥐고 있어 「등대 불빛」에서
둘 다 걸린다.

## 3. 프로브가 둘을 잡았다 — 둘 다 `fiend` 자리

```
안 걸림  작은 악귀들을 보았다        → 「악귀들을」의 「들」이 조사가 아니다. **상징 자체가 안 걸렸다**
다른 뜻  내가 작은 악귀가 되어 있었다 → 1:1 동점으로 앞엣것(「작은 악귀들을 봄」)에 갔다
```

앞엣것은 별칭 「악귀들」을 올려 고쳤다(§29 곁가지 ①의 아홉 번째 얼굴 — 이번은 **복수
접미사 「들」**이다). 뒤엣것은 **서로 다른 낱말 둘씩**을 주어 갈랐다 — 「작은 악귀들」 대
「내가 악귀가」다. 한쪽에만 「작은」을 주면 다른 문장에서 동점이 되고, 동점은 앞이 이긴다.

**고칠 때 패치 스크립트를 고쳐 다시 돌렸다** — 데이터를 손으로 만지지 않고
`git checkout` 으로 되돌린 뒤 스크립트를 고쳐 재실행했다. 스크립트 한 번으로 최종 상태가
재현된다.

## 4. 관문

```
verify-dream-cite (밀러 전수)  항목 2,778개 · 위반 0건
verify-dream-km               위반 0건
audit-km-dead-words           죽은 낱말 0개
커버리지                       입력 표제어 5 ↔ 결과 locator 5 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-match / context-parity / conception-pick / guide-numbers  전부 통과
probe-miller-batch  19건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 2건** — 위 §3)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 5. 지금 사전

| | 배치 58 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,238 | **1,242** |
| 의미 | 3,922 | **3,938** |
| 인용 | 3,940 | **3,956** |
| 밀러 표제어 | 1,016 | **1,021** / 2,264 (남은 **1,243**) |

---

# 배치 60 — 밀러 `Inauguration`~`Independent`

## 1. 넣은 것 — 표제어 여섯, 항목 열넷

```
새 상징 다섯   취임식(2) · 횡설수설(1) · 수입(5) · 늘어남(2) · 자립(2)
기존 상징 하나 마법에 홀림(enchantment) ← 밀러 `Incantation` 둘
```

**`Incest` 는 넣지 않았다 — 배치 58의 `Abortion` 과 같은 셋째 이유다.** 낱말이 비하인
자리도, 같은 그림이 이미 있는 자리도 아니지만 **오늘의 이용자에게 그대로 보여 주기에
해로운 판정**이다(「명예로운 자리에서 떨어지고 손해를 본다」). 되돌리려면 이 줄을 지우고
다시 넣으면 된다.

## 2. ⓪ grep 이 이름 셋을 돌려세웠다

```
주술   → `enchantment` 가 별칭 「주술에」로 이미 쥐고 있다  → 임자에게 붙였다
주문   → 물건을 시키는 뜻으로도 읽힌다                    → 별칭에만 올렸다
벌이   → `bee`(벌)의 「벌」 + 조사 「이」로 **그대로 걸린다**  → 「수입」으로 갔다
```

**「벌이」가 이번 판에서 가장 위험했다.** 앞 글자가 한글이라 막히는 자리(취임식 ⊃ 취임 ·
돈벌이 ⊃ 벌)와 달리, 「벌이」는 **한 글자 상징 + 조사** 꼴이라 `isStandalone` 을 그대로
지난다. 품음 관계를 찾은 자리에서 멈추지 않고 손으로 돌려 본 덕에 걸렀다(§25 곁가지).

## 3. 프로브가 둘, 관문이 둘을 잡았다

```
verify-dream-km       「hear」 ⊂ 「sweet*hear*ts」            형제끼리 글자가 물렸다
audit-km-dead-words   「very」는 엔진이 세지 않는 불용어다
프로브 안 걸림 둘      「독립해 있었다」·「독립했다」          「해」·「했」이 조사가 아니다
```

**「독립」이 §29 곁가지 ①의 열 번째다.** 별칭을 완전한 활용형(「독립해」·「독립했다」·
「독립한」·「독립하는」·「자립해」·「자립했다」)으로 갈아 고쳤다.

## 4. 관문

```
verify-dream-cite (밀러 전수)  항목 2,792개 · 위반 0건
verify-dream-km               위반 0건 (**고치기 전 1건**)
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 1개**)
커버리지                       입력 표제어 6 ↔ 결과 locator 6 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-match / context-parity / conception-pick / guide-numbers  전부 통과
probe-miller-batch  18건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 2건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 5. 지금 사전

| | 배치 59 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,242 | **1,247** |
| 의미 | 3,938 | **3,952** |
| 인용 | 3,956 | **3,970** |
| 밀러 표제어 | 1,021 | **1,027** / 2,264 (남은 **1,237**) |

---

# 배치 61 — 밀러 `India Rubber`~`Industry`

## 1. 넣은 것 — 표제어 일곱, 항목 열셋. **일곱 다 새 상징이다**

```
고무(2) · 무관심(3) · 쪽빛 물감(2) · 체함(1) · 흐릿함(1) · 방종(1) · 부지런함(3)
```

## 2. ⓪ grep 이 이름 하나를 돌려세웠다 — 「쪽빛」

`indigo-clothing`(쪽빛 옷)이 이미 있다. 「쪽빛」으로 지으면 **「쪽빛 옷을 입었다」에서
둘 다 걸린다** — 뒤가 공백이라 `isStandalone` 을 그대로 지난다(§25 곁가지, 배치 29의
「선거 운동」과 같은 자리). 밀러 `Indigo` 는 **물감**이지 옷이 아니므로 이름을
**「쪽빛 물감」**으로 지었다.

그러면 「쪽빛으로 물든 물을 보았다」에 상징이 안 걸리므로 **「쪽빛으로」·「쪽빛이」·
「쪽빛을」을 별칭으로 올렸다** — 셋 다 「쪽빛 옷을」에는 없는 꼴이라 남의 것을 안 뺏는다.

나머지 겹침은 전부 앞 글자가 한글이라 안전했다(무관심 ⊃ 관 · 방종 ⊃ 종 · 소화불량 ⊃ 소·불 ·
고무신 ⊃ 고무). **`shoes` 의 「고무신」은 거꾸로 내 이름이 그 앞에 놓이는 자리**인데,
「고무신」의 「신」이 조사가 아니라 내 「고무」가 막힌다 — 지킴 케이스로 확인했다.

## 3. 기본값 자리에 흔한 말을 두 번 넣을 뻔했다

```
고무를 봄       「고무를 보았다」  ← 「고무를 잡아 늘였다」에도 「고무를」가 있다
쪽빛 물감을 봄  「물감을 보았다」  ← 「…물을 보았다」에도 「보았다」가 있다
```

둘 다 **기본값이 형제의 문장을 동점으로 가로채는** 꼴이다(§30 곁가지). 적는 자리에서
알아채고 「덩어리」·「물감을」로 좁혔다 — 기본값은 아무것도 안 걸렸을 때 떨어지는 자리이므로
0점이어야 제 일을 한다.

## 4. 관문

```
verify-dream-cite (밀러 전수)  항목 2,805개 · 위반 0건
verify-dream-km               위반 0건
audit-km-dead-words           죽은 낱말 0개
커버리지                       입력 표제어 7 ↔ 결과 locator 7 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-match / context-parity / conception-pick / guide-numbers  전부 통과
probe-miller-batch  15건 · 안 걸림 0 · 다른 뜻 0 (**한 번에 통과**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 5. 지금 사전

| | 배치 60 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,247 | **1,254** |
| 의미 | 3,952 | **3,965** |
| 인용 | 3,970 | **3,983** |
| 밀러 표제어 | 1,027 | **1,034** / 2,264 (남은 **1,230**) |

---

# 배치 62 — 밀러 `Infants`~`Ink`

## 1. 넣은 것 — 표제어 다섯, 항목 열넷

```
새 상징 둘    병약함(2) · 영향력(3)
기존 상징 셋  갓난아이(2) · 병원(1) · 먹(6)
```

**⓪ grep 이 셋을 EXACT 로 찍어 새 상징을 못 세우게 했다** — 「갓난아기」는 `newborn-baby`,
「먹물」은 `ink-stick`(먹), 「요양원」은 밀러 자신이 「[100] See Hospital.」이라 적었고
`hospital` 이 이미 있다. 「잉크」로 따로 세우려 했으나 `blotting-paper` 의 「잉크 흡수지」와
**뒤가 공백이라 둘 다 걸린다**(배치 61의 「쪽빛」과 같은 자리) — `ink-stick` 에 붙이고
「잉크」를 그 별칭으로 올렸다.

## 2. 안 넣은 문장 셋 — **커버리지가 비는 것이 옳다**(§24)

세 자리 다 **「같은 그림이 이미 있다」**는 둘째 이유다(§31 곁가지).

```
Infants 첫 문장   「newly born infant 를 봄」   `newborn-baby` 의 주공해몽 「갓 태어난
                                              아들딸을 봄」과 그림이 같다
Inheritance       「유산을 받음」               `bequest` 에 그 그림이 **이미 셋**
                                              (Bequest·Estate·Heir)
Injury            「제가 다침」                 `hurt` 에 이미 있다(밀러 `Hurt`, 배치 54)
```

## 3. 판별어 표가 비어 있던 둘은 **기존 의미까지 채웠다**

`newborn-baby`·`ink-stick` 은 의미가 하나뿐이라 판별어가 없었다. 여럿이 되는 순간
**모두** 있어야 한다 — 붙이는 쪽만 채우면 기존 주공해몽 의미가 0점으로 남는다(배치 35).

**둘 다 기본값이 뒤집혀 얼렸다**(`FALLBACK_FIRST`, 여든둘이 됐다) — 들어온 밀러 의미가
「처녀가 갓난아이를 가짐」·「옷에 먹물이 쏟아진 것을 봄」처럼 조건이 붙은 상황이다.

## 4. 프로브가 **옛 상처 하나**를 찾았다 — 배치 62가 만든 것이 아니다

```
안 걸림  「갓 태어난 아들을 보았다」  → `newborn-baby` 가 아예 안 걸린다
```

이 상징의 **화면 문구가 「갓 태어난 아들딸을 봄」인데 별칭에 그 꼴이 없었다** —
갓난아기·신생아·핏덩이·젖먹이뿐이라, 심어진 뒤로 「갓 태어난 …」이 한 번도 안 걸리고
있었다. 별칭 「갓 태어난」을 올려 고쳤다(뒤가 공백이라 낱말 경계를 지난다).

**지킴 케이스가 아니었으면 못 봤다** — 이번 배치가 그 상징을 열었기 때문에 드러났다
(인계 ⑧과 같은 자리, 여덟 번째다).

## 5. 관문이 셋을 잡았다

```
verify-dream-km      「red」 ⊂ 「slande*red*」 · 「position」 ⊂ 「positions」
audit-km-dead-words  「갓」·「막」이 한 글자라 엔진이 안 센다
```

셋 다 **새로 넣는 쪽만** 갈았다. 고칠 때마다 `git checkout` 으로 데이터를 되돌리고
**패치 스크립트를 고쳐 재실행**했다 — 스크립트 한 번으로 최종 상태가 재현된다.

## 6. 관문

```
verify-dream-cite (밀러 전수)  항목 2,819개 · 위반 0건
verify-dream-km               위반 0건 (**고치기 전 2건**)
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 2개**)
커버리지                       입력 표제어 5 ↔ 결과 locator 5 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-match / context-parity / conception-pick / guide-numbers  전부 통과
probe-miller-batch  18건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 1건** — 위 §4)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  2건 — 둘 다 얼려서 되돌림(위 §3)
```

## 7. 지금 사전

| | 배치 61 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,254 | **1,256** |
| 의미 | 3,965 | **3,979** |
| 인용 | 3,983 | **3,997** |
| 밀러 표제어 | 1,034 | **1,039** / 2,264 (남은 **1,225**) |

---

# 배치 63 — 밀러 `Ink-stand`~`Insolvent`

## 1. 넣은 것 — 표제어 일곱, 항목 열다섯

```
새 상징 여섯   잉크병(2) · 여인숙(2) · 검시(1) · 심문(2) · 정신이 흐려짐(2) · 새긴 글(3)
기존 상징 하나 파산(bankrupt) ← 밀러 `Insolvent` 셋
```

**`Insolvent` 는 새로 안 세웠다** — ⓪ grep 이 「파산」을 EXACT 로 찍었고, `bankrupt` 의
**영어 별칭에 `insolvent` 가 이미 들어 있다.**

**반대로 `Inn` 은 새로 세웠다.** 「여관」은 `hotel` 의 것이지만 밀러가 `Hotel` 과 `Inn` 을
따로 두고 풀이도 다르다 — 이름을 **「여인숙」**으로 지으면 「여관」을 품지 않아 서로 안 뺏는다.
「허름한 여관」 같은 별칭은 **일부러 안 올렸다**(올리면 둘 다 걸린다).

`Insane` 은 §31 앞갈래대로 **화면 이름과 매칭 키를 갈랐다** — 이름은 「정신이 흐려짐」이고
「미쳤다」·「실성」은 **별칭에만** 올렸다(배치 28의 `Dumb`·`Dwarf` 와 같은 자리).

## 2. 프로브가 **아홉**을 잡았다 — 이 세션에서 가장 많다

**아홉 다 「상징 자체가 안 걸림」**이고, 전부 §29 곁가지 ①(별칭 경계)이다.

```
검시   + 하는     ✗   심문 + 하는 ✗   실성 + 해 ✗   파산 + 했다/한 ✗
「정신이 흐려짐」  ✗   이용자는 「정신이 흐려진」이라고 쓴다
「새긴 글」        ✗   「돌에 글을 새겼다」에 그 꼴이 없다
「빚을 갚지 못함」 ✗   `bankrupt` 에 그 꼴의 별칭이 아예 없었다
```

**그 가운데 하나는 옛 상처다** — `bankrupt` 의 별칭 「망했」은 뒤의 「다」가 조사가 아니라
**심어진 뒤로 「망했다」에 한 번도 안 걸리고 있었다**(인계 ⑧과 같은 자리, 아홉 번째).

→ **새 상징을 지을 때 이름이 「~함」·「~짐」 꼴이면 그것만으로는 안 걸린다.** 이용자가 쓰는
활용형(「~하는」·「~한」·「~했다」·「~진」)을 별칭으로 함께 올려야 한다. 이번 판에서 여섯 상징이
같은 이유로 걸렸다 — **한 판에 이렇게 몰린 것은 처음**이고, 이름을 서술형으로 지은 표제어가
많았기 때문이다.

## 3. 판별어 하나가 형제를 동점으로 가로챘다

```
제가 빚을 갚지 못함  「제가 내가 갚지」  ← 「갚지」는 형제 문장에도 다 있다
```

「남이 빚을 갚지 못했다」가 1:1 동점으로 앞엣것에 갔다. **「갚지」를 빼서** 갈랐다(§30 곁가지).

## 4. 관문

```
verify-dream-cite (밀러 전수)  항목 2,834개 · 위반 0건
verify-dream-km               위반 0건
audit-km-dead-words           죽은 낱말 0개
커버리지                       입력 표제어 7 ↔ 결과 locator 7 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-match / context-parity / conception-pick / guide-numbers  전부 통과
probe-miller-batch  18건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 10건** — 위 §2·§3)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 5. 지금 사전

| | 배치 62 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,256 | **1,262** |
| 의미 | 3,979 | **3,994** |
| 인용 | 3,997 | **4,012** |
| 밀러 표제어 | 1,039 | **1,046** / 2,264 (남은 **1,218**) |

## 곁가지 — 배치 62의 큰 diff 를 확인했다

배치 62 커밋이 **2,165 추가 / 6,601 삭제**로 나와 §30 곁가지대로 멈추고 재 봤다.
`git show HEAD~1:<데이터 파일>` 로 꺼내 id 목록을 맞추니 **사라진 id 0 · 새 id 는 새 상징
둘(influence·infirmity)뿐 · 의미 수가 바뀐 것은 건드린 셋뿐**이었다. 큰 diff 는
`newborn-baby`·`ink-stick` 이 배열 앞으로 뛰면서 1,146칸이 밀린 것이다(§30 곁가지 ⑦).

---

# 배치 64 — 밀러 `Intemperance`~`Inundation`

## 1. 넣은 것 — 표제어 일곱, 항목 열하나

```
새 상징 넷    무절제(3) · 두둔(1) · 겹혼인(1) · 통역(1)
기존 상징 셋  창자(1) · 술에 취함(1) · 홍수(3)
```

`Intoxication` 은 원문이 「[103] See Drunk.」이고 `drunk` 가 이미 있다. `Inundation` 은
`flood`(홍수)의 것이고, `Intestine` 은 `entrails`(창자)의 것이다.

## 2. 안 넣은 문장 둘 — **커버리지가 비는 것이 옳다**(§24)

`Intestine` 의 앞 두 문장은 `entrails` 에 **그림이 이미 있다**(밀러 `Entrails`).
셋째 문장(창자를 난로 위에 올려놓아 뜨거워지는 그림)만 새 자리라 그것만 넣었다.

## 3. **극성 하니스가 눈을 떴다** — `flood` 가 의미 하나뿐이던 동안 못 보던 자리

`flood` 는 의미가 하나여서 **무엇을 넣어도 그 답이 나왔다.** 넷이 되자 하니스가 둘을 냈다.

```
「a huge flood of clear water rose」  en 이 기본값으로 떨어졌다 — 「clear」가 판별어에 없었다
「I was swept away by the flood」     en 이 「사람들이 큰물에 휩쓸려 감」으로 갔다 —
                                     그 문장의 임자는 기본값(1인칭)인데 「swept」를 쥐고 있었다
```

**고침이 관문을 빨갛게 만든 것이 아니라 관문이 눈을 뜬 것이다**(§28과 같은 자리).
둘 다 **새로 넣는 쪽만** 갈았다.

## 4. 프로브가 **옛 상처 하나**를 또 찾았다

```
다른 뜻  「자기 창자를 보았다」 → 「사람의 창자를 봄」(기본값)
```

`entrails` 의 「제 창자를 봄」 판별어가 **「자신의 스스로」뿐**이라 「자기 창자를」이 안 걸리고
있었다. 「나의 자기」를 붙여 고쳤다.

→ **「내 창자」·「제 창자」는 엔진이 원리적으로 못 가른다** — 가르는 말이 한 글자다
(`scoringWordsOf` 의 `length >= 2`). 두 글자로 쓰는 꼴만 살릴 수 있고, 그것이 이 엔진의
알려진 한계 넷 가운데 하나다. **못 가르는 것을 적어 둔다.**

## 5. §26 곁가지를 밟았다 — **조립기 1단계만 돌려 별칭이 통째로 날아갔다**

수치를 확인하려고 `build-dream-dictionary-v2.mjs` 만 다시 돌렸더니 `aliases` 가 비워졌고,
바로 다음에 돌린 극성 하니스가 **실패 16건**을 냈다(상징 열두 개가 통째로 안 걸림).
2단계(`build-dream-match-tables-v2.mjs`)를 이어 돌리자 진짜 실패는 **2건**이었다.

→ **수치만 보려고 조립기를 다시 돌리지 않는다.** 봐야 하면 **둘 다** 돌리거나, 이미 만들어진
`dream-symbols.v2.data.json` 을 읽어서 센다. 「16건」을 진짜 결함으로 읽었으면 없는 결함을
쫓느라 한 판을 날릴 뻔했다(§26 곁가지에 이미 적혀 있던 것을 그대로 밟았다).

## 6. 관문

```
verify-dream-cite (밀러 전수)  항목 2,845개 · 위반 0건
verify-dream-km               위반 0건 (**고치기 전 2건**)
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 2개** — 「floods」·「온」)
커버리지                       입력 표제어 7 ↔ 결과 locator 7 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍 (참고 353건)
verify-dream-context-parity   실패 0건 (**고치기 전 2건** — 위 §3)
verify-dream-match / conception-pick / guide-numbers  전부 통과
probe-miller-batch  16건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 3건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 7. 지금 사전

| | 배치 63 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,262 | **1,266** |
| 의미 | 3,994 | **4,005** |
| 인용 | 4,012 | **4,023** |
| 밀러 표제어 | 1,046 | **1,053** / 2,264 (남은 **1,211**) |

---

# 배치 65 — 밀러 `Invective`~`Iron`

## 1. 넣은 것 — 표제어 넷, 항목 열다섯

```
새 상징 둘    발명가(2) · 초대(3)
기존 상징 둘  욕설(verbal-abuse, 2) · 쇠붙이(iron-2, 8)
```

**`iron` 이라는 id 는 「다리미」가 차지하고 있다**(주공해몽). 밀러 `Iron` 은 `iron-2`(쇠붙이)
이고, 밀러 `Ironing` 이 「다리미」 자리다 — 헷갈리기 쉬워 패치 머리말에 적어 뒀다.

## 2. 표제어를 통째로 건너뛴 것 하나 — `Invalid`

두 문장이 `infirmity`(배치 62)의 「제 몸이 병약함」·「남이 병약한 것을 봄」과 **그림이 같다.**
`Iron` 의 첫 문장(「To dream of iron, is a harsh omen of distress.」)도 `iron-2` 의 주공해몽
의미 「쇠로 만든 기물을 봄」과 그림이 같아 안 넣었다. **커버리지가 비는 것이 옳다**(§24).

## 3. 프로브가 넷을 잡았다 — 그 가운데 **옛 상처 하나**

```
안 걸림  「남들이 욕설하는 것을 들었다」  「욕설」+「하」가 막힌다
안 걸림  「여자가 잔치에 초대받았다」     「초대받」+「았다」가 막힌다
안 걸림  「쇠로 만든 기물을 보았다」      **옛 상처** — 아래
다른 뜻  「여자가 잔치에 초대받았다」     동점을 형제가 가로챘다
```

**옛 상처**: `iron-2` 의 화면 문구가 「쇠로 만든 기물을 봄」인데 별칭이 쇳덩이·철제·무쇠뿐이라
**「쇠로」가 한 번도 안 걸리고 있었다.** 배치 62의 `newborn-baby`(「갓 태어난」)와 **정확히 같은
꼴**이다 — **상징의 화면 문구에 쓰인 말이 별칭에 없는 자리**다.

→ **기존 상징에 의미를 붙일 때 그 상징의 화면 문구를 소리 내어 읽고, 그 말이 별칭에 있는지
본다.** 두 판에서 같은 병이 났고 둘 다 지킴 케이스가 아니었으면 못 봤다.

## 4. 관문이 셋을 잡았다

```
verify-dream-km      「값이」를 「쇠붙이 값이 내림」·「오름」이 나눠 갖고 있었다
audit-km-dead-words  「insulted」·「down」이 불용어다
```

## 5. 관문

```
verify-dream-cite (밀러 전수)  항목 2,860개 · 위반 0건
verify-dream-km               위반 0건 (**고치기 전 1건**)
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 2개**)
커버리지                       입력 표제어 4 ↔ 결과 locator 4 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick / guide-numbers  전부 통과
probe-miller-batch  18건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 4건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  2건 — 둘 다 얼려서 되돌림(`FALLBACK_FIRST` 여든넷)
```

## 6. 지금 사전

| | 배치 64 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,266 | **1,268** |
| 의미 | 4,005 | **4,020** |
| 인용 | 4,023 | **4,038** |
| 밀러 표제어 | 1,053 | **1,057** / 2,264 (남은 **1,207**) |

---

# 배치 66 — 밀러 `Ironing`~`Ivy`

## 1. 넣은 것 — 표제어 다섯, 항목 열일곱

```
새 상징 넷    섬(4) · 가려움(4) · 상아(2) · 담쟁이(3)
기존 상징 하나 다리미(iron, 4) ← 밀러 `Ironing`
```

**배치 65에서 밀러 `Iron` 을 `iron-2`(쇠붙이)에 붙였고, 이번 `Ironing` 이 `iron`(다리미)
자리다** — 두 판에 걸쳐 갈라 놓은 것이 이렇게 맞아떨어졌다.

## 2. **기본값을 바꾼 자리다** — 있던 답으로 되돌리지 않았다

```
iron (다리미)  전: 다리미에 불이 담겨 있음  후: 다림질을 함
```

지금까지 스물여덟 판에서 밀어낸 기본값은 거의 다 **있던 답으로 되돌렸다.** 이번은
배치 54(`hunting`·`embrace`)와 같은 **바꾸는 것이 옳은 자리**다 — 다리미 꿈의 가장
일반적인 그림은 「다림질을 함」이고, 있던 주공해몽 의미는 **숯불 다리미**라는 좁은 상황이다.
`FALLBACK_FIRST` 에 **새 값으로** 적어 얼렸다(여든다섯).

## 3. 한 글자 이름 「섬」을 그대로 썼다

§29 곁가지 ③은 **한 글자 이름은 동형이의어 활용형에 걸린다**고 경고한다. 「섬」을 실제로
따져 보니 안전했다 — 「섬기다」·「섬세한」은 뒤가 한글이라 `isStandalone` 이 막고, 「제주섬」
처럼 앞이 한글인 자리도 막힌다. `verify-dream-km` 도 통과했다(막는 것은 **한 글자 이름에
한 글자 별칭**을 다는 자리다). 조사를 붙인 꼴(「섬을」·「섬이」·「섬에」)을 별칭으로 함께 올렸다.

## 4. 프로브가 넷을 잡았다 — 전부 §29 곁가지 ①

```
「다림질하」 + 다     ✗   「다」는 조사가 아니다 — **「다림질하다」까지 올려야 한다**
「차가워」   ≠ 차가웠  ✗   판별어도 이용자가 칠 꼴로
「근질거」   + 렸다   ✗
```

**「~하」로 끝낸 별칭이 「~하다」에 안 걸리는 것**은 배치 30에서 「말을 잘」+해서로 이미 겪은
자리인데, 이번엔 **「하다」 자체가 뒤에 오는** 꼴이라 또 밟았다.

## 5. 관문

```
verify-dream-cite (밀러 전수)  항목 2,877개 · 위반 0건
verify-dream-km               위반 0건
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 1개** — 「clothes」가 제 이름이었다)
커버리지                       입력 표제어 5 ↔ 결과 locator 5 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick / guide-numbers  전부 통과
probe-miller-batch  18건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 4건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  1건 — **새 값으로 얼렸다**(위 §2)
```

## 6. 지금 사전

| | 배치 65 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,268 | **1,272** |
| 의미 | 4,020 | **4,037** |
| 인용 | 4,038 | **4,055** |
| 밀러 표제어 | 1,057 | **1,062** / 2,264 (남은 **1,202**) |

---

# 배치 67 — 밀러 `Jackdaw`~`Jasper`

## 1. 넣은 것 — 표제어 여섯, 항목 열넷

```
새 상징 다섯   갈까마귀(3) · 간수(1) · 과일 잼(2) · 관리인(3) · 벽옥(2)
기존 상징 하나 감옥(prison, 3)
별칭 임자 옮김 「갈까마귀」 `crow` → 새 `jackdaw`
```

## 2. **표제어가 `Jailer` 라도 그 꿈의 사물이 「간수」가 아닐 수 있다**

「무리가 옥문을 부수려 함」을 `jailer` 에 붙였다가 **프로브가 「간수로는 안 걸린다」**고 잡아
`prison` 으로 옮겼다. 꿈에 있는 것은 옥문이지 간수가 아니다(§41 — 가르는 물음은 「그 꿈에
그 사물이 있는가」다). **표제어를 임자로 삼는 습관이 만든 자리**이고, 이번이 처음이다.

## 3. 옛 상처 — **「옥에 갇혔다」가 운영에서 `jade`(옥)로 답하고 있었다**

`prison` 의 화면 문구 여섯이 「옥에…」인데 별칭에 그 꼴이 없었다. 실측하면 이랬다.

```
「옥에 갇혀 앉아 있었다」   → jade / 「금은과 주옥을 봄」    ← 나가던 답이다
「남을 감옥에 집어넣었다」  → prison / 「남을 옥에 들여보냄」 (이쪽은 옳다)
```

「옥」은 한 글자라 별칭으로 못 올린다 — **구슬 옥과 감옥 옥이 같은 글자다.** 구절째
(「옥에 갇혀」·「옥에 들어갔다」·「옥에 집어넣었다」·「옥중」·「옥문」) 올려 **감옥도 함께
걸리게** 했다. **`jade` 쪽은 안 건드렸다** — 누가 「옥」의 임자인지는 사람이 정할 일이고,
코드가 대신 정하면 그 판단이 묻힌다(§28).

**구절 별칭도 낱말 경계에서 끝나야 한다** — 처음에 「옥에 갇」으로 올렸더니 뒤의 「혀」가
조사가 아니라 막혔다. §29 곁가지 ①이 **한 낱말이 아니라 구절에도 그대로 적용된다.**

## 4. 안 넣은 문장 하나 — **그림 자체가 비하다**(§31 앞갈래)

`Jail` 의 「To see negroes in jail…」은 한 인종이 곧 그 꿈의 내용이다. 항목을 만들지 않았고
커버리지가 비는 것이 옳다.

## 5. 관문

```
verify-dream-cite (밀러 전수)  항목 2,891개 · 위반 0건
verify-dream-km               위반 0건 (**고치기 전 1건** — 「ill」 ⊂ 「k*ill*」)
audit-km-dead-words           죽은 낱말 0개
커버리지                       입력 표제어 6 ↔ 결과 locator 6 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick / guide-numbers  전부 통과
probe-miller-batch  19건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 4건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 6. 지금 사전

| | 배치 66 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,272 | **1,277** |
| 의미 | 4,037 | **4,051** |
| 인용 | 4,055 | **4,069** |
| 밀러 표제어 | 1,062 | **1,068** / 2,264 (남은 **1,196**) |

---

# 배치 68 — 밀러 `January`~`Jay-bird`

## 1. 넣은 것 — 표제어 여섯, 항목 열여섯

```
새 상징 다섯   정월(1) · 황달(2) · 투창(3) · 턱(3) · 어치(3)
기존 상징 하나 항아리(jar, 4)
```

## 2. **새 갈래를 쓰면 안내 문서 23로케일이 거짓이 된다** — 이번 판에서 처음 밟았다

`january` 의 `category` 를 **`number`** 로 적었더니 `verify-guide-numbers` 가 빨간불을 냈다.

```
categories / symbol-dictionary — 갈래 수(한글): 문서 여덟(8) · 실제 9
```

**지금까지 쓰인 갈래는 여덟뿐이었고**(object·action·person·nature·animal·place·body·color),
`number` 를 처음 쓰는 순간 아홉이 된다. 안내 문서 열두 편이 **「갈래 여덟」이라고 글로 적고
있고 23개 언어로 번역돼 있다** — 상징 하나 때문에 그 스물세 편을 고칠 일이 아니다.

→ **한 달은 수가 아니다.** `autumn`(가을)·`day`(대낮)가 `nature` 인 것을 보고 같은 갈래로
돌렸다(2026-09-03). **갈래는 `CATEGORIES` 에 아홉이 허용돼 있지만 실제로 쓰이는 것은
여덟이고, 아홉째를 쓰려면 안내 문서 23로케일을 함께 고쳐야 한다.**

이것이 §26(「데이터를 갈면 안내 문서도 함께 거짓이 된다」)의 새 얼굴이다 — 지금까지는
**숫자가 자리표시자라 저절로 따라갔는데**, 이번은 **글로 적힌 「여덟」**이 걸렸고
`verify-guide-numbers` 가 그것을 지키고 있었다.

## 3. 프로브가 하나, 관문이 둘을 잡았다

```
verify-dream-km      「distress」 ⊂ 「distressing」
audit-km-dead-words  「빈」이 한 글자다
프로브 다른 뜻        「죽은 어치를 보았다」 → 「어치를 봄」(기본값이 「보았다」를 쥐고 있었다)
```

세 번째는 §30 곁가지 그대로다 — 기본값의 판별어를 좁혔다(「반갑 지저귀 날아」).

## 4. 관문

```
verify-dream-cite (밀러 전수)  항목 2,907개 · 위반 0건
verify-dream-km               위반 0건 (**고치기 전 1건**)
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 1개**)
verify-guide-numbers          통과 (**고치기 전 5건** — 위 §2)
커버리지                       입력 표제어 6 ↔ 결과 locator 6 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  17건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 1건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  1건(`jar`) — 있던 답으로 얼렸다(`FALLBACK_FIRST` 여든여섯)
```

## 5. 지금 사전

| | 배치 67 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,277 | **1,282** |
| 의미 | 4,051 | **4,067** |
| 인용 | 4,069 | **4,085** |
| 밀러 표제어 | 1,068 | **1,074** / 2,264 (남은 **1,190**) |

---

# 배치 69 — 밀러 `Jealousy`~`Jockey`

## 1. 넣은 것 — 표제어 다섯, 항목 열셋

```
새 상징 셋    젤리(2) · 재스민(1) · 기수(3)
기존 상징 둘  시샘(envy, 5) · 춤(dance, 2)
```

## 2. **표제어 셋을 통째로, 문장 둘을 더 건너뛰었다** — 한 판에 가장 많다

```
Jew         **그림 자체가 비하다**(§31 뒷갈래, `Gypsy` 와 같은 자리) — 여섯 문장이 전부
            한 겨레를 두고 재물욕·아첨·향락을 점친다. 비하를 빼면 **남는 꿈이 없다**
Jester      「어릿광대를 봄」이 `acrobat` 에 이미 있다(밀러 `Harlequin`)
Jew's-harp  한국어 이름 「구금」이 **「구금(拘禁)」과 동음이의어**라 상징 이름으로 못 쓰고,
            「To play one」의 그림은 `harp` 의 「하프를 손수 켬」에 이미 있다
Jig 첫 문장  `dance` 의 「제가 춤을 춤」과 그림이 같다
Jig 둘째     「To see negroes dancing a jig…」 — **그림 자체가 비하다**
```

**`Jew's-harp` 가 새 갈래다** — 지금까지 표제어를 뺀 이유는 ①그림이 비하 ②같은 그림이 이미
있다 ③오늘의 이용자에게 해로운 판정, 셋이었다. 이번은 **「한국어에 쓸 수 있는 이름이 없다」**다
(배치 31의 `Estate` 가 「이용자가 칠 말이 있나」로 붙일 자리를 찾은 것의 **막다른 끝**이다).

## 3. 성별만 다른 갈래를 넣을 수 있었던 까닭

「처녀가 정인을 시샘함」과 「정인을 시샘함」은 가르는 것이 성별뿐인데, 그 갈래가 **하나**이고
짝이 일반 문장이라 1:1 동점이 되고 **좁은 쪽을 앞에** 두어 풀었다(배치 55·57·58과 같은 자리).

## 4. 관문·프로브가 둘을 잡았다

```
verify-dream-km   「merry」를 기존 「즐거운 아이들이 춤추는 것을 봄」이 이미 쥐고 있었다
프로브 다른 뜻     「기수가 말에서 떨어졌다」 — 「떨어지」는 「떨어졌」에 안 걸린다
```

## 5. 관문

```
verify-dream-cite (밀러 전수)  항목 2,920개 · 위반 0건
verify-dream-km               위반 0건 (**고치기 전 1건**)
audit-km-dead-words           죽은 낱말 0개
verify-guide-numbers          통과
커버리지                       입력 표제어 5 ↔ 결과 locator 5 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  17건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 1건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 6. 지금 사전

| | 배치 68 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,282 | **1,285** |
| 의미 | 4,067 | **4,080** |
| 인용 | 4,085 | **4,098** |
| 밀러 표제어 | 1,074 | **1,079** / 2,264 (남은 **1,185**) |

---

# 배치 70 — 밀러 `Jewelry`·`Jewels` (새 상징을 하나도 안 세운 판)

## 1. 넣은 것 — 표제어 둘, 항목 열하나. **전부 `jade`(옥)에 붙였다**

⓪ grep 이 「보석」을 `jade` 의 별칭으로 찍었고, **`jade` 에는 이미 밀러 `Gems` 의 「보석을
봄」이 붙어 있다.** 새 `jewels` 를 세우고 「보석」을 넘기면 그 `Gems` 의미가 이용자의
「보석」으로는 안 걸리게 된다 — 임자에게 붙이는 것이 답이다.

## 2. 프로브가 **아홉**을 잡았다 — 기존 판별어 하나가 새 열하나를 전부 가로챘다

```
「보석을 봄」의 판별어가 「보석 패물」이었다
  → 「보석을 찾아냈다」·「보석을 샀다」·「깨진 보석붙이를 보았다」… 전부 1:1 동점이 되고
    앞에 있는 「보석을 봄」이 이긴다
```

**「보석」은 이 상징의 이름이나 마찬가지라 어느 의미에도 주면 안 된다.** 기존 쪽에서 빼고
(「패물」만 남김), 새 열하나는 **몸짓과 상태**로만 갈랐다(지녔·물려받·잃었·찾아냈·샀다·
깨진·삭아). §30 곁가지(「기존 판별어도 함께 좁힌다」)가 **한 판에 아홉 건**으로 나온 것은 처음이다.

그러면 「보석을 보았다」가 0점이 되어 주공해몽 기본값(「금은과 주옥을 봄」)으로 가므로,
**「보석을 봄」을 `FALLBACK_FIRST` 로 얼렸다**(새 값으로 바꾼 것 — 배치 54·66과 같은 자리).
이 상징에서 아무것도 안 걸렸을 때 떨어질 자리가 그것이다.

## 3. 옛 상처 둘 — **주공해몽 의미 둘이 한 번도 안 걸리고 있었다**

```
「금은과 주옥을 보았다」  → 안 걸림   「금은」이 별칭에 없고, 「주옥을」의 「옥」은 앞 글자
「주옥이 품에 가득했다」  → 안 걸림   「주」가 한글이라 `isStandalone` 이 막는다
```

별칭 「주옥」·「금은」을 올려 고쳤다. **화면 문구에 쓰인 말이 별칭에 없는 자리**의 세 번째다
(`newborn-baby` 62 · `iron-2` 65 · 이번 `jade` 70).

## 4. 안 넣은 문장 둘

```
Jewels 첫 문장           `jade` 의 「보석을 봄」(밀러 `Gems`)과 그림도 이름도 같다
Jewels 「To give jewels away…」  바로 앞 문장과 같은 그림이다(밀러가 한 표제어 안에서 되풀이했다)
```

## 5. 관문

```
verify-dream-cite (밀러 전수)  항목 2,931개 · 위반 0건
verify-dream-km               위반 0건 (**고치기 전 2건** — 「받았」 공유 · 「wear」 ⊂ 「wearing」)
audit-km-dead-words           죽은 낱말 0개
verify-guide-numbers          통과
커버리지                       입력 표제어 2 ↔ 결과 locator 2 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  15건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 11건** — 이 세션 최다)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건(얼림을 먼저 넣었다)
```

## 6. 지금 사전

| | 배치 69 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,285 | **1,285** (새 상징 0) |
| 의미 | 4,080 | **4,091** |
| 인용 | 4,098 | **4,109** |
| 밀러 표제어 | 1,079 | **1,081** / 2,264 (남은 **1,183**) |

---

# 배치 71 — 밀러 `Jolly`~`Judge`

## 1. 넣은 것 — 표제어 여섯, 항목 열넷

```
새 상징 넷    흥겨움(2) · 삯일꾼(2) · 축전(2) · 판사(3)
기존 상징 둘  먼 길 떠남(long-journey, 4) · 기쁨(joy, 1)
```

「여행」은 `long-journey` 의, 「기쁨」은 `joy` 의 것이다. **「판사」는 새로 세웠다** —
「재판」은 `lawsuit`(송사)의 것이지만 「재판관」은 「재판」 뒤가 한글이라 막히고, 판사는
사람이지 송사가 아니다.

## 2. **기본값 둘이 뒤집혔고 둘 다 새 값으로 얼렸다**

지금까지는 거의 다 있던 답으로 되돌렸는데, 이번은 둘 다 **바꾸는 것이 옳았다.**

```
joy           전: 집안에 기쁜 일이 있음   후: 어떤 일로 기쁨을 느낌
              → 있던 것은 「집안」이라는 조건이 붙었고, 들어온 것이 조건 없는 가장 넓은 그림이다
long-journey  전: 먼 길을 오가며 다님     후: 길을 떠남
              → 「길을 떠났다」가 이용자의 가장 흔한 말이고 상징 이름(「먼 길 떠남」)과도 같다
```

**`long-journey` 는 얼릴 수밖에 없었다.** 「떠났」을 판별어에 두면 형제(「벗들이 슬픈 낯으로
떠나는 것을 봄」)를 동점으로 가로채고, 빼면 그 의미가 0점이 된다 — **0점일 때 떨어지는
자리로 삼는 것**이 답이다(§30 곁가지). 풀이가 「즐거우면 이문, 궂으면 실망」이라 얼버무린
것도 아무것도 안 걸렸을 때의 답으로 알맞다.

## 3. 프로브가 넷을 잡았고, 그것을 고치다 둘이 더 나왔다

```
안 걸림  「길을 떠났다」            별칭 「길을 떠」+「났다」가 막힌다(§29 곁가지 ①)
안 걸림  「판결이 내게 이롭게 났다」 상징 이름이 문장에 없다 — 별칭 「판결」을 올렸다
다른 뜻  「벗들이 슬픈 낯으로…」    「떠났」을, 그다음엔 「벗들」을 형제가 나눠 쥐고 있었다
```

**한 문장을 고치자 다음 형제가 드러나는 자리**였다 — 「떠났」을 빼니 「벗들」이 남아 또
동점이 됐다. **판별어는 「형제 문장에도 있는 낱말」을 하나씩 걷어 내야 갈린다.**

## 4. 관문

```
verify-dream-cite (밀러 전수)  항목 2,945개 · 위반 0건
verify-dream-km               위반 0건
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 3개** — 「long」·「event」·「나」)
verify-guide-numbers          통과
커버리지                       입력 표제어 6 ↔ 결과 locator 6 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  16건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 6건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  2건 — **둘 다 새 값으로 얼렸다**(위 §2)
```

## 5. 지금 사전

| | 배치 70 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,285 | **1,289** |
| 의미 | 4,091 | **4,105** |
| 인용 | 4,109 | **4,123** |
| 밀러 표제어 | 1,081 | **1,087** / 2,264 (남은 **1,177**) |

---

# 배치 72 — 밀러 `Judgment Day`~`Jumping-jack`

## 1. 넣은 것 — 표제어 다섯, 항목 열셋

```
새 상징 넷    주전자(5) · 칠월(1) · 뜀뛰기(3) · 꼭두각시 인형(1)
기존 상징 하나 최후의 심판(doomsday, 3) ← 「심판의 날」이 그 별칭이다(밀러 `Doomsday` 로 이미 들어와 있다)
```

## 2. 판별어를 고를 때 부딪친 자리 셋 — **전부 「형제 문장에도 있는 낱말」**

```
doomsday  기존 「젊은 여성에게 …」가 「여자 여성 처녀」를 쥐고 있어 새 「심판대에서 유죄를
          들음」에 성별 말을 못 준다 → 「유죄·심판대」로 갈랐다
doomsday  기존 「최후의 심판을 기다림」이 「기다리」를 쥐고 있어 「죽은 이들이 일어나는 것을
          봄」은 「죽은·일어나·무덤에서」로만 적었다
jug       「주전자로 술을 마심」이 「마셨」을 쥐어 「맛없는 것을 마셨다」를 가로챘다 →
          「술을·포도주·들이켰」로 좁혔다
```

배치 71에 이어 **같은 병이 세 판 연속**이다 — 판별어를 적을 때 **형제 문장을 소리 내어 읽고
그 낱말이 거기에도 있는지** 본다.

## 3. 관문

```
verify-dream-cite (밀러 전수)  항목 2,958개 · 위반 0건
verify-dream-km               위반 0건
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 2개** — 「over」·「down」)
verify-guide-numbers          통과
커버리지                       입력 표제어 5 ↔ 결과 locator 5 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  15건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 1건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 4. 지금 사전

| | 배치 71 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,289 | **1,293** |
| 의미 | 4,105 | **4,118** |
| 인용 | 4,123 | **4,136** |
| 밀러 표제어 | 1,087 | **1,092** / 2,264 (남은 **1,172**) |

---

# 배치 73 — 밀러 `June`~`Kangaroo` (새 상징만 여섯, 패치 없음)

## 1. 넣은 것 — 표제어 여섯, 항목 열넷. **기존 상징을 하나도 안 건드렸다**

```
유월(2) · 노간주나무(2) · 배심원(3) · 정의(2) · 만화경(1) · 캥거루(4)
```

⓪ grep 에서 겹친 자리는 전부 앞 글자가 한글이라 안전했다(노간주나무 ⊃ 나무 · 배심원 ⊃ 배).

## 2. **셸 히어독이 홑따옴표에서 깨졌다** — §7·§31 곁가지를 또 밟았다

`m73.json` 을 히어독으로 쓰려다 인용문의 `a kangaroo's hide` 때문에 셸이
「unexpected EOF while looking for matching ' 」로 죽었고 **파일이 아예 안 만들어졌다.**

배치 56~72에서는 히어독으로 JSON 을 쓰는 것이 통했다 — **밀러 원문에 홑따옴표가 처음
나온 것**이 이번이다. §7이 「정규식·백틱」만 말하는 것으로 읽히기 쉬운데, **홑따옴표
하나로도 같은 사고가 난다.**

→ **원문 인용이 든 파일은 예외 없이 셸을 거치지 않는 도구로 쓴다.** 「지금까지 통했다」는
근거가 아니다(§15과 같은 자리).

## 3. 프로브가 하나 — **기본값이 「보았다」를 쥔 자리, 세 판 연속**

```
「캥거루를 봄」이 「보았다」를 쥐어 「캥거루 가죽을 보았다」를 가로챘다
```

배치 68(`jay-bird`)·70(`jade`)·72(`jug`)에 이어 네 번째다. **「~를 봄」 꼴의 기본값에는
「보았다」를 주지 않는다** — 그 자리는 0점으로 떨어지는 자리다.

## 4. 관문

```
verify-dream-cite (밀러 전수)  항목 2,972개 · 위반 0건
verify-dream-km               위반 0건
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 1개** — 「서 있」이 쪼개져 한 글자)
verify-guide-numbers          통과
커버리지                       입력 표제어 6 ↔ 결과 locator 6 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  14건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 1건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 5. 지금 사전

| | 배치 72 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,293 | **1,299** |
| 의미 | 4,118 | **4,132** |
| 인용 | 4,136 | **4,150** |
| 밀러 표제어 | 1,092 | **1,098** / 2,264 (남은 **1,166**) |

---

# 배치 74 — 밀러 `Katydids`~`Key`

## 1. 넣은 것 — 표제어 넷, 항목 열다섯

```
새 상징 둘    여치(2) · 열쇠(8)
기존 상징 둘  나무통(cask, 2) ← 밀러 `Keg` · 솥(cauldron, 3) ← 밀러 `Kettle`
```

**`Keg` 도 새로 안 세웠다** — 「작은 나무통」·「작은 술통」으로 지으면 그 이름이 `cask` 의
「나무통」·「술통」을 **공백 뒤에서 품어 둘 다 걸린다**(§25 곁가지).

## 2. **`key` 가 극성 하니스의 잠자던 기대치를 깨웠고, 이번엔 기대가 맞았다**

인계 ⑬의 열다섯 id 가운데 `key` 가 처음으로 들어왔다.

```
건너뜀 33쌍 / 15종  →  32쌍 / 14종      (parity 실패 0건)
```

배치 40에서는 깨어난 넷이 **전부 짐작이라 고쳐야 했는데**, 이번은 그대로 통과했다.
남은 열넷 — apple·car·clothes·knife·market·marriage·naked·president·rat·shoe·spider·
toilet·tooth-fall·wine.

## 3. 안 넣은 문장 둘

```
Kettle 「To see kettles…」        → `cauldron` 의 「큰 솥을 봄」과 그림이 같다
Kettle 「To see a broken kettle…」 → `cauldron` 의 「무쇠솥이 깨짐」과 그림이 같다
```

## 4. 프로브가 넷을 잡았다 — **동점 셋을 세 가지 수로 풀었다**

```
안 걸림  「깨진 통을 보았다」       별칭 「통이 깨」가 뒤의 「졌」에 막힌다 → **완전한 꼴로**
다른 뜻  「장신구 열쇠를 잃어버렸다」 「잃어버」와 「장신구」가 1:1 → **좁은 쪽을 파일 앞으로**
다른 뜻  「열쇠로 문을 잠갔다」      「문을」이 형제에도 있다 → **넓은 쪽에서 뺐다**
```

§25 곁가지 3의 세 수(활용형·차례·낱말 빼기)가 한 판에 다 나왔다.

## 5. 관문

```
verify-dream-cite (밀러 전수)  항목 2,987개 · 위반 0건
verify-dream-km               위반 0건
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 3개** — 「over」·「두」·「잠」)
verify-guide-numbers          통과
커버리지                       입력 표제어 4 ↔ 결과 locator 4 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건 (**`key` 가 깨어났고 그대로 통과**)
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  19건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 4건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  1건(`cauldron`) — 있던 답으로 얼렸다(`FALLBACK_FIRST` 아흔)
```

## 6. 지금 사전

| | 배치 73 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,299 | **1,301** |
| 의미 | 4,132 | **4,147** |
| 인용 | 4,150 | **4,165** |
| 밀러 표제어 | 1,098 | **1,102** / 2,264 (남은 **1,162**) |

---

# 배치 75 — 밀러 `Keyhole`~`King` (기존 상징 넷을 건드린 판)

## 1. 넣은 것 — 표제어 다섯, 항목 열다섯

```
새 상징 하나  콩팥(4)
기존 상징 넷  열쇠(key, 3) · 염소(goat, 1) · 살인(killing-someone, 2) · 임금(king, 5)
```

**`Keyhole` 이 「품음」의 가장 뚜렷한 자리다** — 「열쇠 구멍」은 「열쇠」를 **공백 앞에서 품어**
새 상징을 세우면 `key` 도 함께 걸린다. **바로 앞 배치(74)에서 넣은 상징에 붙이게 된 것**이
이번이 처음이다. 「새끼 염소」도 「염소」를 품는다.

## 2. 관문이 **여덟**을 잡았다 — 이 세션 최다

```
verify-dream-km      「defenseless」 ⊃ 「defense」 · 「disclosing」 ⊃ 「losing」 ·
                     「찾아냈」·「되찾」 ⊃ 「찾」 · 「threatened」 ⊃ 「eat」 ·
                     「confidence」를 배치 74의 「열쇠로 문을 엶」이 이미 쥐고 있었다
audit-km-dead-words  「못 찾」·「안」·「제」·「내」·「않」·「손도 못 쓰는」·「온 힘」이
                     공백에서 쪼개져 한 글자가 됐다 · 「over」가 불용어
```

**한 판에 기존 상징 넷을 건드리면 부딪칠 자리가 그만큼 는다** — 기존이 이미 쥔 낱말
(「제가·내가」·「앞에서」·「자리를」·「불러」)을 피해 새 판별어를 고르느라 세 번 되돌렸다.

## 3. 프로브가 둘

```
다른 뜻  「남이 열쇠 구멍으로 엿보고 있었다」  「엿보」와 「남이」가 1:1 → **차례로 풀었다**
안 걸림  「사나운 짐승을 죽였다」              `killing-someone` 의 별칭이 **사람이 든 구절뿐**
```

뒤엣것은 배치 48에서 **일부러 좁게 둔 자리**다(맨 「죽였다」를 올리면 「개가 뱀을 죽였다」까지
살인이 된다). 밀러 문장 그대로 **「사나운 짐승을 죽였다」라는 구절로만** 넓혔다.

## 4. 관문

```
verify-dream-cite (밀러 전수)  항목 3,002개 · 위반 0건
verify-dream-km               위반 0건 (**고치기 전 5건**)
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 8개**)
verify-guide-numbers          통과
커버리지                       입력 표제어 5 ↔ 결과 locator 5 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  19건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 2건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 5. 지금 사전

| | 배치 74 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,301 | **1,302** |
| 의미 | 4,147 | **4,162** |
| 인용 | 4,165 | **4,180** |
| 밀러 표제어 | 1,102 | **1,107** / 2,264 (남은 **1,157**) |

---

# 배치 76 — 밀러 `Kiss` (한 표제어로 한 판)

## 1. 넣은 것 — 표제어 하나, 항목 열셋. **새 상징 `입맞춤` 하나뿐이다**

밀러 `Kiss` 는 열세 문장이 전부 「누구와·어디서·어떻게」로 갈린다 — 아이들·어머니·형제자매·
어두운 데·밝은 데·낯선 여자·몰래·맞수·부부·목·적·들킴·정인이 딴 사람에게.

## 2. 열세 의미가 **한 번에 갈렸다** — 이 세션에서 가장 매끄러운 판

관문도 프로브도 **동점을 하나도 안 냈다.** 까닭은 뚜렷하다 — **가르는 것이 전부 「사람」과
「자리」라서 낱말이 겹칠 일이 없었다**(어머니·누이·낯선 여자·맞수·부부·적…).

지금까지 동점이 났던 판들은 **같은 몸짓을 조건으로 가르는** 자리였다(「보석을 …」·「열쇠를 …」·
「주전자로 …」). **무엇을 하느냐가 같고 조건만 다르면 부딪치고, 누구에게 하느냐가 다르면
안 부딪친다** — 판별어를 고르기 전에 이 갈래를 먼저 보면 일이 준다.

## 3. 프로브가 하나 — 또 별칭 경계다

```
안 걸림  「애인에게 입 맞추다 남에게 들켰다」  「입 맞추」+「다」가 막힌다
```

「입 맞추다」·「입을 맞추다」·「입 맞추는」·「입 맞춘」까지 올려 고쳤다.

## 4. 관문

```
verify-dream-cite (밀러 전수)  항목 3,015개 · 위반 0건
verify-dream-km               위반 0건 (**한 번에 통과** — 의미 열셋인데도)
audit-km-dead-words           죽은 낱말 0개 (**한 번에 통과**)
verify-guide-numbers          통과
커버리지                       입력 표제어 1 ↔ 결과 locator 1 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  13건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 1건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 5. 지금 사전

| | 배치 75 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,302 | **1,303** |
| 의미 | 4,162 | **4,175** |
| 인용 | 4,180 | **4,193** |
| 밀러 표제어 | 1,107 | **1,108** / 2,264 (남은 **1,156**) |

---

# 배치 77 — 밀러 `Kitchen`~`Knapsack`

## 1. 넣은 것 — 표제어 넷, 항목 열넷

```
새 상징 둘    연(5) · 배낭(2)
기존 상징 둘  부엌(kitchen, 2) · 고양이(cat, 5) ← 밀러 `Kitten`(원문이 「See Cats」다)
```

**「새끼 고양이」는 「고양이」를 공백 앞에서 품는다** — 새 상징을 세우면 둘 다 걸린다.
`cat` 의 별칭에 「새끼고양이」가 이미 있었다.

## 2. 한 글자 이름 「연」을 그대로 썼다 — **합성어가 열여섯인데도 안전하다**

⓪ grep 이 「연」을 품는 이름을 열여섯 찾았다(연행·연회·연대장·연장·연시·연지·연주·연기…).
**전부 「연」 뒤가 한글이라 `isStandalone` 이 막는다.** 위험한 것은 「연」 뒤에 **조사가 오는**
자리인데, 그런 말(자연이·우연히·인연을)은 **앞 글자가 한글**이라 또 막힌다.

→ **한 글자 이름은 「품는 것」이 몇 개인지가 아니라 「앞뒤가 무엇인지」로 판정한다.**
배치 66의 「섬」과 같은 자리다.

## 3. `cat` 이 열여섯이 되면서 판별어가 세 겹으로 부딪쳤다

```
기존 「깨끗한 흰 고양이」가 「하얀·깨끗한·흰색」을, 「여위고 지저분한」이 「여위·비쩍·지저분·
더러운」을 쥐고 있다  → 새 둘은 「통통·살진」과 「얼룩진·때가」로 갈랐다
영어 「clean」 ⊃ 「lean」                                → 「lean」을 뺐다
「kitten」이 `cat` 의 영어 별칭이라 제 이름이 됐다        → 판별어에서 뺐다
```

**「새끼」는 가장 넓은 새 의미에만 주고 그것을 파일 맨 뒤에 두었다** — 동점이면 앞의 좁은
쪽이 이기므로, 「새끼 고양이를 죽였다」가 「새끼 고양이를 봄」에 안 뺏긴다.

## 4. 기본값을 또 바꿨다

```
kitchen  전: 부엌과 부뚜막을 고쳐 지음   후: 부엌을 봄
```

있던 것은 **고쳐 짓는다는 조건**이 붙은 좁은 상황이고, 들어온 것이 가장 넓다
(배치 54·66·70·71에 이어 다섯 번째로 **바꾼** 자리다).

## 5. 관문

```
verify-dream-cite (밀러 전수)  항목 3,029개 · 위반 0건
verify-dream-km               위반 0건 (**고치기 전 1건**)
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 1개**)
verify-guide-numbers          통과
커버리지                       입력 표제어 4 ↔ 결과 locator 4 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  18건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 2건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  1건(`kitchen`) — **새 값으로 얼렸다**
```

## 6. 지금 사전

| | 배치 76 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,303 | **1,305** |
| 의미 | 4,175 | **4,189** |
| 인용 | 4,193 | **4,207** |
| 밀러 표제어 | 1,108 | **1,112** / 2,264 (남은 **1,152**) |

---

# 배치 78 — 밀러 `Knee`~`Knife Grinder` (하니스의 잘못된 기대를 고친 판)

## 1. 넣은 것 — 표제어 셋, 항목 열둘

```
새 상징 둘    무릎(6) · 칼갈이(2)
기존 상징 하나 칼(sword, 4) ← 밀러 `Knife`
```

## 2. **극성 하니스의 `knife` 기대는 애초에 생길 수 없는 id 였다**

인계 ⑬의 열넷 가운데 `knife` 는 **「칼」의 임자가 `sword` 라서 영영 안 깨어날 자리**였다.
기다리던 세 문장은 `sword` 에 **이미 있는 그림**이다.

```
「칼을 손에 쥐었다」  ↔ sword 「칼을 차고 길을 감」
「칼에 손을 다쳤다」  ↔ sword 「칼에 스스로 다침」
「칼을 잃어버렸다」   ↔ sword 「칼을 잃어버림」
```

배치 40에서 `god`·`gold` 가 그랬듯 **기대 자체가 틀렸다** — 지우지 않고 **실제 임자로 고쳤다.**
건너뜀이 **32쌍/14종 → 29쌍/13종**으로 줄었다.

**그러자 곧바로 하나가 빨간불이 됐다** — 「칼에 손을 다쳤다」가 ko 는 기본값으로, en 은
「칼에 스스로 다침」으로 갔다. 그 의미의 한국어 판별어가 「베였·베어서·자기손·상처」뿐이라
「손을 다쳤다」를 못 잡고 있었던 것이다. **고침이 관문을 빨갛게 만든 것이 아니라 관문이 눈을
뜬 것이다**(§28 · 배치 64와 같은 자리).

## 3. **차례로도 못 푸는 자리가 처음 나왔다** — 문장을 뺐다

밀러 「To dream that you stab another with a knife…」를 넣었더니 「부러진 칼로 남을 찔렀다」를
가로챘다(「찔렀·남을」로 2점, 기존은 「부러진」 1점).

**차례로 못 푼다** — `m78.json` 이 `r*.json` 보다 먼저 정렬되므로 **새 의미가 늘 앞**이고,
「찔렀」은 형제 문장에도 있어 나눠 가질 수도 없다. `sword` 에 「부러진 칼로 남을 찌름」·
「칼로 남을 벰」이 이미 있으므로 **넣지 않는 것이 답**이었다(§31 곁가지).

→ **기존 상징에 붙일 때는 「차례로 풀 수 있는가」를 먼저 본다.** 새 파일이 기존 파일보다
앞서 정렬되면 **좁은 기존 의미가 늘 진다** — 그때는 낱말로 갈리지 않는 문장을 아예 안 넣는다.

## 4. 기본값을 또 바꿨다

```
sword  전: 깃발과 칼이 산속으로 이끌어 들임   후: 칼을 봄
```

있던 것은 주공해몽의 아주 좁은 그림이다. **의미가 스물일곱인 상징이라 떨어질 자리를 제대로
잡는 것이 특히 중요하다**(여섯 번째로 바꾼 자리).

## 5. 관문

```
verify-dream-cite (밀러 전수)  항목 3,041개 · 위반 0건
verify-dream-km               위반 0건 (**고치기 전 5건** — sharp ⊂ sharpen · stab ⊂ stabbing ·
                              판별어에 상징 이름 그대로)
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 1개**)
verify-guide-numbers          통과
커버리지                       입력 표제어 3 ↔ 결과 locator 3 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건 (**고치기 전 1건** — 위 §2) · 건너뜀 29쌍/13종
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  17건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 1건** — 위 §3)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  1건(`sword`) — **새 값으로 얼렸다**
```

## 6. 지금 사전

| | 배치 77 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,305 | **1,307** |
| 의미 | 4,189 | **4,201** |
| 인용 | 4,207 | **4,219** |
| 밀러 표제어 | 1,112 | **1,115** / 2,264 (남은 **1,149**) |

---

# 배치 79 — 밀러 `Knitting`~`Krishna`

## 1. 넣은 것 — 표제어 다섯, 항목 열하나

```
새 상징 넷    문고리(1) · 노크 소리(2) · 매듭(2) · 크리슈나(1)
기존 상징 하나 코바늘뜨기(crochet-work, 5) ← 밀러 `Knitting`
```

「뜨개질」은 `crochet-work` 의 별칭이다(밀러 `Crochet Work` 로 이미 들어와 있다).

## 2. 성별 갈래 둘을 **넣을 수 있었던 까닭** — 원문이 낱말을 갈라 주었다

「여자가 뜨개질을 함」과 「처녀가 뜨개질을 함」은 그림이 같고 꾸는 사람만 다르다. 배치 50~52
에서 이런 자리를 뺐지만 **그때는 갈래가 「여자·여성·처녀」를 나눠 가질 수 없는 꼴**이었다.
여기는 밀러가 `a woman` 과 `a young woman` 으로 또렷이 나눠 적어서 **「여자」와 「처녀」로
낱말이 갈린다.**

→ **가르는 물음은 「성별 갈래가 몇이냐」가 아니라 「원문이 서로 다른 말로 적었느냐」다**
(배치 55에서 좁힌 규칙의 한 겹 더 안쪽).

## 3. 안 넣은 문장 하나

`Knots` 의 「If your sweetheart notices another…」는 **꿈 그림이 아니라 앞 항목 풀이의
뒷말**이다(배치 55와 같은 자리).

## 4. 이름을 「노크 소리」로 지은 까닭

「문 두드리는 소리」로 지으면 그 이름이 **「문」을 공백 앞에서 품어** `door` 도 함께 걸린다.
「노크 소리」로 짓고 「문 두드리는 소리」는 **별칭으로만** 올렸다 — 별칭은 화면에 안 뜨므로
이용자가 그렇게 써도 걸리되 이름은 겹치지 않는다.

## 5. 관문

```
verify-dream-cite (밀러 전수)  항목 3,052개 · 위반 0건
verify-dream-km               위반 0건 (**한 번에 통과**)
audit-km-dead-words           죽은 낱말 0개 (**고치기 전 3개** — 「crochet」가 제 이름 · 「깼」·「떴」)
verify-guide-numbers          통과
커버리지                       입력 표제어 5 ↔ 결과 locator 5 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  12건 · 안 걸림 0 · 다른 뜻 0 (**한 번에 통과**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 6. 지금 사전

| | 배치 78 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,307 | **1,311** |
| 의미 | 4,201 | **4,212** |
| 인용 | 4,219 | **4,230** |
| 밀러 표제어 | 1,115 | **1,120** / 2,264 (남은 **1,144**) |

---

# 배치 80 — 밀러 `Label`~`Lace` (새 상징만 다섯, 패치 없음)

## 1. 넣은 것 — 표제어 다섯, 항목 열여섯. **기존 상징을 하나도 안 건드렸다**

```
꼬리표(1) · 노역(3) · 실험실(2) · 미로(4) · 레이스(6)
```

## 2. ⓪ grep 에서 겹친 자리 다섯이 **전부 앞 글자가 한글이라 안전했다**

```
실험실 ⊃ 실(thread)      레이스 ⊃ 이(teeth)      연금술 ⊃ 술(liquor)·연(kite)
미로   ⊂ 호미로(hoe)      — 거꾸로 품히는 자리인데 앞 글자가 한글이라 막힌다
```

**「연금술」이 배치 77에서 넣은 「연」과 부딪칠 뻔한 자리**였다 — 뒤가 「금」이라 막힌다.
새 상징이 늘면 이런 자리가 늘지만 **판정은 늘 앞뒤 한 글자로 끝난다.**

## 3. 프로브가 둘 — 또 별칭 경계다

```
안 걸림  「짐승이 … 힘써 일하고 있었다」   별칭 「힘써 일하」 + 「고」가 막힌다
안 걸림  「사람들이 힘써 일하고 있었다」    〃
```

같은 판의 「제가 힘써 일했다」는 별칭 「힘써 일했다」로 걸렸다 — **한 꼴만 올리면 나머지가
전부 죽는다.** 「~고」·「~는」·「~한」까지 올려 고쳤다.

## 4. 관문

```
verify-dream-cite (밀러 전수)  항목 3,068개 · 위반 0건
verify-dream-km               위반 0건 (**한 번에 통과**)
audit-km-dead-words           죽은 낱말 0개 (**한 번에 통과**)
verify-guide-numbers          통과
커버리지                       입력 표제어 5 ↔ 결과 locator 5 — 전부 채움
민감 표현 grep                 걸린 것 없음
audit-cross-symbol-aliases    맞바꿔 쥔 짝 0쌍
verify-dream-context-parity   실패 0건
verify-dream-match / conception-pick  전부 통과
probe-miller-batch  16건 · 안 걸림 0 · 다른 뜻 0 (**고치기 전 2건**)
tsc  exit 0
조립기 「기본값이 바뀐 상징」  0건
```

## 5. 지금 사전

| | 배치 79 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,311 | **1,316** |
| 의미 | 4,212 | **4,228** |
| 인용 | 4,230 | **4,246** |
| 밀러 표제어 | 1,120 | **1,125** / 2,264 (남은 **1,139**) |

---

## 배치 81 — 밀러 `Ladder`~`Lament` (13건)

새 상징 넷 — `ladder`(사다리) · `ladle`(국자) · `lagoon`(석호) · `lament`(애통해함).
기존 상징 하나에 붙였다 — `crippled`(몸이 성치 않은 이) ← 밀러 `Lame`.

**밀러 `Lame` 은 새 상징을 세우지 않았다.** 본문이 한 줄이고 끝이 `[109] See Cripple.` 로
제 임자를 가리킨다. `crippled` 가 밀러 `Crippled` 로 이미 들어와 있었다.

**성별 갈래를 넣을 수 있었다.** 배치 55에서 좁혀 적은 대로 부딪치는 것은 **성별이 유일한
구분일 때**이고, 여기는 그 갈래가 하나뿐이라 「여자가」를 새 의미가 홀로 쥐면 된다.

**별칭에 「다리를 저는」을 안 올렸다.** 그 문장이 `bridge`(다리)에도 걸려 화면에 상징이 둘
뜬다(§25 곁가지 — `findTerm` 은 상징마다 따로 돈다). 「절뚝이는」 꼴로만 올렸다.

**사다리는 「사다리를 올라감」을 첫째로 옮겨 적었다.** 밀러의 차례대로면 「오르라고 사다리를
세워 줌」이 첫째가 되는데, 그것이 곧 **기본값**이라 막연한 「사다리 꿈을 꿨다」가 남이 사다리를
세워 주는 그림으로 떨어진다. 새 상징이라 `FALLBACK_FIRST` 를 쓸 것 없이 **추출 파일 안에서
차례를 바꾸는 것**으로 끝났다.

**프로브의 지킴 케이스가 이미 나 있던 상처를 하나 잡았다.**

```
「몸이 성치 않은 사람들이 굶주리고 있었다」   ✗ 안 걸림
  이름은 「몸이 성치 않은 이」, 별칭은 「성치 않은 몸」 — 가장 자연스러운 말차례가 없었다
  → 「몸이 성치 않은」·「성치 않은 사람」을 별칭에 올렸다
```

이번 배치가 만든 것이 아니라 `crippled` 가 심어진 뒤로 한 번도 안 걸린 자리다. 그 상징을
건드리는 이번이 **유일하게 눈이 가는 때**였다(§29 곁가지 ①과 같은 자리).

**관문 전부 초록불** — 인용 4,276건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 13건 전부 걸림.

**사전 1,320 / 4,241 / 4,259** (상징 / 의미 / 인용).

---

## 배치 82 — 밀러 `Lake` (10건)

새 상징 하나 — `lake`(호수). 표제어 하나가 열두 문장이라 한 판을 통째로 먹었다.

**「호수」는 아무도 안 쓰고 있었다** — ⓪ 전수 grep 0건. `fish-pond`(연못) · `bog`(늪지)가
가까운 자리이나 이름이 겹치지 않는다. 별칭에 **「호숫가」**를 따로 올렸다 — 「호숫가」에는
「호수」라는 글자가 없다(「숫」≠「수」).

**밀러의 차례를 바꿔 「맑고 잔잔한 호수에서 벗들과 배를 띄움」을 첫째로 두었다.** 밀러의
첫 문장은 「처녀가 거친 흙탕 호수에 홀로 있음」인데, 그것이 곧 **기본값**이라 막연한
「호수 꿈을 꿨다」가 그 좁은 그림으로 떨어진다. 배치 81의 사다리와 같은 손질이다.

**안 넣은 문장 하나** — `It may predict the illness of some one near her.` 는 앞 항목 풀이의
뒷말이지 **꿈 그림이 아니다**(배치 55·79와 같은 자리).

**판별어를 나눌 때 어려웠던 자리 — 흙탕 호수가 셋이다.**

```
메마른 바위와 헐벗은 나무   「바위 헐벗」    ← 「메마른」을 안 쥔다(아래와 부딪친다)
푸른 나무                  「푸른」
맑으나 메마른 땅           「메마른 땅에」
```

첫째 문장에도 「메마른」이 들어 있어서, 그 낱말을 첫째가 쥐면 셋째가 영영 안 뽑힌다.
**둘러싼 것이 무엇인가**로만 갈랐다(§30 곁가지 — 넓은 낱말은 그 상황을 실제로 말하는 쪽에만).

**비침 둘도 같은 손질이 필요했다.** 「제 모습이 비침」에 「비쳤」을 주면 「우거진 나무가
호수에 비쳤다」가 동점으로 그쪽에 간다 — 「모습이」 하나만 남겼다.

**프로브 문장 하나를 고쳤다.** 「배에 물이 들어왔지만 애써 나루에 닿았다」에는 **「호수」가
없다.** 원문의 그 꿈에는 호수가 들어 있으므로(배가 그 호수 위에 있다) **데이터가 아니라
문장을 고쳤다**(§25 곁가지 — 가르는 물음은 「원문의 그 꿈에 그 사물이 있는가」).

**관문 전부 초록불** — 인용 10건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 10건 전부 걸림.

**사전 1,321 / 4,251 / 4,269** (상징 / 의미 / 인용).

---

## 배치 83 — 밀러 `Lamb` (15건)

**새 상징을 하나도 안 세웠다.** 열다섯 전부 `sheep`(양)에 붙였다 — 주공해몽 일곱과 합쳐
**스물둘**이 됐다.

**「어린 양」을 새 상징으로 세우지 않은 까닭.** 「어린 양」은 기존 이름 「양」을 **부분
문자열로 품고**, 그 「양」은 앞이 공백이고 뒤가 조사라 `isStandalone` 을 지난다 — 새 상징을
세우면 「어린 양을 보았다」에 **상징이 둘 뜬다**(§25 곁가지). 밀러 자신도 `[108] See Sheep.`
으로 제 임자를 가리킨다.

**기본값을 되돌렸다.** `m83.json` 이 `r*.json` 보다 먼저 정렬되어 「어린 양들이 푸른 들에서
뛰놂」이 첫째로 밀려 올라왔다. 관문이 멈춰 세웠고, `FALLBACK_FIRST` 에 옛 값(「양을 끌고
집으로 돌아옴」)을 적어 얼렸다 — 밀러 쪽은 **어린 양**이라는 좁은 그림이라 막연한 「양 꿈」이
떨어질 자리가 아니다.

**기존 판별어를 함께 좁혔다**(§30 곁가지). 「어미 양과 새끼 양을 봄」이 **「어린」**을 쥐고
있었다. 새로 넣는 열다섯이 전부 「어린 양」이라 그대로 두면 **모든 새 문장이 그 옛 의미에
1점을 준다.** 「어미 새끼 젖먹이」만 남겼다.

**관문 둘이 각각 하나씩 잡았다.**

```
verify-dream-km   [en] 기존 「mother」 ⊂ 새 「mothers」        → 새 쪽에서 뺐다(배치 43)
프로브            「어린 양들이 …」가 하나도 안 걸림           → 별칭에 「양들」을 올렸다
```

뒤엣것이 §25 곁가지 4의 **여섯 번째 재발**이다 — 「들」은 조사가 아니라 접미사라
`isStandalone` 이 「양」도 「어린 양」도 낱말 조각으로 버린다. 「양떼」는 있었지만 「양들」은
없었다. **검사기 셋은 전부 초록불이었다.**

**영어에서 피한 것 셋** — `sheep` 의 `aliases_en` 에 「lamb」이 있어 **제 이름**이라 쓸 수
없고, 「slaughter」는 기존 「slaughtered」의, 「own」은 기존 「town」의, 「skin」은 새
「skins」의 부분 문자열이다.

**관문 전부 초록불** — 인용 15건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 18건(지킴 셋 포함) 전부 걸림.

**사전 1,321 / 4,266 / 4,284** (상징 / 의미 / 인용).

---

## 배치 84 — 밀러 `Lamp` (12건)

**새 상징을 안 세웠다.** 열둘 전부 `lamp`(등불)에 붙였다 — 주공해몽 하나와 합쳐 열셋이 됐다.
그 상징은 의미가 하나뿐이라 **판별어 표가 비어 있었으므로 기존 의미까지 채웠다**
(배치 35·79·81과 같은 자리).

**기본값을 되돌렸다.** 밀러의 첫 문장 「기름이 가득 찬 등불을 봄」은 조건이 붙은 그림이고,
막연한 「등불 꿈」이 떨어질 자리는 주공해몽의 「등불과 촛불이 밝게 빛남」이다.

**별칭에 「램프」를 안 올렸다.** `gas-lamp` 의 별칭 「가스 램프」에 들어 있어, 올리면
「가스 램프를 보았다」에 상징이 둘 뜬다(§25 곁가지). 대신 합성어 **「등잔불」**을 올렸다 —
「등잔」 뒤의 「불」이 조사가 아니라 그대로는 안 걸린다.

**프로브가 동점 하나를 잡았고, 차례로 풀었다.**

```
「들고 가던 등불이 꺼졌다」   들고=1(등불을 들고 다님) : 꺼졌=1(들고 가던 등불이 꺼짐)
  → 넓은 쪽에서 「들고」를 뺄 수 없다(「들고 다」는 공백에서 쪼개져 다시 「들고」가 된다)
  → **좁은 쪽을 추출 파일에서 앞에 두었다**(§25 곁가지 3)
```

**영어에서 피한 것 둘** — 「oil」은 `aliases_en` 의 「oil lamp」에 들어 있어 제 이름이 될 수
있고, 「light」는 「lighted」의 부분 문자열이다.

**관문 전부 초록불** — 인용 12건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 13건(지킴 하나 포함) 전부 걸림.

**사전 1,321 / 4,278 / 4,296** (상징 / 의미 / 인용).

---

## 배치 85 — 밀러 `Lamp-post`~`Landau` (10건)

새 상징 하나 — `lamp-post`(가로등). 나머지 셋은 임자에게 붙였다.

```
spear(창)      ← 밀러 `Lance` 둘     「긴 창」이 「창」을 품는다
ground(땅)     ← 밀러 `Land` 셋
carriage(마차) ← 밀러 `Landau` 둘    「무개마차」가 「마차」를 품는다
```

**안 넣은 문장 하나 — 같은 그림이 이미 들어와 있다**(§31 곁가지).
`Lance` 의 「To dream of a lance, denotes formidable enemies…」는 주공해몽 「창을 봄」과
**같은 그림**이다. `work` 가 달라 이름을 갈라야 하는데, 갈라 봐야 「창을 봄」과 「창이
나타남」이 되어 **판별어를 나눌 수 없다.** 커버리지에서 이 문장이 비는 것이 옳다.

**기본값 둘을 되돌렸다.** `m85.json` 이 `r*.json` 보다 먼저 정렬되어 「창에 찔려 다침」과
「땅이 기름져 보임」이 첫째로 밀려 올라왔다. 둘 다 조건이 붙은 그림이라 주공해몽의 넓은
그림으로 되돌려 얼렸다. **`carriage` 는 안 바뀌었다** — 그 상징은 이미 밀러(`m10.json`)에서
왔고 `"m10.json" < "m85.json"` 이라 옛 의미가 그대로 앞이다.

**「무개마차」·「뭍을」을 별칭으로 따로 올렸다.** 「무개마차」의 「마차」는 앞 글자가 한글이라
그대로는 안 걸리고(합성어), 「뭍」은 한 글자라 **한 글자 이름(`땅`)의 별칭이 될 수 없다**
(§29 곁가지 ② · 배치 41) — 조사를 붙인 「뭍을」·「뭍이」로 올렸다.

**기존 판별어를 피해 갈랐다.** `carriage` 의 「마차를 타고 감」이 「타고 탔다 올라」를 쥐고
있어, 새 「벗이나 정인과 함께 마차를 타고 감」은 **함께 탄 사람**으로만 갈랐다.

**영어에서 피한 것 셋** — `spear` 의 `aliases_en` 에 「lance」가, `ground` 의 것에 「land」·
「earth」가 있어 **제 이름**이라 점수에서 빠진다.

**관문 전부 초록불** — 인용 10건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 12건(지킴 셋 포함) 전부 걸림.

**사전 1,322 / 4,288 / 4,306** (상징 / 의미 / 인용).

---

## 배치 86 — 밀러 `Lantern` (10건)

새 상징 하나 — `lantern`(초롱). 표제어 하나가 여덟 문단이라 한 판을 통째로 먹었다.

**「초롱」은 아무도 안 쓰고 있었다** — ⓪ 전수 grep 0건. `lamp`(등불)의 `aliases_en` 에
「lantern」이 **있지만** 그것은 영어 쪽 이야기이고, 한국어 이름 「등불」·「등잔」·「촛불」·
「호롱불」과는 글자가 겹치지 않는다. 별칭으로 「초롱불」·「등롱」·**「손전등」**을 올렸다 —
셋 다 앞 글자가 한글이라 기존 이름(`back`=등, `fire`=불)에 안 걸린다.

**배치 84에서 만난 동점을 이번에는 처음부터 피했다.** `lamp` 과 똑같이 「들고 다님」과
「들고 가던 것이 꺼짐」이 함께 있어서, **좁은 쪽(꺼짐)을 추출 파일에서 앞에** 두고 적었다
(§25 곁가지 3). 한 배치 앞에서 프로브가 잡아 준 것을 그대로 옮긴 것이다.

**기본값도 그 자리에서 좁혔다.** 첫 의미 「어둠 속에서 초롱이 앞서 감」에 「앞서」를 주면
「앞서 가던 초롱이 갑자기 사라졌다」가 동점으로 기본값에 간다 — **「어둠」·「캄캄」만** 남겼다.

**성별 갈래 둘을 넣었다.** 「처녀가 정인의 초롱에 불을 켬」과 「처녀가 초롱불을 불어 끔」은
가르는 것이 성별이 아니라 **불을 켜느냐 끄느냐**다(배치 55에서 좁혀 적은 대로).

**관문 전부 초록불** — 인용 10건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 10건 전부 걸림.

**사전 1,323 / 4,298 / 4,316** (상징 / 의미 / 인용).

---

## 배치 87 — 밀러 `Lap`~`Latch` (16건)

새 상징 둘 — `lard`(돼지기름) · `lark`(종달새). 기존 상징 둘에 붙였다 — `knee`(무릎) ←
밀러 `Lap` 넷 · `blanket`(담요) ← 밀러 `Lap-robe` 둘.

**안 넣은 표제어 둘 — 같은 그림이 이미 들어와 있다**(§31 곁가지).

```
Lap-dog  두 문장이 `dog` 의 「곱게 꾸민 애완견을 봄」·「여위고 더러운 개를 봄」과 같은
         그림이다(밀러 `Dogs` 에서 이미 들어왔다). 「애완」을 그 의미가 쥐고 있어
         판별어를 나눌 수도 없다
Latch    두 문장이 `bolts` 의 「빗장(볼트) 꿈을 꿈」·「빗장이 낡거나 부러짐」과 같은
         그림이고, 「걸쇠」는 이미 `bolts` 의 별칭이다
         (끝 문장 「Sickness is also foretold in this dream.」은 꿈 그림이 아니다)
```

**커버리지에서 이 둘이 비는 것이 옳다.**

**품는 관계를 세 번 따졌다.**

```
무릎덮개 ⊃ 「무릎」(knee)   위험  → 새 상징을 안 세우고 `blanket` 에 붙였다
                                  (그 별칭에 이미 「무릎담요」가 있다)
돼지기름 ⊃ 「돼지」·「기름」  안전  → 「돼지」 뒤가 「기」, 「기름」 앞이 「지」라 둘 다 막힌다
종달새   ⊃ 「새」·「달」·「종」 안전  → 셋 다 앞 글자가 한글이라 막힌다
```

**`category` 를 `food` 로 적었다가 관문이 잡았다.** 여덟 갈래에 없는 값이라
`verify-dream-cite` 가 위반 2건을 냈다 — `food`(음식) · `bread`(빵) · `meat`(고기) ·
`oil`(기름)이 전부 `object` 다. 그대로 넣었으면 **안내 문서 23로케일의 「갈래 여덟」이
거짓이 됐을 것**이다(§26 곁가지 · 배치 68과 같은 자리).

**프로브가 동점 하나를 잡았고 차례로 풀었다.** 「무릎덮개를 잃어버렸다」가 「무릎덮개를 봄」
으로 갔다 — 넓은 쪽에서 「덮개를」을 빼고 「무릎덮개」 하나만 남긴 뒤 **좁은 쪽을 앞에**
두었다(§25 곁가지 3 · 배치 84·86과 같은 손질).

**관문 전부 초록불** — 인용 16건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 18건(지킴 둘 포함) 전부 걸림.

**사전 1,325 / 4,314 / 4,332** (상징 / 의미 / 인용).

---

## 배치 88 — 밀러 `Latin`~`Laundry` (16건)

새 상징 넷 — `latin`(라틴어) · `laudanum`(아편) · `laughing`(웃음) · `laundry`(빨래).
넷 다 ⓪ 전수 grep 0건이라 임자 다툼이 없었다. 패치 파일이 없는 판이다.

**「웃음」은 `comedy` 의 별칭 「웃음극」에 들어 있지만 안전하다** — 「웃음」 뒤의 「극」이
조사가 아니라 `isStandalone` 이 막는다. 「웃음극을 보았다」는 `laughing` 을 안 부른다.

**프로브가 §29 곁가지 ①을 두 자리에서 잡았다.**

```
「어여쁜 처녀들이 빨래하고 있었다」  ✗  「빨래」 뒤의 「하」가 조사가 아니다
「세탁부가 집에 찾아왔다」          ✗  「세탁」 뒤의 「부」가 조사가 아니다
  → 「빨래하고」·「빨래하는」·「빨래했다」·「세탁부」·「세탁소」를 별칭에 올렸다
```

**이름이 사물이 아니라 일이면 활용형 별칭을 처음부터 함께 올려야 한다**(배치 63과 같은
자리). 「빨래」·「세탁」은 사물이면서 동시에 하는 일이라 이용자가 「빨래하고」라고 쓴다.

**관문 전부 초록불** — 인용 16건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 16건 전부 걸림.

**사전 1,329 / 4,330 / 4,348** (상징 / 의미 / 인용).

---

## 배치 89 — 밀러 `Laurel`~`Lawyer` (12건)

**새 상징을 하나도 안 세웠다.** 넷 다 임자에게 붙였다.

```
bay-tree(월계수)  ← 밀러 `Laurel` 둘        「월계관」이 이미 그 별칭이다
lawsuit(송사)     ← 밀러 `Law and Lawsuits` 넷
grass(풀)         ← 밀러 `Lawns` 다섯       「잔디밭」이 「잔디」를 품는다
attorney(변호사)  ← 밀러 `Lawyer` 하나      끝에 `[112] See Attorney.`
```

**밀러 `Laurel` 의 첫 문장은 인용을 포갰다**(§31 곁가지). 밀러 `Bay Tree` 로 이미 들어와
있는 「월계수(베이 트리) 꿈을 꿈」과 **같은 그림이고 같은 `work`** 라, 이름을 그대로 쓰자
조립기가 인용 둘을 한 의미에 포갰다(배치 32 Fat 과 같다). 첫 의미의 인용이 둘이 됐다.

**기본값을 되돌렸다.** `m89.json` 이 `r*.json` 보다 먼저 정렬되어 「송사에 휘말림」이 첫째로
올라왔다 — 흉하고 좁은 그림이라 주공해몽의 「관가에 들어가 송사함」으로 되돌려 얼렸다.

**관문 셋이 각각 하나씩 잡았다.**

```
verify-dream-km  판별어에 상징 이름(월계수)을 그대로 넣었다  → 별칭 쪽 낱말(「베이 트리」)로
프로브           「젊은이가 법을 배우고 있었다」가 안 걸림    → 「법을 배우고」 꼴 넷을 별칭에
프로브           「풀숲에 뱀이 …」가 안 걸림                 → 「풀숲」을 별칭에(「풀」 뒤의
                                                              「숲」이 조사가 아니다)
```

가운데 것이 새 얼굴이다 — **상징 이름이 문장에 아예 없는데도 넣어야 하는 자리**다. 밀러
표제어가 `Law and Lawsuits` 라 「법」 쪽 말도 이 상징의 것인데, 한국어 이름 「송사」·「소송」·
「재판」·「고소」에는 그 말이 없었다.

**관문 전부 초록불** — 인용 12건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 14건(지킴 셋 포함) 전부 걸림.

**사전 1,329 / 4,341 / 4,360** (상징 / 의미 / 인용).

---

## 배치 90 — 밀러 `Lazy`~`Leaping` (9건)

새 상징 하나 — `leak`(누수). 나머지 셋은 임자에게 붙였다.

```
idleness(게으름)  ← 밀러 `Lazy` 둘
lead(납)          ← 밀러 `Lead` 다섯
jumping(뜀뛰기)   ← 밀러 `Leaping` 하나(끝에 `[113] See Jumping.`)
```

**밀러 `Lazy` 의 첫 문장은 인용을 포갰다**(§31 곁가지 · 배치 89와 같은 손질). 밀러
`Idleness` 로 이미 들어와 있는 「제가 빈둥거림」과 같은 그림이고 같은 `work` 라, 이름을
그대로 쓰자 인용 둘이 한 의미에 포개졌다.

**기본값을 바꿨다**(되돌린 것이 아니다). 주공해몽의 「납과 주석을 봄」은 **주석까지 있어야**
하는 좁은 그림이고, 밀러가 들여온 「납을 봄」이 이 상징의 가장 넓은 자리다 — 배치 54·66·
70·77·78과 같은 판단이다.

**판별어에 상징 이름을 못 쓴다.** `lead` 는 이름이 「납」이라 「납을」·「땜납」 같은 낱말이
전부 `verify-dream-km` 에 걸린다(배치 89에서 「월계수」로 한 번 걸린 자리다).
「잿빛」·「광산」·「광석」처럼 **딴 낱말로** 갈랐다.

**기존 판별어를 좁혔다**(§30 곁가지). `jumping` 의 「무엇을 뛰어넘음」이 「넘었다」까지 쥐고
있어, 새 「처녀가 가로막힌 것을 뛰어넘음」과 동점이 되고 파일 차례로는 기존이 앞이라 새
의미가 영영 안 뽑힌다. 「뛰어넘」 하나만 남겼다 — 「담을 넘었다」는 0점으로 기본값(그
의미 자신)에 떨어지므로 답이 그대로다.

**프로브가 §29 곁가지 ①을 또 잡았다.** 「정인이 게으르다고 여겼다」가 안 걸렸다 — 별칭에
「게을러」·「게으르게」는 있었으나 **「게으르다」 꼴이 없었고**, 「게으르다」 뒤의 「고」는
조사가 아니다. 「게으르다고」·「게으른」·「게을렀다」를 올렸다. 이미 있던 상처다.

**관문 전부 초록불** — 인용 9건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 12건(지킴 다섯 포함) 전부 걸림.

**사전 1,330 / 4,349 / 4,369** (상징 / 의미 / 인용).

---

## 배치 91 — 밀러 `Learning`~`Leaves` (13건)

새 상징 하나 — `leaves`(나뭇잎). 기존 상징 둘에 붙였다 — `being-taught-letters`(글을 배움)
← 밀러 `Learning` 넷 · `hide`(짐승 가죽) ← 밀러 `Leather` 다섯.

**인용을 셋이나 포갰다**(§31 곁가지 · 배치 32·89·90과 같다). 셋 다 같은 그림이고 같은
`work` 다 — 이름을 그대로 쓰자 조립기가 인용을 한 의미에 포갰다.

```
Learning 「To dream of learning…」       → 밀러 `Education` 의 「배움을 얻고자 애씀」
Learning 「To enter halls, or places…」  → 밀러 `Education` 의 「배우는 곳에 있음」
Leather  「To dream of leather…」        → 밀러 `Hide` 의 「짐승 가죽을 봄」
```

**프로브가 이미 나 있던 상처 셋을 잡았다.** 전부 `being-taught-letters` 다.

```
「학식 있는 이들을 보았다」    ✗  별칭에 「학식」 꼴이 없었다 — 이번 배치가 만든 자리
「학교에서 공부하고 있었다」   ✗  별칭 「공부하」 뒤의 「고」가 조사가 아니다   ← 옛 상처
                                별칭 「글자를 배」도 뒤의 「웠」에 막힌다     ← 옛 상처
```

**별칭을 한 줄씩 읽다가 죽은 것 둘을 찾았다**(§29 곁가지 ①). 「공부하」·「글자를 배」는
심어진 뒤로 **한 번도 안 걸렸다.** 지우지 않고 완전한 꼴을 함께 올렸다.

**「학교」는 안 올렸다** — `academy`(학교)가 이미 쥐고 있다. 「학교에 있었다」의 임자는
그쪽이고, 이 상징은 배움 쪽 말이 함께 있어야 걸리는 것이 맞다. **프로브 문장을 고쳤다**
(§25 곁가지 — 데이터가 아니라 문장이 틀린 자리).

**「가죽옷」·「가죽 장신구」를 합성어로 따로 올렸다** — 「가죽옷」의 「가죽」은 뒤가 「옷」,
「옷」은 앞이 「죽」이라 둘 다 막힌다.

**관문 전부 초록불** — 인용 13건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 12건(지킴 셋 포함) 전부 걸림.

**사전 1,331 / 4,359 / 4,382** (상징 / 의미 / 인용).

---

## 배치 92 — 밀러 `Ledger`·`Leeches` (12건)

새 상징 하나 — `ledger`(장부, 의미 아홉). 기존 상징 하나에 붙였다 — `leech`(거머리) ←
밀러 `Leeches` 셋. 그 상징은 **판별어 표도 별칭도 비어 있었다.**

**안 넣은 문장 하나 — 같은 그림이 이미 들어와 있다**(§31 곁가지 · 배치 85 `Lance` 와 같다).
`Leeches` 의 「To dream of leeches, foretells that enemies will run over your interests.」는
주공해몽 「거머리를 봄」과 **같은 그림**이다. `work` 가 달라 이름을 갈라야 하는데 갈라 봐야
둘 다 「거머리를 봄」이라 판별어를 나눌 수 없다. **커버리지가 비는 것이 옳다.**

**기본값을 되돌렸다.** 밀러가 들여온 셋은 전부 조건이 붙은 그림(몸에 붙임 · 남에게 붙임 ·
물림)이라, 막연한 「거머리 꿈」이 떨어질 자리는 주공해몽의 「거머리를 봄」이다.

**한 문장을 의미 둘로 갈랐다.** `Ledger` 의 마지막 문장이 「못 받을 셈이면 흉하고, 셈이
좋으면 길하다」로 갈라져 있어 **같은 인용을 둘이 나눠 가진다**(배치 85 `Land` 와 같다).

**기본값 판별어를 좁혔다.** 「장부를 적음」이 기본값인데 「적었」을 주면 「장부에 잘못
적었다」와 동점이 되어 좁은 쪽이 영영 안 뽑힌다 — 「기록」만 남겼다(§25 곁가지 3).

**관문 전부 초록불** — 인용 12건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 13건(지킴 하나 포함) 전부 걸림.

**사전 1,332 / 4,371 / 4,394** (상징 / 의미 / 인용).

---

## 배치 93 — 밀러 `Leeward`~`Legs` (14건)

새 상징 넷 — `leeward`(순풍) · `legerdemain`(요술) · `legislature`(국회) · `legs`(사람의 다리).
패치 파일이 없는 판이다.

**`legs` 는 이름을 「다리」로 두지 않았다.** 「다리」는 `bridge` 의 이름이다 — **글자가 똑같은
동음이의어**라 어느 쪽으로도 피할 수 없다. 세 가지를 따져 이렇게 정했다.

```
① id 는 term_en 으로 짓는다  → `legs` 와 `bridge` 는 주소가 안 부딪친다
② 화면에 뜨는 이름           → 「사람의 다리」로 갈라 적는다(둘 다 「다리」면 이용자가 못 가른다)
③ 걸리는 말                  → 별칭 「다리가」·「다리를」·「다리에」·「두 다리」로 받는다
```

**그 대가는 적어 둔다** — 「다리를 건넜다」에 `bridge` 와 `legs` 가 **둘 다 걸린다.**
`findTerm` 은 상징마다 따로 돌고 전역 중재가 없다(§25 곁가지). 이것은 품는 관계가 아니라
**같은 낱말**이므로 임자에게 붙여 피할 수 있는 자리가 아니다 — 다리 뜻을 `bridge` 에
붙이는 것은 틀린 일이다.

**「의회」 대신 「국회」로 지었다.** 「의회」는 `city-council` 의 별칭 「시 의회」에 **공백 뒤로**
들어 있어, 새 상징을 「의회」로 세우면 「시 의회를 보았다」에 둘 다 걸린다(§25 곁가지 —
배치 29의 「선거」와 같은 자리). 「국회」는 ⓪ grep 0건이다.

**「요술」은 안전했다.** `conjurer` 의 이름 「요술쟁이」·별칭 「요술사」는 「요술」 뒤가 한글이라
`isStandalone` 이 막는다 — 새 상징을 세워도 서로 안 걸린다.

**죽은 낱말 둘을 관문이 잡았다** — 「곪」(한 글자) · 「세 개」(공백에서 쪼개져 둘 다 한 글자).
「곪았」·「여러」로 바꿨다(§29 곁가지 ②).

**관문 전부 초록불** — 인용 14건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 14건 전부 걸림.

**사전 1,336 / 4,385 / 4,408** (상징 / 의미 / 인용).

---

## 배치 94 — 밀러 `Lemonade`~`Lending` (9건)

새 상징 둘 — `lemonade`(레모네이드) · `lemons`(레몬). 기존 상징 하나에 붙였다 —
`borrowing`(빌림) ← 밀러 `Lending` 넷.

**「빌려줌」을 새 상징으로 안 세웠다.** 「빌려」가 이미 `borrowing` 의 별칭이라 「돈을
빌려주었다」는 그 상징에 걸리게 돼 있고, 그 상징에는 이미 「다른 사람이 자신에게서
빌림」(=내가 빌려준 쪽)이 들어와 있다. 한국어 「빌리다」는 주고받는 두 쪽을 다 덮는다.

**그런데 그 상징은 심어진 뒤로 한 번도 안 걸리고 있었다.** 프로브의 **지킴 케이스 셋이
함께 빨간불**을 내서 드러났다.

```
이름 「빌림」 · 별칭 「빌려」·「대출」·「차용」
  「빌렸다」    ✗  「빌려」가 아예 없다
  「빌려주었다」 ✗  「빌려」 뒤의 「주」가 조사가 아니다
  「빌려갔다」   ✗  「빌려」 뒤의 「갔」가 조사가 아니다
  → 옛 의미 셋도 **아무 문장으로도 안 뽑히고 있었다**
```

완전한 꼴 열둘을 별칭으로 올려 고쳤다(§29 곁가지 ①). **이번 배치가 만든 결함이 아니라,
그 상징을 여는 이번이 유일하게 눈이 가는 때였다.**

**판별어를 「빌려주…」 꼴로 나누려다 관문에 막혔다.** 「빌려주었다」·「빌려주」·「빌려주겠」이
서로 부분 문자열이라 `verify-dream-km` 이 위반 2건을 냈다 — **무엇을 빌려주느냐**(「돈을」·
「물건을」)로만 갈랐고, 그러려면 기존 판별어에서 그 낱말들을 빼야 했다(§30 곁가지).

**「남이」도 기존에서 뺐다.** 「남이 나에게 빌려주겠다고 했다」가 「남이 나에게서 빌려간
것」으로 새는 자리다. 그러고도 남는 동점은 **좁은 쪽(「빌려주겠다고 함」)을 앞에 두어** 풀었다.

**관문 전부 초록불** — 인용 9건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 12건(지킴 셋 포함) 전부 걸림.

**사전 1,338 / 4,394 / 4,417** (상징 / 의미 / 인용).

---

## 배치 95 — 밀러 `Lentil`~`Leprosy` (9건)

새 상징 둘 — `lentil`(렌즈콩) · `leprosy`(나병). 기존 상징 하나에 붙였다 —
`leopard`(표범) ← 밀러 `Leopard` 다섯. 그 상징은 **별칭이 하나도 없었다.**

**「문둥병」은 안 썼다**(§31). 옛말은 사람을 낮추는 말이라 이름을 **「나병」**으로 하고
별칭에 「한센병」을 올렸다. **병 이름 자체를 빼지는 않았다** — 꿈의 그림이 그 병이기
때문이다. §31의 가르는 물음(「비하를 빼면 남는 꿈이 있는가」)에 답이 있다: 낮추는 낱말만
바꾸면 그 꿈은 그대로 남는다.

**「표범을 죽임」과 기존 「표범을 잡음」을 갈랐다.** `work` 가 달라(밀러 ↔ 주공해몽) 이름을
갈라야 하고, 기존이 「잡았·사로잡·붙잡·포획」을 쥐고 있어 새 쪽은 **「죽였」 하나로** 갈린다.
영어에서는 「kill」이 기존 「killed」의 부분 문자열이라 안 썼다(배치 43 — 새로 넣는 쪽만 간다).

**기본값을 되돌렸다.** 밀러의 첫 문장 「표범이 덤벼듦」은 조건이 붙은 그림이고, 막연한
「표범 꿈」이 떨어질 자리는 주공해몽의 「표범을 봄」이다.

**관문 전부 초록불** — 인용 9건 위반 0 · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS ·
tsc 0건 · 프로브 12건(지킴 셋 포함) 전부 걸림.

**사전 1,340 / 4,403 / 4,426** (상징 / 의미 / 인용).

---

## 배치 96 — 밀러 `Letter` (19건)

**새 상징을 하나도 안 세웠다.** 표제어 하나(`Letter`)가 한 판을 통째로 먹었고, 전부 이미
있던 `letter`(편지)에 붙었다. 그 상징은 의미가 **둘뿐**이었다 — 주공해몽 「편지를 봉함」과
밀러 `Envelope` 「봉투를 봄」. 이번에 **스물**이 됐다.

⓪ 전수 grep: 「편지」 EXACT 1건(`letter`) · 「letter」가 `being-taught-letters`(글을 배움)의
영어 별칭 안에 들어 있으나 한국어 이름이 달라 다툼이 없다. **새 이름이 없으니 임자 다툼도
없는 판이다.**

### 안 넣은 문장 둘 — 커버리지가 비는 것이 옳다

```
To write one, foretells that you will be jealous of a rival…
   「익명의 편지를 씀」 — 형제와 판별어를 가를 수 없다. 「익명」은 「익명의 편지를 봄」의
   것이고 「썼」은 「편지를 씀」의 것이라, 어느 쪽을 줘도 남의 자리를 뺏는다(§30 곁가지·배치 78)

Letters nearly always bring worry.
   표제어 전체에 대한 총평이고 걸릴 말이 상징 이름(「편지」)뿐이다. 그 낱말을 어느 의미가
   쥐면 그 상징의 새 의미 열여덟이 전부 동점으로 진다(㉑ `jade` 와 같은 자리)
```

문장 넷(「Truthfulness is often rewarded…」·부부/정인/장사하는 이 갈래 셋)은 앞 문장의
곁가지라 **`interpretation_ko` 에 녹였다** — 배치 55에서 좁힌 규칙 그대로다.

**「To the lover…」는 인용을 포갰다.** 가르는 것이 **꾸는 사람뿐**이라 의미를 따로 세우면
아무 문장으로도 안 뽑힌다 — 같은 문맥 이름(「등기 편지를 봄」)·같은 `work` 로 적어
**인용만 둘로 쌓고** 그 갈래는 풀이 문장에 녹였다(배치 32·90 의 셋째 길).

### 「편지」는 어느 의미에도 안 줬다 (㉑)

의미가 스물인 상징이라 **상징 이름이나 마찬가지인 낱말을 한 의미가 쥐면 나머지 열아홉이
전부 진다.** 가른 축은 **편지의 모습과 그것으로 하는 일** 하나뿐이다 — 등기 · 익명 ·
기별의 좋고 나쁨 · 종이와 잉크의 빛깔 · 검은 테두리 · 씀 · 숨김 · 가로채임 · 찢어짐 ·
손으로 건네받음 · 벗에게서 자주.

### 영어 판별어가 **글자로** 부딪칠 뻔한 자리 셋 (①)

```
red    ⊂ registe**red**    「붉은 빛깔」 ↔ 「등기 편지」   → estrangements · maneuvering 으로 돌렸다
rival  ⊂ ar**rival**       「가슴에 품음」 ↔ 「벗에게서」   → arrival 을 버리고 friend often 으로
envelop ⊂ **envelop**es    「초록빛 종이」 ↔ 기존 「봉투를 봄」 → despondency 로
```

**기존 판별어는 안 건드렸다**(배치 43 — 새로 넣는 쪽만 간다).

### 동점은 추출 파일의 차례로 풀었다

```
「처녀가 등기 편지를 받음」  앞  ↔  「등기 편지를 봄」          뒤
「검은 테두리가 둘린 편지」  앞  ↔  「검은 종이에 흰 잉크…」    뒤
```

둘 다 좁은 쪽이 넓은 쪽의 낱말을 함께 갖는 문장이라, 낱말로는 못 가르고 차례로만 풀린다.

### 프로브가 하나를 잡았다 — 판별어 활용형 (§25 곁가지 1)

```
「누가 내 편지를 가로챘다」  →  바란 것: 편지를 가로채임   나온 것: 편지를 봉함(기본값)
```

판별어를 「가로채」로 적었는데 **「가로챘」은 그것을 품지 않는다**(챘 ≠ 채). 검사기 셋은
전부 초록불이었다 — 낱말이 겹치지도 죽지도 않았기 때문이다. 「가로챘 가로채이 가로채」로
고쳤다.

### 지킴 케이스 둘은 그대로였다

「편지를 봉했다」 → 「편지를 봉함」 · 「봉투가 놓여 있었다」 → 「봉투를 봄」. 열여덟이
들어와도 옛 답이 안 밀렸다 — `FALLBACK_FIRST` 가 「편지를 봉함」을 얼려 두고 있어
조립기의 「기본값이 바뀐 상징」 관문도 안 울렸다.

**상징이 배열 941번째 → 13번째로 뛰었다**(⑦). `m96.json` 이 `r5.json` 보다 먼저 정렬돼서다.
데이터 파일 diff 357줄의 대부분이 그 이동이다 — **`git show HEAD:` 로 id 목록을 맞춰
사라진 id 0 · 새 id 0 을 확인했다.**

**관문 전부 초록불** — 인용 4,462건 위반 0(밀러 파일까지 인자로 넘겨 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 20건(지킴 둘 포함) 전부 걸림.

**사전 1,340 / 4,421 / 4,445** (상징 / 의미 / 인용).

---

## 배치 97 — 밀러 `Letter-carrier`~`Liar` (15건)

새 상징 넷 — `letter-carrier`(집배원) · `letter-file`(편지꽂이) · `lettuce`(상추) ·
`liar`(거짓말쟁이). **기존 상징은 하나도 안 건드렸다** — 패치 파일이 없는 판이다.
문장을 하나도 안 뺐다(표제어 넷 · 문장 열다섯 전부).

### ⓪ 전수 grep — 품음 관계 셋이 전부 안전했다

```
편지꽂이 ⊃ 편지(letter)     안전 — 뒤의 「꽂」이 조사가 아니라 `isStandalone` 이 막는다
liar     ⊂ bil**liar**ds    안전 — 라틴은 낱말 경계를 본다(앞이 `l`)
postman·mailman ⊃ man(남자)  〃
```

배치 29의 규칙대로 **품음 관계에서 멈추지 않고 `isStandalone` 을 손으로 돌려 봤다.**
「편지꽂이」는 그래서 상징을 세울 수 있었다 — 임자에게 붙일 자리가 아니다.

**「쌈채소」는 별칭으로 안 올렸다** — `vegetables`(채소)가 그 안에 든다.

### 「여성이~」 갈래 둘을 넣을 수 있었던 까닭

`Letter-file` 과 `Lettuce` 에 「For a woman…」이 하나씩 있는데 **형제가 성별로만 갈리는
자리가 아니다** — `Letter-file` 은 형제가 하나뿐이라 넓은 쪽이 기본값 자리를 지키면
되고(판별어를 「꽂혀·놓여」로 좁게 줬다), `Lettuce` 는 가르는 것이 **씨를 뿌리는 몸짓**이다
(배치 55에서 좁힌 규칙 · 배치 79).

### 죽은 낱말 하나를 감사기가 잡았다

```
letter-carrier [en] 「집배원이 편지 없이 지나감」  죽은 낱말: without
```

불용어라 엔진이 안 센다. 「passes disappointment sadness befall」로 갈았다.
**판별어를 적을 때 흔한 기능어는 처음부터 안 쓴다.**

**관문 전부 초록불** — 인용 4,460건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 17건(지킴 둘 포함) 전부 걸림.

**사전 1,344 / 4,436 / 4,460** (상징 / 의미 / 인용).

---

## 배치 98 — 밀러 `Library`~`Life-boat` (12건)

새 상징 넷 — `library`(도서관) · `lice`(머릿니) · `license`(허가증) · `life-boat`(구명보트).
기존 상징은 안 건드렸다. 문장 열둘을 다 넣었다.

### 「이」를 쓸 수 없어 이름을 「머릿니」로 지었다

밀러 `Lice` 의 한국어 이름은 「이」인데 **그 이름은 `teeth`(이)가 이미 쥐고 있고, 한 글자
이름은 엔진이 못 다룬다**(⓪ grep 이 EXACT 로 찍었고, 품음 관계만도 쉰 줄 가까이 나왔다 —
고양이·구렁이·아궁이·꽃송이…). 「이가」를 별칭으로 올리면 「이가 아팠다」(치아)가 이쪽에도
걸린다.

→ 이름을 **「머릿니」**로 하고 별칭은 「서캐」·「몸니」·「이 떼」로 두었다. **맨 「이」로 쓴
문장은 이 상징에 안 걸린다 — 알고 남긴 자리다**(엔진의 구조적 한계, §29 곁가지 ③).

### ⓪ grep 의 품음 관계 하나도 안전했다

```
구명보트 ⊃ 보트(boat 의 별칭)   안전 — 앞 글자 「명」이 한글이라 `isStandalone` 이 막는다
```

지킴 케이스로 **`boat` 와 `teeth` 의 옛 답이 그대로인지** 함께 쟀다 — 둘 다 그대로였다.

### 「marriage license」를 영어 별칭으로 안 올렸다

올리면 「marriage」가 그 상징의 제 이름이 되어 **「여성이 혼인 허가증을 봄」의 영어 판별어가
죽는다**(§29 곁가지 — `elephant` 의 「white elephant」와 같은 자리). 한국어 쪽에만
「혼인 허가증」을 별칭으로 두었다.

**관문 전부 초록불** — 인용 4,472건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 14건(지킴 둘 포함) 전부 걸림.

**사전 1,348 / 4,448 / 4,472** (상징 / 의미 / 인용).

---

## 배치 99 — 밀러 `Life-insurance Man`~`Lighthouse` (7건)

새 상징 하나 — `life-insurance-man`(보험 외판원). **⓪ grep 이 새 상징 둘을 못 세우게 했다.**

```
Light      → illumination(환한 빛)   「빛이·빛을·빛과」가 이미 그 상징의 별칭이다
Lighthouse → beacon-light(등대 불빛)  「등대」가 EXACT 로 그 상징의 별칭이다
```

새로 「빛」·「등대」 상징을 세웠으면 **한 문장에 상징이 둘 떴을 것**이다(§25 곁가지).

### 기존 판별어에서 「폭풍」을 빼서 새 의미에 넘겼다 (§30 곁가지)

```
기존 「폭풍이나 곤경 속에서 등대 불빛이 꺼지는 것을 봄」   꺼지 꺼졌 폭풍 사라졌
새   「폭풍 속에서 등대를 봄」                            → 어떤 문장으로도 안 뽑힌다
```

그 기존 의미를 실제로 가르는 것은 **불빛이 꺼진다는 것**이라 「폭풍」은 새 쪽에 넘겼다.
영어(`storm`)도 같이 옮겼다. **지킴 케이스로 옛 답 둘이 그대로인지 함께 쟀다** —
「등대 불빛이 꺼졌다」·「뱃사람이 등대 불빛을 보았다」 둘 다 제자리였다.

### 「기이한 빛」은 새 이름으로 안 적었다

밀러 `Light` 의 둘째 문장은 「weird light, or if the light goes out」인데 **「기이한 빛을
봄」은 이미 밀러 `Illumination` 이 갖고 있다.** 이름을 「빛이 꺼짐」으로 두어 실제로
가르는 것만 남기고 기이한 빛 쪽은 풀이에 녹였다(§31 곁가지 — 같은 그림).

### 지킴 케이스가 **옛 상처 하나**를 찾았다 — `illumination` 의 「빛나는」

```
「빛나는 뱀이 기어 다녔다」  →  안 걸림
```

별칭이 「빛이·빛을·빛과·빛무리·빛살」뿐이라 **「빛」 뒤의 「나」가 조사가 아니어서
`isStandalone` 이 막는다.** 그 상징의 의미 일곱 가운데 둘(「하늘에 빛나는 사람이나 짐승」·
「빛나는 뱀이나 기어 다니는 것」)이 **심어진 뒤로 그 꼴로는 한 번도 안 걸렸다.**
「빛나는」·「빛났다」를 별칭으로 올려 고쳤다 — **이번 배치가 만든 결함이 아니다.**

**관문 전부 초록불** — 인용 4,479건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 11건(지킴 넷 포함) 전부 걸림.

**사전 1,349 / 4,455 / 4,479** (상징 / 의미 / 인용).

---

## 배치 100 — 밀러 `Lightning` (11건)

새 상징을 안 세웠다. 표제어 하나가 통째로 이미 있던 `lightning`(번개)에 붙었다 —
⓪ grep 이 「번개」를 EXACT 로 찍었다. **의미가 하나뿐이던 상징이 열둘이 됐다.**

### 판별어 표가 비어 있던 상징이다 (배치 35)

`km5.json` 의 `lightning` 은 `contexts` 도 `contexts_en` 도 **빈 객체**였다. 의미가 하나일
때는 무엇을 넣어도 그것으로 떨어지니 없어도 됐던 것이다. **이번에 기존 「번갯빛이 몸을
비춤」(주공해몽)의 판별어를 함께 채웠다** — 안 채웠으면 그 의미가 0점으로 남아 새 열하나에
밀려 영영 안 뽑혔다.

### 기본값을 **바꾸는 쪽**으로 얼렸다 (§30 · ⑩)

`m100.json` 이 `r*.json` 보다 먼저 정렬돼 기본값이 밀러 쪽으로 넘어간다. 되돌리지 않고
**「번개를 봄」**을 기본값으로 삼았다 — 밀러가 이 상징의 **가장 일반적인 그림**을 들여왔고,
옛 기본값 「번갯빛이 몸을 비춤」은 조건이 붙은 좁은 자리다(배치 54의 `hunting`·`embrace`,
배치 66 `iron` 과 같은 판단). `FALLBACK_FIRST` 에 적어 얼렸다.

### 방위 판별어는 부분 문자열을 피해 갈랐다

```
「서쪽」 ⊂ 「남서쪽」   → 「남서」와 「서쪽」으로 적으면 검사기는 통과하지만 **엔진에서는 동점**
                      → 추출 파일에서 「남서쪽」을 「서쪽」보다 앞에 두어 차례로 풀었다
```

### 극성 하니스가 눈을 떴다 (§28 · ⑲)

```
번개 — 몸을 비침   ko → 길한 경사   en → 기쁨과 넉넉함(기본값)
```

**고침이 관문을 빨갛게 만든 것이 아니라 관문이 눈을 뜬 것이다.** 그때까지 의미가 하나뿐이라
en 이 무엇을 맞히든 같은 답이 나와 짝이 맞았다. 영어 판별어에 「body lit」을 더해
(한국어에도 「비췄」을 더해) 갈랐다.

**안 넣은 문장 하나** — 「Business men should stay close to business…」는 앞 문장의
곁가지라 풀이에 녹였다.

**관문 전부 초록불** — 인용 4,490건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 12건(지킴 하나 포함) 전부 걸림.

**사전 1,349 / 4,466 / 4,490** (상징 / 의미 / 인용).

---

## 배치 101 — 밀러 `Lightning-rod`~`Limes` (15건)

새 상징 다섯 — `lightning-rod`(피뢰침) · `lily`(백합) · `lime`(석회) ·
`lime-kiln`(석회가마) · `limes`(라임). 기존 상징은 안 건드렸다. 문장 열다섯을 다 넣었다.

### 「석회 가마」를 **붙여 써서** 이름을 지었다

```
석회 가마  ⊃ 석회(lime)   뒤가 공백이라 `isStandalone` 을 지난다 → 둘 다 걸린다
석회가마   ⊃ 석회          뒤의 「가」가 한글이라 막힌다        → 하나만 걸린다
```

같은 판에서 만든 두 상징이 서로 품는 자리다(배치 29의 「선거 운동」과 같은 갈림길).
`term_ko` 를 **「석회가마」**로 붙여 쓰고 띄어 쓴 「석회 가마」는 **별칭으로만** 올렸다 —
그 꼴로 쓰면 둘 다 걸리지만, 붙여 쓰면 정확히 하나다.

### 영어에서는 `lime` 과 `limes` 가 갈리지 않는다 — 대가로 받는다

`limes`(라임 열매)와 `lime`(석회)은 영어로 같은 낱말이고 `EN_SUFFIXES` 가 `s` 를 허용하므로
**「limes」 한 문장에 둘 다 걸린다.** 한국어 이름은 「라임」·「석회」로 갈려 있어 한국어
이용자에게는 문제가 없다. 배치 93의 「다리」(bridge/legs)와 같은 자리 — **동음이의어는
피할 수 없고, id 는 `term_en` 으로 지어 안 부딪치므로 그대로 둔다.**

### 지킴 케이스가 **기존 답 하나를 드러냈다** — 이번 배치와 무관하다

```
「꽃이 피어 있었다」  →  `flower` 의 「마른 나무에 꽃이 핌」
```

가장 막연한 문장이 **조건이 붙은 좁은 의미**로 간다. 배치 101은 `flower` 를 한 줄도 안
건드렸고(추출·매칭 키 둘 다 0건) 고치지 않았다 — **오늘 만든 결함이 아니라 재다가 보인
기존 상태다**(§30 곁가지, 사실만 적는다).

**관문 전부 초록불** — 인용 4,505건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 17건(지킴 둘 포함) 전부 걸림.

**사전 1,354 / 4,481 / 4,505** (상징 / 의미 / 인용).

---

## 배치 102 — 밀러 `Limp`~`Linseed Oil` (7건)

새 상징 둘 — `linen`(린넨) · `linseed-oil`(아마씨기름). 기존 상징 하나에 붙였다 —
`crippled`(몸이 성치 않은 이) ← 밀러 `Limp` 둘. 문장 일곱을 다 넣었다.

### `Limp` 은 새 상징을 안 세웠다 — ⓪ grep 과 **원문 각주가 같은 말을 했다**

```
절뚝 → crippled 의 별칭 셋(절뚝이는 · 절뚝거리는 · 절뚝였다)
원문 각주 「[114] See Cripple and Lamed.」  ← 그 둘이 이미 이 상징에 들어와 있다
```

### 「아마씨기름」도 **붙여 써서** 지었다 (배치 101과 같은 자리)

```
아마씨 기름  ⊃ 아마씨(flax) · 기름(oil)   → 상징이 **셋** 뜬다
아마씨기름   두 자리 다 앞뒤가 한글이라 막힌다 → 하나만 걸린다
```

이미 있는 `castor-oil`(피마자기름)이 같은 꼴이다. 띄어 쓴 「아마씨 기름을」은 별칭으로만
올렸다 — 그 꼴로 쓰면 셋 다 걸리지만 안 올리면 정작 이 상징만 빠진다.

### 관문이 셋을 잡았다

```
① 기본값이 바뀜   crippled 「몸이 성치 않은 이를 봄」 → 「제가 절뚝이며 걸음」
                  되돌려 얼렸다 — 새 것은 좁은 그림이고 넓은 자리는 옛 것이다
② 부분 문자열 겹침 새 판별어 「내가」 ⊂ 기존 「아내가」  → 「제가 나는」으로 갈았다
                  (배치 43 — 기존은 안 건드리고 새로 넣는 쪽만 간다)
③ 죽은 낱말       영어 별칭 「linen garments」가 「garments」를 제 이름으로 만들어
                  판별어를 죽였다 → 별칭을 「linens」로 바꿨다(§29 곁가지)
```

### 지킴 케이스가 **옛 상처 하나**를 찾았다 — `crippled` 의 「다리를 저는」

```
「아내가 다리를 저는 사람을 보았다」  →  안 걸림
```

기존 의미의 **화면 문구가 「여자가 다리를 저는 이를 봄」인데 그 말이 별칭에 없었다** —
그 의미는 「절뚝」 꼴로만 걸리고 있었다(⑳과 같은 자리, `newborn-baby`·`iron-2`·`jade`·
`prison` 에 이은 다섯째). 「다리를 저는」·「다리를 절며」를 별칭으로 올려 고쳤다.
**이번 배치가 만든 결함이 아니다.**

**관문 전부 초록불** — 인용 4,512건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 9건(지킴 둘 포함) 전부 걸림.

**사전 1,356 / 4,488 / 4,512** (상징 / 의미 / 인용).

---

## 배치 103 — 밀러 `Lion` (13건)

새 상징을 안 세웠다. 표제어 하나가 통째로 이미 있던 `lion`(사자)에 붙었다 —
⓪ grep 이 「사자」를 EXACT 로 찍었다. **의미가 하나뿐이던 상징이 열넷이 됐다.**

### 배치 100의 `lightning` 과 **똑같은 자리다** — 판별어 표가 비어 있었다

`km6.json` 의 `lion` 도 `contexts`·`contexts_en` 이 **빈 객체**였다. 기존 의미
(주공해몽 「사자가 크게 울부짖음」)의 판별어를 함께 채웠다 — 안 채웠으면 0점으로 남아
새 열셋에 밀려 안 뽑혔다. **두 판 만에 두 번째다**(배치 100 · 103).

### 기본값을 **바꾸는 쪽**으로 얼렸다

밀러의 첫 문장 「사자를 봄」이 이 상징의 가장 일반적인 그림이다. 옛 기본값
「사자가 크게 울부짖음」은 조건이 붙은 자리다 — `FALLBACK_FIRST` 에 적었다(열째).

### 「우는 것」과 「듣는 것」으로 갈랐다 (§31 곁가지)

주공해몽 「사자가 크게 울부짖음」과 밀러 「To hear the roar of a lion」이 **같은 그림**이다.
`work` 가 다르므로 이름을 갈라 적고, 판별어도 **우는 쪽**(울부짖·포효)과 **듣는 쪽**
(울음소리·들었)으로 나눴다. 영어도 같다(roared·loudly ↔ heard·advancement).
**둘 다 길이라 어느 쪽으로 가도 길흉이 안 뒤집힌다** — `crab`(배치 58)처럼 판정이 안 서는
자리가 아니다.

### 「새끼 사자」는 차례로 풀었다

「처녀가 새끼 사자를 봄」이 「새끼 사자를 봄」의 낱말(새끼)을 함께 가지므로 동점이 난다 —
**좁은 쪽을 추출 파일에서 앞에** 두었다(§25 곁가지 3).

**안 넣은 문장 하나** — 「You will be favorably regarded by women.」은 앞 문장의 곁가지라
풀이에 녹였다.

**관문 전부 초록불** — 인용 4,525건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 14건(지킴 하나 포함) 전부 걸림.

**사전 1,356 / 4,501 / 4,525** (상징 / 의미 / 인용).

---

## 배치 104 — 밀러 `Lips`~`Lizard` (15건)

새 상징 셋 — `lips`(입술) · `liver`(사람의 간) · `lizard`(도마뱀). 기존 상징 하나에
붙였다 — `liquor`(술) ← 밀러 `Liquor` 다섯(이미 의미가 여덟 있었다).

### 「간」은 이름으로 못 쓴다 — 「내가 **간** 길」에 걸린다

한 글자 이름이라 **뒤가 공백이면 `isStandalone` 을 지난다.** 그런데 가다의 관형형 「간」이
한국어에서 너무 흔하다 — 「내가 간 길」·「학교에 간 뒤」가 전부 걸린다.

→ 이름을 **「사람의 간」**으로 하고 **조사를 붙인 꼴**(「간이」·「간을」·「간에」·「간의」)을
별칭으로 올렸다(배치 41의 「징」과 같은 수). 그 꼴은 합성어에는 안 걸린다 —
「간이침대」·「간장」은 뒤가 한글이라 막힌다. ⓪ grep 이 「간」을 품는 자리를 **서른** 찍었는데
(문간·뒷간·헛간·난간·대장간·노간주나무·빨간색…) **전부 앞 글자가 한글이라 안전**했다.

### 「To drink some」은 안 넣었다 (§31 곁가지 — 같은 그림)

주공해몽의 「술을 마심」과 같은 그림이고 **판별어를 나눌 수가 없다**(둘 다 「마셨」밖에 걸
것이 없다). 새 쪽에 「마셨」을 주면 「혼자 술을 마셨다」를 동점으로 가로채는데,
`m104.json` 이 `r*.json` 보다 앞서 정렬되므로 **차례로도 못 푼다**(배치 78·85와 같은 자리).

### 「도마뱀을 죽임」과 「도마뱀이 달아남」은 **한 문장에서 둘로 갈랐다**

원문이 「죽이면 …; 달아나면 …」으로 길흉이 정반대인 두 그림을 한 문장에 담았다.
**같은 인용을 두 의미가 함께 인용한다** — `context` 가 다르므로 조립기가 포개지 않는다.

**안 넣은 곁가지 넷** — 「To a lover, it augurs…」·「She will be good natured…」·
「To treat others…」·「Her husband will be a victim…」은 앞 문장의 곁가지라 풀이에 녹였다.

**관문 전부 초록불** — 인용 4,540건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 18건(지킴 셋 포함) 전부 걸림.

**사전 1,359 / 4,516 / 4,540** (상징 / 의미 / 인용).

---

## 배치 105 — 밀러 `Load`~`Lobster` (12건)

새 상징 둘 — `loadstone`(자석) · `lobster`(바닷가재). 기존 상징 둘에 붙였다 —
`burden`(짐) ← `Load` 둘 · `bread`(빵) ← `Loaves` 넷.

### 「To dream that you carry a load」는 안 넣었다 — **같은 그림에 길흉이 정반대**

`burden` 의 첫 의미 「무거운 짐을 짊어짐」(밀러 `Burden`)이 **흉**인데 `Load` 의 같은
그림은 **길**이다. 판별어도 나눌 수 없다(둘 다 「짊어졌다」밖에 걸 것이 없다).
`crab`(배치 58)과 같은 자리 — 커버리지가 한 문장 비는 것이 옳다.

### 「바닷가재」는 새 상징을 세워도 안전했다

```
바닷가재 ⊃ 가재(crawfish)     앞이 「닷」이라 막힌다
바닷가재 ⊃ 바닷가(sea 의 별칭) 뒤가 「재」라 막힌다
```

⓪ grep 이 품음으로 찍었지만 **배치 29의 판정법대로 손으로 돌려 보니 둘 다 안 걸린다.**

### 프로브가 둘을 잡았다

```
「케이크 덩이를 보았다」    →  안 걸림 — 그 문장에 **「빵」이 없다**
「빵 덩이가 크게 불어났다」 →  「빵 덩이를 봄」으로 — 「불어나」가 「불어났다」를 안 품는다
```

앞엣것은 **문맥 이름이 틀린 것**이었다. 밀러의 「If they be of cake」는 **빵 덩이가
케이크라는 말**이지 케이크 자체가 아니다 — 이름을 「빵 덩이가 케이크임」으로 고쳤다
(§25 곁가지 — 가르는 물음은 「원문의 그 꿈에 그 사물이 있는가」다. 빵 덩이가 있다).

뒤엣것은 **판별어 활용형**이다(배치 96의 「가로채」와 같다, 이 세션 두 번째) —
「불어났」을 함께 올렸다.

### 관문이 기본값 둘을 잡았고 **둘 다 되돌려 얼렸다**

`burden` 「무거운 짐을 짊어짐」 · `bread` 「여성이 빵을 먹는 꿈을 꿈」. 새로 들어온 것이
전부 조건이 붙은 좁은 그림이라 기본값 자리가 아니다.

**안 넣은 곁가지 하나** — 「Lovers will be happy in their chosen ones.」는 앞 문장의
곁가지라 풀이에 녹였다.

**관문 전부 초록불** — 인용 4,552건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 16건(지킴 넷 포함) 전부 걸림.

**사전 1,361 / 4,528 / 4,552** (상징 / 의미 / 인용).

---

## 배치 106 — 밀러 `Lock`~`Lockjaw` (11건)

새 상징 셋 — `lock`(자물쇠) · `locket`(로켓 목걸이) · `lockjaw`(파상풍).
기존 상징은 안 건드렸다. 패치 파일이 없는 판이다.

### 「로켓」만으로는 이름을 못 짓는다

한국어에서 「로켓」은 **날아가는 로켓**이 먼저다. 목에 거는 것을 가리키려면 이름을
**「로켓 목걸이」**로 지어야 한다(별칭에 「사진 목걸이」·「로켓펜던트」·「목걸이 로켓」).
⓪ grep 에서 「목걸이」는 상징 이름이 아니고 `beads`·`gold-ornament` 의 별칭 안에만
있어서(구슬 목걸이 · 금목걸이) 다툼이 없다.

### 판별어 「열리」가 「열리지」를 품는다

```
「자물쇠가 열림」      열렸 열리   ←  「열리」 ⊂ 「열리지」 라 `verify-dream-km` 이 막는다
「자물쇠가 열리지 않음」 열리지
```

**여는 쪽에서 「열리」를 빼고 「열렸」·「풀렸」만 남겼다** — 새로 넣는 쪽끼리의 겹침이라
어느 쪽을 갈아도 되지만, 안 열리는 쪽은 「열리지」 말고는 걸 것이 없다.

**안 넣은 곁가지 둘** — 「If you are in love…」·「The woman he loves will worry him…」은
앞 문장의 곁가지라 풀이에 녹였다.

**관문 전부 초록불** — 인용 4,563건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 12건(지킴 하나 포함) 전부 걸림.

**사전 1,364 / 4,539 / 4,563** (상징 / 의미 / 인용).

---

## 배치 107 — 밀러 `Locomotive`~`Lodger` (9건)

새 상징 하나 — `lodger`(하숙인). 기존 상징 둘에 붙였다 —
`engine`(기관차) ← `Locomotive` 넷 · `grasshopper`(메뚜기) ← `Locust` 둘.

### 안 넣은 것 둘

```
Locomotive 「If it is disabled…」   `engine` 에 이미 「고장 난 기관차를 봄」이 있다(같은 그림)
Looking-glass 표제어 전체          「여성이 거울을 봄」인데 `mirror` 의 「여성이 거울을 깨뜨림」이
                                   **성별 말고 가를 말이 없는 판별어**(여성·여자·아내·그녀)를
                                   쥐고 있어 둘을 가를 수 없다(배치 85와 같은 자리)
```

`Looking-glass` 는 원문 각주가 「[115] See Mirror.」이고 `mirror` 의 `aliases_en` 에 이미
「looking glass」가 있다 — 붙일 자리는 맞는데 **판별어를 나눌 수가 없다.**

### 기존 판별어에서 「부서진」을 새 의미에 넘겼다 (§30 곁가지, 이 세션 두 번째)

기존 「고장 난 기관차를 봄」이 「부서진」을 쥐고 있어 새로 넣는 「기관차가 완전히 부서진
것을 봄」이 안 뽑힌다. 그 의미를 실제로 가르는 것은 **고장**이다.

### 기본값 둘이 갈렸다 — 하나는 되돌리고 하나는 바꿨다

```
engine       되돌림  「기관차를 봄」이 넓다. 새로 온 「빠르게 달림」은 조건이 붙었다
grasshopper  바꿈    있던 넷이 전부 조건이 붙은 그림(푸른 남새 위·시든 풀 위·해와 나 사이·
                     남에게 가리킴)이고, 밀러 `Locust` 의 첫 문장이 가장 일반적인 그림이다
```

**한 판에서 되돌리는 쪽과 바꾸는 쪽이 함께 나온 첫 자리다.** 가르는 물음은 늘 같다 —
**막연한 그 꿈이 떨어질 자리가 어디인가.**

**안 넣은 곁가지 하나** — 「Business will assume changes…」는 앞 문장의 곁가지라 풀이에 녹였다.

**관문 전부 초록불** — 인용 4,572건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 13건(지킴 넷 포함) 전부 걸림.

**사전 1,365 / 4,548 / 4,572** (상징 / 의미 / 인용).

---

## 배치 108 — 밀러 `Loom`~`Louse` (13건)

새 상징 둘 — `lord-s-prayer`(주기도문) · `lottery`(복권). 기존 상징 둘에 붙였다 —
`weaving`(베짜기) ← `Loom` 넷 · `lice`(머릿니) ← `Louse` 하나(각주가 「See Lice.」다).

### `weaving` 도 판별어 표가 비어 있었다 — **이 세션 세 번째다**

배치 100 `lightning` · 103 `lion` 에 이어 셋째다. 의미가 하나뿐이던 상징은 **전부** 이렇다.
→ **기존 상징에 붙일 때 그 상징의 의미가 하나였는지 먼저 본다.**

「짰다」는 기존 의미에 안 줬다 — 「여성이 옛날 베틀로 베를 짬」의 문장에도 그 말이 들어 있다.
기존 의미를 실제로 가르는 것은 **날실을 거는 것**이다(§30 곁가지).

### 지킴 케이스가 **옛 상처 하나**를 찾았다 — `weaving` 의 「베를 짜」

```
「날실을 걸어 베를 짰다」  →  안 걸림
```

별칭이 「베를 짜」까지만이라 **뒤의 「짰」이 조사가 아니어서 막힌다** — 그 상징의 유일한
의미가 **심어진 뒤로 그 꼴로는 한 번도 안 걸렸다.** 「베를 짰다」·「베를 짜서」를 올려 고쳤다.
**이번 배치가 만든 결함이 아니다.**

### id 가 `lords-prayer` 가 아니라 `lord-s-prayer` 였다

`term_en` 이 「lord's prayer」라 홑따옴표가 `-s-` 로 바뀐다. 조립기가 **km id 를 못 찾아
멈췄다**(「사전에 없는 km id: lords-prayer」) — **id 는 짐작하지 말고 조립한 뒤 실물을 본다.**

### 그 밖에 관문이 잡은 것

```
「꽝」  1글자라 엔진이 안 센다(`scoringWordsOf` 의 length >= 2) → 「꽝이」로
기본값 둘  weaving · lice 다 되돌려 얼렸다 — 새로 온 것이 전부 좁은 그림이다
```

**안 넣은 곁가지 다섯** — 전부 앞 문장의 곁가지라 풀이에 녹였다.

**관문 전부 초록불** — 인용 4,585건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 16건(지킴 셋 포함) 전부 걸림.

**사전 1,367 / 4,561 / 4,585** (상징 / 의미 / 인용).

---

## 배치 109 — 밀러 `Love`~`Luggage` (16건)

새 상징 넷 — `love`(사랑) · `lovely`(사랑스러운 것) · `lucky`(행운) · `luggage`(짐가방).
기존 상징 하나에 붙였다 — `candy`(사탕) ← 밀러 `Lozenges` 둘(알사탕).

### 프로브가 잡았다 — **「사랑」은 이름만으로 한 문장도 안 걸린다**

```
「무언가를 사랑하고 있었다」  →  안 걸림 — 「사랑」 뒤의 「하」가 조사가 아니다
```

배치 63(「검시」·「심문」·「실성」)과 같은 자리인데, 그때는 **서술형 이름**이 원인이었고
이번은 **이름이 명사인데 이용자가 동사로 쓰는 것**이다. 「사랑하는」·「사랑했다」·
「사랑하고」·「사랑한다」·「사랑받는」·「사랑받았다」를 별칭으로 함께 올려 고쳤다.

→ **이름이 「~하다」로 쓰이는 명사면 활용형 별칭을 처음부터 올린다.**

### 새 이름이 기존 이름을 품는 자리 셋 — 판정이 갈렸다

```
사랑     ⊂ 「사랑의 도피」(elopement)   뒤가 조사(의)라 **둘 다 걸린다 — 옳다**
                                        (그 꿈에 사랑이 실제로 있다, 배치 29의 「음식」과 같다)
사랑스러운 ⊃ 「사랑」(love)              뒤의 「스」가 한글이라 막힌다
짐가방    ⊃ 「짐」(burden)               뒤의 「가」가 한글이라 막힌다
```

### 「여성이 알사탕을 먹거나 버림」을 앞에 두었다

그 문장의 「먹었」은 기존 「바삭한 새 사탕을 먹음」의 것이고 「알사탕」은 새 「알사탕을 봄」의
것이라 **세 의미가 1점씩 동점**이다. 좁은 쪽을 추출 파일 앞에 두어 풀었다.
`candy` 의 기본값은 되돌려 얼렸다(「사탕을 만듦」).

**안 넣은 곁가지 셋** — 전부 앞 문장의 곁가지라 풀이에 녹였다.

**관문 전부 초록불** — 인용 4,601건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 18건(지킴 둘 포함) 전부 걸림.

**사전 1,371 / 4,577 / 4,601** (상징 / 의미 / 인용).

---

## 배치 110 — 밀러 `Lumber`~`Lyre` (15건)

새 상징 넷 — `lumber`(목재) · `luxury`(사치) · `lynx`(스라소니) · `lyre`(리라).
기존 상징 둘에 붙였다 — `lute`(비파) ← `Lute` 둘 · `liar`(거짓말쟁이) ← `Lying` 셋.

### **파서가 표제어 둘을 세지 않고 있었다** — 배치 24와 같은 병, 여덟·아홉 번째

`Lynx` 를 넣으려는데 `verify-dream-cite` 가 「없는 표제어」로 막았다. 원문을 열어 보니
**밑줄 표시 없이 앞 표제어 끝에 붙어 있었다.**

```
… seeking to entrap you.  Lynx.      → `Lying` 본문에 통째로 묻혀 있었다
Swimming.[219]                        → `Swollen` 본문에 묻혀 있었다
```

**책 자신의 색인과 대조해서 찾았다**(§24 — 세 번째 자를 원본 쪽에서 만든다). 색인 표제어
2,171개 중 파서에 없는 것이 24개였고, 스물둘은 색인 조판 탓(「Vulture/Vultures」·
「Land Landau」 같은 것)이었으며 **진짜는 이 둘**이다. 색인에만 있고 본문에 아예 없는
둘(`Tide`·`Kegs`)은 표제어로 만들 수 없어 넣지 않았다.

고침은 `scripts/parse-miller-1901.mjs` 에 **목록으로 얼린 것**이다(§11) — 「줄끝에 대문자
낱말 + 마침표」를 규칙으로 쓰면 평범한 문장 끝까지 표제어로 센다. **고치기 전에 지금
그대로 돌려 diff 가 0인 것을 먼저 확인했고**, 고친 뒤 diff 는 정확히 **표제어 둘이 늘고
`Lying`·`Swelling` 의 본문이 그만큼 준 것**뿐이었다. 인용 4,616건 전수 재검증 통과.

### ⓪ grep 을 한국어로만 돌려 **id 충돌을 못 봤다**

「류트」로 찾으면 0건인데 **`term_en` 이 `lute` 인 상징이 이미 있었다**(비파, 주공해몽).
**id 는 `term_en` 으로 지으므로 이름이 달라도 같은 상징에 합쳐진다** — 조립기의
「기본값이 바뀐 상징」 관문이 「품속에 비파가 있음 → 류트를 탐」으로 찍어서야 알았다.
이름을 **「비파」**로 맞추고 「류트」는 별칭으로 올렸다. CLAUDE.md §25 곁가지에 적었다.

그 상징도 **판별어 표가 비어 있었다** — 이 세션 네 번째다(배치 100·103·108·110).

### `Lying` 은 새 상징을 안 세웠다

배치 97의 `liar` 가 「거짓말」·「거짓말을」을 쥐고 있다 — 새로 세우면 「거짓말을 했다」가
상징 둘에 걸린다.

**영어 판별어가 글자로 부딪친 자리 둘** — 「hear」 ⊂ 「sweetheart」 · 「rival」 ⊂ 「rivaling」.
둘 다 새로 넣는 쪽을 갈았다(배치 43).

**관문 전부 초록불** — 인용 4,616건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 18건(지킴 셋 포함) 전부 걸림.

**사전 1,375 / 4,592 / 4,616** (상징 / 의미 / 인용) · **밀러 표제어 2,266개**(둘 늘었다).

---

## 배치 106~110 묶음 — 스윕·푸시·배포 확인

전수 스윕 142줄 · 통과 117 / 검사 안 됨 7 / **빨간불 4**(늘 같은 넷).
새 상징 열넷의 주소가 푸시 앞 404 → 뒤 200 — lock · locket · lockjaw · lodger ·
lord-s-prayer · lottery · love · lovely · lucky · luggage · lumber · luxury · lynx · lyre.

---

## 배치 111 — 밀러 `Macadamize`~`Madstone` (11건)

새 상징 다섯 — `macadamize`(자갈길) · `macaroni`(마카로니) · `machinery`(기계) ·
`madness`(광기) · `madstone`(독을 빨아내는 돌). 패치 파일이 없는 판이다.

### `Mad Dog` 은 통째로 안 넣었다 — 같은 그림이 이미 있다

`dog` 에 밀러 `Dogs` 의 「미친개를 봄」이 이미 있다(각주도 「[117] See Dog.」다).
「죽이면 이긴다」는 그 한 문장의 곁가지라 따로 세울 자리가 아니다.

### 이름이 겹치는 자리 셋 — 판정이 갈렸다

```
자갈길   ⊃ 「자갈」(gravel)          뒤의 「길」이 한글이라 막힌다 — 안전
madstone ⊃ 「stone」(돌)             라틴은 낱말 경계를 보므로 앞의 `d` 가 막는다 — 안전
기계     ⊂ 「조면 기계」·「나는 기계」  **앞이 공백이라 둘 다 걸린다 — 옳다**
                                     (조면기도 비행기도 기계다, 배치 29의 「음식」과 같다)
```

### 프로브가 하나를 잡았다

```
「남이 미친 것을 보았다」  →  안 걸림
```

`madness` 의 별칭이 「미쳤다」·「미친 사람」·「실성했다」뿐이라 **「미친 것을」이 어디에도
안 걸린다.** 「미친 것」·「미쳐서」·「미쳐 있」을 올려 고쳤다 — **이름이 「~함」·「~기」 꼴이면
이용자가 쓰는 꼴을 별칭으로 함께 올린다**(배치 63·109와 같은 자리).

**관문 전부 초록불** — 인용 4,627건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 13건(지킴 둘 포함) 전부 걸림.

**사전 1,380 / 4,603 / 4,627** (상징 / 의미 / 인용).

---

## 배치 112 — 밀러 `Magic`~`Magpie` (7건)

새 상징 셋 — `magic`(마술) · `magnifying-glass`(돋보기) · `magpie`(까치).
기존 `judge`(판사)에 `Magistrate` 하나를 붙였다(각주가 「See Judge and Jury.」다).

### `Magnet` 은 통째로 안 넣었다 — **일곱 판 앞에서 내가 세운 상징과 같은 그림**

배치 105의 `loadstone`(자석)이 밀러 `Loadstone` 둘을 갖고 있는데 `Magnet` 의 두 문장이
그것과 같은 그림이다. 게다가 **한 자리는 길흉이 반대**다(끌어당김: `Loadstone` 은 길,
`Magnet` 은 흉). 판별어도 나눌 수 없다 — 커버리지가 비는 것이 옳다.

### 이름을 「마법」이 아니라 「마술」로 지었다

「마법을」·「마법이」가 `enchantment`(마법에 홀림)의 별칭이라 「마법을 부렸다」가 상징 둘에
걸린다. **원문도 「Magic here should not be confounded with sorcery」라고 못 박는다** —
이름을 돌리는 것이 원문의 뜻에도 맞았다. 「마술사」는 `conjurer` 의 별칭이지만 앞 글자가
한글이라 「마술」은 안 걸린다(지킴 케이스로 확인했다).

### 관문이 셋을 잡았다

```
기본값이 바뀜   judge 「판사 앞에 서게 됨」 → 「치안 판사를 봄」  되돌려 얼렸다
영어 겹침       새 「suits」 ⊂ 기존 「suit」                    새 쪽에서 뺐다
한국어 겹침     새 「사람이」 ⊂ 새 「사람」                       둘 다 갈았다(남들이 · 재주꾼)
죽은 낱말       「제 것」이 공백으로 쪼개져 1글자가 됐다          「가졌」으로
```

**관문 전부 초록불** — 인용 4,634건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 10건(지킴 셋 포함) 전부 걸림.

**사전 1,383 / 4,610 / 4,634** (상징 / 의미 / 인용).

---

## 배치 113 — 밀러 `Mallet`~`Man` (9건)

새 상징 둘 — `malice`(악의) · `malt`(맥아). 기존 상징 둘에 붙였다 —
`gavel`(의사봉) ← `Mallet` 하나 · `man`(남자) ← `Man` 넷.

### `Mallet` 을 `gavel` 에 붙인 까닭

한국어로는 둘 다 「나무망치」이고 그 이름을 `gavel` 이 EXACT 로 쥐고 있다.
`hammer`(망치)에 붙이는 길도 있었으나 그쪽은 쇠망치라 원문의 그림과 멀다.

### 「여성이 ~」 둘을 추출 파일 앞에 두어 동점을 풀었다

```
「여자가 잘생긴 남자를 보았다」 →  「잘생긴」(1점) ↔ 「여자가」(1점)  동점
「아내가 못생긴 남자를 보았다」 →  「못생긴」(1점) ↔ 「아내가」(1점)  동점
```

프로브가 둘 다 잡았다. 좁은 쪽(여성이 ~)을 앞으로 옮겨 풀었다(§25 곁가지 3).

**기본값 둘은 되돌려 얼렸다** — `man` 「남자와 몸을 섞음」 · `gavel` 「의사봉을 봄」.
`man` 의 기본값이 좋은 자리는 아니지만(막연한 「남자를 보았다」가 그리로 간다) **이번
변경이 만든 것이 아니다** — 밀러가 들여온 것도 조건이 붙은 그림이라 바꿀 근거가 없다.

**관문 전부 초록불** — 인용 4,643건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 11건(지킴 둘 포함) 전부 걸림.

**사전 1,385 / 4,619 / 4,643** (상징 / 의미 / 인용).

---

## 배치 114 — 밀러 `Manners`~`Manufactory` (11건)

새 상징 셋 — `manners`(몸가짐) · `man-of-war`(군함) · `manufactory`(공장).
기존 상징 셋에 붙였다 — `house`(집) ← `Mansion` 셋 · `killing-someone`(살인) ←
`Manslaughter` 하나(각주 「See Murder.」) · `headgear`(머리쓰개) ← `Mantilla` 하나.

### 지킴 케이스가 **옛 상처 하나**를 찾았다 — `house` 의 「지었」

```
「집을 새로 지었다」  →  「성 위에 집을 지음」
```

「성 위에 집을 지음」이 **「지었·짓고·건축」을 쥐고 있어** 평범한 「집을 지었다」를 가로챈다.
그 의미를 실제로 가르는 것은 **성 위**다 — 「성벽·성곽·성채」만 남기고 넓은 활용형은
「집을 지음」에 넘겼다(§30 곁가지). **이번 배치가 만든 결함이 아니다.**

### 관문이 다섯을 잡았다

```
기본값이 바뀜  house 「집을 지음」 · headgear 「값진 머리쓰개를 봄」   둘 다 되돌려 얼렸다
영어 겹침      새 「mansion」 ⊂ 기존 「우아한 집을 소유함」의 것        새 쪽에서 뺐다
죽은 낱말 셋   「먼 데서」(1글자로 쪼개짐) · 「떠 있」 · 「성 위」        붙은 꼴로 갈았다
죽은 낱말      영어 별칭 「affable manners」·「mannered」가 판별어를 죽였다 → 별칭을 갈았다
```

### 「저택」은 `house` 와 `building` 이 **둘 다** 쥐고 있다

⓪ grep 이 EXACT 를 둘 찍었다 — 저택 꿈은 **이미** 상징 둘이 뜨는 자리다(기존 상태).
밀러 `Mansion` 은 집의 그림이라 `house` 에 붙였다.

**관문 전부 초록불** — 인용 4,654건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 15건(지킴 넷 포함) 전부 걸림.

**사전 1,388 / 4,630 / 4,654** (상징 / 의미 / 인용).

---

## 배치 115 — 밀러 `Manure`~`Marble` (11건)

새 상징 셋 — `manuscript`(원고) · `map`(지도) · `marble`(대리석). 패치 파일이 없는 판이다.

### `Manure` 는 통째로 안 넣었다 — 같은 그림이 이미 있다

`manure`(거름)에 밀러 `Dunghill` 의 「거름더미를 봄」이 이미 있고 **길흉도 같다**(둘 다 길).
「거름을 보는 것은 좋은 조짐」이라는 한 문장뿐이라 따로 세울 자리가 없다.

### 「지도」와 `atlas`(지도책)는 **함께 걸린다 — 옳다**

`atlas` 가 「세계 지도」·「지도를 펼」을 별칭으로 쥐고 있어 그 꼴에서는 상징 둘이 뜬다.
지도책에는 지도가 실제로 들어 있으므로 배치 29의 「음식」과 같은 자리다.
**「지도책」 자체는 뒤 글자가 한글이라 `map` 이 안 걸린다** — 지킴 케이스로 확인했다.

### 영어 판별어에서 「finished」를 못 쓴다

「unfinished」가 그것을 품는다 — 「덜 쓴 원고」와 「다 쓴 원고」가 **글자로 부딪친다.**
다 쓴 쪽은 「clearly hopes realized」로만 갈랐다.

**관문 전부 초록불** — 인용 4,665건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 13건(지킴 둘 포함) 전부 걸림.

**사전 1,391 / 4,641 / 4,665** (상징 / 의미 / 인용).

---

## 배치 116 — 밀러 `March`~`Market` (11건)

새 상징 넷 — `march`(행진) · `march-month`(삼월) · `marigold`(금잔화) · `mariner`(뱃사람).
기존 상징 둘에 붙였다 — `horse`(말) ← `Mare` 셋(각주 「See Horse.」) ·
`marketplace`(시장) ← `Market` 둘.

### 표제어 하나가 한국어로 관계없는 둘이라 상징을 갈랐다

밀러 `March` 는 **행진**이면서 **삼월**이다. 둘 다 `term_en` 이 `march` 라 id 가 부딪치므로
달 쪽은 `term_en` 을 「march month」로 지어 갈랐다(배치 30 `Engagement` 와 같은 자리).
**`cite.locator` 는 둘 다 `March` 그대로**다 — 원문의 표제어는 하나이기 때문이다.

### `Market` 은 넷 중 둘만 넣었다

「To see an empty market」은 주공해몽 「시장에 사람이 하나도 없음」과, 「For a young
woman, a market…」은 밀러 `Fair` 의 「처녀가 장이 선 곳에 감」과 같은 그림이다.

### 프로브가 셋을 잡았다

```
「음악에 맞춰 행진했다」        안 걸림 — 「행진」 뒤의 「했」이 조사가 아니다(㊷ · 배치 109)
「여자가 …행진하는 것을 보았다」 〃
「뱃사람이 탈 배가 …」          프로브 문장에 상징 이름이 없었다(내 문장이 틀렸다)
```

앞의 둘은 **「~하다」로 쓰이는 명사**라 활용형 별칭(「행진하는」·「행진했다」·「행진하고」·
「행진하며」)을 함께 올려 고쳤다 — 배치 109의 `love` 와 같은 자리이고 **그 규칙을 적어 둔
바로 다음 판에서 또 밟았다.**

### 관문이 넷을 잡았다

```
판별어에 상징 이름   「삼월」을 그대로 썼다 → 「3월 셋째」로
영어 겹침            새 「congenial」 ⊂ 기존 「congeniality」 → 새 쪽에서 뺐다
죽은 낱말 셋         「떠」·「달」(1글자) · 영어 「without」·「month」(불용어)
```

**관문 전부 초록불** — 인용 4,676건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 13건(지킴 둘 포함) 전부 걸림.

**사전 1,395 / 4,652 / 4,676** (상징 / 의미 / 인용).

---

## 배치 117 — 밀러 `Marmalade`~`Marriage` (11건)

새 상징 둘 — `marmalade`(마멀레이드) · `marmot`(마멋). 기존 `wedding`(혼례)에
밀러 `Marriage` 여섯을 붙였다.

### 새 상징 `marriage` 를 세우지 않았다

극성 하니스가 「아직 없는 상징」으로 건너뛰는 id 열셋에 `marriage` 가 있다(⑬).
그런데 한국어 「혼인」·「결혼식」의 임자는 `wedding` 이라 새로 세우면 상징이 둘 뜬다 —
**잠자던 기대는 그대로 두고** `wedding` 에 붙였다(§25 곁가지).

### `wedding` 도 판별어 표가 비어 있었다 — **이 세션 다섯 번째**

배치 100 `lightning` · 103 `lion` · 108 `weaving` · 110 `lute` 에 이어 다섯째다.

### 「혼인했다」가 안 걸렸다 — **㊷의 세 번째 재발**

배치 109(`love` 의 「사랑하고」)·116(`march` 의 「행진했다」)에 이어 셋째다.
「혼인」 뒤의 「했」이 조사가 아니다 — 「혼인했다」·「혼인하고」·「혼인하여」를 올려 고쳤다.
**세 판 연속으로 같은 병이 났다** — 「~하다」로 쓰이는 명사를 이름으로 쓸 때는
활용형 별칭을 **처음부터** 함께 적어야 한다.

### 마지막 문장(신부)은 안 넣었다

`bride`(신부)의 「혼례 단장에 불만을 느낌」·「신부가 남편에게 무심한 꿈을 꿈」과 같은
그림이고 판별어를 나눌 수 없다.

**관문 전부 초록불** — 인용 4,687건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 13건(지킴 둘 포함) 전부 걸림.

**사전 1,397 / 4,663 / 4,687** (상징 / 의미 / 인용).

---

## 배치 118 — 밀러 `Mars`~`Mask` (10건)

새 상징 셋 — `mars`(화성) · `martyr`(순교자) · `mask`(가면). 기존 `bog`(늪지)에
밀러 `Marsh` 하나를 붙였다(그 상징의 `aliases_en` 에 이미 「a marsh」가 있다).

### `bog` 도 판별어 표가 비어 있었다 — **이 세션 여섯 번째**

배치 100 · 103 · 108 · 110 · 117에 이어 여섯째다. **의미가 하나뿐이던 상징은 예외 없이
이렇다** — 붙이기 전에 그 상징의 의미 수를 먼저 본다.

### 「가면을 봄」을 첫째에 두고 「처녀가 ~」를 그다음에 두었다

가장 넓은 뜻이 기본값 자리이고(⑤), 「처녀가 가면을 썼다」는 「처녀」와 「썼다」가 각각
1점이라 동점이므로 좁은 쪽을 앞에 두었다(§25 곁가지 3).

**죽은 낱말 셋** — 「떠」(1글자) · 영어 「planet」(`aliases_en` 이 그것을 제 이름으로
만들었다) · 「the planet mars」 별칭을 「the red planet」으로 갈았다.

**관문 전부 초록불** — 인용 4,697건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 11건(지킴 하나 포함) 전부 걸림.

**사전 1,400 / 4,673 / 4,697** (상징 / 의미 / 인용).

---

## 배치 119 — 밀러 `Mason`~`Match` (12건)

새 상징 넷 — `mason`(석공) · `mast`(돛대) · `master`(주인) · `match`(성냥).
기존 상징 둘에 붙였다 — `carnival`(카니발) ← `Masquerade` 둘 ·
`straw-mat`(돗자리) ← `Mat` 하나.

### 「깔개」는 `straw-mat` 과 `mattress` 가 **둘 다** 쥐고 있다

⓪ grep 이 EXACT 를 둘 찍었다 — 이미 상징 둘이 뜨는 자리다(기존 상태). 밀러 `Mat` 은
바닥에 까는 것이라 `straw-mat` 에 붙였다.

### 「주인」은 합성어와 함께 걸린다 — 알고 남긴 자리

「이발소 주인」·「정육점 주인」은 **앞이 공백**이라 `isStandalone` 을 지난다. 그 꿈에
주인이 실제로 있으므로 둘 다 걸리는 것이 옳다(배치 29의 「음식」과 같다).

### 관문이 넷을 잡았다

```
기본값이 바뀜  carnival · straw-mat   둘 다 되돌려 얼렸다
영어 겹침      새 「away」 ⊂ 기존 「돗자리를 집 밖으로 내보냄」의 것 → 새 쪽에서 뺐다
영어 겹침      새끼리 「expected」 ⊂ 「unexpected」 → 넓은 쪽에서 뺐다
죽은 낱말      「있」(1글자) → 「들었」으로
```

**관문 전부 초록불** — 인용 4,709건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 14건(지킴 둘 포함) 전부 걸림.

**사전 1,404 / 4,685 / 4,709** (상징 / 의미 / 인용).

---

## 배치 120 — 밀러 `Matting`~`May Bugs` (9건)

새 상징 둘 — `mausoleum`(영묘) · `may`(오월). 기존 상징 셋에 붙였다 —
`straw-mat`(돗자리) ← `Matting` 하나 · `mattress`(요) ← `Mattress` 셋 ·
`beetles`(딱정벌레) ← `May Bugs` 하나.

### `Matting` 의 둘째 문장은 안 넣었다

「If it is old or torn…」은 `straw-mat` 의 「돗자리가 해어져 찢어짐」과 같은 그림이고
판별어(해어·찢어·낡아)를 나눌 수 없다.

### `mattress` 도 판별어 표가 비어 있었다 — **이 세션 일곱 번째**

배치 100 · 103 · 108 · 110 · 117 · 118에 이어 일곱째다.

### 관문이 셋을 잡았다

```
기본값이 바뀜  mattress 「요를 펴서 깖」 · beetles 「몸에 딱정벌레가 있는 것을 봄」  되돌려 얼렸다
죽은 낱말      영어 「bugs」(그 상징의 `aliases_en` 이라 제 이름이 된다) · 「서」(1글자)
```

**관문 전부 초록불** — 인용 4,718건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 12건(지킴 셋 포함) 전부 걸림.

**사전 1,406 / 4,694 / 4,718** (상징 / 의미 / 인용).

---

## 배치 121 — 밀러 `Meadow`~`Medicine` (10건)

새 상징 다섯 — `meadow`(초원) · `measles`(홍역) · `mechanic`(기계공) · `medal`(훈장) ·
`medicine`(먹는 약). 기존 `eating`(음식을 먹음)에 밀러 `Meals` 하나를 붙였다
(각주 「See Eating.」).

### `Meat` 은 통째로 안 넣었다 — **성별 말고 가를 말이 없다**

`meat`(고기)에 이미 「날고기를 먹음」·「익힌 고기를 먹음」이 있다. 밀러 `Meat` 의 두
문장은 **여성이 날고기/익힌 고기를 보는 것**인데, 날것·익힌 쪽 낱말은 기존이 쥐고 있고
남는 것은 성별뿐이라 **두 문장을 서로 가를 수 없다**(배치 55에서 좁힌 규칙).

### 「약」은 한 글자라 이름으로 안 썼다 — 배치 104의 「간」과 같은 수

「약 3년」처럼 **뒤가 공백인 자리**가 있어 `isStandalone` 을 지난다. 이름을 「먹는 약」으로
하고 조사를 붙인 꼴(「약을」·「약이」·「약은」)을 별칭으로 올렸다 — 그 꼴은 「약속」·「약간」에는
안 걸린다.

### 프로브가 판별어 활용형을 또 잡았다

```
「역겨운 약을 먹었다」 → 「맛이 좋은 약을 먹음」  「역겹」이 「역겨운」을 안 품는다(겹 ≠ 겨)
```

「역겨」로 고쳤다 — 배치 96(「가로채」)·105(「불어나」)에 이어 셋째다.

**관문 전부 초록불** — 인용 4,728건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 12건(지킴 둘 포함) 전부 걸림.

**사전 1,411 / 4,704 / 4,728** (상징 / 의미 / 인용).

---

## 배치 122 — 밀러 `Melancholy`~`Mendicant` (12건)

새 상징 넷 — `melancholy`(우울) · `memorandum`(메모) · `memorial`(기념비) ·
`menagerie`(동물원). 기존 상징 둘에 붙였다 — `melon`(오이) ← `Melon` 셋 ·
`beggar`(거지) ← `Mendicant` 하나.

### `Melon` 은 **id 가 같아서** 기존 상징에 합쳐진다

한국어 이름이 「오이」인데 `term_en` 이 `melon` 이다 — 새로 세워도 같은 상징이 된다
(배치 110의 `lute` 와 같은 자리). 「참외」는 이미 그 상징의 별칭이라 붙일 자리도 맞았다.

### 프로브가 **㊷ 를 두 상징에서 또 잡았다** — 네 번째·다섯 번째

```
「우울해했다」·「우울해하는」  → `melancholy` 안 걸림 — 별칭 「우울해」 뒤의 「했」이 조사가 아니다
「메모하는 것을 보았다」      → `memorandum` 안 걸림 — 「메모」 뒤의 「하」가 조사가 아니다
```

배치 109 · 116 · 117에 이어 **다섯 판째 같은 병**이다. 활용형 별칭을 함께 올려 고쳤다.

### 관문이 넷을 잡았다

```
기본값이 바뀜  melon 「오이 꿈을 꿈」 · beggar 「늙고 쇠약한 거지를 봄」  둘 다 되돌려 얼렸다
죽은 낱말      영어 「melons」(term_en 의 복수라 제 이름이 된다) · 「서 있」(1글자로 쪼개짐)
영어 겹침      새 「health」가 기존 「오이 꿈을 꿈」의 것과 겹쳤다 → 새 쪽에서 뺐다
```

**관문 전부 초록불** — 인용 4,740건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 14건(지킴 둘 포함) 전부 걸림.

**사전 1,415 / 4,716 / 4,740** (상징 / 의미 / 인용).

---

## 배치 123 — 밀러 `Mending`~`Metamorphose` (12건)

새 상징 다섯 — `mending`(옷 깁기) · `mercury`(수은) · `meshes`(그물코) ·
`message`(기별) · `metamorphose`(탈바꿈). 기존 `jolly`(흥겨움)에 `Merry` 하나를 붙였다.

### 「~하다」 활용형을 **이번에는 프로브 앞에서** 올렸다

배치 109·116·117·122에서 다섯 번 밟은 자리다. `mending`(기웠다·깁는·기워·꿰맸다)과
`metamorphose`(탈바꿈하는·탈바꿈했다·탈바꿈하고)는 **적는 그 자리에서** 함께 올렸고,
프로브에서 하나도 안 걸렸다.

### 「전갈」을 이름으로 쓰지 않았다 — **아직 안 들어온 표제어를 내다봤다**

밀러 `Scorpion` 이 아직 안 들어왔는데 그 한국어 이름이 「전갈」이다. 나중에 부딪칠
자리라 이름을 「기별」로 하고 「전갈」·「소식」은 별칭으로만 올렸다.

### 넓은 의미의 판별어를 좁혀 동점을 풀었다

「처녀가 그물코에 얽혔다」가 넓은 「그물코에 얽힘」(얽혔)과 동점이 났다. 넓은 쪽은
**기본값 자리**라 낱말로 이길 필요가 없으므로 「옭아매·휘감겼」로 좁혔다(§25 곁가지 3).
차례로 풀면 기본값이 좁은 쪽으로 넘어가서 안 된다 — **차례와 기본값이 부딪치는 자리**다.

### 관문이 셋을 잡았다

```
기본값이 바뀜  jolly 「벗들과 흥겹게 어울림」  되돌려 얼렸다
영어 겹침      「place」 ⊂ 「placed」          받는 쪽에서 뺐다
```

**관문 전부 초록불** — 인용 4,752건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 14건(지킴 둘 포함) 전부 걸림.

**사전 1,420 / 4,728 / 4,752** (상징 / 의미 / 인용).

---

## 배치 124 — 밀러 `Mice`~`Mile-post` (10건)

새 상징 셋 — `microscope`(현미경) · `midwife`(산파) · `mile-post`(이정표).
기존 `mouse`(쥐)에 밀러 `Mice` 다섯을 붙였다.

### 기본값을 **바꿔서** 얼렸다

있던 다섯은 전부 주공해몽의 조건이 붙은 그림(참새와 싸움 · 고양이에게 잡힘 · 흰 쥐가
길을 인도함 · 옷을 물어뜯음 · 크게 내달림)이고, 밀러의 첫 문장이 **가장 일반적인 그림**
이다 — 막연한 「쥐를 보았다」가 떨어질 자리는 「쥐를 봄」이다.

### 지킴 케이스가 **옛 상처 하나**를 찾았다 — `mouse` 의 「흰쥐」

```
「흰쥐가 앞장서 길을 인도했다」  →  안 걸림
```

**붙여 쓰면 앞 글자가 한글이라 「쥐」가 막힌다.** 그런데 기존 의미 「흰 쥐가 길을 인도함」의
**판별어가 바로 「흰쥐」**다 — 그 꼴로는 상징 자체가 안 걸리고 있었다. 「흰쥐」·「들쥐가」를
별칭으로 올려 고쳤다. **이번 배치가 만든 결함이 아니다.**

### 관문이 셋을 잡았다

```
한국어 겹침  새 「옷」 ⊂ 기존 「옷을」        → 「옷에 옷속 품속」으로
죽은 낱말    「옷 속」(공백으로 쪼개져 1글자) · 영어 「down」(불용어)
```

**관문 전부 초록불** — 인용 4,762건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 12건(지킴 둘 포함) 전부 걸림.

**사전 1,423 / 4,738 / 4,762** (상징 / 의미 / 인용).

---

## 배치 125 — 밀러 `Milk`·`Milking` (11건)

새 상징을 안 세웠다. 표제어 둘이 통째로 이미 있던 `milk`(젖)에 붙었다 —
**의미가 하나뿐이던 상징이 열둘이 됐다.**

### `milk` 도 판별어 표가 비어 있었다 — **이 세션 여덟 번째**

배치 100 · 103 · 108 · 110 · 117 · 118 · 120에 이어 여덟째다.
기본값은 **바꿔서** 얼렸다 — 옛 「남과 함께 젖을 먹음」은 조건이 붙은 자리이고
밀러의 첫 문장 「젖을 마심」이 가장 일반적인 그림이다.

### 「마시」를 못 쓴다

「젖을 마시려다 못 마심」이 「마시려다」를 써야 하는데 **「마시」가 그것의 부분 문자열**이다 —
마시는 쪽은 「마셨」·「들이켰」로 갈랐다. 넓은 쪽에서 낱말을 물리는 자리다.

**관문 전부 초록불** — 인용 4,773건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 12건(지킴 하나 포함) 전부 걸림.

**사전 1,423 / 4,749 / 4,773** (상징 / 의미 / 인용).

---

## 배치 126 — 밀러 `Mill`~`Mineral Water` (12건)

새 상징 여섯 — `mill`(방앗간) · `mill-dam`(물방아 둑) · `miller`(방앗간지기) ·
`mine`(광산) · `mineral`(광물) · `mineral-water`(광천수). 패치 스크립트는 없다.

### 표제어가 X라도 그 꿈에 있는 사물이 X가 아니다 — `Millet`

밀러의 `Millet`(기장) 본문은 **기장 이야기가 아니라 방앗간지기(a miller) 이야기**다.
그대로 `term_en: "millet"` 로 넣었으면 **이미 있는 `millet`(좁쌀)의 기본값을 갈아치웠을**
자리다. `term_en: "miller"` · `term_ko: "방앗간지기"` 로 세우고 `cite.locator` 만
원문대로 `Millet` 을 남겼다(배치 67 `Jailer` 와 같은 자리).

### 판별어도 활용형을 탄다 — 세 자리를 프로브·죽은 낱말이 잡았다

```
「제 것」  → 공백에서 쪼개져 1글자 둘이 된다        → 「제것」
「못 안」  → 「못 돌리 실패 안 돌아」의 1글자 둘     → 「돌리 실패 멈춰」
「돌리지」 → 이용자는 「못 돌리고」라고 쓴다          → 「돌리」
「서 있었」→ 「서」가 1글자로 죽는다                 → 「돌아갔」
being     → 영어 불용어                            → 뺐다
```

앞의 둘은 `audit-km-dead-words` 가, 「돌리지」는 **프로브만** 잡았다.

### 지킴 케이스 둘 다 내 기대가 틀렸다

「석탄 광산을 보았다」는 `colliery` 의 **「탄광에서 광부를 봄」**이 옛 답이고,
「좁쌀이 있었다」는 `millet` 의 **「좁쌀을 먹음」**이 옛 답이다. 이번 배치가 두 상징의
데이터를 건드리지 않았음을 확인하고 **기대치를 실제 답으로 고쳤다**(§25 곁가지).

**관문 전부 초록불** — 인용 4,785건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 14건(지킴 둘 포함) 전부 걸림.

**사전 1,429 / 4,761 / 4,785** (상징 / 의미 / 인용).

---

## 배치 127 — 밀러 `Mining`·`Minister`·`Minuet`·`Minx`·`Mire` (12건)

새 상징 둘(`minuet` 미뉴에트 · `mink` 밍크), 기존 셋에 붙임(`mine` 광산 셋 ·
`clergyman` 목사 셋 · `mud` 진흙 하나).

### ⓪ 전수 grep 이 셋을 미리 갈랐다

```
「광산 일」 ⊃ 광산(mine)        → 상징을 안 세우고 `mine` 에 붙였다(§25 곁가지)
「목사」    = clergyman EXACT   → `clergyman` 에 붙였다
「진창」    = mud 의 별칭        → `mud` 에 붙였다(`aliases_en` 에 mire 가 이미 있었다)
「밍크」    = fur 의 별칭        → **더 정확한 쪽(`mink`)에 넘기고 fur 에서 뺐다**
```

밀러의 `Minx` 는 사람이 아니라 짐승(밍크)이다. 「미뉴에트 춤」은 별칭에 안 넣었다 —
「춤」이 `dance` 의 이름이라 한 문장에 상징이 둘 뜬다.

### 기본값이 바뀌는 자리 둘 — 하나는 바꾸고 하나는 그대로 얼렸다 (§30)

`m127.json` 이 `m16.json`·`r*.json` 보다 앞서 정렬되어 관문이 둘을 멈춰 세웠다.

```
clergyman  장례 설교를 하려고 목사를 부름 → 목사를 봄   **바꿔서 얼림**(옛것은 좁은 자리다)
mud        진흙 속에 빠져 있음 → 진창을 헤치고 감      **옛것 그대로 얼림**
```

### 형제 판별어를 적는 자리에서 갈랐다

`clergyman` 의 옛 의미가 「설교」를 쥐고 있어, 새 「목사의 설교를 들음」은
「들었 강론 훈계」로 적었다(§30 곁가지 — 붙이는 쪽만 채우면 겹친다).
프로브·죽은 낱말이 **첫 판에 초록불**이었다.

**관문 전부 초록불** — 인용 4,797건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 16건(지킴 넷 포함) 전부 걸림.

**사전 1,431 / 4,773 / 4,797** (상징 / 의미 / 인용).

---

## 배치 128 — 밀러 `Miser`·`Mist`·`Mistletoe`·`Mocking-bird`·`Models` (14건)

새 상징 넷(`miser` 구두쇠 · `mistletoe` 겨우살이 · `mocking-bird` 흉내지빠귀 ·
`model` 모델), 기존 하나에 붙임(`fog` 안개 셋).

### `Mirror` 는 **이미 다 들어와 있었다** — 넣지 않았다

밀러 `Mirror` 일곱 문장이 전부 `mirror`(거울) 상징에 `cite.locator: "Mirror"` 로
실려 있다. 배치 40이 밀러 `Glass` 의 「[129] See Glass」 자리에서 함께 옮겼다.
커버리지가 이미 차 있으므로 **빈 자리가 아니다**(§31 곁가지 — 같은 그림).

### 「걷혔」을 옛 의미에서 빼서 새 의미에 넘겼다

`fog` 의 옛 「안개에서 빠져나옴」이 「걷혔」을 쥐고 있었는데, 「안개가 걷혔다」는
새 의미(`Mist` 의 If the mist clears away)의 그림이다. 옛 쪽에는 「빠져나」·「벗어났」이
남으므로 잃는 것이 없다 — **붙이는 쪽만 채우면 새 의미가 영영 안 뽑힌다**(§30 곁가지).

### 별칭 뒤에 「였다」가 오면 막힌다 — 프로브가 잡았다

「벗들이 구두쇠였다」가 안 걸렸다. 「구두쇠」 뒤의 「였」이 조사가 아니라
`isStandalone` 이 낱말 조각으로 버린다 → 별칭 「구두쇠였다」를 올렸다.
「내가 인색하게 굴었다」도 상징 이름이 문장에 없어 안 걸렸다 → 별칭 「인색하게」를 올렸다.

### 지킴 케이스가 **원래 있던 상처**를 하나 드러냈다 (이번 배치가 만든 것이 아니다)

「깨진 거울을 보았다」가 밀러 의미 「깨진 거울을 봄」이 아니라 주공해몽 의미
「깨진 거울로 사람을 비춤」으로 갔다 — 뒤엣것이 넓은 낱말 **「깨진」**을 쥐고 있고
앞엣것은 「파편 조각들 유리조각」뿐이라 0점이었다. **배치 40부터 그랬고 어떤 관문도
못 본다**(낱말이 겹치지도 죽지도 않는다). `scripts/patch-km-128b.mjs` 로 「깨진」을
그 그림의 임자에게 넘겼다.

**남는 자리를 적어 둔다** — 「깨진 거울로 사람을 비췄다」는 여전히 「깨진 거울을 봄」으로
간다(「깨진」 1점 대 「비췄」 1점의 동점이고 앞의 것이 이긴다). 두 낱말을 다 주면
`verify-dream-km` 이 겹침으로 막는다. 의미가 열여덟인 상징이라 낱말을 더 나눌 자리가
없고, 둘 다 흉한 뜻이라 화면의 길흉은 같다.

**관문 전부 초록불** — 인용 4,811건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 17건(지킴 셋 포함) 전부 걸림.

**사전 1,435 / 4,787 / 4,811** (상징 / 의미 / 인용).

---

## 배치 129 — 밀러 `Molasses`·`Moles`·`Monk`·`Monkey` (13건)

새 상징 셋(`molasses` 당밀 · `mole` 두더지 · `blemish` 몸에 난 점),
기존 둘에 붙임(`monk` 스님 셋 · `monkey` 원숭이 넷).

### 한 표제어가 **두 사물**을 말한다 — `Moles`

앞 둘은 짐승(두더지)이고 셋째는 **살갗의 점**이다(「moles, or such blemishes,
on the person」). 상징을 둘로 갈랐다. 뒤엣것의 `term_en` 을 `mole` 계열로 안 쓴 이유는
「mole」이 「skin mole」을 부분 문자열로 품어 영어 문장에서 둘 다 걸리기 때문이다
(§25 곁가지) — 원문에 있는 낱말 `blemish` 를 이름으로 삼았다.

한국어에서는 「점」을 별칭으로 못 쓴다 — `divination`(점치기)이 「점을 치」·「점을 봤다」를
쥐고 있어 「점을 보러 갔다」가 이쪽으로 샌다. 「점이 생겼다」·「몸에 점이」처럼
**살갗의 점일 때만 나오는 꼴**로 올렸다.

### 이름을 서로 맞바꿔 쥐고 있었다 — 옛 상처 (이번 배치가 만든 것이 아니다)

```
apes(유인원)  가 「원숭이」·`monkey`·`monkeys` 를 쥐고 있었다
monkey(원숭이) 가 `ape` 를 쥐고 있었다
```

「원숭이를 보았다」가 **두 상징에 다 걸려 화면에 상징이 둘 뜨고 있었다.**
`audit-cross-symbol-aliases` 의 「맞바꿔 쥔 짝」은 0쌍으로 나온다 — 한국어만 보기 때문이다.
붙이려고 `monkey` 를 여는 그 순간에 보였다(§29 곁가지). 각자 제 이름만 갖게 되돌렸다.

### 기본값 둘을 바꿔서 얼렸다 (§30)

```
monk    법사가 법좌에 오름 → 스님을 봄
monkey  산 원숭이를 봄     → 원숭이를 봄
```

둘 다 옛것이 좁은 자리이고 밀러의 첫 문장이 가장 일반적인 그림이다.
얼린 자리의 판별어는 좁게 적었다(「만났 마주쳤」·「나타났 마주쳤」) — 얼림은 그것을
배열 첫째로 옮기므로 **동점을 전부 가져간다**(§30 곁가지).

**관문 전부 초록불** — 인용 4,824건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 17건(지킴 넷 포함) 전부 걸림.

**사전 1,438 / 4,800 / 4,824** (상징 / 의미 / 인용).

---

## 배치 130 — 밀러 `Money` (9건)

새 상징을 안 세웠다. 표제어 하나가 기존 셋을 건드렸다 —
`money`(돈) 여덟 · `gold`(황금) 하나 · `paper-money`(지전) 별칭 하나 뺌.

### 「To receive gold」는 돈이 아니라 **황금**이다

표제어는 `Money` 지만 그 꿈에 실제로 있는 사물은 황금이다 — `gold` 에 붙이고
`cite.locator` 만 원문대로 `Money` 를 남겼다(배치 67 `Jailer`·배치 126 `Millet` 과 같은 자리).

### 문장 둘을 안 넣었다

```
「To dream of finding money…」  같은 그림이 이미 있다 — `money` 의 주공해몽 의미
                               「돈과 물건을 주움」(財物拾得)이다. 넣으면 「돈을 주웠다」에서
                               낱말을 가를 수 없고, m130.json 이 r*.json 보다 앞서 정렬되어
                               **차례로도 못 푼다**(§30 곁가지, 배치 78)
「The dreamer will find…」      꿈의 그림이 아니라 지은이의 훈계다
「Beware lest the innocent…」
```

### 「지폐」를 두 상징이 쥐고 있었다 — 옛 상처

`money` 와 `paper-money`(지전) 둘 다 「지폐」를 별칭으로 갖고 있어 「지폐를 주웠다」에
**상징이 둘 떴다.** 지전은 노잣돈·종이돈이 제 말이므로 「지폐」는 `money` 에 남겼다.
`audit-cross-symbol-aliases` 는 이것을 **맞바꿈이 아니라 한쪽 겹침**으로 보아 빨간불을
내지 않는다 — 붙이려고 `money` 를 여는 그 순간에 보였다(§29 곁가지).

### 기본값 둘 — 하나는 바꾸고 하나는 그대로 얼렸다 (§30)

```
money  남에게 돈과 물건을 돌려줌 → 돈이 많이 있는 것을 봄   **바꿔서 얼림**
gold   황금을 손에 쥠 → 황금을 받음                        **옛것 그대로 얼림**
```

`gold` 가 바뀔 줄은 몰랐다 — `m130.json` 이 `m40.json` 보다 **앞서** 정렬된다(`1` < `4`).
관문이 없었으면 못 봤다.

### 영어 판별어가 두 번 걸렸다

`repaid` ⊃ `paid`(기존과 겹침) → `pay` 로 · `out` 은 불용어라 죽었다 → 뺐다.
`payments` ⊃ `pay` 도 함께 갈랐다(`making` 으로).

**관문 전부 초록불** — 인용 4,833건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 15건(지킴 여섯 포함) 전부 걸림.

**사전 1,438 / 4,809 / 4,833** (상징 / 의미 / 인용).

---

## 배치 131 — 밀러 `Monster`·`Moon`·`Morgue`·`Morning`·`Morocco` (15건)

새 상징 둘(`monster` 괴물 · `morgue` 영안실), 기존 셋에 붙임
(`moon` 달 여덟 · `daybreak` 새벽 둘 · `hide` 짐승 가죽 하나).

### 이름을 세 번 다르게 골랐다 — 전부 ⓪ grep 이 갈랐다

```
「시체 안치소」 → 「영안실」   「시체」가 `dead-person` 의 별칭이다
「아침」        → 상징 안 세움  `breakfast` 의 별칭 「아침 식사」와 한 문장에서 둘 다 걸린다
                              (「아침 」 뒤가 공백이라 isStandalone 을 지난다)
「모로코 가죽」 → 상징 안 세움  `hide` 의 별칭 「가죽」을 품는다(§25 곁가지)
```

`Morning` 은 같은 그림의 임자인 `daybreak`(새벽)에 붙였다 — 밀러 `Daybreak` 가
이미 거기 있고, 가르는 것은 **맑음/흐림**이다.

### 의미가 스물이 되자 **영어가 글자로 부딪쳤다** — 배치 43과 같은 자리

`moon` 이 열둘에서 스물이 되니 `verify-dream-km` 이 넷을 냈다.

```
red   ⊂ appea**red**        「핏빛으로 붉은 달」  → 「crimson」으로
ate   ⊂ prognostic**ate**s  「여느 때와 같은 달」 → 「normal」로
ate   ⊂ f**ate**            「제 앞날을 물음」    → 그 낱말을 뺐다
red   ⊂ ente**red**         〃
```

**새로 넣는 쪽만 갈았다**(배치 43 규칙) — 기존을 건드리면 이미 돌던 극성 하니스가 깨진다.
한국어에서도 셋을 미리 피했다(「가려지」⊃「가려」 · 「삼켜져」⊃「삼켜」 · `dim`).

### 「월식을 보았다」에 상징이 안 걸렸다 — 프로브만 잡는다

의미 이름은 「월식이 드는 것을 봄」인데 **문장에 「달」이 없다.** 별칭 「월식」을 올렸다.
판별어를 아무리 잘 적어도 **상징이 안 걸리면 아무 일도 안 일어난다**(§25 곁가지).

### 기본값 셋이 바뀌었다 — 하나만 바꾸고 둘은 그대로 얼렸다 (§30)

```
moon      달이 막 떠오름 → 하늘이 여느 때와 같은데 달을 봄   **바꿔서 얼림**
daybreak  날이 밝아 오는 것을 봄                              **옛것 그대로**
hide      짐승 가죽을 봄                                      **옛것 그대로**
```

`m131.json` 이 `m24.json`·`m47.json` 보다 **앞서** 정렬된다(`1` < `2`·`4`) —
배치 130의 `gold` 와 같은 자리이고, 관문이 없었으면 못 봤다.

**관문 전부 초록불** — 인용 4,848건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 20건(지킴 다섯 포함) 전부 걸림.

**사전 1,440 / 4,824 / 4,848** (상징 / 의미 / 인용).

---

## 배치 132 — 밀러 `Mortgage`·`Morose`·`Mortification`·`Moses`·`Mosquito`·`Moss` (13건)

새 상징 여섯(`mortgage` 저당 · `morose` 시무룩함 · `mortification` 부끄러움 ·
`mortified-flesh` 썩은 살 · `moses` 모세 · `moss` 이끼), 기존 하나에 붙임(`mosquito` 모기 둘).

### `Mortification` 도 **한 표제어가 두 사물**이다 — 배치 129 `Moles` 와 같은 자리

앞은 마음의 부끄러움(feel mortified over a deed), 뒤는 **썩은 살**(mortified flesh)이다.
한 상징에 두면 「썩은 살을 보았다」가 「부끄러움」에 안 걸린다 — 갈랐다.

### `Moss` 는 한 문장에 조건이 붙어 있어 의미를 둘로 갈랐다

「…unless the moss grows in rich soil, when you will be favored with honors」 —
길흉이 정반대라 한 의미로 둘 수 없다. **인용은 같은 문장을 둘 다 싣는다.**

### 이름이 상태면 활용형 별칭을 함께 올린다 (배치 63)

`morose`(시무룩함)에 「시무룩한」·「시무룩해」·「시무룩했다」를 처음부터 넣었고
프로브가 첫 판에 걸렸다. `mortification`(부끄러움)도 같다.

### `mosquito` 도 **판별어 표가 비어 있었다** — 이 세션 아홉 번째 (배치 35)

의미가 하나(주공해몽 「모기가 사람을 묾」)뿐이라 `contexts` 가 빈 객체였다.
셋이 되므로 기존 의미부터 채웠고, 기본값은 밀러의 「모기를 봄」으로 바꿔 얼렸다.

### 영어가 또 글자로 부딪쳤다

```
a**gain**st(저당을 잡음) ⊃ gain(문서를 읽어 봄)   → 「against」를 뺐다
bite  = `mosquito bite` 가 제 별칭이라 죽었다      → 「bitten stung」으로
just  = 불용어                                    → 뺐다
```

**관문 전부 초록불** — 인용 4,861건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 14건(지킴 하나 포함) 전부 걸림.

**사전 1,446 / 4,837 / 4,861** (상징 / 의미 / 인용).

---

## 배치 133 — 밀러 `Moth`·`Mother`·`Mother-in-law`·`Mourning` (11건)

새 상징 둘(`mother` 어머니 · `mother-in-law` 시어머니), 기존 둘에 붙임
(`moth` 나방 하나 · `mourning-clothes` 상복 둘).

### 「어머니」와 「시어머니」 — **한국어는 안전하고 영어는 안 안전하다**

```
시어머니를 보았다        「어머니」 앞 글자가 한글이라 isStandalone 이 막는다 → 안 걸린다 ✓
my mother-in-law         `mother` 뒤가 붙임표라 낱말 끝으로 읽혀 **둘 다 걸린다** ✗
```

표제어가 둘이므로 상징도 둘로 두되, 이 사실을 배치 파일에 적어 두었다.
`findTerm` 은 상징마다 따로 돌아 전역 중재가 없다(§25 곁가지).

### 「상복을 입음」은 두 원문이 **길흉을 정반대로** 말한다 — 안 넣었다

주공해몽 「몸에 상복을 입음」(哀樂病死歌唱)은 **길**, 밀러 「To dream that you wear
mourning」은 **흉**이다. 꿈의 그림이 같아 낱말로도 차례로도 가를 수 없으므로
넣으면 뒤엣것이 어떤 문장으로도 안 뽑힌다 — 옛 판정을 그대로 두고 건너뛴 것을 적었다.

### 판별어 표가 비어 있던 상징이 둘 더 나왔다 — 열 번째·열한 번째

`moth`·`mourning-clothes` 둘 다 의미가 하나뿐이라 `contexts` 가 빈 객체였다.
기존 의미부터 채웠다.

### 얼린 기본값이 「입고」를 쥐고 있어 새 의미를 가로챘다

「남들이 상복을 입고 있었다」가 기본값 「몸에 상복을 입음」으로 갔다 — 그쪽이
「입고」를 쥐고 있어 1:1 동점이 되고 얼린 것이 앞이기 때문이다. **얼린 자리의 판별어를
좁혔다**(「입었 걸쳤」) — 그 자리는 아무것도 안 걸렸을 때 떨어지는 자리다(§30 곁가지).
지킴 케이스 「상복을 입고 있었다」는 여전히 그 기본값으로 옳게 떨어진다.

**관문 전부 초록불** — 인용 4,872건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 13건(지킴 둘 포함) 전부 걸림.

**사전 1,448 / 4,848 / 4,872** (상징 / 의미 / 인용).

---

## 배치 134 — 밀러 `Mountain`·`Mouse-trap`·`Mud` (12건)

새 상징 하나(`mouse-trap` 쥐덫), 기존 둘에 붙임(`mountain` 산 다섯 · `mud` 진흙 넷).

### 표제어 하나와 문장 하나를 안 넣었다

```
`Mouse` (표제어 통째)          「For a woman to dream of a mouse … artfulness and treachery」는
                               `mouse` 에 이미 있는 밀러 `Mice` 의 「처녀가 쥐 꿈을 꿈」과
                               같은 꿈·같은 풀이다
「To see mud on your clothing」 같은 그림이 주공해몽 「진흙이 옷을 더럽힘」에 있다.
                               낱말(「옷」)을 못 가르고 m134 < r1 이라 차례로도 못 푼다
```

### 기존 열의 판별어를 한 줄씩 읽고 새 다섯을 골랐다 (§30 곁가지)

`mountain` 은 이미 「떨어져」·「두려」·「무너」·「유람」·「자리잡」·「재물」·「안고」·「농사」에
임자가 있다. 새 다섯은 「사촌 오라비 넘었」·「지쳐 마다」·「푸른 우거진」·「가파른 꼭대기」·
「잠이 깼다」로 갈랐고 **관문이 한 번도 안 물렸다.**

### 넓은 낱말을 새것끼리 물었다 — 프로브가 잡았다

「남들이 진흙을 밟고 가는 것을 보았다」가 형제 「진흙을 밟고 걸어감」으로 갔다.
그쪽이 **「밟고」**를 쥐고 있어 1:1 동점이 되고 앞의 것이 이겼다 — 「밟고」를 빼고
「걸어 걸었」만 남기자 갈렸다. **기존 대 새것만이 아니라 새것끼리도 같은 병이 난다.**

### 영어는 또 글자로 부딪쳤다

```
walk ⊂ walking      새것 둘 사이   → 「walk」를 뺐다
full ⊂ art**full**y  〃            → 「mice」로
```

### 기본값은 옛것 그대로 얼렸다 (§30)

밀러 다섯이 앞으로 정렬되지만 **다섯 다 조건이 붙은 그림**이라 기본값 자리에 어울리지
않는다. 산의 옛 기본값(「산이 해와 달을 물고 있음」)이 막연한 산 꿈의 답으로 어울리지
않는 것은 **이번 배치가 만든 것이 아니라 원래 그랬던 것**이다 — 사실만 적어 둔다.

**관문 전부 초록불** — 인용 4,884건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 17건(지킴 다섯 포함) 전부 걸림.

**사전 1,449 / 4,860 / 4,884** (상징 / 의미 / 인용).

---

## 배치 135 — 밀러 `Muff`·`Mulberries`·`Mule`·`Muscle`·`Museum` (13건)

새 상징 셋(`muff` 토시 · `muscle` 근육 · `museum` 박물관),
기존 둘에 붙임(`mulberry` 오디 하나 · `mule` 노새 넷).

### 표제어 둘을 통째로 뺐다 — 이유가 서로 다르다 (§31)

```
Mulatto   **그림 자체가 비하다** — 한 인종이 나타나는 것을 두고 「새 사귐과 낯선 여자를
          조심하라, 돈과 도덕을 잃는다」로 점친다. 비하를 빼면 남는 꿈이 없다
          (`Gypsy` 42 · `Jew` 69와 같은 자리)
Murder    **세 문장이 전부 같은 그림이 이미 있는 자리다.** 원문 자신이
          「[132] See Killing and kindred words」라고 적어 두었다
```

`Murder` 의 ①「To see murder committed」와 ③「you are murdered」는 주공해몽이 **길**,
밀러가 **흉**이다 — 배치 133 `Mourning` 과 같은 자리이고, 꿈의 그림이 같아 가를 수 없다.

### 문장 둘도 같은 이유로 뺐다

「riding on a mule」(= 주공해몽 「노새를 타고 감」) · 「To eat them」(= 「오디를 먹음」).
둘 다 길흉이 반대다. **`Mule` 은 뒷절만 살렸다** — 「탈 없이 갈 곳에 다다르면」은
그림이 달라 넣을 수 있다.

### 판별어 표가 비어 있던 상징이 하나 더 — 열두 번째

`mulberry` 는 의미가 하나(주공해몽 「오디를 먹음」)뿐이라 `contexts` 가 빈 객체였다.

### 「끼고」를 기본값이 쥐어 형제를 가로챘다 — 프로브가 잡았다

「애인이 토시를 끼고 있었다」가 「토시를 낌」으로 갔다(끼고 1 : 애인 1, 앞의 것 승).
기본값 쪽을 「끼었 꼈다」로 좁혔다 — 그 자리는 아무것도 안 걸렸을 때 떨어지는 자리다.

### 1글자·불용어가 또 셋 죽었다

「넣」(「손을 넣」이 쪼개짐) · 「흰」(1글자) · `without`(불용어).
**「흰」은 이 사전에서 자주 나오는 함정이다** — 「하얀」·「흰색」으로 적는다.

**관문 전부 초록불** — 인용 4,897건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 16건(지킴 셋 포함) 전부 걸림.

**사전 1,452 / 4,873 / 4,897** (상징 / 의미 / 인용).

---

## 배치 136 — 밀러 `Music`·`Musical Instruments`·`Mushroom`·`Musk`·`Mussels`·`Mustache` (13건)

새 상징 넷(`musical-instrument` 악기 · `mushroom` 버섯 · `musk` 사향 · `mustache` 콧수염),
기존 둘에 붙임(`music` 풍악 둘 · `clam` 조개 하나).

### 「악기」를 풍악에서 악기에게 돌려줬다

`music`(풍악)이 별칭으로 「악기」를 쥐고 있었다. 밀러는 `Music` 과 `Musical Instruments`
를 따로 세우고, 「악기」는 **사물의 이름**이다 — 더 정확한 쪽에 넘겼다(§25 곁가지).
`music` 에는 「음악」·「가락」·「연주」가 남는다.

### 이름이 남을 품는 자리가 셋이었는데 전부 안전했다

```
사향   ⊃ 향(incense)    앞 글자가 한글이라 isStandalone 이 막는다
콧수염 ⊃ 수염(beard)    〃
박물관 ⊃ 물·관          〃 (배치 135)
```

**품음 관계를 찾았으면 거기서 멈추지 말고 `isStandalone` 을 손으로 돌려 본다**(§25 곁가지,
배치 29) — 이번에는 셋 다 걸리지 않아 그대로 세웠다.

### 판별어 표가 비어 있던 상징이 하나 더 — 열세 번째

`music` 은 의미가 하나(주공해몽 「남이 풍악을 울림」)뿐이라 `contexts` 가 빈 객체였다.

### 기본값 둘 다 옛것 그대로 얼렸다

밀러 쪽이 조건이 붙은 그림(「고운 가락」·「민물 홍합」)이라 기본값 자리에 어울리지 않는다.

**관문 전부 초록불** — 인용 4,910건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 16건(지킴 셋 포함) 전부 걸림 — **첫 판에 초록불이었다.**

**사전 1,456 / 4,886 / 4,910** (상징 / 의미 / 인용).

---

## 배치 137 — 밀러 `Mustard`·`Mute`·`Myrrh`·`Myrtle`·`Mystery` (12건) — **밀러 M 끝**

새 상징 넷(`mustard` 겨자 · `myrrh` 몰약 · `myrtle` 도금양 · `mystery` 수수께끼),
기존 하나에 붙임(`dumb` 말을 못 함 하나).

### 문장 하나를 안 넣었다

「To dream that you are a mute」는 `dumb` 의 밀러 `Dumb` 의미 「말을 못 하게 됨」과
같은 그림이다. 풀이는 다르지만 판별어를 가를 수 없고 `m137.json` 이 앞으로 정렬되어
차례로도 못 푼다.

### 판별어 표가 비어 있던 상징이 하나 더 — 열네 번째

`dumb` 은 의미가 하나(밀러 `Dumb`)뿐이라 `contexts` 가 빈 객체였다.

### 「자란」을 먼저 온 의미가 쥐어 형제를 가로챘다 — 프로브가 잡았다

「처녀가 갓 자란 겨자를 먹었다」가 「푸르게 자란 겨자를 봄」으로 갔다(자란 1 : 처녀 1,
앞의 것 승). **새것끼리도 같은 병이 난다**(배치 134와 같다) — 앞엣것에서 「자란」을 빼고
「푸르 밭에」만 남겼다.

### 1글자·불용어가 또 둘

「걸」(「말을 걸」이 쪼개짐) → 「말을 걸었다」로 · `unable`(불용어) → 「persuade others」로.

**밀러 원문의 M 이 여기서 끝난다** — 다음은 `Nails` 부터다.

**관문 전부 초록불** — 인용 4,922건 위반 0(밀러 포함 전수) · km 위반 0 ·
커버리지 판별어 빠진 의미 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 · 회귀 하니스 ALL PASS ·
극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 14건(지킴 둘 포함) 전부 걸림.

**사전 1,460 / 4,898 / 4,922** (상징 / 의미 / 인용).
