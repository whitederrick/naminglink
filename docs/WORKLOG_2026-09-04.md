# 2026-09-04 작업 기록

전날(`WORKLOG_2026-09-03.md`)에서 이어진다 — 밀러 배치를 5판 묶음(139~143)으로 넣는
작업 중이다. 배치 139~141은 09-03에, 142부터는 날짜가 바뀌어 여기에 적는다.
절차·규칙은 CLAUDE.md와 메모리 `dream-dictionary-v2-rebuild-in-progress`에 있다.

---

## 배치 142 — 밀러 `Oak`~`Ocean` (12건, O 의 시작)

새 상징 열하나(`oak`·`oar`·`oath`·`oatmeal`·`oats`·`obedience`·`obelisk`·`obituary`·
`obligation`·`observatory`·`occultist`), 기존 하나에 붙임(`sea`(Ocean) 넷).
건너뛴 표제어 없음 — 다만 `Ocean`의 마지막 문장(「고요한 바다에서 배를 몲」)은
첫 문장과 같은 그림(잔잔한 바다=길함)이라 뺐다.

### `sea`(바다)가 밀러 콘텐츠를 처음 받았다 — 판별어 표가 비어 있었다(스물두 번째)

기존 의미는 주공해몽 하나(「강과 바다가 넘쳐 불어남」)뿐이었다. `Ocean` 넷을 붙이며
기존 의미의 판별어도 채웠다. 기본값이 「강과 바다가 넘쳐 불어남」(홍수, 좁은 자리)에서
「바다가 잔잔함」(밀러, 가장 막연한 그림)으로 **바뀌었다** — `FALLBACK_FIRST`로 얼렸다.

### 「먼바다」가 「바다」를 안 걸리게 한다 — 합성어 규칙의 새 사례

「먼바다에서 파도가 배를 쳤다」가 `sea`에 안 걸렸다. 「먼바다」의 「먼」이 한글이라
`isStandalone`이 「바다」를 막는다(§29 곁가지 「품음」 규칙과 같은 자리, 이번엔 **품기는
쪽**이 막힌 것). 「먼바다」를 통째로 별칭에 올렸다.

### `verify-dream-context-parity`(극성 하니스)가 처음으로 실패했다 — 하니스 자신의 시험 문장이 깨어났다

하니스에 이미 `{ id: "sea", label: "바다 — 거친 파도", ko: "바다에 거친 파도가
몰아쳤다", en: "rough waves crashed against me" }`라는 시험 쌍이 있었다 — `sea`에
관련 의미가 없던 동안은 둘 다 기본값으로 떨어져 짝이 맞았고(§28), 이번에 진짜
의미가 생기자 **ko는 새 의미로 갈리는데 en은 여전히 기본값(잔잔함, 긍정)에
머물렀다.** en 판별어에 「waves」를 넣었지만 **이미 `aliases_en`에 「waves」가
있어 `ownTermsOf`가 판별에서 지워버렸다**(§29 곁가지 — 별칭에 올린 낱말은 영어
판별어에서 죽는다) — 「crashed」로 바꾸고서야 걸렸다.

### `audit-km-dead-words`가 넷을 잡았다

`oak`의 「벼락 맞았」(활용형이 「맞은」과 어긋남) · `obedience`의 en 「obedient」
(이미 별칭이라 영어 판별어에서 죽음) · `obelisk`의 「서」(1글자) · `obituary`의
「썼」(1글자). 전부 완전한 활용형이나 2글자 이상으로 갈았다.

### 프로브가 여덟을 잡았다

```
oak         「벼락 맞은…」            다른 뜻 — 판별어가 「맞았」만 있고 「맞은」이 없었다
obedience   두 문장 다 안 걸림        「~하다」형 명사(§29 곁가지, 배치 109·116·117과 같다)인데
                                     「순종했다」 꼴 별칭이 없었다 — 추가
sea(둘)     「먼바다…」·「물가에서…」  안 걸림 — 「먼바다」 합성어 문제(위) · 「파도」가
                                     아예 별칭에 없어 「물가에서 파도가…」도 안 걸렸다
                                     (「대양」·「바닷물」만 있었다) — 「파도가」·「파도를」 추가
```

### 관문

인용 5,043건 위반 0(밀러 포함 전수) · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 82건(지킴 하나 포함) 전부 걸림.

**사전 1,500 / 5,019 / 5,043** (상징 / 의미 / 인용).

---

## 배치 143 — 밀러 `October`~`Oilcloth` (10건) — **5판 묶음(139~143)의 마지막 판**

새 상징 여섯(`october`·`oculist`·`odd-fellow`·`odor`·`offering`·`oilcloth`), 기존
다섯에 붙임(`humiliation`(Offense) 셋 · `official-post`(Office) 셋 · `child`(Offspring
의 사람 아이) 하나 · `brood`(Offspring 의 짐승 새끼) 하나 · `oil`(Oil) 넷). 건너뛴
표제어 없음.

### 「관직」·「모욕」·「자식」— 이미 여러 상징이 나눠 쥔 낱말에 또 붙였다

`Office`(관직)는 이미 `official-post`·`official-s-seat`·`receiving-an-official-post`
셋이 나눠 쥐고 있어, 가장 일반적인 `official-post`(벼슬)에 붙였다. `Offense`(모욕)는
이미 `humiliation`·`verbal-abuse` 둘이 있어 그중 뜻이 가까운 `humiliation`(밀러
`Affront`)에 붙였다 — Affront는 「당함」, Offense는 「제 잘못이 드러남·남에게 줌·
처녀가 주거나 받음」으로 각도가 달라 겹치지 않았다. `Offspring`(자식)은 한 표제어를
**둘로 갈랐다** — 「제 자식을 봄」은 사람이라 `child`(아이)에, 「집짐승의 새끼를 봄」은
짐승이라 `brood`(새끼, 밀러 자신의 다른 표제어 `Brood`에서 온 상징)에.

### 기본값이 넷 후보에 올랐다 — 하나만 바꾸고 셋은 되돌렸다

```
humiliation    되돌림 — 새 「모욕당했다고 느낌」보다 옛 「모욕당하는 꿈을 꿈」이 더 막연하다
official-post  되돌림 — 새 「관직을 지님」보다 옛 「새로 벼슬을 받음」이 조건 없이 더 막연하다
child          되돌림 — 「제 자식을 봄」과 「아름다운 아이들을 여럿 봄」 어느 쪽도 명백히
               더 넓지 않아, 이미 나가 있는 화면을 흔들지 않는 쪽을 골랐다(처음 있는 선택)
brood          바꿈  — 새 「집짐승의 새끼를 봄」이 옛 「여성이 새끼를 거느린 가금을 봄」보다
               성별·가금이라는 조건이 없어 더 막연하다
```

`child`가 새로운 자리다 — 지금까지는 "더 넓은 쪽"이 있으면 그쪽으로 정했는데, 이번엔
**두 그림이 다른 축**(소유 여부 대 수식어)이라 우열을 가릴 수 없었다. 그럴 때는
**기존 화면을 흔들지 않는 쪽**(옛 기본값)을 고른다고 처음으로 명시했다.

### 「~당하다」꼴 활용형이 또 새 얼굴로 났다 — 계사(§29, 배치 141)와 다른 결

`humiliation`의 판별어 표가 비어 있던 것은 아니었지만(이미 문맥 셋이 있었다),
**그중 아무것도 자연스러운 「모욕당했다」류 문장을 못 걸었다** — 별칭이 「치욕·망신·
수모·굴욕」(전부 동의어)뿐이고 상징 이름 「모욕」 자체와 그 활용형(모욕당하는·
모욕당했다)이 하나도 별칭에 없었다. 중간 토막(「모욕당」)만 별칭에 올려도 안 걸렸다 —
뒤에 오는 「했」·「하는」이 조사가 아니기 때문이다(§29 곁가지 ①과 같은 경계). **완전한
활용형**(「모욕당하는」·「모욕당했다」·「모욕당해」)을 통째로 올리고서야 걸렸다.
가장 나쁜 것은 이 상처가 **오늘 배치가 만든 것이 아니라, `humiliation`이 만들어진
날부터 있었다**는 것이다 — 이번에 그 상징을 열었기 때문에만 드러났다(§29 곁가지 ①의
"그 상징을 여는 그 순간이 유일하게 눈이 가는 때"와 같다).

### 관문

인용 5,064건 위반 0(밀러 포함 전수) · km 위반 0 · 죽은 낱말 0 · 맞바꿔 쥔 짝 0 ·
회귀 하니스 ALL PASS · 극성 하니스 실패 0 · 태몽 9건 어긋남 0 · 안내 숫자 ALL PASS · tsc 0건 ·
프로브 106건(지킴 셋 포함) 전부 걸림.

**사전 1,506 / 5,040 / 5,064** (상징 / 의미 / 인용).

**5판 묶음(139~143)이 끝났다.** 다음은 전수 스윕·푸시·배포 확인이다.

### 스윕·푸시·배포 확인

전수 스윕 142줄 · 통과 117 / 검사 안 됨 7 / 빨간불 4 — 빨간불 넷은 이 작업과 무관한
상시 항목(§④와 동일, 새로 난 것 없음): `audit-dream-ownterms-multiword`(폐기된 옛
사전) · `verify-company-display`(mailOrderNumber 로마자화) · `verify-rendered-locale`
(vi/ar) · `naminglink/verify-locale-manifest`(낡은 검수 기록).

푸시 완료(`bdf5458..e3db5ed`). 배포 확인 — `oak`·`nymph`·`oilcloth`·`navy` 넷 다
404 → 200(약 225초, 15번째 시도).

**사용자 지시(2026-09-04) — 남은 배치를 전부 이어서 진행하고, 다섯 판마다
스윕·푸시·배포 확인을 한다. 중간에 확인을 구하지 않는다.**

---

## 배치 144 — 밀러 `Ointment`~`Opium` (9건, 새 5판 묶음 144~148의 첫 판)

새 상징 일곱(`ointment`·`old-man-or-woman`·`olives`·`omelet`·`one-eyed`·`onions`·
`opera`), 기존 둘에 붙임(`carriage`(Omnibus) · `laudanum`(Opium)). 건너뛴 표제어 없음.

`Old Man, or Woman`은 원문이 「See Faces, Men, and Women」이라 적었지만 그런 일반
상징이 없고 내용도 `face`와 달라 새로 세웠다. `Opium`은 「아편」이 이미 `laudanum`의
이름이라 붙였고, 기본값이 「아편을 먹음」(행위)에서 「아편 꿈을 꿈」(가장 막연한
그림)으로 바뀌어 얼렸다.

id 하나를 잘못 짐작했다 — `old man, or woman`이 조립기에서 `old-man-or-woman`으로
슬러그화되는데 `old-person`으로 지레짐작해 kmm 파일에 적었더니 `build-dream-match-
tables-v2.mjs`가 곧바로 "사전에 없는 id"로 잡았다. **build를 한 번 돌려 실제 id를
확인한 뒤에 kmm 파일을 쓰는 순서가 더 안전하다.**

`verify-dream-km`이 영어 판별어 부분 문자열 겹침 둘을 잡았다(bottles⊃bottle,
eat⊂defeated). 관문 전부 통과(인용 5,082 위반 0 등, 프로브 126건 전부 걸림).

**사전 1,513 / 5,058 / 5,082** (상징 / 의미 / 인용).

---

## 배치 145 — 밀러 `Opulence`~`Orchard` (5건)

새 상징 셋(`oranges`·`orang-utang`·`orator`), 기존 셋에 붙임(`luxury`(Opulence 절반) ·
`wealth`(Opulence 절반, 판별어 표 비어 있었다 — 스물다섯 번째) · `orchard`(Orchard 아홉,
판별어 표 비어 있었다 — 스물여섯 번째)). `Opulence`는 「요정 같은 호사」/「참된 재물」로
표제어를 갈랐고, 마지막 총평 문장(「몽상 대신 기운을 내라」)은 안 넣었다.

기본값이 둘 바뀌었다 — `wealth`는 새 것이 더 막연해 바꾸고, `orchard`는 옛 것이 더
막연해 되돌렸다. `verify-dream-km`이 부분 문자열 겹침 넷을 잡았다(ripening⊃ripe,
fruition⊃fruit, eat⊂death, 떨어진⊃진). `audit-km-dead-words`가 다섯을 잡았다(「꽃 핀」이
공백에 쪼개짐 등). 프로브에서 **동점 문제를 두 판 만에 고쳤다** — 「돼지가 …열매를
먹었」의 「열매를」이 너무 흔해 「거두었」과 동점이 났다. 관문 전부 통과(프로브 150건).

**사전 1,516 / 5,079 / 5,103** (상징 / 의미 / 인용).

---

## 배치 146 — 밀러 `Orchestra`~`Ottoman` (8건)

새 상징 여섯(`orchestra`·`organ`·`organist`·`ornament`·`orphan`·`ostrich`·`otter`),
기존 하나에 붙임(`couch`(Ottoman), 판별어 표 비어 있었다 — 스물일곱 번째). 건너뛴
표제어 없음. `Organ`은 `mouth-organ`(하모니카)과 다른 악기라 안 겹친다.

기본값이 하나 바뀔 뻔했으나(couch) 옛 「긴 소파에 기대어 누움」이 더 막연해 되돌렸다.
`verify-dream-km`이 영어 부분 문자열 겹침 하나(sweetheart⊃hear)를 잡았다. 프로브가
「~임을」계사 경계(§29, 배치 141과 같은 결)를 하나 더 잡았다(오르간 연주자임을).
관문 전부 통과(프로브 169건).

**사전 1,523 / 5,097 / 5,121** (상징 / 의미 / 인용).

---

## 배치 147 — 밀러 `Ouija`~`Owl` (5건)

새 상징 넷(`ouija`·`oven`·`over-alls`·`owl`), 기존 하나에 붙임(`coat`(Overcoat) 둘).
문장 셋을 뺐다 — `Ouija`의 「흑인이 훔쳐 감」은 그림 자체가 비하다(§31, 다섯 번째).
`Oven`의 「빵을 굽는 중」은 이미 있는 `baking`(밀러 `Baking`)과 같은 그림인데 결말이
달라 못 가른다. `Overcoat`의 「멋진 새 외투」는 `coat`의 「새 외투를 봄」과 같은 그림이다.

기본값이 바뀌었다 — 옛 「남의 외투를 입음」(조건 붙음)보다 새 「외투 꿈을 꿈」(가장
막연함)이 더 넓어 바꿨다. `verify-dream-km`이 상징 이름 그대로 포함(over-alls) 하나를
잡았다.

**지킴 케이스가 `baking`(밀러 배치와 무관, 오래전부터 있던 상징)의 옛 상처를 찾았다**
— 별칭이 「빵을 굽·제빵·오븐에 굽·빵 만들」뿐이라 「빵 굽는」(관형형)이 한 번도 안
걸리고 있었다. `over-alls`·`owl`도 프로브에서 동점·문장 불충분 문제가 나 문장을
보강했다. 관문 전부 통과(프로브 187건).

**사전 1,527 / 5,110 / 5,134** (상징 / 의미 / 인용).

---

## 배치 148 — 밀러 `Ox`~`Packet` (5건) — **밀러 O 가 끝나고 P 가 시작된다. 5판 묶음(144~148)의 마지막 판**

새 상징 넷(`oysters`·`oyster-shells`·`pacify`·`packet`), 기존 하나에 붙임(`ox`(Ox) 넷).
문장 하나(둘로 이어진)를 뺐다 — `Ox`의 「푸른 목장의 살진/여윈 소」는 이미 있는
`cattle`(밀러 `Cattle`)의 같은 그림과 결말이 달라 못 가른다. 원문 자신이 「[144] See
Cattle」이라 적어 그 방향을 미리 가리켰다.

기본값이 바뀌었다 — 옛 「누런 소가 집으로 옴」(색·귀가라는 조건 붙음)보다 새 「잘 먹여
살진 소를 봄」(가장 막연함)이 더 넓어 바꿨다. `verify-dream-km`이 영어 부분 문자열
겹침 셋(leading 공유, in⊂going, in⊂disappointments)을 잡았다. 관문 전부 통과(프로브
202건).

**사전 1,531 / 5,123 / 5,147** (상징 / 의미 / 인용).

**5판 묶음(144~148)이 끝났다.** 다음은 전수 스윕·푸시·배포 확인이다.

### 스윕·푸시·배포 확인

전수 스윕 142줄 · 통과 117 / 검사 안 됨 7 / 빨간불 4 — 상시 항목과 동일, 새로 난
것 없음. 푸시 완료(`b5a7f1f..2a112ed`). 배포 확인 — `pacify`·`ointment`·`owl`·
`packet` 넷 다 404 → 200(약 135초).

**두 번째 5판 묶음(144~148) 완료.** 이어서 세 번째 묶음(149~153)을 진행한다.

## 배치 149 — 밀러 `Pail`~`Palisade` (5건) — 세 번째 5판 묶음(149~153)의 첫 판

새 상징 둘(`painting`·`palisade`), 기존 셋에 붙임(`bucket`(Pail) 둘·`agony`(Pain) 둘·
`palace`(Palace) 넷). 문장 둘을 뺐다 — `Palace`의 마지막 「어머니의 훈계를 새겨라」는
총평이고, `Pail`의 「빈 통은 흉작·굶주림의 조짐」은 이미 있는 `bucket`의 「물통이 비어
물이 없음」(주공해몽 「桶無水者主大凶」)과 같은 그림(빈 물통=흉조)이라 가를 수 없었다 —
처음엔 넣었다가 프로브에서 기본값(FALLBACK_FIRST로 얼려 둔 「물통에 물이 가득 담김」)
으로 새 버려서, 판별어를 궁리하는 대신 §31 곁가지의 "같은 그림" 판정으로 되돌렸다.

프로브에서 셋을 더 잡았다 — ① `bucket`의 새 문맥들이 내가 쓴 문구 "통을"이 그 상징의
등록된 별칭이 아니어서(term_ko는 "물통") 하나도 안 걸렸다. `km5.json`에 "통을"·"통이"를
별칭으로 더했다. ② `agony`(고통)의 새 문맥 둘이 "아파하다" 계열 자연스러운 표현을
써서 대상 상징의 이름·별칭 어디에도 안 걸렸다(기존 별칭이 "극심한 아픔"·"고뇌"·
"몸부림"·"괴로워함"뿐). "아파하는"·"아파했다"를 별칭으로 더했다. ③ `painting`의
"페인트칠한"에서 "한"이 조사가 아니라 막혔다 — "페인트칠한"을 완전한 별칭으로 더했다
(§29 곁가지 ①과 같은 자리, 셋 다 새 관문은 못 본다).

관문 전부 통과(프로브 219건, tsc·verify-dream-cite 전수 포함).

**사전 1,533 / 5,137 / 5,161** (상징 / 의미 / 인용).

## 배치 150 — 밀러 `Pall`~`Palm Tree` (5건)

새 상징 다섯(`pall`·`pallbearer`·`pallet`·`palmistry`·`palm-tree`) 전부. 기존 상징을
건드리지 않았다. `pallet`의 원문 표현("잠자리")은 곤충 상징(잠자리, 잠자리)과 이름이
겹쳐 "낡은 침상"으로 피했다.

`verify-dream-km`이 셋을 잡았다 — palmistry의 두 문맥이 한국어 "손금"과 영어
"her"⊂"others"·"read"⊂"reads"를 공유. 판별어에서 상징 자체 낱말("손금")을 빼고
영어도 겹치지 않게 다시 썼다. `audit-km-dead-words`가 그 자리에서 또 둘을 잡았다 —
새 discriminator에 쓴 "reading"·"avenue"가 그 상징 자신의 영어 별칭("palm reading"·
"avenue of palms")과 겹쳐 ownTerm으로 죽었다. 다른 낱말로 바꿨다.

프로브가 넷을 더 잡았다 — pallbearer의 두 문맥(「~꿈을 꿈」·「~을 봄」)이 판별어에
그 구분을 반영하는 낱말이 아예 없어 둘 다 기본값으로 샜다("꾸었다"·"보았다"를
판별어로 추가). palmistry의 "제 손금을 봄"도 같은 이유로 안 갈렸다 — "남의"와
자연스럽게 대비되는 "나의"로 문맥 이름 자체를 바꿔 판별어로 세웠다(§29 곁가지,
동점을 판별어 하나 더 얹어 이기게 하는 대신 문맥 이름을 명확히 갈랐다). palm-tree의
"시들어"가 활용형 "시들었다"와 안 맞아 "시들었"으로 고쳤다(§29 곁가지 ①과 같은 자리).

관문 전부 통과(프로브 232건, tsc·verify-dream-cite 전수 포함).

**사전 1,538 / 5,150 / 5,174** (상징 / 의미 / 인용).

## 배치 151 — 밀러 `Palsy`~`Panorama` (4건)

새 상징 셋(`palsy`·`pancake`·`panorama`), 기존 `glass`(유리, 밀러 `Glass`)에 둘을
더함(`Pane of Glass`). 문장 둘을 뺐다 — 「유리판을 깨면 실패가 두드러짐」은 이미
있는 `glass`의 「유리 그릇이나 창을 깨뜨림」과 같은 그림(유리를 깨뜨림=불리한
결말)이라 가를 수 없었고, `Panorama`의 「거처를 바꾸려는 마음을 억눌러야」는 총평.

기본값이 흔들렸다 — 새 「유리판을 다룸」과 옛 「유리 너머로 내다봄」은 둘 다 구체적인
행동(다루다/내다보다)이라 어느 쪽이 더 넓은지 가릴 수 없어, 이미 나가 있는 화면을
그대로 두는 쪽으로 얼렸다(§30, 배치 143과 같은 자리).

프로브가 셋을 잡았다 — 「유리판을 다뤘다」가 "유리판"이 "유리"+"판"(조사 아님)
합성어로 막혀 안 걸림(별칭에 "유리판" 통째로 추가), 판별어 "다루"가 활용형
"다뤘다"와 안 맞아 다시 안 걸림("다뤘"으로 고침), 「유리창 너머로 이야기했다」가
옛 문맥의 판별어 "너머로"와 겹쳐 동점으로 새것(옛 판별어를 "내다봤 내다보"로
좁혀 "너머로"를 뺐다).

관문 전부 통과(프로브 242건, tsc·verify-dream-cite 전수 포함).

**사전 1,541 / 5,158 / 5,182** (상징 / 의미 / 인용).

## 배치 152 — 밀러 `Panther`~`Paper or parchment` (3건) — 세 번째 5판 묶음(149~153)의 네 번째 판

새 상징 둘(`panther`(흑표범)·`pantomime`(무언극)), 기존 `paper`(종이, 주공해몽)에
셋을 더함. `panther`는 이미 있는 `leopard`(표범)와 다른 짐승으로 갈랐다(원문에서도
별개 표제어) — 한국어에서 흔히 뭉뚱그리는 두 낱말이라 term_ko를 "흑표범"으로 갈라
`leopard`의 "표범"과 안 겹치게 했다. 문장 둘을 뺐다 — 밀러 자신의 마지막 요약
「고양이처럼, 죽이지 않으면 흉하다」가 앞의 겁에 질림·죽임 두 문장을 되풀이한
것이었고, 「덜 겁내면 덜 흉하다」는 총평.

기본값이 바뀌었다 — 옛 「오색 종이를 봄」(주공해몽, 「오색」 조건 붙음)보다 새
「종이나 양피지를 다루거나 참조함」(밀러, 색·용도 조건 없음)이 더 넓어 바꿨다.

프로브에서 하나 잡았다 — `pantomime`의 "참여하"가 활용형 "참여했다"와 안 맞아
"참여했"으로 고쳤다(§29 곁가지 ①과 같은 자리).

관문 전부 통과(프로브 253건, tsc·verify-dream-cite 전수 포함).

**사전 1,543 / 5,167 / 5,191** (상징 / 의미 / 인용).

**세 번째 5판 묶음(149~153)의 네 판이 모였다.** 다음 판(153)까지 마치면 전수
스윕·푸시·배포 확인으로 묶음을 닫는다.

## 배치 153 — 밀러 `Parables`~`Parcel` (5건) — 세 번째 5판 묶음(149~153)의 마지막 판

새 상징 셋(`parable`(비유)·`paradise`(낙원)·`paralysis`(마비)), 기존 둘에 붙임
(`umbrella`(우산, 주공해몽)에 `Parasol` 둘, `packet`(소포, 밀러 `Packet`)에
`Parcel` 셋). 이름을 세 번 갈랐다 — `paradise`는 이미 있는 `heaven`(하늘나라,
별칭 「천국」)과 겹치지 않게 "낙원"으로, `paralysis`는 이미 있는 `palsy`(중풍,
배치 151)와 겹치지 않게 "마비"로 나눴다(한국어에서도 「중풍」은 뇌졸중형,
「마비」는 일반형이라 실제로 다른 낱말이다). `Parasol`은 원문 자신이 「[146]
See Umbrella」라 적어 방향을 미리 가리켰고, `Parcel`은 이미 있는 `packet`과
같은 소포를 가리키는 다른 낱말이라 상징을 나누지 않았다.

기본값이 흔들렸다 — 새 「기혼자가 양산 꿈을 꿈」(신분 조건)과 옛 「남과 우산을
나눔」(행동 조건, 주공해몽)은 축이 달라 못 갈라, 옛 것을 그대로 얼렸다(§30,
배치 143·151과 같은 자리).

`verify-dream-km`이 판별어 빠진 자리 둘(다의어가 된 상징의 첫 의미에 discriminator
가 없었다)과 en 부분 문자열 겹침 둘("pleasant"⊂"pleasantly"/"unpleasant")을
잡았다. `audit-km-dead-words`가 1글자 판별어("벗"·"잃")를 잡았다.

프로브가 셋을 잡았다 — "배달돼"가 활용형 "배달되었다"와 안 맞음, "소포를"이
모든 소포 문장에 다 있는 너무 흔한 판별어라 동점을 만듦(빼고 "나르"만 남김),
"나르"가 불규칙활용 "날랐다"와 안 맞음(§29 곁가지 ①과 같은 자리, 세 번 다).

관문 전부 통과(프로브 272건, tsc·verify-dream-cite 전수 포함).

**사전 1,546 / 5,182 / 5,206** (상징 / 의미 / 인용).

**세 번째 5판 묶음(149~153)이 끝났다.** 다음은 전수 스윕·푸시·배포 확인이다.

### 스윕·푸시·배포 확인

전수 스윕 142줄 · 통과 117 / 검사 안 됨 7 / 빨간불 4 — 상시 항목과 동일, 새로 난
것 없음. 푸시 완료(`7bbe594..010745c`). 배포 확인 — `pallet`·`paradise` 둘 다
404 → 200(약 90초, 6번째 시도).

**세 번째 5판 묶음(149~153) 완료.** 이어서 네 번째 묶음(154~158)을 진행한다.

## 배치 154 — 밀러 `Pardon`~`Parents` (2건) — 네 번째 5판 묶음(154~158)의 첫 판

새 상징 하나(`parents`(부모)), 기존 `pardon`(사면, 주공해몽)에 둘을 더함. 문장
둘을 뺐다 — 「사면을 받으면 번영함」은 이미 있는 `pardon`의 「큰 사면을 받음」과
같은 그림(사면을 받음)인데 주공해몽은 흉, 밀러는 길로 정반대라 옛 판정을 그대로
두고 건너뛰었다(§31, 노새·오디와 같은 자리). 「[147] See kindred words」는 구체적
대상이 없는 cross-ref.

`parents`는 원문이 「[148] See Father and Mother」로 이미 있는 `father`·`mother`를
가리키지만, 일곱 문장이 부모 한쪽이 아니라 둘을 뭉뚱그린 그림이라 새로 세웠다
(Palace가 「[145] See Opulence」를 가리키면서도 독립 상징으로 남은 것과 같은
자리, 배치 149).

기본값이 흔들렸다 — 새 「저지르지 않은 잘못을 용서받으려 함」(구체적 서사가
붙음)보다 옛 「큰 사면을 받음」(주공해몽, 단순히 받음)이 더 막연해 그대로
얼렸다.

`verify-dream-km`이 셋을 잡았다 — 판별어가 상징 이름(사면) 그대로였던 것,
en "committed" 공유, 다의어가 된 상징의 첫 의미에 판별어가 없던 것.
`audit-km-dead-words`가 1글자("옷")와 별칭과 겹쳐 죽은 en("amnesty")을 잡았다.

프로브가 둘을 잡았다 — "용서받으려"가 "받"이 조사 아니라 막혀 별칭에 통째로
추가(§29 곁가지 ①과 같은 자리), "처녀가 부모가 집에서 행복한 꿈을 꿈"이 같은
장면을 공유하는 옛 문맥과 동점 — 그 문맥의 판별어를 "살아있는"(양쪽 다 옛
문장에 있지만 새 문장엔 없는 말)으로 좁혀 갈랐다.

관문 전부 통과(프로브 284건, tsc·verify-dream-cite 전수 포함).

**사전 1,547 / 5,191 / 5,215** (상징 / 의미 / 인용).

## 배치 155 — 밀러 `Park`~`Parsnips` (4건)

새 상징 셋(`park`(공원)·`parsley`(파슬리)·`parsnip`(파스닙)), 기존 `parrot`
(앵무새, 주공해몽)에 다섯을 더함. `Parsnips`는 한 문장에 사업(길)과 애정(흉)이
함께 있어 하나로 남겼다(polarity neutral).

기본값이 흔들렸다 — 새 「앵무새들이 재잘거림」(재잘거리는 행동 조건)보다 옛
「앵무새를 봄」(주공해몽, 조건 없음)이 더 막연해 그대로 얼렸다.

`verify-dream-km`이 en 부분 문자열 겹침 둘("healthful"⊂"health", "gossip" 공유)을,
`audit-km-dead-words`가 1글자("잘")를 잡았다. 프로브가 "가르치"가 불규칙활용
"가르쳤다"와 안 맞는 것을 잡았다(§29 곁가지 ①과 같은 자리).

관문 전부 통과(프로브 296건, tsc·verify-dream-cite 전수 포함).

**사전 1,550 / 5,202 / 5,226** (상징 / 의미 / 인용).

## 배치 156 — 밀러 `Parting`~`Partridge` (4건)

새 상징 넷(`parting`(작별)·`partner`(동업자)·`partnership`(동업)·`partridge`
(자고새)). 이어진 두 문장(`Partnership`의 「해소함」)이 인용 원문에서 세미콜론
하나로 이어진 한 문장이라, 둘 다 cite.original을 전체 문장으로 채웠다(배치
152의 panther와 같은 자리).

`verify-dream-km`이 en 부분 문자열 겹침 둘("man"⊂"woman", "unpleasant"⊂
"pleasant")을, `audit-km-dead-words`가 1글자 둘("맺"·"덫")을 잡았다.

프로브가 넷을 잡았다 — `parting`이 "작별"의 활용형("작별했다")과 동의어
("헤어짐"/"헤어졌다")를 별칭에 안 갖고 있어 둘 다 안 걸림, `partnership`도
"동업했다"가 안 걸림(전부 §29 곁가지 ①과 같은 자리), "즐거웠던 동업을
해소했다"가 "해소"를 공유하는 옛 형제와 동점 — "해소" 의존을 빼고 갈랐다.

관문 전부 통과(프로브 309건, tsc·verify-dream-cite 전수 포함).

**사전 1,554 / 5,215 / 5,239** (상징 / 의미 / 인용).

## 배치 157 — 밀러 `Party`~`Password` (4건)

새 상징 셋(`gang`(무리)·`passenger`(승객)·`password`(암호)), 기존 둘에 붙임
(`entertainment`(연회, 별칭 「파티」)·`bell`(종, 밀러 `Bells`)). `Party` 한
표제어에 「무리(사람들)」와 「모임」 두 뜻이 섞여 있어 갈랐다 — 모임 쪽은 이미
`entertainment`가 「파티」를 별칭으로 쥐고 있어 그쪽에 붙였다. 문장 하나를 뺐다
— `Passing Bell`의 「조종 소리를 들음」은 이미 있는 `bell`의 「종이 조종처럼
울리는 소리를 들음」(밀러 `Bells`)과 같은 그림(먼 벗의 죽음을 알리는 조종)이다.

`password`가 이미 있는 `cipher`(암호문)와 화면 이름이 겹쳐 `LABEL_KO`에 「암호
(문서)」·「암호(비밀번호)」로 갈랐다(§10 #「눈」·「발」과 같은 자리).

기본값이 둘 흔들렸다 — `entertainment`는 새 것(조건 없음)이 더 넓어 바꿨고,
`bell`은 새 「직접 조종을 울림」과 옛 「조종 소리를 들음」이 둘 다 구체적
행동(울리다/듣다)이라 못 갈라 그대로 얼렸다.

`verify-dream-km`이 셋(ko "떠나는" 공유, en "leaving" 공유, en "pleasures"⊂
"pleasure")을 잡았다. 프로브가 여섯을 잡았다 — `gang`의 이어지는 문장이
"무리"를 다시 안 담아 안 걸림(문장에 "무리에게서"를 보태 고침), `passenger`가
"승객들을"(목적격) 꼴 별칭이 없어 안 걸림, "암호를 넘겨줌"·"직접 조종을 울림"이
활용형 불일치로 안 걸림, `bell`의 두 「조종」의미가 "조종"을 공유해 동점(각각
"조종처럼"/"조종을"로 나눠 갈랐다).

관문 전부 통과(프로브 321건, tsc·verify-dream-cite 전수 포함).

**사전 1,557 / 5,224 / 5,248** (상징 / 의미 / 인용).

**네 번째 5판 묶음(154~158)의 네 판이 모였다.** 다음 판(158)까지 마치면 전수
스윕·푸시·배포 확인으로 묶음을 닫는다.

## 배치 158 — 밀러 `Pasteboard`~`Pastry` (2건) — 네 번째 5판 묶음(154~158)의 마지막 판

새 상징 둘(`pasteboard`(판지)·`pastry`(패스트리)). 「[149] See Pies」는 아직
`pie` 상징이 사전에 없는 cross-reference라 뺐다.

`verify-dream-km`이 en 부분 문자열 겹침 하나("deceive"⊂"deceived")를 잡았다.
프로브는 처음부터 전부 걸렸다(326건).

**사전 1,559 / 5,229 / 5,253** (상징 / 의미 / 인용).

**네 번째 5판 묶음(154~158)이 끝났다.** 다음은 전수 스윕·푸시·배포 확인이다.

### 스윕·푸시·배포 확인

전수 스윕 142줄 · 통과 117 / 검사 안 됨 7 / 빨간불 4 — 상시 항목과 동일, 새로 난
것 없음. 푸시 완료(`010745c..54db52a`). 배포 확인 — `pasteboard`·`parents` 둘 다
404 → 200(약 135초, 9번째 시도).

**네 번째 5판 묶음(154~158) 완료.** 이어서 다섯 번째 묶음(159~163)을 진행한다.

## 배치 159 — 밀러 `Patch` (1건, 여섯 문장) — 다섯 번째 5판 묶음(159~163)의 첫 판

새 상징 하나(`patch`). 이름을 처음부터 갈랐다 — 원문 그대로 옮기면 "헝겊 조각"
인데, 이미 있는 `cloth`(천, 별칭 「헝겊」)가 그 낱말을 쥐고 있어 「헝겊 조각」·
「헝겊을 깁고」 어느 쪽으로 적어도 뒤가 공백이라 `cloth`가 함께 걸린다(§25
곁가지, 「지하 감옥」과 같은 자리). term_ko를 "기운 자국"으로, "헝겊을 깁고
있음" 문맥도 "옷을 깁고 있음"으로 바꿔 "헝겊"을 아예 안 썼다.

`audit-km-dead-words`가 1글자("댔")와 공백으로 쪼개진 두 글자("새"+"옷")를
잡았다. 프로브는 처음부터 전부 걸렸다(333건, `cloth` 지킴 케이스 포함).

**사전 1,560 / 5,235 / 5,259** (상징 / 의미 / 인용).

## 배치 160 — 밀러 `Patent`~`Path` (3건)

새 상징 둘(`patent`(특허)·`patent-medicine`(특효약)), 기존 `road`(길, 주공해몽)
에 셋을 더함. 「오솔길」이 이미 그 상징의 별칭이다. `patent-medicine`은 이미
있는 `medicine`(먹는 약)과는 다른 물건(상표 의약품)이라 새로 세웠다.

기본값이 흔들렸다 — 새 「좁고 험한 길에서 돌부리에 걸려 넘어짐」(조건 겹겹이
붙음)보다 옛 「사방으로 길이 뚫려 있음」(주공해몽, 조건 없음)이 더 막연해
그대로 얼렸다.

`audit-km-dead-words`가 셋을 잡았다 — en "path"가 road의 기존 별칭과 겹쳐
죽음, 1글자("샀"), 공백으로 쪼개진 것("무리한 수"). 프로브가 셋을 잡았다 —
"실패하"가 활용형 "실패했다"와 안 맞음(§29 곁가지 ①과 같은 자리), "특허를
봄"이 판별어에 낱말이 없어 기본값으로 샘(동사 "보았"을 판별어로 추가).

관문 전부 통과(프로브 343건, tsc·verify-dream-cite 전수 포함).

**사전 1,562 / 5,244 / 5,268** (상징 / 의미 / 인용).

## 배치 161 — 밀러 `Paunch`~`Pawn-shop` (3건)

새 상징 셋(`paunch`(똥배)·`pauper`(극빈자)·`pawn-shop`(전당포)). `pauper`는
이미 있는 `beggar`(거지)와는 다른 낱말(재산 없음 대 구걸함)이라 갈랐다.
「[150] See Beggars and kindred words」는 구체적 문장이 없는 cross-ref.

프로브가 다섯을 잡았다 — `pauper`의 "극빈자임을" 활용형이 별칭에 없어 안
걸림, "극빈자들을 봄"·"전당포를 봄"이 판별어에 "보다" 계열 낱말이 없어 기본값
으로 샘(둘 다 "보았"을 판별어로 추가), `pawn-shop`의 "전당 잡히다" 표현
자체가 별칭에 없어 두 문맥이 안 걸림(별칭에 통째로 추가), "잡히"가 활용형
"잡혔다"와 안 맞음(§29 곁가지 ①과 같은 자리).

관문 전부 통과(프로브 353건, tsc·verify-dream-cite 전수 포함).

**사전 1,565 / 5,253 / 5,277** (상징 / 의미 / 인용).

## 배치 162 — 밀러 `Peaches`~`Peacock` (2건, 여덟 문장)

기존 `peach`(복숭아)에 다섯, `peacock`(공작)에 셋(둘 다 주공해몽). 이어진 두
문장(`Peaches`의 첫 둘)이 원문에서 세미콜론 하나로 이어진 한 문장이라 둘 다
cite.original을 전체 문장으로 채웠다(배치 152·156과 같은 자리). 「[151] See
Orchard」는 이미 있는 `orchard`가 다른 내용을 갖고 있어 옮길 문장이 없었다.

기본값이 둘 바뀌었다 — 옛 「감과 복숭아를 먹음」(감이 함께 있어야 하는 좁은
그림)보다 새 「복숭아를 보거나 먹음」(조건 없음)이 더 넓어 바꿨고, 「공작 꿈을
꿈」도 「공작을 봄」보다 "보다"라는 행동 조건조차 없어 더 넓어 바꿨다.

프로브가 하나를 잡았다 — 옛 「감과 복숭아를 먹음」의 판별어에 "복숭아를"이
섞여 있어 모든 복숭아 문장과 동점을 만들었다("감과 함께"로 좁혔다).

관문 전부 통과(프로브 363건, tsc·verify-dream-cite 전수 포함).

**사전 1,565 / 5,261 / 5,285** (상징 / 의미 / 인용).

## 배치 163 — 밀러 `Pearls`~`Pears` (2건) — 다섯 번째 5판 묶음(159~163)의 마지막 판

새 상징 하나(`pearl`(진주)), 기존 `pear`(배, 주공해몽)에 넷을 더함. 문장 하나를
뺐다 — `Pears`의 「배를 먹으면」은 이미 있는 `pear`의 「배를 먹음」(주공해몽
「식이자주실재백」, 재물을 잃음)과 같은 그림(배를 먹음=흉)이라 가를 수 없었다.

기본값이 흔들렸다 — 새 「우아한 나무에 달린 황금빛 열매를 감상함」(나무·황금빛
수식이 겹겹이 붙음)보다 옛 「배를 먹음」(주공해몽, 조건 없음)이 더 막연해
그대로 얼렸다.

`audit-km-dead-words`가 공백으로 쪼개진 것("홀린 듯"의 "듯")을 잡았다. 프로브가
하나를 잡았다 — "우아한 나무에 달린 황금빛 열매를 감상했다"에 "배"가 아예
없어 안 걸림(문장에 "배의"를 보태 고쳤다).

관문 전부 통과(프로브 372건, tsc·verify-dream-cite 전수 포함).

**사전 1,566 / 5,269 / 5,293** (상징 / 의미 / 인용).

**다섯 번째 5판 묶음(159~163)이 끝났다.** 다음은 전수 스윕·푸시·배포 확인이다.

### 스윕·푸시·배포 확인

전수 스윕 142줄 · 통과 117 / 검사 안 됨 7 / 빨간불 4 — 상시 항목과 동일, 새로 난
것 없음. 푸시 완료(`a11f397..c194c82`). 배포 확인 — `pearl`·`patch` 둘 다 404 →
200(약 210초, 13번째 시도).

**다섯 번째 5판 묶음(159~163) 완료.** 이어서 여섯 번째 묶음(164~168)을 진행한다.

## 배치 164 — 밀러 `Peas` (1건, 일곱 문장) — 여섯 번째 5판 묶음(164~168)의 첫 판

새 상징 하나(`pea`(완두콩)).

`verify-dream-km`이 셋을 잡았다 — ko "마른"이 「마른 완두콩을 봄」·「마른
완두콩을 먹음」 두 곳에 있어 동점 위험, en "health"가 「완두콩을 먹음」·
「마른 완두콩을 봄」에 공유. 「보다」계열 두 곳(자라는~봄/마른~봄)의 판별어를
서로 다른 낱말로 갈랐다 — 마른~봄은 "보았"(2글자 행동사) 단독으로, 마른~먹음은
"마른" 단독으로 쥐게 해 겹치지 않게 했다.

`audit-km-dead-words`가 1글자 둘("잘"·"뒤")을 잡았다.

프로브가 하나 더 잡았다 — 「마른 완두콩을 먹음」의 판별어에 "먹었"을 남겨
두었더니 **일반** 「완두콩을 먹었다」까지 가로챘다(§30 곁가지, 「소포를」과
같은 자리 — 너무 흔한 동사 하나를 특정 문맥이 독점하면 안 된다). "먹었"을 빼고
"마른" 하나로만 좁혔다.

관문 전부 통과(프로브 379건, tsc·verify-dream-cite 전수 포함).

**사전 1,567 / 5,276 / 5,300** (상징 / 의미 / 인용).

## 배치 165 — 밀러 `Pebbles`~`Pelican` (3건)

새 상징 셋(`pebble`(조약돌)·`pecan`(피칸)·`pelican`(펠리컨)). `pecan`은 이미
있는 `nuts`(견과, 밀러 `Nuts`)와는 다른 특정 견과라 새로 세웠다.

`audit-km-dead-words`가 공백으로 쪼개진 것("잎 사이")을 잡았다. 프로브가
셋을 잡았다 — `pebble`의 두 문맥이 순서상 특정형(「처녀가 조약돌 깔린 길」)이
먼저라 일반형(「조약돌 꿈을 꿈」)이 동점에서 졌다(순서를 바꿔 일반형을 앞으로
옮겼다 — 이번 배치 안에서 처음 넣는 상징이라 §30의 "이미 나가 있는 화면"이
아니라 "옳은 순서를 처음부터 정한다"에 해당한다), `pelican`의 "죽이"가 활용형
"죽였다"와 안 맞음(§29 곁가지 ①과 같은 자리).

관문 전부 통과(프로브 389건, tsc·verify-dream-cite 전수 포함).

**사전 1,570 / 5,286 / 5,310** (상징 / 의미 / 인용).

## 배치 166 — 밀러 `Pen`~`Penitentiary` (4건)

새 상징 셋(`pen`(펜)·`penalty`(벌금)·`pencil`(연필)), 기존 `prison`(감옥, 밀러
`Gaol` 포함)에 하나를 더함. 문장 둘을 뺐다 — 「탈출함」은 이미 있는 `prison`의
「감옥에서 빠져나옴」(밀러 `Gaol`, 길함)과 같은 그림이라 못 갈랐고, 「갇힌
사람이 됨」도 처음엔 넣었다가 프로브 전에 이미 있는 `prison`의 「옥에 갇혀
앉아 있음」(주공해몽, 사면 받을 길조)과 같은 그림(갇혀 있음)인데 밀러는
흉(집안 불만·사업 기울)으로 정반대임을 알아채 뺐다(§31, 노새·오디·사면과
같은 자리 — 이번엔 프로브가 아니라 판별어를 짜는 중에 먼저 보였다).

기본값이 바뀌었다 — 옛 「감옥이 무너짐」(주공해몽, 무너진다는 조건 붙음)보다
새 「감옥 꿈을 꿈」(조건 없음)이 더 넓어 바꿨다.

`verify-dream-km`이 셋을 잡았다(같은 상징 안 "갇힌" 공유, "수감"⊂"수감자가").
`audit-km-dead-words`가 1글자 둘("냈"·"일")을 잡았다. 프로브가 넷을 잡았다 —
`pencil`의 이어지는 문장이 "연필"을 다시 안 담아 안 걸림(문장을 보강), 두
활용형 불일치(§29 곁가지 ①과 같은 자리), 동점 하나(글을 쓰는 쪽 판별어를
"처녀가"에서 "썼다"로 좁혀 갈랐다).

관문 전부 통과(프로브 400건, tsc·verify-dream-cite 전수 포함).

**사전 1,573 / 5,295 / 5,319** (상징 / 의미 / 인용).

## 배치 167 — 밀러 `Penny`~`Pepper` (3건 + cross-ref 하나)

새 상징 둘(`pension`(연금)·`pepper`(후추)), 기존 `coins`(동전, 밀러 다른
표제어)에 넷을 더함. 「동전」이 이미 그 상징의 별칭이라 처음엔 새 상징
`penny`로 지었다가 조립기의 "화면 이름 겹침" 관문이 잡아 `coins`로 옮겼다.
`People`은 「[152] See Crowd」뿐인 cross-ref라 옮길 문장이 없어 건너뛰었다.

기본값이 바뀌었다 — 옛 「금돈을 봄」(밀러, 금이라는 재질 조건 붙음)보다 새
「동전 꿈을 꿈」(조건 없음)이 더 넓어 바꿨다.

`audit-km-dead-words`가 1글자("셌")를 잡았다. 프로브가 하나를 잡았다 —
`pepper`의 두 문맥이 밀러 원문의 "red pepper"를 한국어로 "고추"(붉은
고추/고추 식물)로 옮겼는데, 상징 이름·별칭은 "후추"(향신료)뿐이라 그 두
문장이 하나도 안 걸렸다. 별칭에 "고추"를 더했다.

관문 전부 통과(프로브 413건, tsc·verify-dream-cite 전수 포함).

**사전 1,575 / 5,307 / 5,331** (상징 / 의미 / 인용).

## 배치 168 — 밀러 `Peppermint` (1건, 네 문장) — 여섯 번째 5판 묶음(164~168)의 마지막 판

새 상징 하나(`peppermint`(박하)). `Perspiration`은 통째로 건너뛰었다 — 이미
있는 `sweat`(땀, 주공해몽 「몸에 땀이 흐름」, 흉)와 같은 그림(땀이 남)인데
밀러는 길(새 영예)로 정반대라 가를 수 없었다(§31, 이번 묶음에서만 세 번째
같은 자리 — pardon·prison에 이어).

프로브가 하나를 잡았다 — 「처녀가…즐기는 꿈을 꿈」이 같은 장면을 공유하는
형제와 동점(둘 다 "음료"를 갖고 있었다) — "처녀가"·"즐기는"을 판별어에
더해 갈랐다.

관문 전부 통과(프로브 418건, tsc·verify-dream-cite 전수 포함).

**사전 1,576 / 5,311 / 5,335** (상징 / 의미 / 인용).

**여섯 번째 5판 묶음(164~168)이 끝났다.** 다음은 전수 스윕·푸시·배포 확인이다.

### 스윕·푸시·배포 확인

전수 스윕 142줄 · 통과 117 / 검사 안 됨 7 / 빨간불 4 — 상시 항목과 동일, 새로 난
것 없음. 푸시 완료(`2474b57..e5e83c6`). 배포 확인 — `peppermint`·`pecan` 둘 다
404 → 200(약 105초, 7번째 시도).

**여섯 번째 5판 묶음(164~168) 완료.** 이어서 일곱 번째 묶음(169~173)을 진행한다.

## 배치 169 — 밀러 `Perfume`~`Pest` (2건, 열 문장) — 일곱 번째 5판 묶음(169~173)의 첫 판

새 상징 둘(`perfume`(향수)·`pest`(골칫거리)).

`audit-km-dead-words`가 공백으로 쪼개진 것("들이마심"과 관계된 "일")을
잡았다. 프로브가 넷을 잡았다 — "증류하"가 활용형 "증류했다"와 안 맞음(§29
곁가지 ①과 같은 자리), "목욕물에 향을 더하는" 문맥이 "향수"만 별칭에 있어
"향"이 안 걸림(별칭에 "향을" 추가), `pest`의 두 문맥이 밀러 원문 자체가
막연한 "a pest of any nature"라 판별어를 걸 낱말이 문장에 없어 안 걸림(문장에
"골칫거리"를 명시하도록 고쳤다).

관문 전부 통과(프로브 428건, tsc·verify-dream-cite 전수 포함).

**사전 1,578 / 5,321 / 5,345** (상징 / 의미 / 인용).

## 배치 170 — 밀러 `Petticoat`~`Phosphorus` (5건)

새 상징 다섯(`petticoat`(속치마)·`pewter`(백랍)·`phantom`(환영)·`pheasant`
(꿩)·`phosphorus`(인)). `Phantom`은 이미 있는 `ghost`(귀신)와는 다른 낱말
(넋 대 홀린 형상)이고, 쫓아오거나 달아나는 행동은 `ghost`에 없는 그림이라
새로 세웠다. 「[153] See Dishes」·「[154] See Ghost」는 참고 표시일 뿐이라
문장으로 옮길 것이 없어 건너뛰었다.

`verify-dream-km`이 en 부분 문자열 겹침 하나("raillery"⊃"ill")를,
`audit-km-dead-words`가 1글자("새")를 잡았다. 프로브가 하나를 잡았다 —
`phantom`의 "달아나"가 활용형 "달아났다"와 안 맞음(§29 곁가지 ①과 같은 자리).

관문 전부 통과(프로브 441건, tsc·verify-dream-cite 전수 포함).

**사전 1,583 / 5,334 / 5,358** (상징 / 의미 / 인용).

## 배치 171 — 밀러 `Photography`~`Physician` (2건)

새 상징 하나(`photograph`(사진)), 기존 `doctor`(의사, 밀러 다른 표제어)에
셋을 더함. physician과 doctor는 같은 낱말이라 상징을 나누지 않았다.

기본값이 흔들렸다 — 새 「처녀가 의사 꿈을 꿈」(처녀라는 조건)과 옛 「의사를
사사로이 만남」(밀러, 사사로이 만난다는 행동 조건)은 축이 달라 못 갈라 그대로
얼렸다.

`audit-km-dead-words`가 1글자("곧")를 잡았다. 프로브가 셋을 잡았다 —
`photograph`가 "사진들을"(복수+목적격) 꼴 별칭이 없어 안 걸림, 형제인
「처녀가 의사 꿈을 꿈」·「아픈 처녀가 의사 꿈을 꿈」이 둘 다 "처녀가"를 공유해
동점 — 판별어로는 못 가르니(더 좁은 문장이 넓은 문장을 완전히 품는 자리라
낱말로 나눌 수 없다) §29 곁가지의 "동점은 차례로 푼다"를 따라 더 구체적인
「아픈…」을 배열에서 앞으로 옮겨 갈랐다.

관문 전부 통과(프로브 450건, tsc·verify-dream-cite 전수 포함).

**사전 1,584 / 5,341 / 5,365** (상징 / 의미 / 인용).

## 배치 172 — 밀러 `Piano` (1건, 일곱 문장)

새 상징 하나(`piano`(피아노)). 이미 있는 `musical-instrument`(악기)와는
특정 악기라 다르다.

`audit-km-dead-words`가 1글자("일")와 공백으로 안 걸리는 것("안맞")을
잡았다. 프로브가 셋을 잡았다 — 이어지는 세 문장(귀에 거슬리는 음악/슬프고
애처로운 음악/처녀가 곡을 연주함)이 "피아노"를 다시 안 담아 안 걸림(문장에
"피아노"를 보태 고쳤다).

관문 전부 통과(프로브 457건, tsc·verify-dream-cite 전수 포함).

**사전 1,585 / 5,348 / 5,372** (상징 / 의미 / 인용).

## 배치 173 — 밀러 `Pickaxe`~`Pickles` (2건) — 일곱 번째 5판 묶음(169~173)의 마지막 판

새 상징 둘(`pickaxe`(곡괭이)·`pickle`(피클)). 원문 자신이 「To dream of
pickles, denotes...」 문장을 그대로 두 번 적었다(1901년판 원문 그대로,
오탈자로 보이지 않는다). 둘째 것(「사랑의 번민이나 결국 이김」)은 첫째와
영어 트리거 문구가 완전히 같은 채 결말만 달라 — 같은 work·같은 문맥 이름은
조립기가 하나로 합치고, 판별어로도 못 가르는 자리라 뺐다.

관문 전부 통과(프로브 463건, tsc·verify-dream-cite 전수 포함, 처음부터
전부 걸림).

**사전 1,587 / 5,354 / 5,378** (상징 / 의미 / 인용).

**일곱 번째 5판 묶음(169~173)이 끝났다.** 다음은 전수 스윕·푸시·배포 확인이다.

### 스윕·푸시·배포 확인

전수 스윕 142줄 · 통과 117 / 검사 안 됨 7 / 빨간불 4 — 상시 항목과 동일, 새로 난
것 없음. 푸시 완료(`c0abe21..d892d35`). 배포 확인 — `pickle`·`piano` 둘 다 404 →
200(약 320초, 15번째 시도).

**일곱 번째 5판 묶음(169~173) 완료.** 이어서 여덟 번째 묶음(174~178)을 진행한다.

## 배치 174 — 밀러 `Pickpocket`~`Picnic` (2건) — 여덟 번째 5판 묶음(174~178)의 첫 판

새 상징 둘(`pickpocket`(소매치기)·`picnic`(소풍)). 「[155] See Kindred
Words」는 구체적 대상이 없는 cross-ref라 뺐다.

새 상징 두 곳에서 순서를 바꿨다 — `pickpocket`의 「남의 주머니를 훔침」은
"소매치기" 낱말이 아예 없어 안 걸려 "처녀가 소매치기 짓을 함"으로 고쳐 쓰고,
형제 「당함」과 동점이라 더 구체적인 쪽을 앞으로 옮겼다(§29 곁가지). `picnic`도
「소풍에 참석함」(참석 조건 붙음)보다 「소풍 꿈을 꿈」(원문 "Dreams of
picnics"의 일반 진술)이 더 막연해 앞으로 옮겼다(배치 165 pebble과 같은 자리).

관문 전부 통과(프로브 469건, tsc·verify-dream-cite 전수 포함).

**사전 1,589 / 5,360 / 5,384** (상징 / 의미 / 인용).

## 배치 175 — 밀러 `Pictures`~`Pier` (2건)

기존 `painting`(그림, 밀러 `Paint and Painting`)에 여섯, `docks`(부두, 밀러
`Docks`)에 둘을 더함. 「[156] See Painting and Photographs」가 정확히
`painting` 을 가리켰다. pier와 docks는 한국어로 같은 낱말이라 상징을 나누지
않았다.

기본값이 흔들렸다 — 새 「부두 위에 섬」("서다"라는 자세 조건 붙음)보다 옛
「부두에 있음」(밀러 `Docks`, 있기만 하면 되는 더 막연한 자리)이 더 넓어
그대로 얼렸다.

`verify-dream-km`이 「서」·「서성」공유 하나를 잡았다. `audit-km-dead-words`가
1글자 둘("서"·"샀")을 잡았다. 프로브가 넷을 잡았다 — 활용형 불일치 하나,
이어지는 두 문장(살아있는 나무의 모습·거장의 걸작들)이 "그림"을 안 담아
안 걸림(전용 구절 별칭 추가), 그중 하나는 별칭 뒤에 조사 아닌 "들"이 붙어
막힘(§29 곁가지 ①과 같은 자리 — 별칭을 「거장의 걸작들」로 완전한 꼴로 고침).

관문 전부 통과(프로브 479건, tsc·verify-dream-cite 전수 포함).

**사전 1,589 / 5,368 / 5,392** (상징 / 의미 / 인용).

## 배치 176 — 밀러 `Pies`~`Pig` (2건)

새 상징 하나(`pie`(파이)), 기존 `pig`(돼지, 밀러 `Hogs`)에 둘을 더함. 문장
하나를 뺐다 — 「살지고 건강한 돼지」는 이미 있는 `pig`의 「살지고 튼튼한
돼지를 봄」(밀러 `Hogs`, 길함)과 같은 그림이라 가를 수 없었다. `pie`는
이미 있는 `pastry`(패스트리)와는 다른 음식이고 결말도 달라 새로 세웠다.

`audit-km-dead-words`가 1글자("잘")를 잡았다. 프로브가 셋을 잡았다 —
`pig`가 "돼지들을"(복수+목적격) 꼴 별칭이 없어 안 걸림, 형제 「진흙탕에서
뒹구는 돼지들을 봄」·「처녀가 진흙탕 돼지 꿈을 꿈」이 "진흙탕"을 공유해
동점 — 더 구체적인 「처녀가…」를 배열에서 앞으로 옮겨 갈랐다(§29 곁가지,
배치 171·174와 같은 자리).

관문 전부 통과(프로브 485건, tsc·verify-dream-cite 전수 포함).

**사전 1,590 / 5,372 / 5,396** (상징 / 의미 / 인용).

## 배치 177 — 밀러 `Pigeon`~`Pill` (3건)

새 상징 둘(`pilgrim`(순례자)·`pill`(알약)), 기존 `dove`(비둘기, 밀러 `Doves`)에
둘을 더함. 앞 두 문장(구구대는 소리를 들음·처녀의 이른 혼인)은 뺐다 — 이미
있는 `dove`의 「비둘기가 짝을 짓고 둥지를 지음」(밀러 `Doves`)과 같은
그림(가정의 평온·자녀)이라 가를 수 없었다.

프로브가 지킴 케이스에서 몇 주 묵은 상처 하나를 찾았다 — `dove`의 「흰
비둘기를 봄」이 판별어로 "하얀" 하나만 쥐고 있어 "흰 비둘기를 보았다"(더
흔한 표현)는 한 번도 안 걸리고 있었다. 판별어에 "비둘기"(상징 이름 그대로)나
"흰"(1글자)을 더할 수 없어(관문이 둘 다 막는다) 프로브 문장을 기존 판별어에
맞춰 "하얀"으로 고쳤다 — 여전히 "흰"으로 쓰는 문장은 못 걸리는 채로 남아
있다는 것을 여기에 적어 둔다.

프로브가 또 둘을 잡았다 — `pilgrim`의 "순례자임을" 활용형이 별칭에 없어
안 걸림, "떠나"가 활용형 "떠났다"와 안 맞음(§29 곁가지 ①과 같은 자리).

관문 전부 통과(프로브 495건, tsc·verify-dream-cite 전수 포함).

**사전 1,592 / 5,380 / 5,404** (상징 / 의미 / 인용).

## 배치 178 — 밀러 `Pillow`~`Pimple` (5건)

새 상징 하나(`pimple`(여드름), 셋), 기존 `pillow`(베개, 주공해몽)에 둘을 더함.

`pillow`는 옛 의미가 「좋은 베개를 봄」(조건부, 길) 하나뿐이었는데 새로 들어온
「베개 꿈을 꿈」(무조건, 길)이 더 일반적이라 `FALLBACK_FIRST`로 기본값을
옮겼다. 그러자 옛 의미가 판별어 없이 남아(단일 의미였을 때는 필요 없었다)
관문이 막아, `km2.json`의 「좋은 베개를 봄」에 판별어 "좋은"/"benefactor
support"를 새로 채웠다(§30 곁가지 — 판별어 없던 상징이 여럿이 되면 전부
있어야 한다).

`pimple`의 세 번째 의미("여성이 여드름으로 미모가 상함")에서 1글자 죽은
낱말("상")이 하나 잡혀 "여성이 미모가 비판받"으로 고쳤다.

프로브에 지킴 케이스로 `pillow`의 옛 답("좋은 베개를 봄")을 넣어 기본값
이동 뒤에도 제 뜻으로 걸리는지 재확인했다 — 통과.

관문 전부 통과(프로브 501건, 교차 별칭 0쌍, tsc·verify-dream-cite 전수 포함).

**사전 1,593 / 5,385 / 5,409** (상징 / 의미 / 인용).


---

## 인계 — 배치 174~178 묶음 스윕·푸시·배포 확인 완료

배치 174(Pickpocket~Picnic)~178(Pillow~Pimple) 다섯 판을 마쳤다.

- 전수 스윕: 검사기 110개 · 실행 121회 · 통과 117 / 검사 안 됨 7 / **빨간불 4건**
  — 전부 상시 알려진 것(`mailOrderNumber` 로마자화 미판정 2건, `verify-locale-manifest`
  origin 판정 1건, `audit-dream-ownterms-multiword` 1건). 이번 다섯 판이 새로 낸
  빨간불은 없다.
- `git push origin main` — `babc711..eef467a`.
- 배포 확인: `https://dreams-link.com/ko/dream/symbol/pimple`(이번 묶음에만 있는
  주소) 푸시 전 404 → 푸시 후 폴링 12회(약 3분)만에 200.

**사전 1,593 / 5,385 / 5,409** (상징 / 의미 / 인용). 다음은 밀러 `Pin`부터.

## 배치 179 — 밀러 `Pincers`~`Pins` (12건)

새 상징 셋(`pincers`(집게)·`pineapple`(파인애플)·`pins`(옷핀)), 기존
`pine-tree`(소나무, 주공해몽)에 둘을 더함.

`needle`(바늘)의 옛 영어 별칭에 "pin"이 그대로 있던 것을 찾았다(⓪ 전수 grep) —
바늘과 옷핀은 원문에서도 다른 표제어(다른 항목)라 겹칠 이유가 없는 상처였다.
`pins`를 새로 세우며 `needle`에서 제거했다(§28과 같은 자리).

`pine-tree`는 옛 세 의미(주공해몽, 전부 「배 위에」·「집 안에」·「지붕 위에」처럼
자리 조건 붙은 좁은 그림)보다 밀러의 「소나무를 봄」이 조건 없는 더 넓은
자리라 `FALLBACK_FIRST`로 기본값을 옮겼다.

빈 문자열 판별어 넷(pincers·pineapple·pins의 일반/기본 문맥과 pine-tree의
새 기본값)을 `verify-dream-km`이 막았다 — §30 곁가지 "빈 문자열도 안 된다"를
다시 확인했다. 좁고 흔치 않은 낱말(언뜻·탐스러운·반짝이는·우뚝)로 채워
통과시켰다 — 어차피 자연스러운 시험 문장에는 안 나오므로 기본값 자리를
그대로 지킨다.

프로브에서 처음엔 pine-tree 지킴 케이스 둘이 걸렸는데, **데이터가 아니라
내 시험 문장의 띄어쓰기가 원인**이었다 — 옛 판별어가 "배위"·"집안"(붙여 쓴
꼴)인데 내가 "배 위에"·"집 안에"(띄어 쓴 자연스러운 표현)로 써서 하나도 안
걸렸다. "뱃전에"·"마당에"로 바꿔 통과시켰다(§25 곁가지 "프로브 문장을 쓸 때
… 띄어쓰기가 이용자가 쓰는 대로인가"와 같은 자리 — 이번엔 옛 판별어 쪽이
붙여 쓴 꼴이라는 것을 먼저 확인해야 했다).

관문 전부 통과(프로브 516건, 교차 별칭 0쌍, tsc·verify-dream-cite 전수 포함).

**사전 1,596 / 5,397 / 5,421** (상징 / 의미 / 인용). 다음은 밀러 `Pipe`부터.

## 배치 180 — 밀러 `Pipe`~`Pirate` (8건)

새 상징 둘(`pipe`(파이프)·`pirate`(해적)), 기존 상징에 붙인 것 없음.

`flute`(피리)의 옛 영어 별칭에 bare "pipe"가 있던 것을 ⓪ 전수 grep이
찾았다 — 담배 파이프·수도관과 갈대 피리("reed pipe")는 같은 낱말을
공유하는 서로 다른 그림이다. 더 흔히 쓰는 쪽(담배·수도관)을 새 `pipe`에
주고, `flute`는 "reed pipe"(이미 있던, 더 구체적인 표현)만 남겼다.
지킴 케이스로 "he was playing a reed pipe"가 여전히 걸리는지 확인했다.

프로브가 하나 잡았다 — "해적임을"의 "임"이 계사인데 조사가 아니라 안
걸림(§29 곁가지, 배치 141의 nuns·nurse·nymph와 같은 자리). "해적임을"·
"해적인"을 별칭에 더해 고쳤다.

관문 전부 통과(프로브 525건, 교차 별칭 0쌍, tsc·verify-dream-cite 전수 포함).

**사전 1,598 / 5,405 / 5,429** (상징 / 의미 / 인용). 다음은 밀러 `Pistol`부터.

## 배치 181 — 밀러 `Pistol`~`Pit` (6건, 2문장 건너뜀)

새 상징 없음. 기존 `gun`(총, 밀러 `Gun`)에 둘, 기존 `pit`(구덩이, 주공해몽)에
넷을 더함 — 둘 다 Miller의 새 표제어가 이미 있는 상징과 같은 낱말을 쓰는
경우였다(권총=gun의 별칭에 이미 있었고, 구덩이=pit의 term_ko와 정확히 같다).

Pistol의 문장 둘을 뺐다 — "총소리를 들음"과 "총을 쏨"이 각각 기존 gun의
같은 그림·같은 극성(흉) 의미와 겹쳐 가를 수 없었다(§31).

`gun`은 옛 「총소리를 들음」(조건부)보다 새 「권총 꿈을 봄」(무조건)이 더
넓어 FALLBACK_FIRST로 기본값을 옮겼다. `pit`은 새 넷도 전부 동작 조건이
있어 옛 「문 앞에 구덩이와 도랑이 있음」(주공해몽)을 그대로 얼렸다.

지킴 케이스로 gun의 옛 넷을 재다가 **몇 주 묵은 상처**를 하나 찾았다 —
"총으로 남을 쏨"의 판별어에 있던 "쐈다"가 "여성이 총을 쏨"과 겹쳐,
"여성이 총을 쐈다"가 동점 끝에 앞의 것(총으로 남을 쏨)으로 갔다. "쐈다"를
빼고 "남을"·"사람을"만 남겨 고쳤다(§30 곁가지, §28과 같은 자리).

관문 전부 통과(프로브 536건, 교차 별칭 0쌍, tsc·verify-dream-cite 전수 포함).

**사전 1,598 / 5,411 / 5,435** (상징 / 의미 / 인용). 다음은 밀러 `Pitcher`부터.
