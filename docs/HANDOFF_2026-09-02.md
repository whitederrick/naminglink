# 인계 — 2026-09-02 (세션 재시작용)

**이 문서는 세션이 끊긴 자리에서 그대로 이어 가기 위한 것이다.** 배경과 실수 기록은
`docs/WORKLOG_2026-09-02.md`(§1~52)와 `CLAUDE.md`에 있다. 여기에는 **지금 상태와 다음 한 걸음**만
적는다.

## 지금 어디까지 와 있나

```
배치 28(밀러 Drouth~Dynamo)까지 넣었다 — 밀러 D 절이 끝났다.
관문 전부 통과 · 전수 스윕은 앞 판과 같다(빨간불 2건도 같은 둘).
```

**배포를 재는 자**(§27) — 새 데이터에만 있는 주소를 하나 골라 curl 한다.

```
https://dreams-link.com/ko/dream/symbol/dynamo    ← 배치 28에만 있다
https://dreams-link.com/ko/dream/symbol/dromedary ← 배치 27b에만 있다
```

## 해몽 사전 v2 — 지금 수치

```
상징 1,006 · 의미 2,931 · 인용 2,945
밀러 표제어 677 / 2,264   (남은 1,587)
```

**다음은 밀러 `Eagles` 부터.** 배치 파일(`mb29.json`)은 **아직 안 만들었다.**
절차 아홉 단계와 되풀이해 밟는 것들은 메모리
`dream-dictionary-v2-rebuild-in-progress` 에 있다 — **그것부터 읽고 시작할 것.**

## 이 배치(28)에서 새로 생긴 것

**① `FALLBACK_FIRST` 에 다섯이 더 들어갔다**(`scripts/build-dream-dictionary-v2.mjs`) —
북 · 오리 · 물오리 · 감옥 · 거름. 전부 **있던 기본값 그대로** 얼린 것이다. 27b의 관문이
그대로 일해서 조립이 exit 1로 멈췄다.

**② 일회성 패치 하나**(`scripts/patch-km-28.mjs`) — 이미 적용됐다. 다시 돌리면 「이미
있다」로 멈춘다(exit 2). 무엇을 왜 했는지는 그 파일 머리말에 있다. 같이 고친 것 둘:

- `duck`(오리)이 `wild-duck`(물오리)의 이름을 별칭으로 쥐고 있던 것을 돌려줬다.
- `duck`의 「오리를 잡음」이 판별어 「사냥」을 쥐고 있어 밀러의 「오리를 사냥함」과
  나눠 갖게 되던 것을 갈랐다(손으로 붙잡는 쪽은 「손으로」).

**③ 프로브 CASES가 배치 28 것으로 갈렸다**(`apps/dreamslink/scripts/probe-miller-batch.ts`,
45건). 다음 배치에서 **반드시 다시 갈 것** — 안 갈면 옛 배치를 재면서 새 배치를 쟀다고
착각한다.

## 기록만 해 둔 것 — 오늘 할 일이 아니다

재다가 보인 **기존 상태**다. 이번 변경이 만든 것이 아니다.

- **기본값이 답을 정하는 상징이 669개**이고 화면은 그것을 「골라낸 답」과 똑같이 보여준다
  (엔진은 `contextMatched: false`로 알고 있는데 화면이 그 값을 안 쓴다,
  `DreamResultView.tsx:100`). 그중 89개는 기본값의 길흉이 그 상징의 다수와 반대다.
- **`scripts/audit-dream-doc-numbers.mjs`가 폐기된 옛 사전을 읽는다**(상징 218·의미 350).
  지금 초록불이지만 v2를 한 번도 안 본 것이다 — v2 쪽 숫자는 `verify-guide-numbers.ts`가 본다.
- **전수 스윕의 빨간불 둘**은 여러 판째 같다 — `audit-dream-ownterms-multiword.mjs`(옛 사전을
  읽는다) · `verify-rendered-locale.mjs`(vi/ar 에 한국어 한 토막).
- `audit-unreachable-meanings` 1건(`wife`) — 앞 판과 같다. 감사라서 빨간불이 아니다.

**사용자에게 결정을 요청하지 말 것** — 물으면 그 자체가 할 일을 늘리는 것이다
(CLAUDE.md §30 곁가지).

## 다음 세션이 먼저 읽을 것

1. `CLAUDE.md` — **§30**과 시작 전 체크리스트
2. 메모리 `dream-dictionary-v2-rebuild-in-progress` — 절차 아홉 단계
3. 메모리 `new-symbol-steals-existing-names-and-ids` — ⓪단계에서 무엇을 grep 하는가
4. `docs/WORKLOG_2026-09-02.md` §46~52 — 배치 28의 전말
