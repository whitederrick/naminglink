# 2026-09-07 작업 기록

드림링크 밀러 배치 작업과는 별개로, 애드센스 콘솔 상태 점검과 사주링크 콘텐츠 보강을
같은 세션에서 진행했다.

---

## 애드센스 콘솔 상태 확인 — naming 승인 · saju 승인+저가치콘텐츠 경고 · inyeon·dreams 3주째 정체

사용자가 콘솔 화면(사이트 목록·상세 페이지)을 붙여넣어 넷을 실측 대조했다.

| 사이트 | 상태 | 세부 |
|---|---|---|
| naming-link.com | 준비됨 | 승인됨 |
| saju-link.com | 주의 필요 | 가치가 별로 없는 콘텐츠(승인은 됨) |
| inyeon-link.com | 준비 중 | 찾을 수 없음(소유권 확인 대기, 08-18부터) |
| dreams-link.com | 준비 중 | 찾을 수 없음(소유권 확인 대기, 08-18부터) |

**사용자가 "inyeon·dreams는 ads.txt를 못 찾아서 그런 것 같다"고 추측 → 확인해서 반박함.**
`curl`로 네 도메인의 `/ads.txt`(200·동일 퍼블리셔 ID)·`robots.txt`(`Allow: /`)·
`google-site-verification` 메타 태그(Googlebot UA로 apex→`/ko` 리다이렉트까지 따라가서 확인)를
전부 대조했고, 지금 서버 상태는 넷이 완전히 동일하다. **기술적 결함은 없다.**

콘솔의 "Ads.txt" 상태 칼럼(사이트 목록 셋째 칸)이 실제 파일과 별개로 **마지막 크롤 시점의
스냅샷**이라는 것은 naming-link.com이 2026-08-06에 이미 겪은 전례([[real-domain-and-adsense-live]])다.
inyeon·dreams의 소유권 확인 요청 시각이 둘 다 08-18 21:44로 정확히 같아, 08-18 크롤 이후
재크롤이 안 된 것으로 보인다 — 추정이고, 저희 쪽에서 고칠 것은 없다.

메모리 갱신: `adsense-site-list-state.md`.

## saju-link.com "가치가 별로 없는 콘텐츠" — 실제 콘텐츠 점검

naminglink이 08-10에 같은 문구로 걸렸던 두 원인(A. 안내문서 ko/en 미번역, B. 한국어 한 벌을
24개 언어 주소로 복제)을 saju에 대조했다 — **둘 다 아니었다.** `/en/reading`을 실측하니
한글은 회사명·언어선택 메뉴뿐이고 본문은 실제로 번역돼 있다.

페이지별 텍스트를 직접 추출해 대조한 결과:

- **`/guide/*` 안내 문서 9~11편**: 문서마다 실질적인 설명(한글 700~1,200자, 여러 단락).
  얇지 않다.
- **`/reading`·`/today` 핵심 폼 화면**: 언어선택기·이름칸·성별·출생지 도시 목록(60여 개
  지명)이 텍스트 대부분이고, **사람이 읽는 설명은 `form.description` 한 문장(200자 안팎)뿐**이었다.
  방문자 대부분이 처음 보는 화면이 바로 이곳이다.

원인을 단정하지 않았다 — "폼 화면이 얇은 것이 이번 판정의 실제 원인이다"는 추정이라고
사용자에게 명시했다.

## `/reading` 화면에 설명 문단 추가

사용자 확인: `/today`(재방문자용, 의도적으로 `/reading`과 같은 폼을 재사용)는 제외하고
`/reading`에만 넣기로, 지금 바로 구현하기로 결정.

- `apps/sajulink/src/lib/i18n.ts` — `Dictionary.form`에 `intro: string` 추가, ko·en 문구
  작성(오행 세력·십신·진태양시 보정·오늘의 운세 스무 항목 — 전부 `/guide` 문서에 이미
  공개된 내용만 요약, 새 근거를 지어내지 않음). en 쪽에 `\n` 하나로 두 문단 구조를 맞춤
  (i18n.ts 자체 주석이 "en은 모든 번역의 기준이라 `\n` 개수가 어긋나면 로케일마다
  복제된다"고 경고해 둔 자리).
- 나머지 21개 로케일은 `scripts/translate-i18n.ts <locale> --paths form.intro`로 새 잎
  하나만 번역해 채움 — 기존 420개 잎은 그대로(`git diff` 로케일당 정확히 1줄 추가만
  확인). `verify-i18n` 경고 5건은 전부 이번 변경과 무관한 기존 항목
  (`footer.privacyOfficer`·`tenGods.SIKSIN`·`dayMasters.庚/辛`·`landing.privacyBody`)이라
  손대지 않았다.
- `apps/sajulink/src/app/[locale]/reading/page.tsx`에 `dictionary.form.intro` 문단 배선.
- 관문: `tsc --noEmit`(0건) · `verify-i18n`(실패 0건) · `verify-product-consistency`
  (ALL PASS) · `eslint`(0건) — 전부 커밋 전에 확인.

**아직 안 한 것(당시)**: 배포·애드센스 재검토 요청은 사용자 판단. 아래에서 inyeon·dreams도
같은 조치를 하고 한 번에 커밋·배포하기로 이어졌다.

## inyeon-link.com·dreams-link.com도 같은 자리를 점검 — 실측으로 같은 병을 확인

사용자 지시: "inyeon과 dreams도 비슷한 이슈가 발생할 수 있으니 점검하고, 다 되면 한 번에
커밋·배포." saju에서 쓴 것과 같은 텍스트 추출 방식으로 `/compatibility`·`/affinity`(인연링크)·
`/dream`(드림링크)을 실측했다 — **셋 다 같은 구조**(폼 라벨·도시 목록이 텍스트 대부분,
사람이 읽는 설명은 한 문장뿐)였다. `/dream`이 가장 얇았다(한글 470자, 텍스트 입력창 하나뿐).

각 화면의 안내 문서(`/guide/how-compatibility`·`/guide/affinity`·드림링크 `/guide`)를 원문
그대로 읽어 근거를 확인한 뒤 문단을 썼다 — 궁합의 "네 항목"(일간 관계 32%·오행 보완 25%·
배우자성 23%·일지 관계 20%)과 "인연의 결"의 "총점을 안 내는 이유"(궁합 엔진을 거꾸로 돌려
항목별로만 보여줌)는 실제 안내 문서 본문을 열어서 정확한 구조를 확인했다(비중 숫자는 흔들릴
수 있어 문단에는 안 넣었다). `/affinity`는 결과 화면에 이미 있던 `affinity.intro`(다른 자리,
결과 설명용)와 겹치지 않게 `affinity.formIntro`로 새 키를 만들었다.

### 인연링크 21개 로케일 파일이 사주·드림과 다른 방식으로 관리되고 있었다 — `translate-i18n.ts`가 없었다

사주링크·드림링크의 `translate-i18n.ts`는 인연링크를 복제해 만들어질 때 함께 복제됐는데,
**정작 원본인 인연링크에는 이 스크립트가 없었다** — 21개 로케일 파일이 처음부터 에이전트가
손으로 채운 것이었다(각 파일 머리말에 그 흔적이 남아 있다). `apps/inyeonlink/scripts/
translate-i18n.ts`를 새로 만들어 옮겼다.

**옮기면서 한 가지를 실측으로 걸러냈다.** 사주·드림 버전의 `seedFromExistingFile`은 기존
로케일 파일을 `JSON.parse`로 되읽어 안 바뀐 잎을 이어받는데, 그건 그 두 앱의 파일이 이
스크립트가 `JSON.stringify`로 찍어 낸 것이라 키가 전부 따옴표로 감싸여 있기 때문이다.
**인연링크의 21개 파일은 손으로 쓴 JS라 키에 따옴표가 없다**(`{ brand: "..." }`) —
`JSON.parse`가 그 자리에서 던진다는 것을 `ja`로 실제 테스트해 확인했다. 그대로 뒀으면
"파일을 못 읽어 나머지 절을 en으로 되돌린다"는 조용한 실패로 21개 언어의 기존 번역이
통째로 영어로 깎일 뻔했다.

→ `seedFromExistingFile`을 파일 텍스트 재파싱이 아니라 **이미 import된
`getDictionary(locale)`을 그대로 잎으로 펼쳐 이어받는** `seedFromCurrentDictionary`로
바꿨다 — 텍스트 문법에 기대지 않으니 파일이 어떤 표기든 안전하다.

**두 번째로 걸러낸 것 — `write()`가 파일 전체를 재구성한다.** `ja`로 정식 실행해 보니 diff가
361줄로 나왔다. 실제로 바뀐 값은 두 개뿐이었는데, `write()`가 `JSON.stringify(built, null,
2)`로 사전 전체를 다시 찍어 **따옴표 없는 키 → 있는 키, 여러 줄 문자열 → 한 줄**로 인연링크
21개 파일의 표기를 전부 바꿔 버렸다(값 자체는 정확히 같았다 — `git diff`로 대조해 확인).
결과물은 손상이 아니지만 21개 파일 전체를 포맷만 바꿔 커밋하는 것은 이번에 하려던 일보다
훨씬 크다 — 되돌리고(`git checkout`), 스크립트에 `PRINT_ONLY` 모드를 추가해 **번역된 값만
JSON으로 찍고 파일은 건드리지 않게** 했다. 그 값 두 개를 Node 스크립트로 각 파일의 정확한
자리(`personA:`·`meLegend:` 앞, 둘 다 유일한 키라 안전하게 앵커로 썼다)에 원래 표기 그대로
끼워 넣었다 — `git diff --stat`으로 21개 파일 전부 정확히 2줄 추가만 확인했다.

### 관문

- `tsc --noEmit`: naminglink·inyeonlink·dreamslink 셋 다 0건.
- `verify-i18n`: 셋 다 실패 0건. 경고는 전부 이번 변경과 무관한 기존 항목
  (`footer.privacyOfficer`·`tenGods.SIKSIN`·`dayMasters.庚/辛`·`zh`의 `\n` 개수 경고 등).
- `verify-product-consistency`: 인연링크·드림링크 둘 다 ALL PASS.
- `eslint`: 바뀐 파일 전부 0건.

### 커밋 범위

사주·인연·드림 세 앱의 폼 화면 설명 문단(6개 문단: sajulink 1 · inyeonlink 2 ·
dreamslink 1 — 정확히는 화면 4개)과 21개 로케일 × 3앱, 인연링크의 새 번역 스크립트,
이 워크로그를 한 커밋으로 묶어 커밋·푸시했다(사용자 지시: "다 수정 및 보완되면 한번에
커밋하고 배포"). 커밋 해시·배포 확인은 아래 §27 방식(새 데이터에만 있는 주소로 확인)으로
뒤따른다.

**아직 안 한 것(당시)**: 애드센스 재검토 요청은 사용자 판단. 배포 뒤 실측으로 네 화면
전부 글자 수가 늘었음을 확인했다(saju 757→908 · inyeon 궁합 1,133→1,310 · 인연의 결
811→1,007 · dream 470→635, 전부 한글 기준).

## 사용자가 "더 보강할 것이 없는지 불안하다"고 재확인 요청 — 폼 화면 밖도 훑었다

`/reading`·`/compatibility`·`/affinity`·`/dream` 네 화면만 보고 끝낸 것이 불안하다는
지적을 받고, 두 앱의 사이트맵 전체를 다시 실측했다.

**saju-link.com — `/today`가 새로 가장 얇은 페이지가 됐다.** `/reading`을 고치며
"재방문자용이라 뺀다"고 의도적으로 제외했는데, 그 결과 `/today`가 한글 744자로 사이트에서
가장 얇은 채 사이트맵에 그대로 남아 있었다. 사용자 선택(reading과 다른 문단을 씀)에 따라
`form.todayIntro`를 새로 썼다 — `/reading`이 "원국을 어떻게 세우는지"라면 이건 "오늘의
운세가 원국과 무엇을 대조해 어떻게 점수를 내는지"(오늘의 일진 구성·억부 관계·오늘 천간-
일간 관계·지지 관계표·신강신약 보정·분야별 배분)로 실제 `/guide/today-fortune` 문서를
읽고 정확히 요약했다 — `/reading`과 겹치는 문장이 없다. 21개 로케일까지 채우고 관문
전부 통과(`tsc`·`verify-i18n`·`verify-product-consistency`·`eslint`).

**dreams-link.com — 진짜 위험은 상징 사전 1,808개 중 412개(23%)가 뜻이 하나뿐인 것이다.**
사이트맵 5,955개 URL 중 대부분이 `/dream/symbol/*`다. `dream-symbols.v2.data.json`을 직접
세어 보니 의미 1개짜리가 412개(23%)·2개가 579개(32%)로, **뜻이 하나뿐인 상징이 나머지
전부를 합친 것보다 조금 적은 수준으로 많다.** 실측(`외국` 상징)해 보니 실제 내용은
「밀러 원문 인용 한 문장 + 한국어 번역 한 문장」이 전부였다(한글 약 500~560자, 대부분
메뉴·푸터). `/reading`형("설명이 아예 없음")과는 다른 위험 — "인용 하나짜리 페이지가
400개 넘게 반복되는" 대량 생성형 얇은 콘텐츠 패턴에 더 가깝다. 다만 진짜 문헌 인용에
출처를 붙여 둔 것이라 순수 스팸형 얇은 콘텐츠와는 다르다는 점도 함께 적어 둔다.

→ **사용자 결정: 지금은 손대지 않고 보류.** dreams-link.com은 아직 08-18부터 소유권
확인 단계에 머물러 있어 콘텐츠 심사 자체가 시작되지 않았다([[adsense-site-list-state]]) —
실제 심사에서 지적되면 그때 대응한다. 대응 후보로는 ① 상징마다 이웃 상징 추천처럼
공통 문구를 보강 ② 뜻 하나짜리 412개를 noindex/사이트맵 제외로 검색 노출만 줄이기

## 밀러 배치 252(Somnambulist~Sowing) 재개 — 09-04에 중단된 것을 이어받아 완료

사용자 지시: "dreams 배치 진행하던거 마저 하자." 메모리 `dream-dictionary-v2-rebuild-in-progress`
와 `docs/WORKLOG_2026-09-04.md`를 읽어 09-04 세션이 콘텐츠 필터링 에러로 파일을 하나도
못 쓴 채 남긴 판단(king에 Sovereign 합침·ghost의 "영혼"을 새 soul로 회수·broth에 Soup
두 문장만 붙임)을 확인하고 그대로 이어받았다.

⓪ 전수 grep에서 원래 계획을 셋 더 바꿨다:
- **Sores → 새 상징이 아니라 기존 `boil`에 붙임.** boil의 term_ko가 이미 "종기"였다.
- **sorcerer의 별칭 "요술사" 제외.** `conjurer`(요술쟁이)가 이미 쥐고 있었다. sorcerer
  자체는 conjurer와 다른 헤드워드·다른 풀이라 새 상징으로는 그대로 세웠다.
- **Sowing → 새 상징이 아니라 기존 `farming`에 붙임.** farming이 이미 별칭 "파종"·
  EN "sowing"을 쥐고 있었다.

원문(`miller-1901-raw.txt` 20873~20947줄)에서 `mb252.json`·`m252.json`(항목 16개)을
만들고 9단계 절차를 그대로 밟았다. `verify-dream-cite`(항목 16개 · 위반 0건) →
커버리지 대조 → 인종·민감 표현 없음 확인 → `kmm252.json`(새 상징 셋: somnambulist·
sorcerer·soul) + 기존 넷(boil·broth·king·farming) 패치 → 조립(둘 다 exit 0) →
`verify-dream-km`·`audit-km-dead-words`·`audit-cross-symbol-aliases` 전부 exit 0.

**프로브(⑨단계) 1차에서 9건이 걸렸다.** 대부분 같은 병이었다 — **판별어를 꿈 장면이
아니라 풀이(운세) 낱말로 적어서** 자연스러운 문장과 안 맞았다. 예: "종기를 봄"의
판별어를 "질병 손실 고통"(밀러의 풀이 문장에서 그대로 가져온 낱말)으로 적었더니
"종기를 보았다"라는 평범한 문장이 그 자리로 안 가고 boil의 기존 기본값("만성 단계에
이른 것처럼 보이는 종기가 난 꿈을 꿈")으로 떨어졌다. 같은 실수를 「군주를 봄」·
「자신에게 종기가 남」·「남의 영혼이 자신 안에 있다고 여김」·「굴 수프를 마심」
네 자리에서 더 냈다 — 전부 꿈 장면 자체에서 나올 낱말로 다시 썼다(§25와 같은
원칙, 이번엔 다섯 자리에서 한꺼번에 몰아서 밟았다). farming은 다른 병이었다 —
"씨를 뿌렸다"(를+공백이 낀 자연스러운 꼴)에 대응하는 별칭이 아예 없어 상징
자체가 안 걸렸다("씨뿌리"만 있어 "씨" 다음에 곧바로 "뿌리"가 와야 했다). 별칭
넷("씨를 뿌렸다"·"씨를 뿌리는"·"씨 뿌리는"·"씨 뿌렸다")을 추가했다.

프로브 재실행 24건 전부 통과할 때까지 반복 — 손댄 기존 상징(boil·broth·king·
farming·ghost)의 옛 답이 그대로인지 재는 지킴 케이스도 함께 넣었다.

관문: `verify-dream-cite`(전수 6,221건) · `verify-dream-match` · `verify-dream-context-parity`
· `verify-conception-pick` · `verify-guide-numbers` · `tsc --noEmit` 전부 통과.

**사전 1,808→1,811 상징 · 6,154→6,170 의미 · 6,188→6,204 인용.** 커밋만 했고
푸시는 안 했다 — 20판 묶음(248~267)의 관례대로 267까지 채운 뒤 전수 스윕과
함께 한 번에 푸시한다(지금 5/20). 다음은 밀러 표제어 `Spade`부터.

## 밀러 배치 253(Spade~Spice)·254(Spider) — 사용자 지시로 계속 진행

새 상징 둘(spice·spider), 기존 넷(shovel·cards·sparrow·eyeglass)에 그림 추가.
⓪ grep에서 Spade·Sparrow·Spectacles 셋 다 기존 상징이 이미 이름을 쥐고 있는 것을
확인해 붙였다. Spider는 문장 여덟 개(그중 하나가 여섯 갈래로 갈림) 항목 열셋으로
일감이 커서 §33과 같은 이유로 254로 따로 뗐다.

**파일명 사전식 정렬로 기본값이 둘 밀렸다** — `eyeglass`는 그대로 얼리고, `sparrow`는
조건 없는 가장 넓은 자리로 바꿨다(smoke·snow·soldier와 같은 판단).

**254의 프로브에서 두 가지 새 패턴을 만났다.** ① 한 글자 형용사("큰"·"작은")는
판별어로 못 세어(엔진이 1글자를 안 센다) "커다란"·"조그만" 두 글자 동의어로 바꿨다.
② 좁은 그림이 넓은 형제에게 동점으로 밀리는 자리(쫓아오던 거미를 죽임 vs 거미를
죽임, 큰/작은 거미에게 물림 vs 거미에게 물림)는 **배열에서 좁은 쪽을 앞으로
재배치**해 풀었다 — 낱말을 더 얹는 게 아니라 순서로 푸는 게 맞다(§25 곁가지 3).

**`verify-dream-context-parity`의 기존 대조 쌍 둘도 이번에 처음 걸렸다** — "거미 —
얽힘"·"거미 — 죽임". 하나는 "killing"이 "killed"의 부분 문자열이 아니어서, 하나는
"거미줄에 얽혔다"에 대응하는 그림이 아예 없어서였다. "kill"로 바꾸고 "거미가
거미줄을 짓는 것을 봄"에 "얽혔다"를 더해 고쳤다 — **하니스가 미리 심어 둔 대조
쌍은 그 상징이 실제로 들어오는 날에야 처음 검증된다**는 패턴(god·gold 사례와
같은 자리, 메모리에 기록)이 또 났다.

관문: 두 배치 다 `verify-dream-cite`(전수) · `verify-dream-km` · `audit-km-dead-words` ·
`audit-cross-symbol-aliases` · `verify-dream-match` · `verify-dream-context-parity` ·
`verify-conception-pick` · `verify-guide-numbers` · `tsc --noEmit` 전부 통과.

**사전 1,811→1,813 상징 · 6,170→6,192 의미 · 6,204→6,226 인용.** 커밋만 했고 아직
안 밀었다(20판 묶음 관례, 지금 7/20). 다음은 밀러 표제어 `Spider-web`부터.

## VS Code 강제 종료 뒤 세션 재시작 — 배치 255~267로 20판 묶음을 끝까지 이었다

작업 중 VS Code가 갑자기 종료됐다. 새 세션이 이 워크로그와 메모리
`dream-dictionary-v2-rebuild-in-progress`를 읽고 배치 254(Spider)까지 커밋된
상태에서 그대로 이어받았다. 사용자 지시: "md 파일 읽고, 20판 배치까지 이어서
진행해 주십시오."

배치 255(Spider-web~Spools)부터 267(Tapestry~Taxes)까지 열세 판을 한 판씩
절차 9단계 그대로 밟아 커밋했다 — ⓪ 전수 grep(한국어 이름 + term_en) →
추출 → `verify-dream-cite` → 커버리지 대조 → 매칭 키 → 조립 둘(`| tail`로
안 자름, `EXIT=$?`로 판정) → `verify-dream-km`·`audit-km-dead-words`·
`audit-cross-symbol-aliases` → `apps/dreamslink`에서 프로브. 그 뒤 배치
관문(`verify-dream-match`·`verify-dream-context-parity`·`verify-conception-pick`·
`verify-guide-numbers`·`tsc --noEmit`·`verify-dream-cite` 전수) 전부 통과할 때만
커밋했다.

**되풀이해 밟은 패턴(요약, 각 배치 커밋 메시지에 자세히 적어 뒀다):**

- **같은 그림, 다른 헤드워드, 다른 풀이는 판별어로 못 가른다 → 건너뛴다**(§31 곁가지).
  이번 열세 판에서 유난히 많이 났다 — Ghost/Spirit, Horse/Stallion, Barn/Stable,
  Ascend/Stairs vs Steps(각주가 스스로 "See Stairs"), Star/Stars, Thief/Stealing,
  Famish/Starving, Image/Statues, Hurricane/Storm(각주 "See Hurricane and Rain"),
  Mason/Stone Mason, Cistern/Tank, Brimstone/Sulphur, Sword(두 자리) 등. 건너뛴
  자리는 전부 `skipped_sentences`에 이유를 적어 뒀다 — 다음 사람이 「빠뜨렸다」로
  읽고 다시 넣지 않도록.
- **밀러가 자기 헤드워드 안에서도 문장을 되풀이하는 자리를 처음 만났다** — Switch의
  넷째 문장이 첫째와 같은 그림("그냥 a switch")인데 풀이가 달랐다. 같은 work·같은
  context면 인용이 포개지므로 그대로 두면 안 되고, 건너뛰는 것이 맞다.
- **파일명 사전식 정렬로 기본값이 계속 흔들렸다.** 조건 없는 더 넓은 그림이 새로
  들어오면 바꾸고(ghost·spinning-thread·barn·taking-one-s-own-life·table·swallow·
  cleaning), 그렇지 않으면 얼렸다(bobbin·spoon·star·thief·sword·brimstone·sun·
  stone은 정반대로 바꾼 자리). 매번 "새로 들어온 것이 조건이 있는가"만 물었다.
- **term_en을 잘못 적으면 상징이 조용히 하나 더 생긴다.** Sunshade를 umbrella에
  합치려다 term_en을 "sunshade"로 적어 새 상징이 생겼다 — 조립 직후 **상징 수
  증가분이 이번에 세운 새 심볼 수와 맞는지** 대조해서 그 자리에서 잡았다. 이후
  매 배치 끝에서 이 대조를 관례로 삼았다.
- **오래된 상처를 프로브가 계속 찾아냈다** — coach의 "마부", ascend의 "계단"(bare
  명사 트리거 없음), thief의 "도둑들"·"도둑질"(들 접미사·파생 명사), cleaning의
  "비질"·"쓸었다"(별칭이 "대청소"·"물청소"뿐이라 자연스러운 말이 전혀 안 걸리고
  있었다), wind의 "큰바람"(합성어라 bare "바람"으로 안 걸림). 전부 그 자리에서
  고쳤다.
- **판별어 동점을 배열 순서로 푸는 자리가 여럿 났다** — spider의 "거미줄을 봄"(빌딩
  그림과 겹치는 낱말 대신 "잔뜩 쳐진"으로 갈랐다), bobbin·spoon·table의 옛
  기본값(demote되면서 "그냥"이 아니라 진짜 discriminator가 필요했는데 처음엔 반대로
  둬서 두 번 헛짚었다).
- **한국어 동음이의 회피** — standard-bearer(기수/旗手)가 jockey(기수/騎手)와 매칭
  별칭에서 겹치지만 진짜 동형이의어라 받아들였다(참고 목록에만 남음). sybil의
  "무녀"는 기존 divine-woman이 이미 쥐고 있어 ⓪ 단계에서 빼고 다른 말로 갈았다.
  sticks(막대기)·switch(회초리)는 기존 club·chastise의 별칭과 겹쳐 저쪽에서 놓아줬다.

각 배치의 자세한 내용·건너뛴 이유·판별어 설계는 개별 커밋 메시지와
`data-sources/miller-batches/mb255.json`~`mb267.json`에 있다.

### 20판 묶음(248~267) 마감 — 전수 스윕·push·배포 확인

배치 267(Taxes)까지 커밋한 뒤 관례대로 마감 절차를 밟았다.

- **전수 스윕**(`node scripts/audit-verifiers.mjs`, 포그라운드로 직접, `| tail` 안 씀):
  검사기 121회 · 통과 117 · 검사 안 됨 7 · **빨간불 4건**. 넷 다 이 작업과 무관한
  기존 상시 항목이다 — `audit-dream-ownterms-multiword.mjs`(폐기된 옛 218개 사전을
  읽는 늘-빨간불 스크립트), `verify-company-display`·`naminglink/verify-locale-manifest`
  (통신판매업 번호 「제2026-서울금천-1798호」를 로마자/번역 문구로 못 바꾸는 기존 사안,
  [[mail-order-registration-number]]), `verify-rendered-locale.mjs`(naminglink
  vi/ar 로케일 문제, dreamslink와 무관). 이전 20판 묶음(228~247)의 마감 스윕과
  정확히 같은 넷이다.
- **git push**(`e83baa0c..cc003269`, 커밋 19개).
- **배포 확인**: `gh api .../commits/cc003269/status`로 넷 다 폴링, 전부 success.
  `npx vercel inspect <id> --logs`로 각각 열어 실제로 무엇을 했는지 확인—
  naminglink·sajulink·inyeonlink 셋 다 `Ignored Build Step command returned
  exit code 0` → **Canceled**(정상 스킵), dreamslink만 `Build Completed` →
  `Deployment completed` → **Ready**(정상 재빌드). 이번 20판 묶음이 새로 만든
  주소 셋(`/ko/dream/symbol/tapestry`·`tannery`·`taxes`)을 push 전 404 →
  push 후 200으로 확인했다(§27 방식).

**최종 수치: 상징 1,875개 · 의미 6,405개 · 인용 6,440개.** 다음 20판 묶음의
시작점은 밀러 표제어 **`Tea`**(Taxes 바로 다음 표제어)부터다.

## 새 20판 묶음(268~287) 진행 중 — 배치 268~275(8/20)

같은 날 이어서 새 20판 묶음을 시작했다. 배치 268~275 여덟 판을 관문 아홉 개
(build·tables·verify-dream-km·audit-km-dead-words·probe-miller-batch·
verify-dream-match·verify-dream-context-parity·verify-conception-pick·
verify-guide-numbers·tsc·verify-dream-cite 전수)를 매번 통과시켜 커밋했다.
**아직 전수 스윕·push·배포 확인은 안 했다** — 20판을 다 채운 뒤에 한다(관례).

- **268(Tea~Teasing)**: 새 상징 셋(tea·teakettle·teasing), 기존 cup에 문맥 2개.
  cup의 「찻잔」/「찻잔으로」 판별어 부분 문자열 겹침을 낱말 분리 + 배열 순서로 풀었다.
- **269(Teeth, 단독)**: 밀러가 19문장으로 가장 길게 다루는 표제어라 horse류처럼
  단독 배치로 뗌. 새 상징 없이 전부 기존 teeth(이)에 붙임 — 13개 추가, 6개는
  기존 teeth·brushing-teeth와 같은 그림이거나 개수 표현이 판별 불가라 건너뜀.
- **270(Telegram~Thaw)**: ⓪ 전수 grep에서 전보=cable, 망원경=spyglass,
  세입자=lodger, 텐트=camp, 구주희=ninepins가 이미 다른 상징의 별칭임을 발견 —
  전부 기존 상징에 병합하거나 더 정확한 임자로 옮겼다. Tempest·Terror는
  hurricane·afraid와 같은 그림이라 통째로 건너뜀.
- **271(Theater~Threshing)**: 새 상징 여섯, 기존 둘(thief·thread)에 문맥 추가.
  Thigh는 legs와 같은 그림이라 통째로 건너뜀.
- **272(Throat~Tiger)**: 새 상징 넷, 기존 셋(neck·thunder·tiger)에 문맥 추가.
  Throat은 「목」이 이미 neck의 term_ko라 그쪽에 병합.
- **273(Till~Tomb)**: 새 상징 여섯, 기존 셋(lumber·alarm-bell·grave)에 문맥 추가.
  Timber→lumber, Tocsin→alarm-bell 병합 — alarm-bell 쪽은 term_en을 원문
  그대로("alarm bell", 공백) 안 맞추고 슬러그 모양("alarm-bell", 하이픈)으로
  지어 처음엔 딴 상징이 새로 생겼다(배치 232 india-rubber와 같은 실수), 원문
  term_en을 그대로 복사해 바로잡음.
- **274(Tongue~Tornado)**: 새 상징 넷, 기존 둘(teeth·firebrand)에 문맥 추가.
  Torch→firebrand 병합, Tornado는 hurricane과 같은 그림이라 통째로 건너뜀.
- **275(Torrent~Tragedy)**: 새 상징 다섯, 기존 둘(rapids·trading)에 문맥 추가.
  Torrent→rapids, Trade→trading 병합(둘 다 aliases_en에 이미 동의어가 있었고,
  조립기의 「한글 이름 겹침」 관문이 잡아 줌).

**되풀이해서 걸린 것들**(자세한 사례는 각 배치 커밋 메시지에):
같은 그림·다른 헤드워드 병합(cable/telegram류)이 이 여덟 판에서 특히 잦았다 —
밀러가 유의어 헤드워드를 따로 적어 둔 경우(Cable/Telegram, Spy-glass/Telescope,
Lodger/Tenant, Ninepins/Tenpins, Camp/Tent 옮김, Lumber/Timber, Alarm
Bell/Tocsin, Rapids/Torrent, Trading/Trade)가 이전 배치들보다 밀도 높게
나왔다. 활성화 틈(별칭이 활용형·복수 접미사·조사 앞에서 막히는 것)도 배치마다
한둘씩 났고, 전부 그 자리에서 고쳤다.

**현재 수치(배치 275 기준): 상징 1,908개 · 의미 6,557개 · 인용 6,592개.**
**다음은 밀러 표제어 `Train`부터** — Torrent~Tragedy를 마쳤으므로.
20판 묶음이 다 차면(287까지, 앞으로 12판) 전수 스윕·push·배포 확인 절차를 밟는다.

- **276(Train~Tray)**: 새 상징 다섯(train·traitor·transfiguration·trap·tray),
  기존 long-journey(먼 길 떠남)에 문맥 5개 추가. Traveling의 첫 문장(여행
  일반의 결과)은 기존 「길을 떠남」과 같은 그림이라 §31로 건너뜀.
  **프로브 1차에서 long-journey 새 문맥 다섯이 전부 「안 걸림」**이었다 —
  기존 별칭 "여행"이 조사 없이 "-하다" 어미와 바로 붙으면(여행하다가·여행했다)
  `PARTICLES` 경계를 못 지난다. 배치 270에서 house·camp로 이미 겪은 자리와
  같은 병인데 이번엔 데이터가 아니라 **시험 문장** 쪽을 "여행을 ~" 꼴로 고쳐
  풀었다 — 데이터(별칭·판별어)는 옳았다. 그 과정에서 **몇 년째 기존이던
  「먼 길을 오가며 다님」 지킴 케이스도 원래 시험 문장으로는 안 걸리고 있던
  것**을 발견해 문장을 고쳐 확인했다(§29 곁가지 ①과 같은 자리 — 이번엔
  판별어가 아니라 프로브의 시험 문장 자체가 활성화 경계를 몰랐던 경우).
  trap의 「빈 함정을 봄」도 판별어 "비어"가 자연스러운 "빈"과 안 맞아
  "아무것도"로 고쳤다. transfiguration의 EN 판별어에 자기 별칭("transfigured")을
  썼다가 `audit-km-dead-words`가 죽은 낱말로 잡아 바로 고침.
  **현재 수치: 상징 1,913개 · 의미 6,577개 · 인용 6,612개.**
  **다음은 밀러 표제어 `Treasures`부터** — Train~Tray를 마쳤으므로.

- **277(Treasures~Trousers)**: 새 상징 다섯(triangle·tripe·triplets·trophy·
  trousers), 기존 tree·ditch·gold-and-silver-treasure에 문맥을 나눠
  붙였다. Trees의 「dead trees」·「climb a tree」는 기존 tree와 같은
  그림이라, 「cut one down」은 같은 그림인데 극성이 정반대(주공해몽은
  길, 밀러는 흉)라 §31로 건너뜀.
  **⓪ grep이 뚫린 자리를 처음으로 실물로 봤다.** Treasures를 새 상징
  「보물」로 세우려 `s.aliases_en` 필드까지 훑는 사전 grep을 돌렸는데
  0건이 나와 안전하다고 판단했다. 그런데 조립 뒤 `audit-cross-symbol-
  aliases`가 「보물」·"treasure"가 이미 gold-and-silver-treasure(금은
  보화)의 별칭이라고 잡았다 — **`dream-symbols.v2.data.json`의 심볼
  객체에는 `aliases_en` 필드가 아예 없다.** 영어 별칭은 전부 별도 표
  `dream-aliases-en.v2.ts`(`ALIASES_EN_V2`)에만 있고, 조립 산출물 쪽엔
  실려 있지 않다. `s.aliases_en`을 읽는 사전 grep은 **처음부터 빈
  배열만 읽고 있었다** — 배치 276 때도 같은 방식으로 grep했지만 그때는
  우연히 안 걸렸을 뿐이다. gold-and-silver-treasure에 합쳐 고쳤고,
  같은 이유로 ditch에 aliases_en "trench"를 더하려던 것도 pit이 이미
  그 낱말을 쥐고 있어(`ALIASES_EN_V2`에서 직접 확인) 뺐다.
  → **⓪ grep은 `dream-symbols.v2.data.json`이 아니라 `ALIASES_EN_V2`
  (`dream-aliases-en.v2.ts`)를 영어 별칭 소스로 삼아야 한다.**
  둘 다 m277이 원본 r-파일(r6·r8)보다 사전순으로 앞서 기본값이 바뀔
  뻔해 FALLBACK_FIRST로 얼렸다(tree·gold-and-silver-treasure).
  **현재 수치: 상징 1,918개 · 의미 6,594개 · 인용 6,629개.**
  **다음은 밀러 표제어 `Trout`부터** — Treasures~Trousers를 마쳤으므로.

- **278(Trout~Trunk)**: 새 상징 둘(trout·trowel), 기존 bugle(나팔)·
  luggage(짐가방)에 문맥을 나눠 붙였다. Trunk의 첫 문장(트렁크 꿈을 꿈)은
  기존 luggage 「짐가방을 봄」과 같은 그림이라 §31로 건너뜀. 「drummer」는
  옛말로 순회 판매원 — "행상인"으로 옮김.
  **⓪ grep을 이번엔 `ALIASES_EN_V2`(`dream-aliases-en.v2.ts`)까지 미리
  확인해 돌렸다**(배치 277의 재발 방지가 바로 효과를 봄) — "trumpet"이
  이미 bugle의 영어 별칭이었던 것을 새 상징을 세우기 **전에** 찾아
  합쳤다. bugle은 m278이 기존 나팔 출처(m49·m9b)보다 사전순으로 앞서
  기본값이 「나팔 꿈을 꿈」(밀러, 조건 없음)으로 바뀌었는데, 옛 「즐거운
  소리를 들음」보다 더 막연해 그쪽으로 얼렸다.
  **현재 수치: 상징 1,920개 · 의미 6,610개 · 인용 6,645개.**
  **다음은 밀러 표제어 `Truss`부터** — Trout~Trunk를 마쳤으므로.

- **279(Truss~Turf)**: 새 상징 셋(trusts·tub·tunnel)과 racetrack(경마장),
  기존 rupture(탈장)·fall(추락)에 문맥을 나눠 붙였다. Truss는 이름
  「탈장대」가 「탈장」을 품어(§25 곁가지) rupture에 합침. Tumble은 fall이
  이미 별칭 「굴러떨어」를 쥐고 있어 그쪽에 합침. Turf 둘째 문장(푸른
  잔디를 봄)은 기존 grass 「푸른 풀밭을 봄」과 같은 그림이라 §31로
  건너뜀 — 「잔디」가 이미 grass의 별칭이라(§25 곁가지) 경마장은 이름을
  갈라 새로 세웠다.
  **프로브 1차에서 둘이 걸렸다.** rupture의 새 문맥은 "그냥" 판별어를
  줬더니 동점에서 기존 기본값("자신이 탈장됨")에 밀려 엉뚱한 뜻으로
  갔다 — 자기 이름("탈장대")을 판별어로 쓰는 것으로 고쳤다(한국어는
  `ownTerms` 제외가 없어 안전). fall의 "굴러떨어"는 뒤에 붙는 어미
  (-졌다/-지는)가 조사가 아니라 막혀 있었고("굴러떨어졌다"·
  "굴러떨어지는"으로 완전한 활용형 추가), 지킴 케이스에서 "떨어져"
  (연결형)도 기존에 한 번도 안 걸리고 있던 것을 찾아 별칭에 더했다.
  fall은 m279가 기존 추락 출처(m32)보다 사전순으로 앞서 기본값이 바뀔
  뻔했으나 있던 답을 그대로 얼렸다.
  **현재 수치: 상징 1,924개 · 의미 6,624개 · 인용 6,659개.**
  **다음은 밀러 표제어 `Turkey`부터** — Truss~Turf를 마쳤으므로.

- **280(Turkey~Turquoise)**: 새 상징 넷(turkey·turnips·turpentine·
  turquoise), 기존 bathing(목욕)에 문맥 2개 추가. Turkish Baths는 이름
  「터키식 목욕」이 공백 뒤에서 「목욕」을 품어(§25 곁가지) bathing에 합침.
  **프로브 1차에서 둘 걸림** — bathing의 새 문맥 둘이 둘 다 "터키식"을
  공유해 동점으로 뒤엉켰다(한쪽을 "즐거움"으로 갈라 풀음). 지킴
  케이스에서 bathing의 기존 기본값이 "목욕"+"하는" 어미가 조사가 아니라
  몇 년째 한 번도 안 걸리고 있던 오래된 상처를 또 찾아 "목욕했다"·
  "목욕하는" 완전한 활용형을 추가했다. turnips의 「잎을 먹음」·「먹음」
  두 형제가 "먹었"을 공유해 동점 위험이라, 더 구체적인 「잎을 먹음」을
  배열에서 앞으로 옮겨 순서로 풀었다(§30 곁가지). bathing은 m280이
  기존 목욕 출처(m6·r1)보다 사전순으로 앞서 기본값이 바뀔 뻔했으나
  있던 답을 그대로 얼렸다.
  **현재 수치: 상징 1,928개 · 의미 6,643개 · 인용 6,678개.**
  **다음은 밀러 표제어 `Turtle`부터** — Turkey~Turquoise를 마쳤으므로.
