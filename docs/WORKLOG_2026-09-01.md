# 2026-09-01 — 안내 문서 23개 로케일을 v2 사실에 맞추고, 어제 커밋이 안 나가 있던 것을 발견했다

`docs/WORKLOG_2026-08-31.md` §「다음에 할 일」 1번(안내 문서 23개 로케일)을 받아 했다.
사용자 지시: **「2번하고 관문 전체 돌린 뒤 넷을 함께 푸시」.**

## 오늘 한 일 — 요약

1. **어제 커밋 넷이 푸시돼 있지 않았다**(§1). 워크로그는 「프로덕션에 올라갔다」고 적혀
   있었지만 운영에는 옛 218개 사전이 그대로 떠 있었다.
2. **안내 문서 23개 로케일 재작성**(§2·§3) — 숫자 5건뿐 아니라 **글로 적힌 사실 여럿이
   거짓**이었다. 한국어 30자리를 고치고 en 10편을 다시 만들어 21개 언어를 다시 돌렸다.
3. **어제 커밋에 들어 있던 결함 셋을 찾아 고쳤다**(§4) — 「자주 찾는 상징」이 빈 목록이
   된 것 · 소스에 박힌 NUL 바이트 · 한 번도 안 맞던 `EXPLICIT_IDS` 표.
4. **예문 관문을 새로 만들었다**(§5) — 문서가 「이렇게 적으면 걸립니다」라고 약속한 문장을
   제품 엔진에 실제로 넣어 본다.
5. 관문 전부 돌렸다(§6). **빨간불 2건이 남았고 둘 다 오늘 것이 아니다**(§7).

---

## 1. 「프로덕션에 올라갔다」가 사실이 아니었다

어제 워크로그 마무리가 이렇게 시작한다.

> **해몽 사전 재구축이 프로덕션에 올라갔다.**

```
$ git status -sb
## main...origin/main [ahead 4]
```

`3c6531e` · `366fcdc` · `cb8e4f8` · `1271c0c` 넷이 **로컬에만** 있었다. origin은 `09aa0e5`
(전량 폐기 결정 문서)에 멈춰 있었고, 운영 화면은 **옛 218개 사전**을 그대로 쓰고 있었다.

커밋·푸시·배포는 셋 다 다른 사건인데 「커밋했다」로 일이 끝난 느낌이 든다. CLAUDE.md §26에
적었다 — **「배포됐다」를 적기 전에 `git status -sb`를 본다.**

---

## 2. 문서가 틀린 것은 숫자 5건이 아니었다

어제 남긴 빨간불은 `verify-guide-numbers`의 ✗ 5건(「아홉 갈래」)뿐이었다. 그런데 문서를
실제로 읽으며 v2 데이터와 대조하니 **글로 적힌 사실이 여럿 거짓**이었다.

| 문서가 적은 것 | v2의 사실 | 누가 잡았나 |
| --- | --- | --- |
| 「아홉 갈래」 | **여덟** (`number` 갈래가 비었다) | `verify-guide-numbers` |
| 「좋은 쪽이 절반을 넘는다」 | **29.6%**. 가장 많은 것은 「갈리는 것」(34.5%) | 아무도 |
| 「뜻이 하나뿐인 경우가 대부분」 | **61%가 여럿** (678개 중 411개) | 아무도 |
| 「뱀을 품는 것과 물리는 것은 정반대」 | **둘 다 길하다** — 원문이 그렇게 적혀 있다 | 아무도 |
| 「높은 곳에서 떨어졌다」가 걸린다 | **안 걸린다** — `추락` 표제어가 v2에 없다 | 아무도 |
| 「복숭아·밤처럼 열매」 | `밤`이 v2에 없다 | 아무도 |
| 「빛깔과 수」 | `수` 갈래가 비었다. 빛깔은 둘(푸른색·붉은색) | 아무도 |
| 「사물·동물·자연이 가장 두꺼운 셋」 | **사물·행위·동물** (233·131·82) | 아무도 |
| 「행위 — 쫓기다·떨어지다·날다」 | 셋 다 v2에 없다(목욕·잔치·매맞음이 실물) | 아무도 |
| 두 원문(주공해몽·밀러)을 **한 번도 안 밝힘** | 화면은 출처를 갈라 보여 준다 | 아무도 |

**숫자는 자리표시자(`{symbolTotal}`)라 저절로 따라갔다. 글은 안 따라간다.** 그리고 이 중
관문이 보는 것은 숫자뿐이다. CLAUDE.md §26에 §25의 새 얼굴로 적었다.

### 뱀 예시가 가장 나쁘다

「뱀을 품에 안는 것과 물리는 것은 정반대」는 문서 **넷**의 대표 예시였다(about ·
one-symbol-many-meanings · how-to-write · good-and-bad). v2에서 실제로 재 보면:

```
뱀이 품속으로 들어옴      [positive]  귀한 자식을 낳을 조짐
뱀이 사람에게 덤벼듦      [positive]  큰 재물을 얻을 조짐   ← 「물렸다」가 여기로 간다
```

**둘 다 길하다.** 주공해몽은 뱀이 덤벼드는 것을 재물의 조짐으로 읽는다.

`verify-dream-context-parity`는 이 자리를 **통과시켰다** — 그 검사기는 「뜻이 갈리는가」를
묻지 「반대인가」를 묻지 않기 때문이다. **「다르다」와 「반대다」는 다른 주장이다.**

대신 실제로 반대인 짝으로 바꿨다(둘 다 엔진으로 확인함).

```
해가 막 떠오름 [positive] 집안이 번창   ↔  해가 떨어짐 [negative] 부모를 잃을 근심
돼지를 잡음   [positive] 길한 조짐     ↔  돼지가 저절로 죽음 [negative] 흉한 조짐
```

---

## 3. 23개 로케일 — 한국어 30자리 · en 10편 · 나머지 21개

순서는 `docs/I18N_DOC_CONTENT.md`대로 **① 한국어 → ② 영어 → ③ 21개**다.

1. **ko 30자리**를 고쳤다. 치환마다 「찾은 횟수가 정확히 1인가」를 세고, 하나라도 어긋나면
   **아무것도 쓰지 않고 멈추게** 했다(§10 #44 — `replace`는 실패를 조용히 넘긴다).
   첫 판은 여러 줄 치환 9건이 전부 0건이었다 — **CRLF**다(§7 · §10 #45). LF로 펴서 고치고
   CRLF로 되돌리게 바꾸니 30건 전부 붙었다.
2. **en은 바뀐 10편만 지우고 `--fill-en`**으로 다시 만들었다. 안 바뀐 문서(guide ·
   how-matching-works · no-storage · what-we-do-not-do · notice · contact)는 건드리지 않았다.
3. **21개를 7묶음으로 병렬**로 돌렸다.

### en을 읽어 보고 나서 21개를 돌렸다 — 그 줄이 값을 했다

`--fill-en`이 만든 영어가 서비스를 **1인칭 「I」**로 가리키고 있었다(「I did not create…」).
지우기 전 en에는 그런 자리가 **0건**이었고 전부 「we」였다. 한국어 원문은 주어가 없는
존댓말이라 모델이 매번 다르게 고른다.

**en은 21개 언어의 본이다.** 그대로 돌렸으면 어투가 **스물한 번 복제**됐을 것이다.
13자리를 고치고 나서 21개를 돌렸다. 검사기는 이것을 못 잡는다 — 문법도 맞고 한글도 안 남는다.

### 번역에서 터진 것 셋

| 무엇 | 어떻게 알았나 | 고침 |
| --- | --- | --- |
| **km이 통째로 죽었다** | 백그라운드 도구는 「exit 0」이라 알렸다 — 내가 명령 끝에 `echo`를 붙여 **종료 코드가 `echo`의 것**이 됐다(§10 #3의 새 얼굴). 로그 본문에 `fetch failed`가 있었다 | km만 다시 돌림 |
| **km의 한 문단이 크메르어가 아니라 중국어로 나왔다** | `verify-locale-script` — 「han 문자가 96%다」 | 영어 원문을 다시 써서 세 판 만에 잡음 |
| **zh의 한 줄이 한글 45%** | 같은 검사기 | 영어 예시에 **로마자 표기**를 넣어 한글 비율 자체를 낮춤 |

세 번째가 구조적이다. 「한 글자짜리 상징」을 설명하는 줄은 본디 한글 예시가 많아 임계값
언저리에서 돈다 — 옛 벌은 **아슬아슬하게 통과했을 뿐**이다. 로마자를 넣어 어느 언어로
옮겨도 그 언어의 문자가 다수가 되게 했다.

---

## 4. 어제 커밋에 들어 있던 결함 셋

문서를 고치다 데이터 쪽을 보게 되면서 나왔다. 셋 다 **어제 커밋된 것**이다.

### 4-1. 「자주 찾는 상징」이 빈 목록이 됐다

상징을 하나도 못 찾은 결과 화면은 대표 상징 아홉을 보여 준다.

```ts
DREAM_SYMBOLS.filter((s) => (s.weight ?? 1) >= 3).slice(0, 9)
```

**v2는 678개 전부가 `weight: 1`**이다(조립기가 그렇게 박아 넣는다). 목록이 빈 배열이 되어
그 절이 화면에서 통째로 사라졌다. tsc·검사기·하니스 어느 것도 안 잡는다 — CLAUDE.md §25와
정확히 같은 자리이고, **같은 교체에서 두 번째**다.

되살릴 때 **새로 고르지 않았다.** 새로 고르면 그것은 내가 지어낸 목록이 된다. 옛 사전에서
`weight: 3`이던 아홉 중 v2에 살아남은 일곱을 그대로 옮겼다 —
**돼지 · 뱀 · 용 · 똥 · 죽음 · 돈 · 이.** (「이빨 빠짐」·「추락」·「쫓김」은 두 원문 어디에도
표제어가 없다.)

그리고 **판정을 한 곳에 모았다**(`POPULAR_SYMBOLS`, §6 — 규칙이 두 곳에 적히면 하나만
고쳐지는 날이 온다). 안내 문서의 `{popularSymbolCount}` 자리표시자는 **아무도 채우지 않아
23개 언어 전부에서 그 문단이 한 번도 안 그려지고 있었다** — 못 채운 자리표시자가 있는
블록은 통째로 빠지기 때문이다. 그것도 함께 채웠다.

### 4-2. 소스에 진짜 NUL 바이트가 박혀 있었다 — 세 번째다

```
$ file scripts/build-dream-dictionary-v2.mjs
scripts/build-dream-dictionary-v2.mjs: data          ← 텍스트가 아니다
```

`grep`은 「Binary file matches」만 내고 내용을 안 보여 준다. §10 #30·#43과 **정확히 같은
사고**이고, 그 두 항목이 이미 CLAUDE.md에 적혀 있었다.

### 4-3. 그 NUL 때문에 `EXPLICIT_IDS`가 한 번도 안 맞았다

```
symbolKey()      "iron" + NUL(0x00) + "다리미"
EXPLICIT_IDS 키  "iron" + 공백(0x20) + "다리미"
```

**구분자가 달랐다.** 그 표는 갈라 놓은 상징의 주소(`/dream/symbol/iron`)를 못 박으려고
있는 것인데 **한 번도 조회에 맞은 적이 없다.** 지금 id가 맞는 것은 slugify의 중복 접미사가
우연히 같은 순서를 냈기 때문이고, 추출 파일이 하나 늘면 조용히 뒤바뀐다.

셋 다 `JSON.stringify([en, ko])`로 감싸 고쳤다(§22 곁가지에 이미 적혀 있던 방법이다).
그리고 **관문 둘을 함께 남겼다** — ① `EXPLICIT_IDS`에 적혔는데 한 번도 안 쓰인 키가 있으면
멈춘다 ② `POPULAR_SYMBOL_IDS`에 적혔는데 사전에 없는 상징이 있으면 멈춘다.
**둘 다 일부러 망가뜨려 exit 1이 나는 것을 확인**한 뒤 남겼다.

### 곁가지 — 조립기를 한 단계만 돌려 별칭 2,072개를 날릴 뻔했다

`build-dream-dictionary-v2.mjs`는 `aliases: []`로 비워 쓰고,
`build-dream-match-tables-v2.mjs`가 km 파일에서 별칭을 채운다. **앞엣것만 돌렸다.**

```
git diff --stat →  644 insertions(+), 3360 deletions(-)
```

§10 #49에 적어 둔 「diff 크기가 내가 넣은 항목 수와 비례하는가」가 그 자리에서 멈춰 세웠다.
`git checkout`으로 되돌리고, **둘을 이어 돌리면 바이트까지 같다**는 것을 먼저 확인한 뒤
스크립트를 고쳤다.

---

## 5. 새 관문 — `check-guide-examples.ts`

문서가 **약속한 예문**을 제품 엔진(`matchDream`)에 실제로 넣어 본다. 재구현하지 않는다(§23).

```
문서가 「걸린다」고 약속한 문장        13개
문서가 「안 걸린다」고 약속한 문장       4개
문서가 「정반대」라고 적은 짝            2쌍 ← 극성이 실제로 반대인지까지 본다
```

**대조군:** 고치기 전 트리에서 돌리니 **✗ 2건**(「높은 곳에서 떨어졌다」 두 자리). 고친 뒤 0건.

머리말에 **「이 검사기가 못 잡는 것」**을 먼저 적었다(§22) — 문서에 적힌 *뜻*이 맞는지는
안 본다 · 한국어 예문만 본다 · 예문 목록을 손으로 옮겨 적는다.

---

## 6. 관문 (커밋 앞에 한 번)

```
apps/dreamslink 에서
  tsc --noEmit                                  exit 0
  verify-guide-numbers.ts                       ALL PASS   ← 어제 ✗ 5건
  check-guide-examples.ts                       0건        ← 새로 만듦(고치기 전 ✗ 2건)
  verify-dream-match.ts                         ALL PASS
  verify-dream-context-parity.ts                실패 0건
  audit-km-dead-words.mts                       죽은 낱말 0개
  verify-i18n.ts                                exit 0

뿌리에서
  verify-doc-stages.mjs        ALL PASS — 네 앱 다 ① ko → ② en → ③ 21개
  verify-doc-locales.mjs       ALL PASS — dreamslink 로케일 23/23 · 번역 대기 0
  verify-locale-script.mjs     ALL PASS — 파일 115개 · 문자열 25,564개   ← 고치는 중 빨간불 2건
  verify-doc-source-safe.mjs   exit 0
  verify-locale-leakage.mjs    exit 0
  verify-dream-km.mjs          위반 0 · 커버리지 678/678
  verify-dream-cite.mjs        항목 1,964개 · 위반 0
  audit-dream-doc-numbers.mjs  exit 0

전수 스윕  node scripts/audit-verifiers.mjs
  --self-test                  exit 0
  검사기 107개 · 실행 118회 — 통과 114 / 검사 안 됨 8 / 빨간불 3
```

`eslint`는 **빨간불이 있지만 전부 오늘 것이 아니다**(`audit-dream-match-real-sweep.mts`의
`any` 셋 · `extract-ko-legal.ts`의 `require` · `translate-doc-content.ts`의 `prefer-const`).
오늘 만지거나 새로 만든 파일에서 나온 것은 **0건**이다.

---

## 7. 남은 빨간불 2건 — 둘 다 오늘 것이 아니다

스윕의 빨간불 셋 중 하나(`verify-locale-script`)는 고쳐서 초록이 됐다. 남은 둘은 **HEAD
에서도 빨간불**임을 확인했다.

### ① `audit-dream-ownterms-multiword.mjs` — **폐기된 사전을 검사하고 있다**

```js
const old = JSON.parse(readFileSync(path.join(LIB, "dream-symbols.data.json")));
```

이 파일은 **옛 218개 사전**이고, 08-31 교체 뒤로 제품 코드는 아무도 읽지 않는다.
검사 대상인 `tooth-fall`·`naked`·`ancestor`는 v2에 없는 id다. 08-31 워크로그가 「그 사전
자체가 교체됐으므로 이제 무의미할 수 있다」고 적어 둔 바로 그 자리다.

**손대지 않았다** — 지우는 것은 되돌릴 수 없고 사용자가 결정할 일이다. 같은 처지인 것이
`apps/dreamslink/scripts/audit-dream-match-real-sweep.mts`이다.

### ② `verify-rendered-locale.mjs` — **일부러 남긴 한국어 예시를 잡는다**

여섯 로케일(ja·es·ru·th·vi·ar) 전부 같은 잎 하나다 —
`guide/how-matching-works`의 요약에 든 **「특별할」**. 이 문서는 「낱말 안에 우연히 들어간
글자」를 설명하는 곳이라 **한국어 예시가 있어야 뜻이 선다.** HEAD에서도 여섯 다 똑같이 1건이다.

고치려면 검사기 쪽에 예외 목록이 필요하다(§11 — 얼리려면 목록으로). **오늘 범위 밖이라
그대로 두고 적어만 둔다.**

### ③ hi의 잎 하나가 영어로 남아 있다

`guide/how-matching-works`의 요약 — ②와 **같은 잎**이다. 번역기의 「한글 잔류」 판정에 걸려
영어로 되돌아간다. **HEAD에서도 영어였다** — 오늘 나빠진 것이 아니다.

---

## 8. 다음 세션이 이어받을 것

1. **밀러 나머지 표제어** — 2,257개 중 301개 처리. **다음은 `Burial`부터**(스크립트로 셌다:
   남은 것 1,956개. `Abortion`·`Absalom`·`Barrel`은 앞서 걸러진 것). 절차 고정: 20개 이하로
   쪼개기 → `verify-dream-cite.mjs` → 인종·민감 표현 grep → **커버리지 대조**.
2. **의미가 중복인 6곳** — `blood`·`shackles`·`bee`·`bathing`·`annoy`·`bride`.
   판별어로 못 푸는 자리라 **사람이 정해야 한다**. 08-31(2) §8 참고.
3. **폐기된 사전을 검사하는 스크립트 둘**(§7-①) — 지울지 남길지 결정.
4. **`verify-rendered-locale`의 예시 예외 목록**(§7-②) — 지금은 늘 빨간불이라 아무도 안 본다.
5. **현대 생활 사물**(핸드폰·시험 등) — 원문에 없다. 「지어내지 않는다」와 정면으로 부딪힌다.

### 알아 둘 것

- `verify-dream-cite.mjs`는 **인자 없이 돌리면 주공해몽(`r*`)만** 본다. 밀러(`m*`)까지 보려면
  경로를 손으로 넘겨야 한다 — 기본 스윕에서는 **밀러 인용이 검사되지 않는다**(§24와 같은 갈래).
- 백그라운드 명령 끝에 `echo`를 붙이면 **도구가 보고하는 종료 코드가 `echo`의 것**이 된다.
  오늘 km이 죽은 것을 「exit 0」으로 볼 뻔했다.

## 파일

- `apps/dreamslink/src/lib/doc-content/ko.ts` — 30자리 재작성(사실·예문·두 원문 밝히기)
- `apps/dreamslink/src/lib/doc-content/en.ts` — 10편 재생성 + 어투 13자리 + 예시 잎 둘
- `apps/dreamslink/src/lib/doc-content/{21개}.ts` — 다시 번역
- `apps/dreamslink/src/lib/doc-values.ts` — `popularSymbolCount` 채움 · `cultureNoteTotal` 주석
- `apps/dreamslink/src/lib/dream-symbols.ts` — `POPULAR_SYMBOLS` 신설(판정을 한 곳에)
- `apps/dreamslink/src/app/[locale]/dream/result/page.tsx` — 그 목록을 쓴다
- `apps/dreamslink/scripts/check-guide-examples.ts` — **새 관문**
- `apps/dreamslink/scripts/verify-guide-numbers.ts` — 박아 둔 `9`를 실제 목록에서 세게
- `scripts/build-dream-dictionary-v2.mjs` — NUL 제거 · `POPULAR_SYMBOL_IDS` · 관문 둘
- `apps/dreamslink/src/lib/dream-symbols.v2.data.json` — weight 7자리
- `CLAUDE.md` — §26 추가 · 체크리스트 4줄

---

# 2026-09-01 (2) — 푸시하고 나서: 드림링크만 배포가 실패했다

§1에서 「푸시하면 끝」이라고 생각한 것이 또 한 겹 있었다.

## 9. 배포 확인 — 드림링크만 빨간불

```
dreamslink   53f6ed2  failure
dreamslink   9129c4f  failure
dreamslink   09aa0e5  success      ← 8/27, 옛 사전을 쓰던 마지막 성공
naminglink / inyeonlink / sajulink  전부 success
```

**둘 다 실패했다는 것이 갈랐다** — `9129c4f`에는 내 문서 수정이 없어도 어제 커밋 넷이
들어 있다. 그래서 원인은 문서가 아니라 **v2 교체**였다.

운영 화면을 직접 두드려 보니 답이 나왔다.

```
https://dreams-link.com/ko/dream/symbol/apparel  404   ← v2 에만 있다
https://dreams-link.com/ko/dream/symbol/pig      200   ← 옛 사전에도 있다
```

**운영은 아직 옛 218개 사전이었다.** 이 두 줄이 그날 가장 값싼 자였다.

## 10. 원인을 한 번 틀렸다 — 10분을 버렸다

첫 가설은 **빌드 메모리**였다. 근거도 있었다 — 이 컴퓨터에서 한 번에 `next build`를
돌리면 일꾼이 죽는다(`build worker exited with code: 3221225794`). `experimental.cpus: 2`로
묶으니 **로컬 크래시가 사라졌다.** 푸시했고, **배포는 그대로 실패했다.**

다시 재니 진짜 수치가 나왔다.

| | 옛 사전(08-27 성공) | v2(실패) | 고친 뒤 |
| --- | --- | --- | --- |
| 미리 그린 장 | 약 5,573 | **16,153** | 1,915 |
| 산출물 파일 | 약 6만 | **193,463** | **22,607** |
| 크기 | — | **2.9GB** | 394MB |
| 빌드 | — | 4.4분 | 26초 |

**장수가 아니라 파일 수다.** 미리 그린 장 하나가 `.html`·`.rsc`·`.meta`·`.segments`를
낳아 장당 열 개 넘게 붙는다.

가설을 바꾼 것은 `find .next -type f | wc -l` 한 줄이었다. **하나만 재고 설명하면
그럴듯한 오답이 나온다**(CLAUDE.md §27).

## 11. 고침 — 미리 그리는 범위를 ko·en으로

상징 페이지를 두 언어만 미리 그린다. 나머지 21개는 **처음 열릴 때 그려지고 그 뒤로는
캐시**된다 — 크롤러도 200을 받는다. 열리지 않는 긴 꼬리를 빌드 때 미리 그릴 이유가 없다.

### 첫 판이 21개 언어를 404로 만들었다

잎(`symbol/[id]/page.tsx`)에 `dynamicParams = true`를 적었는데 **안 먹었다.**
`[locale]/layout.tsx`의 `dynamicParams = false`가 그 갈래 전체를 지배한다.

```
/ja/dream/symbol/pig   404
/es/dream/symbol/apparel 404   ← 21개 언어 전부
```

서버를 띄워 재고서 알았다. 그래서 **레이아웃 쪽을 열었다.** 없는 언어를 막던 것은
그 설정이 아니라 `routeLocale()`의 `notFound()`와 미들웨어 308이었다 — 둘 다 그대로 산다.

## 12. 확인 (production 빌드 · 그리고 운영)

```
로컬 production 빌드
  23개 언어 상징 페이지 200 — 어긋난 로케일 0개
  /zzz/... 308(미들웨어) · 없는 상징 404 (ko 도 ja 도)
  ja ar uz km mn kk 의 홈·dream·guide/categories·about 전부 200

운영 (배포 6193768264 success 뒤)
  /ko/dream/symbol/apparel  200   ← 아침엔 404였다
  표본 10개 언어 전부 200 · 없는 상징 404
  안내: 여덟 갈래 · 사물 233 · 행위 131
       「뜻마다 원문 구절이 붙어 있습니다」 + 주공해몽 · 밀러 해몽서
       「자주 찾는 상징 7개」
```

## 13. 아직 확정이 아닌 것

**Vercel 빌드 로그를 끝까지 못 읽었다** — 이 컴퓨터에 vercel CLI 도 토큰도 없고
브라우저 확장도 연결되지 않았다. 「파일 수가 임계였다」는 **가장 잘 맞는 후보이고
배포가 실제로 섰다는 것이 그 증거**이지만, 로그로 확인한 것은 아니다. 앞 가설(메모리)은
배포 결과로 이미 반증됐다.

**다음에 같은 일이 나면 `npx vercel inspect <dpl_...> --logs`를 사용자에게 부탁하는 것이
가장 빠르다** — 자격증명이 있어야 하는 자리다.

## 14. 커밋

- `17a7bef` `experimental.cpus: 2` — **틀린 가설. 다음 커밋에서 되돌렸다**
- `48d91ac` 미리 그리는 범위를 ko·en으로 · 레이아웃 `dynamicParams` 열기 · cpus 되돌리기

---

# 2026-09-01 (3) — 밀러 20개를 잇다(Burial~Calendar)

§8 「다음 세션이 이어받을 것」 1번. `Burial`부터 20개 표제어.

## 15. 뽑은 것

원문 20개에서 **의미 59개.** 새 상징 18개, 기존 둘(나비·달력)에 붙었다.

| | 전 | 후 |
| --- | --- | --- |
| 상징 | 678 | **696** |
| 의미 | 1,954 | **2,012** |
| 인용 | 1,964 | **2,023** |
| 밀러 진행 | 301 / 2,257 | **321 / 2,257** (남은 1,936) |

**다음 시작점은 `Calm`이다**(`Abortion`·`Absalom`·`Barrel`은 앞서 걸러진 것).

절차 넷을 다 돌렸다 — `verify-dream-cite`(위반 0) · **커버리지 대조**(입력 20개 중 20개
덮음) · 인종·민감 표현 grep(0건) · `verify-dream-km`(위반 0 · 696/696).

**제외한 것**: 참조 지시 둘(`[29] See Funeral`·`[30] See Cattle`), 조언 문장
(`Beware of writing letters`), `Called` 끝의 이론 문단(조짐이 아니라 설명).

## 16. km 파일 목록이 손으로 적혀 있었다 — 새 파일이 통째로 안 읽혔다

`kmm8.json`을 더했는데 스크립트 셋이 `km1~9`·`kmm1~7`을 **수를 세어** 적어 두고 있었다.

```
build-dream-match-tables-v2.mjs   새 상징 18개의 별칭·판별어가 빠진 표를 만듦
verify-dream-km.mjs                커버리지를 678개로 셈(실제 696)
audit-km-dead-words.mts            새 18개를 한 낱말도 안 보고 「죽은 낱말 0개」
```

**마지막 것이 제일 나쁘다.** 디렉터리를 읽게 고치자 곧바로 죽은 낱말 18개가 나왔다.
그리고 `build-dream-match-tables-v2.mjs`의 주석에는 **그 위험이 그대로 적혀 있었다** —
「이 목록이 옛것에 머물러 있으면 통째로 빠진 표가 만들어지고 그러고도 스크립트는 조용히
성공한다」. 경고를 적어 두는 것과 목록을 없애는 것은 다른 일이다(CLAUDE.md §5·§24).

셋 다 `readdirSync`로 고쳤다.

## 17. 검사기가 잡아 준 것

| 검사기 | 잡은 것 |
| --- | --- |
| `verify-dream-km` | **22건** — 판별어에 상징 이름 그대로 5건, 형제끼리 낱말 공유·부분 문자열 겹침 17건 |
| `audit-km-dead-words` | **18개** — 한 글자 한국어(썰·갓·쉰·한·하·탔·꿀·뜯), 이미 별칭인 낱말(buried·cablegram·cackling·hens·fresh), 영어 짧은 낱말(one·with·a) |

## 18. 그런데 검사기가 다 초록불인 뒤에 다섯이 안 걸렸다

자연스러운 문장 24개를 `matchDream`에 직접 넣었다.

```
MISS  손을 데었다                     burn
MISS  산 채로 파묻혔다                buried-alive
MISS  암탉이 우는 소리를 들었다         cackle
MISS  배의 객실에 갇혀 있었다           cabin
MISS  낯선 목소리가 내 이름을 불렀다    called
```

내가 별칭을 **어간**으로 적었기 때문이다(`데었`·`파묻힘`·`암탉 소리`).
`dream-match.ts`의 `PARTICLES`는 **조사만** 담는다 — 어미를 넣었다가 「말았다」가 말(馬)로,
「새었다」가 새(鳥)로 걸리는 오탐이 나서 되돌린 자리다. **그 주석의 결론이 이미 적혀
있었다** — 「어미를 담으려면 상징별 `aliases`에 완전한 활용형을 하나씩 올린다」.

`데었다`·`데어서`·`산 채로 파묻혔다`처럼 완전한 꼴로 바꾸자 다섯 다 올바른 상황으로
걸렸다. **검사기 셋이 전부 초록불인데 실물은 안 걸렸다** — CLAUDE.md §23 그대로다.

## 19. 관문

```
verify-dream-km        위반 0 · 커버리지 696/696 · 판별어 빠진 의미 0
verify-dream-cite      항목 2,023개 · 위반 0
audit-km-dead-words    항목 696개 · 죽은 낱말 0
audit-dream-doc-numbers exit 0
tsc                    exit 0
verify-dream-match     ALL PASS
verify-dream-context-parity  실패 0 (43쌍은 아직 사전에 없어 건너뜀)
verify-guide-numbers   ALL PASS
check-guide-examples   0건
```

## 20. 다음 세션이 이어받을 것

1. **밀러 `Calm`부터** — 남은 1,936개. 절차는 고정이고 이번에 한 번 더 확인했다.
   **여기에 한 단계를 더한다** — 검사기가 다 통과한 뒤 `matchDream`에 자연스러운 문장을
   넣어 새 상징이 실제로 걸리는지 본다(§18에서 다섯이 걸렸다).
2. 의미가 중복인 6곳 · 폐기된 사전을 검사하는 스크립트 둘 · `verify-rendered-locale`의
   예시 예외 목록 · 현대 생활 사물 — 08-31·09-01(1)에 적은 그대로.

---

# 2026-09-01 (4) — 밀러 20개를 더 잇고(Calm~Cap), **원문 파서가 표제어 일곱을 통째로 놓치고 있던 것**을 찾았다

시작할 때 핸드오프 권고대로 **전수 스윕을 먼저** 돌렸다(트리가 깨끗한 그 시점이
움직이지 않는 것을 재는 유일한 자리다).

```
검사기 107개 · 실행 118회 — 통과 116 / 검사 안 됨 7 / 빨간불 2
빨간불 둘 다 핸드오프 §3-1·§3-2에 적힌 기존 것 — 새로 생긴 것 0건
```

## 21. 파서가 표제어 일곱을 못 보고 있었다 — 그리고 **두 관문 다 그것을 못 본다**

`Canal` 항목의 본문을 읽다가 그 안에 `_Canary Birds_.` 라는 다른 표제어가 통째로
들어앉아 있는 것을 봤다. 세어 보니 **일곱**이었다.

```
줄머리형  _Canary Birds_.  To dream of this sweet songster, …   Canary Birds · Cistern · Quoits
줄끝형    … pleasant amusements and rivalries.  _Gale_.         Gale · Jailer · Katydids · Yule Log
```

`parse-miller-1901.mjs`의 표제어 정규식은 **줄 전체가 표제어일 때만** 센다. 이 일곱은
앞뒤 문장과 한 줄에 붙어 있어(원본 조판 사고로 보인다) 표제어로 세어지지 않았고,
**앞 표제어의 본문 속에 묻혀 있었다.** 책 자신의 색인에는 일곱 다 표제어로 실려 있다.

**이것이 §24의 새 얼굴이다.** 배치 절차의 관문 둘이 이것을 원리적으로 못 본다 —

- ③ `verify-dream-cite`는 **남긴 인용이 진짜인가**를 본다. 안 뽑은 것은 시야에 없다.
- ④ 커버리지 대조는 **입력 표제어 ↔ 결과 locator**를 맞춘다. 그런데 **그 입력 표제어
  목록 자체가 파서 산출물이다.** 파서가 만들지 않은 표제어는 「빠뜨린 것」이 아니라
  「애초에 없던 것」이 되어 **양쪽 다 초록불**이다.

08-27부터 세 세션이 321개를 뽑는 동안 아무도 못 봤다. 발견은 관문이 아니라
**원문을 눈으로 읽다가** 나왔다.

### 고침과 대조군

먼저 **지금 파서를 그대로 돌려 커밋된 파일과 diff가 0인지** 봤다(대조군). 0이었다.
그다음 두 갈래(줄머리형·줄끝형)를 더했다. 성경 인용 넷도 `_…_.` 꼴이라 함께 걸릴
뻔했는데, 그쪽은 ``…''로 감싸여 **줄머리에도 줄끝에도 오지 않아** 갈렸다(실제로 확인).

```
표제어 2,257 → 2,264 (정확히 +7)
diff 본문 = 일곱 자리의 분리뿐 · 성경 인용 넷 그대로
기존 추출 2,023건 전부 재검증 — 위반 0건
```

## 22. 배치 11 — `Calm`~`Cap` 20개 · 항목 63개

파서를 고치자 `Canary Birds`가 이 배치 안으로 들어와, 20개는 `Calm`~`Cap`이 됐다
(`Captive`는 다음 배치 첫 표제어).

```
③ verify-dream-cite    항목 63개 · 위반 0건
④ 커버리지             입력 20 ↔ 결과 20 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현            fallen women(제외함) · shrew(인용문에만 남김)
```

**새 상징 18개**(calm·calumny·camera·cameo-brooch·camp·campaign·cane·cancer·canal·
canary·candlestick·canker·cannon·cannon-ball·canoe·candy·canopy·cap)와
**기존 상징 둘에 의미 추가**(camel +3 · candle +5).

### 제외한 것과 그 이유

| 무엇 | 왜 |
| --- | --- |
| Campaign 셋째 문단(`campaign against fallen women`) | 1901년의 여성 비하 표현. 조짐 자체는 「어려움을 이기고 용기를 낸다」로 평범하다 |
| Cannon 셋째 문단 | 조짐이 아니라 **읽는 이에게 하는 해석 지침**이다 |
| Canoe 마지막 문단(`Water is typical of futurity…`) | 이론 서술 |
| Canary의 `Your over-confidence is your worst enemy.` 등 | 조언 문장 |

`Cancer`는 원문이 **같은 상황을 두 문단으로 두 번** 말한다. 버리지 않고 **의미 하나에
인용 둘**로 붙였다(조립기가 `work`가 같을 때만 합친다). 그래서 항목 63개 → 의미 62개.

### `candle`의 별칭에 「촛대」가 있었다

새 상징 `candlestick`(촛대)을 만들면서 발견했다. 그대로 뒀으면 「촛대를 봤다」가
**촛불로 걸렸을 것**이다. 별칭에서 뺐다.

## 23. ⑨단계가 **9건을 잡았다** — 검사기 셋은 그때도 전부 초록불이었다

어제 §18에서 「검사기가 다 통과한 뒤에 다섯이 안 걸렸다」를 겪고 절차에 ⑨를 더했다.
이번에 그 단계가 값을 했다.

```
verify-dream-km        위반 0 · 커버리지 714/714 · 판별어 빠진 의미 0
audit-km-dead-words    죽은 낱말 0
verify-dream-cite      위반 0
  ↓ 그런데
probe-miller-batch     시험 63건 · 안 걸림 2건 · 엉뚱한 뜻 7건
```

| 무엇 | 왜 |
| --- | --- |
| `calm` 두 문장이 **하나도 안 걸림** | 별칭을 `고요했다`로만 적었다. 이용자는 「고요한 바다」·「고요하고 행복했다」라고 쓴다 — §25의 활용형 문제를 **또** 밟았다 |
| 「암에 걸렸다」가 「암을 고쳐 낫게 함」으로 | 판별어가 `걸린`인데 문장은 `걸렸다`. 점수 0이 되어 **대표(첫) 의미로 떨어졌다** |
| 「물이 탁했다」가 「흐리고 괴어 있음」으로 | 판별어 `탁하`가 `탁했다`의 부분 문자열이 아니다 |
| 「처녀가 포탄을 보았다」가 대표 의미로 | 대표 의미 판별어에 `보았`이 있어 구체적 의미와 **동점**이 됐고, 동점이면 앞의 것이 이긴다 |
| 「죄수가 쓴 챙모자」·「광부가 쓴 챙모자」가 「여성이 봄」으로 | 같은 병 — 대표 의미에 `보았`이 있었다 |
| 「사탕 상자를 보냈다」가 「받음」으로 | `상자를`이 받음·보냄 **둘 다**에 있어 동점 |

**여기서 새로 배운 것 하나.** 「무엇을 봄」처럼 **가장 넓은 대표 의미의 판별어에
`보았` 같은 흔한 말을 넣으면 안 된다.** 대표 의미는 **아무 판별어도 안 걸렸을 때
떨어지는 자리**이므로 점수 0으로 두는 것이 옳다. 넣으면 구체적인 형제 의미와 늘
동점이 되고, 동점은 앞의 것이 이기므로 **구체적인 뜻이 영영 안 뽑힌다.**
`verify-dream-km`은 이것을 못 본다 — 판별어가 겹치지 않았고 죽지도 않았기 때문이다.

고친 뒤 다시 돌려 **63건 전부 통과.**

## 24. 관문 (커밋 앞에 한 번)

```
뿌리에서
  verify-dream-cite (밀러+주공 전부)   항목 2,086개 · 위반 0건
  verify-dream-km                      항목 714개 · 위반 0 · 커버리지 714/714
  audit-dream-doc-numbers              23개 로케일 전부 사전과 같음
  audit-dream-source-coverage          exit 0

apps/dreamslink 에서
  tsc --noEmit                         exit 0
  audit-km-dead-words                  판별어 13,813개 · 죽은 낱말 0
  verify-dream-match                   ALL PASS
  verify-dream-context-parity          실패 0
  verify-guide-numbers                 ALL PASS
  check-guide-examples                 어긋난 예문 0건
  verify-i18n                          실패 0 (경고 1은 번역 누락 의심)
  probe-miller-batch                   63건 전부 통과
```

## 25. 지금 사전

| | 앞 세션 | 지금 |
| --- | --- | --- |
| 상징 | 696 | **714** |
| 의미 | 2,012 | **2,074** |
| 인용 | 2,023 | **2,086** |
| 밀러 진행 | 321 / 2,257 | **341 / 2,264** (남은 **1,923**) |

갈래는 여전히 여덟(사물 251·행위 137·동물 85·자연 73·사람 72·장소 56·몸 38·빛깔 2),
길흉은 갈리는 것 254·조심할 쪽 212·좋은 쪽 203·어느 쪽도 아닌 것 45.
태몽 21 · 「자주 찾는 상징」 7 — 둘 다 그대로다.

## 26. 다음 세션이 이어받을 것

1. **밀러 `Captive`부터** — 남은 1,923개. 절차 아홉 단계는 그대로이고,
   ⑨는 이제 `apps/dreamslink/scripts/probe-miller-batch.ts`가 맡는다.
   **새 배치를 넣으면 그 파일의 `CASES`를 갈아야 한다** — 안 갈면 옛 배치를 다시
   재면서 「쟀다」고 착각한다(파일 머리말에 적어 뒀다).
2. 나머지는 09-01(1)·(3)에 적은 그대로 — 의미가 중복인 6곳(사용자 결정 필요) ·
   폐기된 사전을 검사하는 스크립트 둘 · `verify-rendered-locale` 예외 목록 ·
   현대 생활 사물.
3. **새로 알아 둘 것**: 파서가 되찾은 표제어 일곱 중 여섯(`Cistern`·`Gale`·`Jailer`·
   `Katydids`·`Quoits`·`Yule Log`)은 아직 알파벳 순서가 오지 않았다. 그 자리에 오면
   저절로 배치에 들어온다 — 따로 챙길 것은 없다.

---

# 2026-09-01 (5) — 밀러 배치 12(Captive~Castoria) · 새 상징이 **남이 쓰던 이름과 자리를 가로챈다**

## 27. 배치 12 — 20개 · 항목 60개

```
③ verify-dream-cite   항목 60개 · 위반 0건
④ 커버리지            입력 20 ↔ 결과 20 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현           `lewd`(침대차 인용) · `widow`(스페이드) — 둘 다 인용문 안에만 있고
                      우리가 쓴 해석 문장에는 없다
```

새 상징 18개, 그리고 **기존 둘에 의미 추가** — `cart`(수레)에 4, `city-wall`(성)에 3.
밀러 `Castle`은 `SYNONYMS`가 이미 `city wall`로 접어 두었으므로 그대로 합쳐졌다.

제외한 것: `Cardinal`의 「사제가 영적 조언자라면…」 문단(조짐이 아니라 태도 서술).

`Cars`는 열 갈래로 갈렸다(봄·올라탐·놓침·내림·침대차·전차·타고 감·발판에 섬·발판이
높음·낮음). `Cards`는 무늬 넷(다이아·클로버·하트·스페이드)을 각각 의미로 두었다 —
원문이 한 문장에 넷을 담고 있어 절 하나로 뭉치면 뜻이 흐려진다.

### 원문에 없는 한정어를 하나 붙일 뻔했다

`To see a cart`를 「**빈** 수레를 봄」으로 적었다. 기존 주공해몽 문구와 겹칠까 봐 미리
피한 것인데, **원문에 「빈」이 없다.** 겹치는지 먼저 확인했어야 했고(확인해 보니 안
겹쳤다), 겹쳤더라도 지어낸 말로 가르는 것은 이 사전이 폐기한 옛 사전의 병이다.
「수레를 봄」으로 되돌렸다.

## 28. 새 상징이 **남이 이미 쓰던 이름과 자리**를 가로챈다 — 넷

| 무엇 | 어떻게 죽나 |
| --- | --- |
| `carriage`(마차) | 「마차」가 **`cart`와 `cab` 둘의 별칭**이었다. 그대로 뒀으면 「마차를 봤다」가 수레나 삯마차로 걸린다. 둘에서 뺐다 |
| `cart`의 `aliases_en` | `carriage`가 들어 있었다. 같은 이유로 뺐다 |
| **`car` id** | 밀러 `Cars`를 `term_en: "car"`로 넣자 id 가 `car`가 됐는데, **회귀 하니스가 그 id 를 「자동차」로 기다리고 있었다**(밀러 `Automobile`이 오면 채워질 자리). 「건너뜀」이던 세 쌍이 **빨간불**로 바뀌었다 — 없던 결함을 신고한 것이다 |
| 별칭 낱말 | 「험담」은 `verbal-abuse`·`accuse`가, 「부스럼」은 `boil`이, 「천막」은 `curtain`이 이미 쓰고 있었다. 셋 다 안 쓰기로 하고 다른 말로 바꿨다 |

**셋째가 새 갈래다.** id 는 주소이고 **다른 코드가 그것을 기다린다.** 1901년의 `Cars`는
철도 객차와 전차이지 자동차가 아니므로 `railway car`로 갈랐다 — 그러자 하니스가 다시
「아직 없는 상징」으로 건너뛴다(그것이 사실이다).

**검사기 셋이 넷 다 못 본다.** `verify-dream-km`도 `audit-km-dead-words`도 **한 상징 안**만
본다. 잡은 것은 별칭 전수 grep 과 회귀 하니스였다.

## 29. ⑨단계 프로브 — 이번엔 3건

```
verify-dream-km / audit-km-dead-words / verify-dream-cite   전부 초록불
  ↓
probe-miller-batch   60건 중 안 걸림 3건
```

| 무엇 | 원인 |
| --- | --- |
| 「목수들이 일하는 것을 보았다」가 **하나도 안 걸림** | `isStandalone`이 상징 뒤의 한글을 **조사일 때만** 허용한다. 「들」은 조사가 아니라 접미사라 `PARTICLES`에 없다 → 별칭에 `목수들`을 올렸다 |
| 「닭고기를 썰었다」가 carving 이 아니라 `chicken-meat`로 | **이것은 옳은 동작이다.** 별칭 「고기를 썰었다」 앞에 한글(닭)이 붙어 낱말 조각으로 걸러졌고, 닭고기는 실재하는 상징이다. carving 은 「칼질」로 닿는다 |
| 「칼질**해** 썰었다」가 안 걸림 | 같은 이유 — 어미 「해」는 조사가 아니다. §25 그대로 **완전한 활용형**(`칼질해`·`칼질했다`·`칼질하는`)을 하나씩 올렸다 |

그리고 문장을 쓰다가 **데이터 쪽 결함 넷을 먼저 잡았다** — 「침대차」가 별칭에 없어 상징
자체가 안 걸리던 것, 「발판」이 발판-높음/낮음과 늘 동점이던 것, 「빌린」이 현금의 두
상황 모두에 있어 첫 의미가 늘 이기던 것, 「애인과 함께 수레를 탔다」가 「수레를 타고 감」과
동점이던 것.

**프로브 문장은 통과하도록 고르지 않는다.** 안 걸리면 문장이 아니라 데이터를 고친다 —
이용자는 그 파일을 안 보고 자기 말로 쓴다. 파일 머리말에 그 줄을 적어 뒀다.

## 30. 관문 (커밋 앞에 한 번)

```
뿌리에서
  verify-dream-cite     항목 2,146개 · 위반 0건
  verify-dream-km       항목 732개 · 위반 0 · 커버리지 732/732
  audit-dream-doc-numbers      23개 로케일 전부 사전과 같음
  audit-dream-source-coverage  exit 0

apps/dreamslink 에서
  tsc --noEmit                 exit 0
  audit-km-dead-words          죽은 낱말 0
  verify-dream-match           ALL PASS
  verify-dream-context-parity  실패 0 (40쌍은 사전에 없어 건너뜀)
  verify-guide-numbers         ALL PASS
  check-guide-examples         0건
  verify-i18n                  실패 0
  eslint(새 파일)              0건
  probe-miller-batch           60건 전부 통과
```

## 31. 지금 사전

| | 배치 11 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 714 | **732** |
| 의미 | 2,074 | **2,134** |
| 인용 | 2,086 | **2,146** |
| 밀러 진행 | 341 / 2,264 | **361 / 2,264** (남은 **1,903**) |

갈래 여덟(사물 262·행위 139·동물 85·자연 73·사람 77·장소 56·몸 38·빛깔 2) ·
길흉은 갈리는 것 260·조심할 쪽 220·좋은 쪽 206·어느 쪽도 아닌 것 46 ·
태몽 21 · 「자주 찾는 상징」 7.

## 32. 다음 세션이 이어받을 것

1. **밀러 `Cats`부터** — 남은 1,903개. 절차 아홉 단계 그대로.
2. **`cart`의 `aliases_en`에 아직 `wagon`이 남아 있다.** 밀러 `Wagon`이 제 표제어로
   들어오는 날 **그 배치에서 빼야 한다** — 안 빼면 「wagon」이 두 상징에 걸린다(§28과
   같은 갈래). 오늘은 아직 상징이 아니라 그대로 뒀다.
3. **밀러 `Automobile`이 들어오면** 회귀 하니스가 기다리는 `car` id 를 채운다. 그때
   건너뛰던 세 쌍(자동차 — 잘 몲/사고/아픈 채로 탐)이 실제로 갈리는지 본다.
4. 나머지는 앞 절들에 적은 그대로 — 의미가 중복인 6곳(사용자 결정 필요) · 폐기된
   사전을 검사하는 스크립트 둘 · `verify-rendered-locale` 예외 목록 · 현대 생활 사물.
