# 작업 로그 — 2026-07-24

결제 채널 키 등록 전 처리 대상(감사 잔여 항목) 소화 + 도장 굿즈 후보 연동.
전부 커밋·프로덕션 배포됨(마지막 커밋 `ee9e199`). 자동 배포 webhook이 중간부터 끊겨
후반 커밋은 `vercel deploy --prod` 수동 배포로 올림(별도 이슈, 아래 "환경/인프라" 참조).

---

## 완료 (커밋순)

### 결제 안전 (1단계)
- **`dfbefce` 빈 유료 리포트·통화 자릿수·페이팔 유실 방지**
  - $9.99 글로벌 리포트: mini가 camelCase 키로 답하면 `text()`가 빈 문자열로 흡수해 빈 PDF가
    READY 되던 결함. snake/camel 양쪽 흡수 + 교정 재호출 1회 + 그래도 비면 FAILED(재생성 가능).
  - product_settings: amount가 통화 최소 단위인데 통화만 바꿔도 저장돼 ₩39,000→US$390.00 위험.
    통화 변경 시 금액 필수 + 통화별 범위 검사 + 해석된 금액 회신.
  - 페이팔 SPB: 승인 직후 탭 닫힘 시 `paid` 표시 못 남겨 이어받기 미노출·결제 유실. 마운트 시
    서버에 결제 확인(confirm 멱등) + 결제된 미완 체크아웃 덮어쓰기 방지.

### 개인정보 (2단계) — [[naminglink-privacy-phase2]]
- **`47875e2` 도장 PII 파기 크론 + 계정 삭제**
  - `src/lib/goods-pii-cleanup.ts`: 미결제 이탈 24h·배송완료/취소 90일 후 배송 PII만 제거
    (거래기록 유지). premium-cleanup 크론 앞단에 호출(세션 0건 조기반환에 안 걸리게).
    실검증: 실주문 6건 시드→파기→확인→삭제, 6/6 PASS.
  - `/api/admin/operations` DELETE_USER: auth 사용자 삭제(naming_logs cascade, orders set null).
    자기삭제 차단, 이력은 user_id만 로깅. 관리자 화면 삭제 버튼+확인창.
  - 개인정보처리방침 §3에 90일·24h 명시, 23로케일 반영.

### 법적 문서 가격 표기
- **`1318947`·`a2cbbec` 가격 표기 로케일 통일**
  - validate-legal-content 기존 19건 실패 원인은 가격 누락이 아니라 **로케일별 형식 불일치**:
    KRW 천단위 구분자(2.900/2 900), USD 접미·현지어(1,99 US$ / دولار). 전부 `₩2,900`·`US$1.99`로
    통일(displayPrice와 동일). 검증기가 `₩2,900` 형식 강제하도록 조임.
  - 재발 방지 스크립트: `scripts/normalize-legal-prices.mjs`, `normalize-legal-usd.mjs`.
    translate-legal-content 실행 후 반드시 함께 돌릴 것(헤더에 명시).
- **`2459ad9` 번역 치환 오역 차단**
  - km 약관이 "한국"을 전부 "크메르"로 치환(한자 리포트→크메르 문자 등). 팔지 않는 상품을
    고지하던 상태. km 재번역 + 번역 프롬프트에 "독자 언어로 치환 금지" 규칙. 21로케일 스윕은
    이상 없음(kk·uz는 어형 변화라 오탐). `scripts/extract-ko-docs.ts` 신설(ko 스냅샷 재생성).

### 프리미엄 AI 사용량·비용 가시화
- **`27c0b4b` 프리미엄 AI 사용량 로깅** — `src/lib/ai-usage.ts` `AiUsageRecorder`.
  프리미엄 생성이 lib에서 OpenAI 직접 호출해 ai_usage_logs에 아무것도 안 남기던 것을,
  리포트 단위 합산 1행으로 기록(호출 단위면 무료 트래픽이 묻힘). 실패도 flush(토큰 이미 소비).
  전역 일일 상한에는 **의도적으로 미포함**(결제 완료 요청을 무료 한도로 막으면 안 됨). 실검증 PASS.
- **`343f700`→`3736852` 서비스별 비용** — `src/lib/ai-pricing.ts` 단가 단일 원천.
  관리자 AI 화면에 서비스·모델별 토큰·추정 원가(₩) 표. 상세표는 최근 500건, 원가는 기간 전체 합산.
  실측 원가: 한자 리포트 ₩8.44, 글로벌 리포트 ₩2.30, 무료 발음/한국이름 ₩0.83~1.44,
  아트 PDF·한자매칭 ₩0(모델 미사용). **유료 상품 AI 원가는 매출 대비 0.02~0.09%로 무시 가능.**
  실질 비용은 무료 서비스뿐이며 현 상한(전역 2000/일·IP 20/일)에서 월 최대 ~₩86,000.

### confirm 라우트 하드닝
- **`80c23b9`** — 3개 confirm 라우트가 raw error.message 회신(어느 방어에 걸렸는지 노출).
  `src/lib/payment-errors.ts`로 분류·상태코드 통일, 포트원 검증 실패는 단일 문구. 프리미엄
  confirm에만 없던 바디 제한·레이트리밋 추가(60/h).

### 결제 취소 UX
- **`3736852`** — 페이팔 SPB 단계에서 빠져나갈 버튼 없어 갇히던 것. 취소 버튼(23로케일 문구).
  결제 전이라 상태만 idle 복귀, 미결제 주문은 크론이 정리.

### product_settings 이력
- **`61e3f5b`** — enabled 토글이 이력에 안 남던 것. `old_enabled`/`new_enabled` 컬럼 추가
  (마이그레이션 `20260724120000`, 원격 적용·검증). 이력표에 "판매" 열, 토글 시 반영 시점 안내.

### i18n / PDF 품질 (감사 [LOW])
- **`6f91756` 도장 폼 23로케일** — region만 보고 ko/en만 쓰던 폼을 로케일화(영어 원본 기준 번역,
  국내는 ko 고정). km에 영어 단어 "carve" 잔존 → 용어집 주입으로 해결.
- **`33864f1`+`199abd8` html lang/dir** — `lang="ko"` 고정 제거, 요청 로케일로 lang·dir(ar RTL).
  루트 레이아웃은 searchParams 못 받아 ?lang= 미반영 → `LocaleHtmlSync` 클라이언트 컴포넌트로
  보정. **미들웨어는 검토했으나 제거**(매 요청 엣지 레이어는 과함, 자동감지 사용자엔 서버값이 정확).
- **`7b3a3b0` PDF 로마자 두부 + 폰트 캐시 sha** — global-name-report 헤더·hangul-art metaValue·
  art-shared romanized가 raw Text라 NotoSansKR 상속으로 성조·발음부호가 두부. MixedText로 라우팅.
  실렌더 PNG로 Nguyễn·Zhāng·José·Œ 정상 확인. 폰트 캐시는 sha 없으면 code 폴백해 웜 람다가 옛
  파일 고정하던 것 → sha 필수·캐시 키로. 18개 폰트 전부 sha 보유 확인.

### 도장 굿즈 후보 연동 — [[naminglink-stamp-goods]]
- **`16b2074` 한글 이름 생성 → 도장** — 결과 도장 카드가 이름 미전달이라 재타이핑하던 것.
  오픈된 후보(잠금 제외)를 칩으로, 선택 시 `?name=`으로 폼에 주입. 한국 이름 2~6자라 그대로 적합.
- **`ee9e199` 발음 표기 → 도장 파트 선택** — `src/components/HangulStampCard.tsx` 신설.
  음차는 "다니엘 브룩스"처럼 다토큰이라 8자 초과 쉬움. 표기 후보 선택 → 이름 조각(공백 단위)
  다중 선택 → 순서대로 공백 없이 합쳐 도장 문구, 8자 초과 시 신청 차단. 3키 23로케일.
  조립·상한 로직 단위 검증 8/8 PASS.

---

## 진행 필요 (우선순위순)

### 코드로 바로 가능
1. **도장 형태별 구성 규칙** (사용자 요청, 다음 차례) — 주문 폼 모델 선택(원형/사각/흑단)과 도장
   문구 연동. 원형=이름부(given)만, 사각=성+이름, 형태별 글자수 상한. 현재는 모델이 형태 라벨만
   있고 문구 구성 규칙은 동일. 발음 표기 파트 선택(`ee9e199`)의 후속.

### 사용자 결정 필요
2. **가격 region 서버 판정** [MEDIUM] — candidate-unlock·stamp가 domestic/global(₩/US$)을
   클라이언트가 선택해 보냄. IP 검증은 한국 여행자 오탐 리스크. 카카오페이 자연 장벽으로 실위험 낮음.
3. **23로케일 AI 번역 약관 법무 검토** — 특히 "상품 설명이 실제 상품과 맞는지"(km 사례처럼 자동
   검사가 못 잡는 의미 오역) 항목 포함해 검토 의뢰.
4. **`LEGAL_EFFECTIVE_DATE` 갱신** — 현재 2026-07-22. 4개 문서 공유 상수라 런칭 직전 1회만 갱신 권장.
   90일 방침 명시했으니 배송 PII 크론이 실제로 도는 것 확인 후 시행일 조정.

### 포트원 채널 키 등록 후 (실질 런칭)
5. 상품별 실결제 검증(결제→웹훅→confirm→산출물), 카카오페이 모바일 리디렉션, 환불 경로.
6. `PORTONE_ALLOW_TEST_CHANNEL` 제거(정식 오픈 시 필수).
7. 전체 script-src CSP 확정(결제창 테스트 가능해진 뒤).

### 오픈 직전 운영
8. 테스트·샘플 데이터 일괄 삭제.
9. `companyInfo.privacyOfficer` "확인 예정" → 실제 값(비한국어 페이지에도 한글 placeholder 노출 중).

---

## 환경 / 인프라 이슈 (코드 아님)

- **naminglink 자동 배포 끊김** — git push는 정상인데 Vercel이 새 배포를 안 만듦(오늘 후반부터).
  Vercel Connected Git Repository는 정상(whitederrick/naminglink, Jul 9)이라 원인은 GitHub→Vercel
  push webhook 전달 레벨. **당장 배포는 `vercel deploy --prod` 수동으로 정상 작동**(오늘 후반
  커밋 전부 이렇게 배포·Ready). 복구: GitHub Codex Connector 정지 해제 후에도 안 되면 Vercel
  Settings→Git 재연결(Disconnect→Connect)로 webhook 재설치.
- **GitHub Codex Connector Suspended** — placelink(별도 repo, `C:\myProjects\placelink`)에 codex CLI
  연결 시 앱이 정지돼 인증 실패했던 것. GitHub Installed Apps에서 **Unsuspend 완료**. placelink
  저장소 자체는 정상이었음(remote·인증 테스트 통과, 미push 커밋 2건 대기). Vercel 앱은 내내 정상.

## 검증 방식 메모
- DB 쓰기 마이그레이션: 스크래치패드에 `pg` 설치 후 `SUPABASE_DB_URL`로 직접 적용(psql 없음).
- 실검증 스크립트(재사용): `scripts/verify-goods-pii-cleanup.ts`, `verify-premium-ai-usage.ts`,
  `report-ai-cost.mjs`(→ ai-pricing.ts로 통합돼 삭제됨).
- 로컬 dev는 사용자 관리. 검증 빌드 후 `.next` 삭제 필요.
