# 2026-09-02 — 태몽 의미가 둘 이상인 상징에서 **뒤엣것이 영영 안 뽑히고 있었다**

어제(09-01) 배치 20을 커밋만 해 두고 끝냈다. 오늘 그것을 푸시하고, 「1글자 판별어 자리를
훑어보자」는 남은 일을 하다가 **엔진 결함 하나**를 찾았다.

## 1. 배치 20 배포 확인

푸시 전후를 같은 주소로 쟀다(§27 — 새 데이터에만 있는 주소가 가장 싼 자다).

```
푸시 전   /ko/dream/symbol/corn 404 · /cotton 404 · /pig 200
푸시 후   /ko/dream/symbol/corn 200 · /cotton 200 · /corset 200 · /pig 200
```

## 2. 「1글자 판별어」를 훑다가 **엔진 결함**이 나왔다

### 2-1. 첫 판은 347건이었고, 그것은 후보였다

의미마다 **제 화면 문구**(`meaning.context`)를 `matchDream`에 넣어 그 의미가 뽑히는지 봤다.
2,239개 중 **347개**가 다른 의미로 갔다. 그런데 화면 문구에는 판별어가 안 들어 있을 수 있다 —
「불이 제 집을 태움」의 판별어는 「불길」·「타올랐」이다. **재는 자가 약했던 것이지 결함이
347개인 것이 아니다**(§23 — 근사는 후보로만 쓴다).

**그 의미가 받을 수 있는 가장 센 입력**(상징 이름 + 그 의미의 판별어 전부)으로 바꿔 재니
**2건**이 됐다. 이것으로도 지면 어떤 이용자 문장으로도 못 이긴다 — 판단이 필요 없는
부분집합이다(§28).

```
birth(출산)  「미혼 여성이 아이를 낳는 꿈을 꿈」 → 「기혼 여성이 …」
wife(아내)   「아내가 아이를 뱀」               → 「아내가 비단옷을 입음」
```

### 2-2. 원인 — 태몽 지름길이 **점수를 안 보고 첫 태몽 의미를 돌려줬다**

`chooseMeaning`은 꿈 글에 태몽 낱말(임신·태몽·출산…)이 있으면 태몽 의미를 먼저 본다.
그 규칙 자체는 옳다(「임신했는데 돼지가 집에 들어왔다」에서 낱말 수로만 고르면 재물 꿈이
이긴다). 그런데 첫 판이 이랬다.

```ts
const conceptionMeaning = meanings.find((m) => isConceptionMeaning(m));
if (conceptionMeaning) return { meaning: conceptionMeaning, contextMatched: true };
//                              ^^^^^^^^^^^^^^^^ 맨 앞의 태몽 의미를 무조건
```

**태몽 의미가 둘 이상인 상징이 다섯 개다** — 그 다섯에서 뒤엣것이 **영영 안 뽑혔다.**

| 상징 | 늘 이것 | 못 뽑힘 |
| --- | --- | --- |
| snake(뱀) | 뱀이 품속으로 들어옴 | **뱀이 몸을 휘감음** |
| sun(해) | 해를 삼킴 | **해가 품 안으로 들어옴** |
| moon(달) | 달을 삼킴 | **달이 품 안으로 들어옴** |
| face-powder(분) | 분을 얻음 | **분을 얻어 아내에게 줌** |
| birth(출산) | 기혼 여성이 아이를 낳는 꿈을 꿈 | **미혼 여성이 …** |

**유료 태몽 리포트가 읽는 자리다.** 「임신 중인데 뱀이 몸을 휘감았다」가 「뱀이 품속으로
들어옴」으로 나갔다. tsc·검사기·하니스 어느 것도 못 잡는다 — 문법도 데이터도 멀쩡하다.

### 2-3. 고침과 대조군

고치기 **전** 트리에서 시험 7건 중 **5건 빨간불**을 먼저 확인했다(§3). 고침은 **태몽 의미들
안에서 점수로 고르는 것**이다 — 태몽 의미가 하나뿐인 상징에서는 예전과 똑같이 돈다.

고친 뒤 5건 전부 초록불. `sun` 하나는 판별어 쪽 문제라 따로 적는다(아래 2-4).

관문을 함께 남겼다 — **`apps/dreamslink/scripts/verify-conception-pick.ts`**(9건).
「태몽 의미가 둘 이상인 상징」을 스스로 세고, 시험 문장이 없는 상징이 있으면 그것도
빨간불로 낸다(사전이 커지면 목록을 함께 늘리라는 뜻이다).

### 2-4. 못 고친 것 둘 — **엔진의 구조적 한계**

| 무엇 | 왜 |
| --- | --- |
| 「흰 산호」가 안 갈린다 | 판별어는 **2글자 이상만** 센다(`scoringWordsOf`의 `length >= 2`). 「흰」은 못 쓴다 → 「하얀」·「흰색」으로 돌렸다 |
| 「해가 **품 안으로** 들어왔다」가 안 갈린다 | 판별어는 **공백으로 쪼개진다** — 한 토큰이 「품 안」을 걸칠 수 없다. 사전은 「품안」(붙여 쓴 꼴)을 갖고 있다 |

둘 다 데이터로는 못 푼다. **적어 두고 넘어간다** — 고치려면 엔진의 판별어 표기 자체를
바꿔야 하고, 그것은 이 배치 작업의 범위가 아니다(§14).

### 2-5. 감사 하나를 남겼다 (관문이 아니다)

**`apps/dreamslink/scripts/audit-unreachable-meanings.ts`** — 위 방법으로 「어떤 문장으로도
안 뽑히는 의미」를 센다. **빨간불을 내지 않는다.** 지금 남는 1건(`wife`)은 태몽 지름길이
「임신」을 태몽 쪽으로 보내기 때문이고, 그것이 옳은지는 사람이 정할 일이다 —
코드가 대신 정하면 관문이 죽는다(§28).

머리말에 **「이 감사가 못 잡는 것」**을 먼저 적었다(§22).

---

## 3. 배치 21 — `Cotton Gin`~`Crawfish` · 항목 39개

```
⓪ 이름·별칭 전수 grep
③ verify-dream-cite   항목 39개 · 위반 0건
④ 커버리지            입력 20 ↔ 결과 20 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현           해석 문장 0건
```

**ⓠ grep이 일곱을 미리 걸렀다 — 이번 배치는 「이미 있는 상징」이 유난히 많았다.**

| 밀러 표제어 | 이미 있던 것 | 어떻게 했나 |
| --- | --- | --- |
| `Crabs` | `crab`(게) — 주공해몽 1개 | 그 상징에 붙였다(의미 1 → 3) |
| `Crane` | `crane`(학) — 주공해몽 5개 | 그 상징에 붙였다(5 → 8) |
| `Cows` | `cattle`(소떼) | 그 상징에 붙였다 — **아래 3-1** |
| `Counselor` | `attorney`(변호사)가 「변호사」를 쥠 | 이름을 「조언자」로 |
| `Countenance` | `complexion`(낯빛) | 밀러가 표제어를 따로 두므로 「얼굴 생김새」로 가름 |
| `Crape` | `mourning-clothes`(상복) | 「상포」로 |
| `Couch` | `bench`(벤치)가 「긴 의자」를 쥠 | 「소파」로 |

`Counterpane`(침대보)·`Cradle`(요람)·`Crawfish`(가재)는 앞 글자가 한글이라
`isStandalone`이 걸러 준다(`침대`·`요`·`재`) — 확인했다.

### 3-1. `Cows`가 여덟째 중복 후보다

밀러 `Cows`는 `[45] See Cattle.` 한 줄 + 조짐 한 문장이다. 그런데 그 문장이
**밀러 `Cattle`이 이미 말한 것과 같은 장면**이다.

```
Cattle  "a herd of cows at milking time"        → 재물의 주인이 된다
Cows    "cows waiting for the milking hour"     → 바라던 것이 넉넉히 이루어진다
```

**같은 상황, 다른 해석.** 조립기는 `context`가 같고 `work`가 같으면 인용을 합치는데,
그러면 **살아남은 해석 아래에 그것을 뒷받침하지 않는 인용이 붙는다**(§21·§22의 병 —
조립기 주석이 그 위험을 그대로 적어 두었다). 그래서 문맥을 갈라 따로 넣었고,
**결정 대기 목록에 여덟째로 올린다.**

### 3-2. ⑨단계 프로브 — 2건 (검사기 셋은 그때도 초록불)

| 무엇 | 원인 |
| --- | --- |
| 「남이 **기침하는** 소리를 들었다」 안 걸림 | 별칭이 「기침을 했다」뿐 — 「기침」 뒤에 어미 「하」가 붙어 낱말 조각으로 걸러졌다 |
| 「아이들을 **세었는데**」 안 걸림 | 별칭이 「세었다」뿐 — 「세었는데」는 부분 문자열이 아니다 |

**§25 활용형 문제, 이 저장소에서 몇 번째인지 세기 어렵다.** 완전한 꼴을 하나씩 올렸다.

`verify-dream-km`이 잡은 것 하나 — 영어 판별어 `own`이 형제의 `downfall`에 **부분 문자열로
들어 있었다**(d-**own**-fall). `audit-km-dead-words`는 `your`가 불용어라 죽은 것을 잡았다.

## 4. 관문 (커밋 앞에 한 번)

```
뿌리에서
  verify-dream-cite (주공+밀러 전부)  항목 2,609개 · 위반 0건
  verify-dream-km                     항목 891개 · 위반 0 · 커버리지 891/891
  audit-cross-symbol-aliases          맞바꿔 쥔 짝 0쌍
  audit-dream-doc-numbers · audit-dream-source-coverage   exit 0

apps/dreamslink 에서
  tsc · audit-km-dead-words(죽은 낱말 0) · verify-dream-match · verify-dream-context-parity ·
  verify-guide-numbers · check-guide-examples · verify-i18n · eslint   전부 초록불
  verify-conception-pick   9건 전부 통과   ← 오늘 만듦(고치기 전 5건 빨간불)
  probe-miller-batch       40건 전부 통과
  audit-unreachable-meanings  1건(wife · 사람 판정 대기)

전수 스윕  포그라운드
  검사기 110개 · 실행 121회 — 통과 119 / 검사 안 됨 7 / 빨간불 2
  검사기가 108 → 110으로 는 것뿐(오늘 만든 둘) · 빨간불 둘 다 기존 것
```

## 5. 지금 사전

| | 배치 20 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 874 | **891** |
| 의미 | 2,540 | **2,596** |
| 인용 | 2,553 | **2,609** |
| 밀러 진행 | 522 / 2,264 | **542 / 2,264** (남은 **1,722** · 23.9%) |

## 6. 다음

1. **밀러 `Crawl`부터** — 남은 1,722개.
2. **결정 대기가 여덟이 됐다** — `blood`·`shackles`·`bee`·`bathing`·`annoy`·`bride`·
   `childbed`/`birth` · **`cattle`(Cows/Cattle 젖 짜는 때)**.
3. 엔진 한계 둘(1글자 판별어 · 띄어쓴 꼴) — 2-4에 적었다.
4. `House`·`Mirror`는 초기 pilot 에서 이미 뽑았다 — 알파벳 차례가 오면 **건너뛴다.**

---

# 2026-09-02 (2) — 밀러 배치 22(Crawl~Crown) · **이름을 셋 옮겼다**

## 7. 배치 22 — 20개 · 항목 52개

```
⓪ 이름·별칭 전수 grep
③ verify-dream-cite   항목 52개 · 위반 0건
④ 커버리지            입력 20 ↔ 결과 20 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현           crippled · maimed · designing women — **전부 인용문에만** 있다
```

기존 상징 셋에 붙였다 — `cricket`(귀뚜라미) 1→3 · `alligator`(악어) 1→3 · `crow`(까마귀) 1→4.
밀러 `Crocodile`은 새로 만들지 않고 `alligator`에 붙였다(한국어로 같은 것이다).

## 8. ⓠ grep 이 이름 셋을 옮기게 했다 (§28)

| 이름 | 쥐고 있던 곳 | 넘긴 곳 |
| --- | --- | --- |
| 「갈림길」 | `road`(길) | **`cross-roads`(갈림길)** — 밀러 `Cross Roads` 가 들어왔다 |
| 「사기그릇」 | `plate`(접시) | **`crockery`(사기그릇)** |
| 「왕관을 씌우는」 | `coronation`(대관식) | **`crown`(왕관)** — 배치 20에서 내가 넣은 별칭이다 |

**셋째가 새 얼굴이다.** ⓠ 는 「새 이름이 남에게 있나」를 보는데, 이번에는 **두 배치 전에 내가
넣은 별칭**이 새 상징의 이름을 미리 쥐고 있었다. 배치가 이어질수록 이런 자리가 는다.

### 피한 이름 셋

`Cremate`를 「화장」으로 두려다 **말았다** — 「화장」은 화장(化粧)과 같은 글자라
`face-powder`(분)의 「화장분」·`outhouse`의 「화장실」과 뒤섞인다. 「화장터」로 좁혔다.
`Crockery`도 「그릇」(=`vessel`)을 피해 「사기그릇」으로, `Couch`도 「긴 의자」(=`bench`)를
피해 「소파」로 했다.

## 9. 하니스 둘이 깨졌고, 둘 다 옮긴 별칭 위에 서 있었다

```
✗ 길 — 갈림길        road 가 「갈림길」을 잃어 ko 가 안 걸린다
✗ 까마귀 — 다툼      ko 와 en 이 다른 의미를 골랐다
```

첫째는 **기대치를 새 임자 쪽으로 옮겼다**(`cross-roads`) — 옛 기대가 근거하던 별칭이
사라졌기 때문이다(§25 곁가지 기준). 둘째는 en 판별어가 문장의 낱말과 안 맞아 난 것이라
**데이터를 고쳤다**(`noisily quarreling chattering`).

## 10. ⑨단계 프로브 — 2건, 그리고 「기어가」로는 안 되고 「기어가다」여야 했다

| 무엇 | 원인 |
| --- | --- |
| 「들짐승이 **우는 소리**를 들었다」 안 걸림 | 별칭이 「울부짖는」뿐이었다 |
| 「땅을 **기어가다** 손을 다쳤다」 안 걸림 | 「기어갔다」뿐이라 안 맞았다 |

두 번째가 배울 것이 있다. **어간 「기어가」를 올렸는데도 안 걸렸다** — 뒤에 오는 「다」가
조사가 아니라서 `isStandalone`이 낱말 조각으로 버린다. **완전한 꼴 「기어가다」를 올려야**
걸렸다. §25의 「어간을 올리지 말 것」이 여기서는 **2글자 어간도 소용없다**는 형태로 나왔다.

`audit-km-dead-words`가 넷을 잡았다 — 1글자(「텅」·「빈」·「서」)와, **별칭으로 올려 제
이름이 되어 죽은 낱말 셋**(`asking`·`distress`·`being`은 불용어). 어제 §29 곁가지에 적어 둔
그 자리를 오늘 세 번 밟았다.

## 11. 관문

```
verify-dream-cite 2,644건 위반 0 · verify-dream-km 908개 위반 0 · 커버리지 908/908 ·
audit-cross-symbol-aliases 0쌍 · audit-dream-doc-numbers · audit-dream-source-coverage ·
tsc · eslint · audit-km-dead-words 0 · verify-dream-match · verify-dream-context-parity ·
verify-guide-numbers · check-guide-examples · verify-i18n · verify-conception-pick 9건 ·
probe-miller-batch 52건 — 전부 초록불
전수 스윕  검사기 110개 · 실행 121회 — 통과 119 / 검사 안 됨 7 / 빨간불 2 (앞 판과 같다)
```

## 12. 지금 사전

| | 배치 21 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 891 | **908** |
| 의미 | 2,596 | **2,631** |
| 인용 | 2,609 | **2,644** |
| 밀러 진행 | 542 / 2,264 | **562 / 2,264** (남은 **1,702** · 24.8%) |

**다음은 `Crucifixion`부터.**

---

# 2026-09-02 (3) — 밀러 배치 23(Crucifixion~Cut)

## 13. 배치 23 — 20개 · 항목 42개

```
⓪ 이름·별칭 전수 grep
③ verify-dream-cite   항목 42개 · 위반 0건
④ 커버리지            입력 20 ↔ 결과 20 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현           해석 문장 0건
```

기존 상징 셋에 붙였다 — `weeping`(울음) 5→7 · `melon`(오이) 2→5 · `curtain`(장막) 3→5.
밀러 `Crying`·`Cucumber`·`Curtains`가 각각 그 자리다. **한국어 이름이 이미 임자가 있으면
새로 만들지 않는다** — 「울음」·「오이」·「커튼」이 그랬다.

### ⓠ grep 이 피하게 한 이름 넷

| 쓰려던 이름 | 임자 | 대신 쓴 이름 |
| --- | --- | --- |
| 「학대」 | `abuse`(학대) | **「모진 짓」**(Cruelty) |
| 「속임수」 | `cheated`(속임수) | **「꾀」**(Cunning) |
| 「빵 껍질」(띄어) | `bread`(빵)가 걸린다 | **「빵껍질」**(붙여 — 앞이 한글이면 걸러진다) |
| 「십자가 …」 | `cross`(십자가) — 배치 22에서 내가 만든 것 | **「십자가형」·「십자고상」**(뒤가 한글이라 걸러진다) |

넷째가 배치 22의 「왕관」과 같은 자리다 — **바로 앞 배치에서 내가 만든 상징**이 이번 이름을
쥐고 있었다. `Cross`·`Crucifix`·`Crucifixion`이 원문에서 이어 나오니 당연한 일이다.

## 14. ⑨단계 프로브 — 1건

「다른 사람이 **우는 것**을 보았다」가 안 걸렸다. `weeping`의 별칭이 통곡·눈물·흐느·오열뿐
이라 「우는」 꼴이 하나도 없었다. **주공해몽에서 온 옛 상징의 별칭이 밀러 의미를 받으면서
모자라진 자리**다 — 새 상징만 보는 ⓠ 로는 안 보인다.

`verify-dream-km`이 하나(`love-making` ⊃ `making`), `audit-km-dead-words`가 둘
(1글자 「잘」·「냈」)을 잡았다.

## 15. 관문

```
verify-dream-cite 2,686건 위반 0 · verify-dream-km 925개 위반 0 · 커버리지 925/925 ·
audit-cross-symbol-aliases 0쌍 · 나머지 관문 전부 초록불 · probe-miller-batch 42건
전수 스윕  검사기 110개 · 실행 121회 — 통과 119 / 검사 안 됨 7 / 빨간불 2 (앞 판과 같다)
```

## 16. 지금 사전

| | 배치 22 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 908 | **925** |
| 의미 | 2,631 | **2,673** |
| 인용 | 2,644 | **2,686** |
| 밀러 진행 | 562 / 2,264 | **582 / 2,264** (남은 **1,682** · 25.7%) |

**다음은 `Cymbal`부터.**

---

# 2026-09-02 (4) — 밀러 배치 24(Cymbal~Death) · **머리말 문장 하나를 뺐다**

## 17. 배치 24 — 20개 · 항목 39개

```
⓪ 이름·별칭 전수 grep
③ verify-dream-cite   항목 39개 · 위반 0건
④ 커버리지            입력 20 ↔ 결과 20 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현           해석 문장 0건
```

기존 상징 셋에 붙였다 — `dance`(춤) 1→4 · `death`(죽음) 3→5 · `dead-person`(죽은 사람) 15→18.

### 피한 이름 넷

| 쓰려던 이름 | 임자 | 대신 |
| --- | --- | --- |
| 「단검」·「비수」 | `sword`(칼) | **「단도」** |
| 「대추」 | `jujube`(대추) | **「대추야자」**(뒤가 한글이라 걸러진다) |
| 「춤 선생」(띄어) | `dance`(춤)가 걸린다 | **「춤선생」**(붙여) |
| 「낮」 | — | **「대낮」** — 「낮」은 1글자라 **「낮은 목소리」의 「낮은」이 조사 「은」으로 읽혀 걸린다.** 놓치는 쪽을 택했다 |

넷째가 이 배치에서 새로 배운 것이다. `isStandalone`은 한 글자 뒤에 **조사**가 오면 통과시키는데,
「낮은」·「낮이」처럼 **형용사 어간과 조사가 겹치는 한 글자**는 그 규칙에 그대로 걸린다.

## 18. `Dead`의 머리말 문장을 뺐다

밀러 `Dead`는 「To dream of the dead, is usually a dream of warning.」로 시작한다.
이것을 의미로 넣었다가 **뺐다** — 조짐이 아니라 **그 항목 전체를 소개하는 서술**이고,
넣으면 「시신을 봄」과 같은 자리를 두고 다투는 **또 하나의 가장 넓은 의미**가 된다.
이론 문단을 빼 온 기준과 같다. `Death`의 긴 이론 문단도 같은 이유로 뺐다.

## 19. ⑨단계 프로브 — 6건, 그중 하나가 **동점 자리를 드러냈다**

| 무엇 | 원인 |
| --- | --- |
| 「애인이 **춤선생인** 꿈」 안 걸림 | 어미 「인」은 조사가 아니라 걸러진다 — §67과 같은 자리 |
| 「**돌아가신** 아버지/어머니/형제」 셋 다 안 걸림 | `dead-person`의 별칭이 시신·시체·망자·고인·주검뿐이었다 → 「돌아가신」을 올렸다 |
| 「가까운 식구가 **죽어 있는**」·「**죽었다는** 소식」 안 걸림 | `death`의 별칭이 사망·임종·별세뿐 |

별칭 「돌아가신」을 올리자 이번엔 **「돌아가신 어머니를 보았다」가 「시신을 봄」으로 갔다** —
그 문맥의 판별어가 「보았 봤다 보는」이라 어느 문장에서나 1점을 받아 **구체적인 형제와 늘
동점**이었다(§25 곁가지 3). 판별어를 「시신을 시체를 주검을」로 좁혀 풀었다.

**여기서 알아 둘 것** — 별칭에 올린 낱말은 판별어에서 죽지만(§29 곁가지), **「시신을」처럼
조사가 붙은 꼴은 죽지 않는다.** `ownTerms`는 낱말을 **정확히 같은지**로만 거른다.

## 20. 관문

```
verify-dream-cite 2,725건 위반 0 · verify-dream-km 942개 위반 0 · 커버리지 942/942 ·
audit-cross-symbol-aliases 0쌍 · 나머지 관문 전부 초록불 · probe-miller-batch 39건
전수 스윕  검사기 110개 · 실행 121회 — 통과 119 / 검사 안 됨 7 / 빨간불 2 (앞 판과 같다)
```

## 21. 지금 사전

| | 배치 23 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 925 | **942** |
| 의미 | 2,673 | **2,712** |
| 인용 | 2,686 | **2,725** |
| 밀러 진행 | 582 / 2,264 | **602 / 2,264** (남은 **1,662** · 26.6%) |

**다음은 `Debt`부터.**

---

# 2026-09-02 (5) — 밀러 배치 25(Debt~Diamonds) · **밀러가 「See Boat」이라 해서 boat 에 붙였다가 되돌렸다**

## 22. 배치 25 — 20개 · 항목 46개

```
⓪ 이름·별칭 전수 grep
③ verify-dream-cite   항목 46개 · 위반 0건
④ 커버리지            입력 20 ↔ 결과 20 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현           해석 문장 0건 (`chastity`는 인용문에만)
```

기존 상징 하나에 붙였다 — `deer`(사슴) 2→5.

## 23. `Deck`을 `boat`에 붙였다가 **판별어가 통째로 겹쳐 되돌렸다**

밀러 `Deck` 본문은 「배 위에 있는데 폭풍우가 몰아친다 / 바다가 잔잔하다」이고 항목 끝에
**`[54] See Boat.`**가 붙어 있다. 그 지시대로 `boat`(배)에 붙였더니 `verify-dream-km`이
**일곱 자리**를 냈다 — `boat`에는 이미 주공해몽의 「폭풍우 치는 물에서…」·「맑은 물 위의
배를 봄」·「물이 사납고 거침」이 있어서 **폭풍·잔잔·바람이 전부 겹쳤다.**

되돌리고 **`deck`(갑판)을 따로 세웠다.** 밀러의 표제어가 `Deck`이므로 그것이 원문에 더
가깝고, 「갑판」은 겹치지 않는 이름이다.

→ **원문이 「See X」라고 해도 그것이 「X에 붙이라」는 뜻은 아니다.** 우리 사전에 이미
그 자리를 채운 의미가 있으면 **판별이 불가능해진다** — 붙일지 가를지는 **기존 의미와의
겹침**으로 정한다.

## 24. 이름 셋을 넘겨받고 셋을 피했다

| 이름 | 쥐고 있던 곳 | 어떻게 |
| --- | --- | --- |
| 「책상」 | `table`(탁자) | **`desk`(책상)로 넘겼다**(§28) |
| 「기쁨」 | `joy`(기쁨) | 피해서 **「흐뭇함」**(Delight) |
| 「증서」 | `document`(문서) | 피해서 **「땅문서」**(Deed) |
| 「보석」 | `jade`(옥) | 피해서 **「다이아몬드」** |
| 「빚」 | — | **「채무」로 갔다.** 「빚」은 「송편을 빚은」의 「빚은」이 조사 「은」으로 읽혀 걸린다 — §17의 「낮」과 같은 함정 |
| 「보관」 | — | **「금관」으로 갔다.** 「보관(寶冠)」은 「보관(保管)」과 같은 글자다 |

## 25. ⑨단계 프로브 — 3건, 그리고 **「~였다」 자리를 하나 적어 둔다**

| 무엇 | 원인 |
| --- | --- |
| 「빚에 **쪼들려** 시달렸다」 안 걸림 | 별칭이 「빚에 쪼들」이라 뒤에 어미가 붙자 걸러졌다 → 「빚에」·「빚이」를 올렸다 |
| 「무덤을 하얀 꽃으로 **꾸민** 것을」 안 걸림 | 「꾸몄다」·「꾸미는」뿐이었다 → 「꾸민」·「꾸며」 |
| 「…훔친 **다이아몬드였다**」 안 걸림 | **이름 뒤의 「였다」는 조사가 아니라 걸러진다.** 이것은 상징마다 별칭을 늘려 막을 일이 아니라 **엔진의 성질**이므로, 시험 문장을 「다이아몬드를 훔쳤다」로 바꾸고 **여기 적어만 둔다** |

셋째가 §2-4에 적은 구조적 한계 목록에 하나 더 붙는 것이다 — **1글자 · 띄어쓴 꼴 · 명사 뒤 「~였다/이었다」.**

## 26. 관문

```
verify-dream-cite 2,771건 위반 0 · verify-dream-km 961개 위반 0 · 커버리지 961/961 ·
audit-cross-symbol-aliases 0쌍 · 나머지 관문 전부 초록불 · probe-miller-batch 46건
전수 스윕  검사기 110개 · 실행 121회 — 통과 119 / 검사 안 됨 7 / 빨간불 2 (앞 판과 같다)
```

## 27. 지금 사전

| | 배치 24 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 942 | **961** |
| 의미 | 2,712 | **2,758** |
| 인용 | 2,725 | **2,771** |
| 밀러 진행 | 602 / 2,264 | **622 / 2,264** (남은 **1,642** · 27.5%) |

**다음은 `Dice`부터.**

---

# 2026-09-02 (6) — 밀러 배치 26(Dice~Docks) · **프로브가 13건, 그중 열이 활용형**

## 28. 배치 26 — 20개 · 항목 54개

```
⓪ 이름·별칭 전수 grep
③ verify-dream-cite   항목 54개 · 위반 0건
④ 커버리지            입력 20 ↔ 결과 20 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현           `maimed`가 **내 해석 문장에 있었다** → 인용문에만 두고 해석에서 뺐다
```

기존 상징 둘에 붙였다 — `soil`(흙) 2→5(Dirt) · `plate`(접시) 1→5(Dish).
`plate`는 의미가 하나뿐이라 판별어 표가 비어 있었다 — 다섯이 되어 전부 채웠다.

### 피한 이름 다섯

「병」(`illness`) → **「병듦」** · 「망신」(`humiliation`) → **「불명예」** ·
「말다툼」(`fight`·`quarrel`) → **「언쟁」** · 「상속」(`bequest`) → **「상속에서 밀려남」** ·
「흙」·「접시」는 임자에게 **붙였다**(새로 만들지 않았다).

## 29. ⑨단계 프로브가 **13건** — 이번 배치에서 가장 컸다

열 건이 **활용형**이고, 그 안에 새로 배운 것이 하나 있다.

```
「땅을 파는데」   별칭 「땅을 파는」 + 「데」   → 「데」는 조사가 아니다
「낯을 잃었다」   별칭 「낯을 잃었」 + 「다」   → **「다」도 조사가 아니다**
「잠수하는」     term_ko 「잠수」 + 「하」     → 「하」도 아니다
```

**「~았/었」까지만 올리면 모자란다.** 뒤에 오는 「다」가 조사가 아니라 `isStandalone`이
낱말 조각으로 버리기 때문이다. **「낯을 잃었다」처럼 「다」까지 붙인 완전한 꼴**을 올려야
걸렸다. §25의 「완전한 활용형」이 여기서 **「다」까지**라는 뜻으로 드러났다.

그리고 동점 하나 — 「여자가 어려움에 놓였다」가 **넓은 쪽**(「어려움에 놓임」)으로 갔다.
그 문맥의 판별어에 「놓였」이 있어 어느 문장에서나 1점을 받았다. 넓은 쪽에서 뺐다.

`audit-km-dead-words`가 **15건**을 잡았다 — 거의 다 **판별어에 띄어쓰기를 넣어 생긴
1글자 토막**이다(「텅 빈」→「텅」·「빈」, 「못 받」→「못」·「받」, 「서 있」→「서」·「있」).
**판별어에 공백을 넣으면 두 낱말로 쪼개진다** — 한 낱말로 붙여 적을 것.

## 30. 관문

```
verify-dream-cite 2,825건 위반 0 · verify-dream-km 979개 위반 0 · 커버리지 979/979 ·
audit-cross-symbol-aliases 0쌍 · 나머지 관문 전부 초록불 · probe-miller-batch 54건
전수 스윕  검사기 110개 · 실행 121회 — 통과 119 / 검사 안 됨 7 / 빨간불 2 (앞 판과 같다)
```

## 31. 지금 사전

| | 배치 25 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 961 | **979** |
| 의미 | 2,758 | **2,812** |
| 인용 | 2,771 | **2,825** |
| 밀러 진행 | 622 / 2,264 | **642 / 2,264** (남은 **1,622** · 28.4%) |

**다음은 `Doctor`부터.**

---

# 2026-09-02 (7) — 밀러 배치 27a(Doctor~Doomsday) · **원문이 커서 반으로 갈랐다**

## 32. 왜 27a·27b 로 갈랐나

`Doctor`~`Dropsy` 스무 표제어의 원문이 유난히 크다 — `Dogs` 3,016자 · `Donkey` 2,660자 ·
`Doves` 1,173자 · `Door` 1,116자. 한 배치로 넣으면 항목이 80개를 넘고, 한 상징 안의 형제가
스물일곱이 되어 판별어를 가를 수 없다. **앞선 `mb4a`/`mb4b`·`mb7a`/`mb7b` 예대로 갈랐다.**

- **27a** = `Doctor`~`Doomsday` 7개 (이번)
- **27b** = `Door`~`Dropsy` 13개 (다음)

## 33. 배치 27a — 7개 · 항목 43개

```
③ verify-dream-cite   항목 43개 · 위반 0건
④ 커버리지            입력 7 ↔ 결과 7 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현           `vice`·`lewd` — 둘 다 인용문에만
```

기존 상징 둘에 붙였다 — **`dog`(개) 3→27** · **`donkey`(당나귀) 6→15**.
`dog`는 이 사전에서 가장 의미가 많은 상징이 됐다.

## 34. **파일 이름이 규칙에 안 맞아 통째로 안 읽혔다**

매칭 키를 추출 파일과 맞춰 `kmm27a.json`으로 지었더니 **조립기가 그 파일을 읽지 않았다** —
km 파일 이름 규칙은 `kmm<숫자>.json`이고 글자 꼬리를 안 받는다. 새 상징 다섯이 매칭 키
없이 사전에 들어갔다.

**잡은 것은 `verify-dream-km`의 커버리지다** — 「상징 984개 중 키 있음 979개」. §5·§24가
말한 그 자리이고, **커버리지 대조가 없었으면 조용히 지나갔을 것**이다. `kmm27.json`으로
이름을 바꾸자 곧바로 984/984가 됐다.

→ **추출 파일은 `m27a`로 갈라도 매칭 키 파일은 숫자만 쓴다.**

## 35. ⑨단계 프로브 — 6건, 전부 **개를 부르는 말**이 없어서

```
「사냥개가」 「애완견을」 「맹견을」 「광견이」 「미친개에게」
```

다섯 다 `dog`에 **하나도 안 걸렸다.** 「개」가 앞 글자에 붙어 낱말 조각으로 걸러지거나
(사냥개·미친개), 아예 「견」이라 없다(애완견·맹견·광견). **한자어로 부르는 이름을 별칭에
올려야 한다** — 일곱을 더했다(사냥개·애완견·맹견·광견·미친개·삽살개·똥개).

그리고 **별칭에 올린 낱말은 판별어에서 죽으므로**(§29 곁가지) 그 넷의 판별어를 함께 갈았다.

## 36. 회귀 하니스 둘이 깨졌고, 그것이 옳았다

`dog`에 스물넷을 붙이자 「개 — 온순」·「개 — 물림」 두 자리에서 **ko와 en이 다른 의미를
골랐다.** 새 의미들이 옛 판별어보다 센 점수를 받은 것이다.

고침은 **기대치가 아니라 데이터**다 — 「개가 다정하게 굶」에 「온순」과 `gentle`을,
「개에게 물림」에 「물렸」과 `bit`를 넣어 **하니스가 기대하던 자리를 되찾았다.**
그 대가로 「미친개에게 물렸다」는 이제 「개에게 물림」으로 간다(둘 다 물린 것이니 틀리지
않다). 프로브 문장을 「광견에게 물려 침을 흘렸다」로 바꿔 그 갈래를 따로 지킨다.

## 37. 관문

```
verify-dream-cite 2,868건 위반 0 · verify-dream-km 984개 위반 0 · 커버리지 984/984 ·
audit-cross-symbol-aliases 0쌍 · 나머지 관문 전부 초록불 · probe-miller-batch 43건 ·
verify-dream-context-parity 실패 0
전수 스윕  검사기 110개 · 실행 121회 — 통과 119 / 검사 안 됨 7 / 빨간불 2 (앞 판과 같다)
```

## 38. 지금 사전

| | 배치 26 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 979 | **984** |
| 의미 | 2,812 | **2,855** |
| 인용 | 2,825 | **2,868** |
| 밀러 진행 | 642 / 2,264 | **649 / 2,264** (남은 **1,615** · 28.7%) |

**다음은 `Door`부터 — `mb27b.json`이 이미 만들어져 있다.**

---

# 두 번째 판 — 재부팅으로 끊긴 뒤 (배치 27b)

밤사이 작업이 배치 27a 커밋(02:05)에서 끊겼다. PC가 재부팅됐고, 작업 트리는 깨끗했으며
**커밋 여덟이 로컬에만 있었다**(§26 — 커밋했다 ≠ 배포됐다). 먼저 그 여덟을 푸시했다
(`2334d7b..09da979`). 잃은 것은 없었다.

## 39. 사용자가 물 하나를 짚었는데, 재 보니 **다섯이었다**

배치 27b의 밀러 `Drinking`에는 「맑은 물을 마시려 해도 마시지 못함」이 있다. `water`에
붙이려는데, `m27b.json`이 `r4/r6/r9.json`보다 **먼저 정렬**되므로 그 의미가 **물의 첫
의미**가 된다. 그런데 `chooseMeaning`의 `pickByScore`는 판별어 점수가 전부 0이면
`from[0]`을 돌려준다 — **첫 의미가 곧 그 상징의 기본값**이다.

고치기 전에 **대조군부터 세웠다**(§3). `probe-water-fallback.ts`로 물 다섯 문장을 재니
전부 「물을 마심」(길)으로 떨어졌다. 조립해 보니 **다섯 다 「마시지 못함」(흉)으로 뒤집혔다.**

그리고 **물만이 아니었다.** 덮어쓰기 전 사전과 대조하니 다섯이었다.

```
물     「물을 마심」(길)            → 「맑은 물을 마시려 해도 마시지 못함」(흉)
문     「문이 높고 큼」(길)          → 「문으로 들어감」(흉)
용     「용을 타고 하늘에 오름」(길) → 「용을 봄」(흉)
술     「남이 권하는 술을 마심」(길) → 「여성이 흥청거리며 술을 마심」(흉)
비둘기 「비둘기가 욺」(흉)           → 「비둘기가 짝을 짓고 둥지를 지음」(길)
```

**tsc·검사기 아홉·회귀 하니스가 전부 초록불이다.** 화면에 뜨는 답만 바뀐다. 그리고 이것은
이번에 처음 난 것이 아니다 — **밀러 배치 스물일곱 판 내내 그랬고 아무도 안 쟀다.**

## 40. 고침 — 목록으로 얼리고, 관문을 함께 남겼다

`build-dream-dictionary-v2.mjs`에 둘을 넣었다.

- **`FALLBACK_FIRST`** — 상징 id → 기본값으로 쓸 문맥. 그 의미를 맨 앞으로 옮기고, 적어 둔
  문맥이 사라지면 조립을 멈춘다. `EXPLICIT_IDS`가 id에 대해 하던 일과 같다(§11 — 순서가
  정하는 것을 순서에 맡기지 말고 목록으로 얼린다). 위 다섯을 **바뀌기 전 값으로** 못 박았다.
- **「기본값이 바뀐 상징」 관문** — 덮어쓰기 **전의** 사전을 자로 삼아 첫 의미가 바뀐 상징을
  세고 exit 1. 목록에 없는 상징까지 잡으므로 다음 배치가 같은 자리에 빠지지 않는다.

**대조군으로 관문이 진짜인지 봤다** — `FALLBACK_FIRST`를 비운 채 돌리니 정확히 다섯을
찍고 exit 1로 멈췄다(사전은 안 써졌다). 되돌려 돌리니 exit 0, 물 프로브도 5/5 제자리.

관문은 **「바뀐 것이 옳은가」를 판정하지 않는다.** 사람이 보고 목록에 적으라고 멈출 뿐이다 —
판정을 코드에 맡기면 그 관문이 죽는다(§28).

## 41. 배치 27b — 13개 표제어 · 항목 37개

`Door`~`Dropsy`. 붙일 자리를 먼저 갈랐다 — 이미 있는 상징 여덟(door·dove·dragon·water·
liquor·carriage·cab·camel)과 새 상징 아홉.

원문이 「See X」라고 해도 붙일 자리는 **겹침이 정한다**(§26). `Drinking`은 술 이야기와 물
이야기가 한 표제어에 있어 `liquor`와 `water`로 갈랐고, `Driving`은 `carriage`·`cab`·
`wagon` 셋으로 갈랐다. `cab`의 「삯마차를 몲」은 이미 `Cab` 표제어로 있던 자리라 **의미
하나에 인용을 둘로** 붙였다(같은 사건을 두 표제어가 말한다).

**단봉낙타는 `camel`에서 떼어 `dromedary`에 넘겼다** — 「단봉낙타」가 낙타의 별칭이었고
영어 별칭에는 아예 `dromedary`가 들어 있었다. 더 정확한 쪽이 갖는다(§25 곁가지).
같은 이유로 「wagon」을 `cart`에서 떼어 새 `wagon`에 넘겼다.

## 42. 판별어를 처음부터 다시 짰다 — 형제끼리 나눠 가질 수 없다

첫 판은 「넓은 쪽에 낱말을 하나 더 얹어 이기게 한다」로 짰다. `verify-dream-km`이 **42건**을
냈다 — 같은 상징의 두 문맥이 낱말을 공유하거나 한쪽이 다른 쪽의 부분 문자열이면 전부
「동점 위험」이다. 빈 문자열도 막는다.

그러면 남는 수는 하나다 — **좁은 쪽을 파일에서 앞에 두는 것**(§25 곁가지 3에 이미 적혀
있었다). 셋을 그렇게 풀었다.

| 자리 | 어떻게 |
| --- | --- |
| 「흰 비둘기」 ↔ 「흰 비둘기 떼」 | 「하얀」을 나눠 못 가지므로 **떼를 앞으로** |
| 「남이 모는 마차」 ↔ 남자·여자 갈래 | 두루뭉술한 쪽을 **뒤로** |
| 「수레를 몲」 ↔ 「짐수레를 몲」 | 앞뒤로도 못 푼다(수레 쪽이 `m12`라 늘 앞) → **`wagon` 상징을 세워 옮겼다** |

**판별어를 안 적는 것과 빈 문자열은 다르다.** `contextFor`는 `CONTEXT_KO[key] ?? display` —
표에 없으면 **화면 문구를 그대로** 판별어로 쓴다. 물의 새 문맥을 안 적어 두자 그 문구 속
「물을」이 「물을 보았다」에 걸려 기본값이 샜다. 대표 의미에는 **좁은 낱말**을 적는다.

**용은 못 가르는 자리로 남겼다.** 밀러의 용은 「그냥 본 용」이라 한국어로 쓸 말이 「보았」밖에
없는데, 그것을 주면 「용이 하늘로 날아오르는 것을 보았다」에서 형제 열다섯을 전부 가로챈다.
「나타났·나타나·마주쳤」만 주었다 — 「용이 나타났다」로는 걸리고, 「용을 보았다」는 여전히
기본값(「용을 타고 하늘에 오름」)으로 간다. **바꾸려면 사용자 판단이 필요하다**(아래 §45).

## 43. ⑨단계 프로브 — 3건, 검사기 아홉이 전부 초록불일 때

| 문장 | 무엇이 잘못됐나 |
| --- | --- |
| 「남들이 문간을 지나가는 것을 보았다」 | **상징이 아예 안 걸렸다.** 「문간을」은 「문」 뒤에 조사가 아닌 「간」이 와서 `isStandalone`이 낱말 조각으로 버린다 → 「문간」을 **별칭**으로 올리고, 별칭이 된 낱말은 판별어에서 죽으므로 그 자리 판별어에서 뺐다 |
| 「편지를 가져온 비둘기가 지쳐 보였다」 | 「비둘기가 편지를 물어다 줌」으로 갔다 |
| 「비둘기가 가져온 편지에 파멸을…」 | 〃 |

뒤 둘은 같은 원인이다 — 「편지」를 물어다 주는 쪽이 쥐고 있었고 그쪽이 **앞에** 있어서
1점 대 1점 동점으로 가로챘다. **셋 다 편지 이야기이므로 편지는 가르는 말이 아니다** —
아무에게도 주지 않고 물어다·지쳐·파멸만 남겼다.

## 44. 관문

```
verify-dream-cite  2,937건 위반 0  ← 밀러 파일을 인자로 넘겨서. 인자 없이 돌리면
                                     주공해몽 1,178건만 본다(§30 곁가지)
verify-dream-km    993개 위반 0 · 커버리지 993/993 · 판별어 빠진 의미 0
audit-km-dead-words  판별어 낱말 18,447개 · 죽은 낱말 0
audit-cross-symbol-aliases  맞바꿔 쥔 짝 0쌍 (참고 308건)
verify-dream-match / verify-conception-pick / check-guide-examples /
verify-dream-context-parity  전부 통과
probe-miller-batch  37건 · 안 걸림 0 · 다른 뜻 0
probe-water-fallback  5건 · 기본값 제자리
tsc  exit 0
커버리지(입력↔결과)  13/13 표제어 · 원본 텍스트를 세 번째 자로 대조해 빠뜨린 표제어 0
```

## 45. 지금 사전 · 사용자 판단이 필요한 것

| | 배치 27a 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 984 | **993** |
| 의미 | 2,855 | **2,891** |
| 인용 | 2,868 | **2,905** |
| 밀러 진행 | 649 / 2,264 | **662 / 2,264** (남은 **1,602**) |

(사전에 실제로 인용이 실린 밀러 표제어로 세면 646 → **659**다. 앞 판들이 적어 온
「처리한 표제어」 기준과 셋 차이가 나는데, 원문이 참조 지시뿐이라 항목을 못 낸 자리들이다.)

**다음은 `Drouth`부터.**

### 사용자 판단이 필요한 자리 — 기본값 다섯

§40에서 다섯을 **바뀌기 전 값으로** 못 박았다. 「이번 작업은 배치 27b를 넣는 것이지
기존 답을 바꾸는 것이 아니다」로 판단한 것이지, **그 다섯이 옳은 기본값이라서가 아니다.**
둘은 지금도 어색하다.

- **용** — 「용을 보았다」의 답이 「용을 타고 하늘에 오름」이다. 이용자는 탄 적이 없다.
  밀러의 「용을 봄」이 글자 그대로는 맞지만 **흉**이고, 용꿈은 흔히 길몽으로 여겨진다.
- **비둘기** — 「비둘기를 보았다」의 답이 「비둘기가 욺」(부인의 구설)이다.

바꾸려면 `FALLBACK_FIRST` 한 줄씩이면 된다.

---

# 2026-09-02 (8) — 밀러 배치 28(Drouth~Dynamo) · **새 상징을 만들지 않고 임자에게 붙인 자리가 여섯**

## 46. 배치 28 — 표제어 18개 · 항목 40개

`Drouth`부터 `Dynamo`까지, 밀러의 **D 절을 끝냈다.**

```
⓪ 이름·별칭 전수 grep   새 이름 13개 · 계획한 별칭 전부를 기존 term_ko·term_en·aliases 와 대조
③ verify-dream-cite     항목 40개 · 위반 0건
   대조군                인용 한 줄을 위조한 사본으로 exit 1 확인(밀러 갈래를 실제로 지난다, §24)
④ 커버리지              입력 표제어 18 ↔ 결과 locator 18 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현             해석·이름·번역문 0건 — `dumb`·`dwarf` 는 **인용문에만** 남겼다
```

**⑤에서 이름을 두 번 피했다.** 밀러 `Dumb`·`Dwarf`를 한국어 이름으로 옮기면
「벙어리」·「난쟁이」가 되는데 **화면에 뜨는 이름**이다. `말을 못 함`·`키 작은 사람`으로
짓고, 그 낱말들은 **별칭(안 보이는 매칭 키)에만** 올렸다 — 이용자는 그 말로 쓰기 때문이다.

## 47. 새 상징을 만들지 않고 **임자에게 붙인 것이 여섯**

| 밀러 표제어 | 어디에 붙였나 | 왜 |
| --- | --- | --- |
| `Dungeon` | `prison`(감옥) | 「지하 감옥」은 `감옥`을 품는다 — 새 상징으로 세우면 한 문장에 **둘이 함께 뜬다** |
| `Dunghill` | `manure`(거름) | 「거름더미」도 같다 |
| `Drum` | `drum`(북) | 이미 주공해몽으로 있었다 |
| `Ducks` | `duck`(오리) 넷 · `wild-duck`(물오리) 하나 | 「맑은 시내의 물오리」는 물오리 것이다 |
| `Dying` | `death`(죽음) **두 자리만** | 밀러 자신이 「[60] See Death」다. 나머지 셋(자기가 죽음·남이 죽음·죽게 됨)은 **이미 있는 의미와 겹친다** — 겹치는 것은 안 넣었다. 짐승 둘만 새 자리다 |

**「See X」라도 붙일 자리는 겹침이 정한다**(절차에 적혀 있던 것을 이번에도 그대로 썼다).
`Dying`은 다섯 중 셋이 이미 있는 자리였고, 그 셋은 **주공해몽과 길흉이 정반대**다
(자기가 죽음 = 주공해몽 길 / 밀러 흉). **한쪽을 지우지도, 둘 다 넣지도 않았다** — 밀러의
`Death` 표제어는 이미 들어와 있고, `Dying`은 그 표제어를 가리키는 참조이기 때문이다.

`findTerm`은 **상징마다 따로** 돈다 — 전역에서 긴 이름이 이기는 구조가 아니다. 그래서
「지하 감옥」을 새 상징으로 세우면 `prison`도 같이 걸려 화면에 상징이 둘 뜬다. 이름이
남의 이름을 **품는** 새 상징은 세우지 않는다.

## 48. 이미 나 있던 상처 — `duck`이 `wild-duck`의 이름을 쥐고 있었다

`duck`(오리)의 별칭에 **「물오리」·「청둥오리」**가 있었다. 둘 다 `wild-duck`의 이름이다.
이번 배치가 밀러 「wild ducks on a clear stream」을 `wild-duck`에 붙이므로, 그대로 두면
**「맑은 시내에 물오리가 있었다」가 오리로도 걸려** 두 상징이 함께 뜬다(§28).
이름의 임자에게 돌려줬다.

**판별어도 하나 나눠 갖고 있었다.** `duck`의 「오리를 잡음」(주공해몽 捕)이 「사냥」을 쥐고
있어서, 밀러의 「오리를 사냥함」이 들어오면 형제 둘이 같은 낱말을 나눠 갖게 된다
(§30 곁가지 — 그것은 못 한다). 손으로 붙잡는 쪽에서 「사냥」을 빼고 「손으로」를 주었다.

`audit-cross-symbol-aliases` — **맞바꿔 쥔 짝 0쌍**(참고 306건).

## 49. 기본값이 **또 다섯** 뒤집혔다 — 전부 얼렸다

배치 27b에서 세운 관문이 그대로 일했다. 조립기가 exit 1로 멈추며 다섯을 찍었다.

```
북      「북을 침」                  → 「북이 낮게 울리는 소리를 들음」
물오리  「물오리가 집 안으로 들어옴」 → 「맑은 시내에 물오리가 있음」
오리    「오리가 거위와 함께 헤엄침」 → 「흰 오리가 농가 둘레에 있음」
감옥    「감옥이 무너짐」            → 「여성이 지하 감옥에 갇혀 있음」
거름    「땅을 쓸어 거름을 치움」    → 「처녀가 거름더미를 봄」
```

**다섯 다 있던 답으로 얼렸다**(`FALLBACK_FIRST`). 이번엔 판단이 어렵지 않았다 — 밀러 쪽
새 의미가 다섯 다 **「흰 오리가 농가 둘레에」·「여성이 지하 감옥에」처럼 좁은 상황**이라,
아무것에도 안 걸린 꿈이 떨어질 자리가 아니다.

**동점은 차례로 풀었다.** 「여자가 지하 감옥에 갇혀 있었다」는 「여자가」와 「지하」가 1점씩
동점이 된다 — 추출 파일에서 **좁은 쪽(여성·불이 밝혀진)을 앞에** 두어 풀었다. 거름도 같다.
`dust`는 새 상징이라 첫째가 곧 기본값이므로 차례를 못 쓴다 — **일반 쪽 판별어를 그 좁은
문장에 없는 말**(「뒤덮·온몸·옷에」)로 지어 애초에 동점이 안 나게 했다.

## 50. ⑨단계 프로브 — **8건**. 검사기가 전부 초록불일 때

`verify-dream-km`·`audit-km-dead-words`·`verify-dream-cite`가 전부 통과한 뒤였다.
**여덟 다 「상징이 아예 안 걸림」**이고, 뜻이 엉뚱한 것은 없었다.

| 문장 | 왜 안 걸렸나 |
| --- | --- |
| 「물에 빠진 사람을 구하러 뛰어들었다」 | 별칭이 「물에 빠졌다」·「물에 빠져 죽었다」뿐 — **「물에 빠진」이 없었다** |
| 「애인이 물에 빠져 죽는 것을 보았다」 | 〃 (「물에 빠져 죽는」) |
| 「포도주에 취했다」 | 이름이 「술에 취함」이라 **술이 없는 문장**은 못 걸린다 → 「포도주에 취했다/취해」·「와인에…」를 올렸다 |
| 「사나운 맹수가 죽어 가는 것을 보았다」 | `death` 별칭에 「죽어 있」은 있고 **「죽어 가는」이 없었다** |
| 「집짐승이 죽어 가는 것을 보았다」 | 〃 |
| 「거름더미를 보았다」 | **「거름」 뒤의 「더」가 조사가 아니라** `isStandalone`이 낱말 조각으로 버린다 → 「거름더미」를 별칭으로 |
| 「처녀가 거름더미를 보았다」 | 〃 |
| 「친구가 난쟁이처럼 작아진 것을 보았다」 | 「난쟁이가」·「난쟁이를」만 올렸다 — **맨 「난쟁이」**가 있어야 「난쟁이처럼」이 걸린다 |

**맨 이름을 올리는 것을 망설인 자리가 하나 있다.** `bantam`(밴텀 닭)이 별칭으로
「난쟁이 닭」을 쥐고 있어서, `dwarf`에 맨 「난쟁이」를 주면 「난쟁이 닭을 보았다」에 둘이
함께 걸린다. 그래도 **맨 「난쟁이」의 임자는 `dwarf`**다(§25 곁가지 — 겹치면 더 정확한 쪽).
닭 쪽은 「난쟁이 닭」이라는 긴 이름을 그대로 갖는다.

**한 자리는 문장을 고쳤다.** 「친구가 작아진 것을 보았다」에는 상징 이름이 **한 낱말도**
없다. 걸리게 하려면 별칭을 「작아진」으로 넓혀야 하는데 그러면 「옷이 작아진」까지 난쟁이가
된다. 데이터가 아니라 **문장 쪽이 틀린 드문 경우**여서 「난쟁이처럼」을 넣은 말로 바꿨다.

## 51. 관문

```
verify-dream-cite   2,962건 위반 0   ← 밀러·주공해몽 파일을 전부 인자로 넘겨서(§30 곁가지)
  대조군            m28 인용 한 줄 위조 → exit 1 (밀러 갈래가 실제로 검사된다)
verify-dream-km     1,006개 위반 0 · 커버리지 1006/1006 · 판별어 빠진 의미 0
  잡은 것 1건       death [en] 「animal」과 「animals」가 부분 문자열로 겹침 → 갈랐다
audit-km-dead-words 판별어 낱말 18,683개 · 죽은 낱말 0
  잡은 것 4건       prison「dungeon」·drowning「drowned」는 제 별칭이라 0점 · dynamo「out of」는 불용어
audit-cross-symbol-aliases  맞바꿔 쥔 짝 0쌍 (참고 306건)
verify-dream-match / verify-conception-pick / verify-dream-context-parity /
verify-guide-numbers / check-guide-examples   전부 통과
audit-unreachable-meanings  1건(wife) — 앞 판과 같다. 감사라서 빨간불 아님
probe-miller-batch  45건 · 안 걸림 0 · 다른 뜻 0
probe-water-fallback  5건 · 기본값 제자리
tsc  exit 0
전수 스윕  검사기 110개 · 실행 121회 — 통과 119 / 검사 안 됨 7 / 빨간불 2 (**앞 판과 같다**)
```

**곁에서 본 것 하나 — 오늘 만든 것이 아니다.** `scripts/audit-dream-doc-numbers.mjs`는
**폐기된 옛 사전**(`dream-symbols.data.json`, 상징 218·의미 350)을 읽는다. 지금 초록불이지만
그것은 「23개 로케일에 그 숫자가 안 박혀 있다」는 뜻이고, 이 감사기는 **v2를 한 번도 안 봤다.**
v2 쪽 숫자는 `verify-guide-numbers.ts`가 따로 본다. 사실만 적어 둔다.

## 52. 지금 사전

| | 배치 27b 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 993 | **1,006** |
| 의미 | 2,891 | **2,931** |
| 인용 | 2,905 | **2,945** |
| 밀러 표제어 | 659 | **677** / 2,264 (남은 **1,587**) |

(밀러 진행은 **사전에 실제로 인용이 실린 표제어**로 셌다 — 앞 판의 같은 기준 수가 659이고,
이번에 정확히 열여덟이 늘었다.)

**다음은 밀러 `Eagles`부터.** `Drouth`~`Dynamo`로 D 절이 끝났다.

---

# 2026-09-02 (9) — 배치 28을 **배포까지 확인**하고, 밀러 배치 29(Eagles~Elevator)

## 53. 배포 확인 — 커밋·푸시·배포를 셋으로 갈라 쟀다

배치 28을 푸시하기 전에 **대조군을 먼저 만들었다**(§3 — 고치기 전에 그 검사가 빨간불을
내는지 본다).

```
푸시 전   /ko/dream/symbol/dynamo    404   ← 배치 28에만 있다
          /ko/dream/symbol/dromedary 200   ← 배치 27b는 이미 살아 있다
푸시 후   dynamo 200 · 네 앱 모두 6a30ced 로 배포(gh api deployments 로 확인)
```

**404 → 200 이 배포의 증거다.** 「배포 success」만 보면 그것은 「빌드가 끝났다」이지
「내 변경이 산다」가 아니다(§27). 앞뒤로 재면 그 구분이 한 줄로 끝난다.

## 54. 배치 29 — 표제어 19개 · 항목 51개

```
⓪ 이름·별칭 전수 grep   품음 관계까지 봤다(아래 §55)
③ verify-dream-cite     항목 51개 · 위반 0건
④ 커버리지              입력 표제어 19 ↔ 결과 locator 19 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현             해석·이름 0건
```

임자에게 붙인 것 둘 — `being-taught-letters`(글을 배움) ← `Education` 둘 ·
`elephant`(코끼리) ← `Elephant` 넷. **둘 다 의미가 하나뿐이라 판별어 표가 비어 있었다.**
새 상징 열일곱.

## 55. ⓪단계가 **품음 관계**를 잡았다 — 「선거」를 「투표」로 돌렸다

배치 28에서 세운 규칙(새 이름이 남의 이름을 품으면 둘이 함께 걸린다)을 이번엔 **넣기 전에**
썼다. grep 이 낸 것 가운데 실제로 위험한 것은 하나였다.

| 후보 이름 | 관계 | 판정 |
| --- | --- | --- |
| **선거** | `campaign` 의 별칭 **「선거 운동」**이 품는다. 뒤가 **공백**이라 `isStandalone` 을 지난다 | **이름을 「투표」로 돌렸다.** 별칭은 「선거를」·「선거에」·「선거하는」처럼 조사를 붙여 올려, 「선거 운동」에는 안 걸리게 했다 |
| 귀 | `당나귀`·`까마귀`·`귀뚜라미`가 품는다 | **괜찮다** — 앞 글자가 한글이라 `isStandalone` 이 막는다 |
| 전기 | `발전기`·`전기톱`이 품는다 | **괜찮다** — 앞이 한글이거나 뒤가 조사가 아니다 |
| 집게벌레·흑단나무·딱총나무 | 각각 `벌레`·`나무`를 품는다 | **괜찮다** — 앞 글자가 한글이다 |
| 음식을 먹음 | `food`(음식)를 품고 **뒤가 조사(을)** 라 함께 걸린다 | **그대로 뒀다** — 「음식을 먹었다」에 음식이 실제로 나오므로 둘 다 뜨는 것이 옳다 |

**「품는다」가 곧 결함은 아니다.** 앞 글자가 한글이면 `isStandalone` 이 막고, 뒤가 조사면
지난다. **띄어쓰기가 갈림길**이다 — 「선거 운동」만 위험했던 이유가 그것이다.

## 56. 「흰」은 판별어가 될 수 없다 — 기본값 자리로 푼다

`elephant` 에 밀러 넷을 붙이자 의미가 다섯이 됐고, 기존 「흰 코끼리를 봄」에도 판별어가
필요해졌다. **「흰」은 1글자라 엔진이 안 센다**(`scoringWordsOf` 의 `length >= 2`), 그리고
「흰 코끼리」는 공백에서 쪼개져 「흰」이 된다.

→ 그 의미를 **기본값으로 얼렸다**(`FALLBACK_FIRST`). 「흰 코끼리를 보았다」는 다른 판별어에도
안 걸리므로 **0점으로 제자리에 떨어진다.** 1글자 한계를 기본값 자리로 우회한 첫 사례다.

기본값이 바뀐 상징은 이번에 둘이었고(`being-taught-letters`·`elephant`) **둘 다 있던 값으로**
얼렸다.

## 57. `audit-km-dead-words` 가 다섯을 잡았고, 그중 하나는 **판별어가 통째로 죽은 자리**였다

```
⚠ elephant [en] 「흰 코끼리를 봄」 살아 있는 판별어가 0개다
  elephant [en] 죽은 낱말: white          ← 별칭 `white elephant` 때문에 제 이름이 됐다
  being-taught-letters [en] taught        ← 별칭 `taught to read`
  eating [en] with                        ← 불용어
  eel [ko] 꼭 · elevator [ko] 서          ← 1글자
```

**`white elephant` 를 영어 별칭에서 뺐다.** 「elephant」 하나로 이미 걸리는데, 그것이 있으면
「white」가 제 이름이 되어 그 의미의 영어 판별어가 **하나도 남지 않는다.**
별칭을 늘리는 것이 판별을 **줄일 수 있다**는 것을 영어 쪽에서 실물로 봤다.

「서 있는」은 **공백 때문에 쓸 수 없다**(「서」가 1글자로 죽는다). 「멈춰·멎은·서있·정지」로
갈았고, 「승강기가 서 있었다」는 못 걸린다는 것을 알고 남긴다.

## 58. ⑨단계 프로브 — **7건**(안 걸림 6 · 다른 뜻 1)

| 문장 | 무엇이 잘못됐나 |
| --- | --- |
| 「배우고 싶은 마음이 간절해 애썼다」 | `being-taught-letters` 이름이 「글을 배움」뿐 — **사람이 쓰는 말이 별칭에 하나도 없었다** |
| 「학교에서 배우고 있었다」 | 〃 |
| 「스승이 글을 가르쳐 주었다」 | 〃 → 「배움·배우고·배우는·공부를·공부하·글을 가르쳐」를 올렸다 |
| 「다 먹기도 전에 딸이 접시를 치웠다」 | `eating` 별칭이 「먹었다」 꼴뿐 → 「먹다가·먹는 중·먹기도 전」 |
| 「알이 든 둥지를 찾았다」 | 맨 「알」은 못 올린다(쌀알·옥수수알…) → 「알이 든·알을 품」 |
| 「새알을 보았다」 | 판별어가 「새의」라 안 걸렸다 → 「새알 들새 산새」 |
| 「승강기가 내려가고 나만 남겨졌다」 | **다른 뜻** — 「내려가」와 「남겨졌」이 1점씩 **동점**이라 앞의 「내려감」이 이겼다 → 좁은 쪽을 파일에서 **앞으로** 옮겼다(§30 곁가지) |

**「배우고싶」을 붙여 적은 것이 판별어에서 안 걸린 것**도 같은 판에서 나왔다 — 사람은
「배우고 싶은」이라고 띄어 쓴다. 판별어는 **사람 말의 띄어쓰기를 따라야** 한다.

## 59. 관문

```
verify-dream-cite   3,013건 위반 0   ← 밀러·주공해몽 전부 인자로
verify-dream-km     1,023개 위반 0 · 커버리지 1023/1023 · 판별어 빠진 의미 0
audit-km-dead-words 판별어 낱말 18,963개 · 죽은 낱말 0 (다섯을 잡아 갈았다)
audit-cross-symbol-aliases  맞바꿔 쥔 짝 0쌍 (참고 308건)
verify-dream-match / conception-pick / context-parity / guide-numbers /
check-guide-examples   전부 통과
audit-unreachable-meanings  1건(wife) — 앞 판과 같다
probe-miller-batch  54건 · 안 걸림 0 · 다른 뜻 0
probe-water-fallback  5건 · 기본값 제자리
tsc  exit 0
전수 스윕  검사기 110개 · 실행 121회 — 통과 119 / 검사 안 됨 7 / 빨간불 2 (**앞 판과 같다**)
```

## 60. 지금 사전

| | 배치 28 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,006 | **1,023** |
| 의미 | 2,931 | **2,982** |
| 인용 | 2,945 | **2,996** |
| 밀러 표제어 | 677 | **696** / 2,264 (남은 **1,568**) |

**다음은 밀러 `Elixir of Life` 부터.**

---

# 2026-09-02 (10) — 배치 29 배포 확인 · 밀러 배치 30(Elixir of Life~Engineer)

## 61. 배포 확인

푸시 전 대조군 → 푸시 후. **셋 다 404 → 200.**

```
elevator · eagle · eclipse   푸시 전 404 → 푸시 후 200 (96a6a25)
```

## 62. 배치 30 — 표제어 19개 · **항목 43개**(표제어 하나는 원문에 내용이 없다)

```
⓪ 이름·별칭 전수 grep   품음 관계 일곱을 봤고 **위험한 것은 0개**였다(아래 §63)
③ verify-dream-cite     항목 43개 · 위반 0건
④ 커버리지              입력 19 ↔ 결과 18 · **빠뜨림 1(Embarrassment)**
⑤ 민감 표현             해석·이름 0건
```

**빠뜨림 1건은 원문을 열어 보고 넘겼다**(§24 — 「없다」를 안 찾고 단정하지 말 것).

```
Embarrassment 의 원문 전문(20자):  [62] See Difficulty.
```

**참조 지시뿐이고 내용이 없다.** 그리고 그 `Difficulty` 는 **이미 사전에 있다**(`difficulty`
(어려움) 의미 넷). `Barrel`(배치 11)과 같은 자리다.

## 63. ⓪단계 — 품음 관계 일곱, 위험한 것은 **하나도 없었다**

배치 29에서 「띄어쓰기가 갈림길」이라고 적어 둔 것을 그대로 썼다.

```
야반도주 ⊃ 도주(fleeing)          앞이 한글 → 막힌다
염습판(cooling-board) ⊃ 염습       뒤가 「판」 → 막힌다
녹보석 ⊃ 보석(jade)                앞이 한글 → 막힌다
백과사전 ⊃ 사전(dictionary)        앞이 한글 → 막힌다
멋부리는 사람(coxcomb) ⊃ 부리는 사람  앞이 한글 → 막힌다
법원 직원(bailiff) ⊃ 직원           **뒤가 공백 → 걸린다.** 그대로 뒀다 —
                                   법원 직원도 「부리는 사람」이 맞다
거래 약속 ⊃ 거래(trading)          걸린다 → **그 별칭을 아예 안 올렸다**
```

**「황제」·「황후」는 이미 임자가 있었다.** `king`(임금)이 「황제」를, `queen`(왕비)이
「황후」를 별칭으로 쥐고 있다 — 그래서 밀러 `Emperor`·`Empress` 를 **새 상징으로 세우지
않고 그 둘에 붙였다.**

**`Enemy` 는 다섯 중 둘만 넣었다.** 「적을 이겨냄」·「적과 맞섬」은 밀러 `Adversary` 로
이미 들어와 있다. 겹치는 것은 넣지 않는다.

**`Engagement` 는 상징을 둘로 갈랐다.** 밀러의 한 낱말이 한국어로는 **약혼**과 **사업상
약속** 둘이다. 한 상징에 두면 이름이 어느 쪽도 못 가리킨다.

## 64. ⑨단계 프로브 — **8건**, 그리고 그중 셋은 **이미 나 있던 상처**

**여덟 다 「상징이 아예 안 걸림」이다.** 그리고 여섯이 **낱말 경계**에서 끊긴 것이었다.

| 문장 | 왜 안 걸렸나 |
| --- | --- |
| 「말을 잘해서 술술 나왔다」 | 별칭 「말을 잘」 뒤에 **「해」**가 붙어 낱말 조각이 된다 |
| 「여자가 수를 놓고 있었다」 | 「수를 놓」 뒤에 **「고」** |
| 「마법에 홀려 있었다」·「마법을 뿌리쳤다」·「남을 홀리려 했다」 | 이름이 「마법에 홀림」뿐 — 사람이 쓰는 꼴이 하나도 없었다 |
| **「적에게 험담을 들었다」·「적에게 졌다」·「적을 이겼다」** | **`adversary`(적수)의 별칭이 적수·원수·맞수·경쟁자·대적뿐이었다** |

**마지막 셋이 이번 배치가 만든 것이 아니다.** 밀러 `Adversary` 는 여러 판 앞에 들어왔고,
그때부터 **「적을 이겼다」가 한 번도 안 걸리고 있었다.** 오늘 그 상징에 의미를 붙이면서
프로브를 돌렸기 때문에 드러났다 — `ox`의 「송아지」, `money`의 「현금」과 같은 자리다.

맨 **「적이」·「적을」은 별칭으로 올리지 않았다** — 「본 적이 있다」·「일기를 적을」에
걸린다. 대신 「적에게·적과·적군·적들」과 **「적을 이겼다」처럼 「다」까지 붙인 꼴**을 올렸다.

**「적을 이겼」까지만 올렸을 때 여전히 안 걸렸다** — 뒤의 「다」가 조사가 아니기 때문이다
(§29 곁가지 ①, 이번이 네 번째 재발). 별칭은 **「다」까지** 붙이거나 **공백 앞에서 끝나야**
한다(「적에게」는 뒤가 공백이라 산다).

## 65. 검사기가 잡은 것 — 동점 위험 셋 · 죽은 낱말 일곱

```
verify-dream-km      married ⊂ unmarried · woman ⊃ man · 걸려 ⊂ 걸려고
audit-km-dead-words  some one · without · out work · spell · she  (전부 [en])
```

`spell` 은 `under a spell` 이 제 별칭이라 죽었고, `work` 는 `out of work` 때문이다 —
**영어 별칭이 판별어를 먹는 자리가 배치 29에 이어 또 나왔다.**

## 66. 관문

```
verify-dream-cite   3,056건 위반 0
verify-dream-km     1,039개 위반 0 · 커버리지 1039/1039 · 판별어 빠진 의미 0
audit-km-dead-words 죽은 낱말 0
audit-cross-symbol-aliases  맞바꿔 쥔 짝 0쌍
verify-dream-match / conception-pick / context-parity / guide-numbers /
check-guide-examples / probe-water-fallback   전부 통과
audit-unreachable-meanings  1건(wife) — 앞 판과 같다
probe-miller-batch  46건 · 안 걸림 0 · 다른 뜻 0
tsc  exit 0
전수 스윕  통과 119 / 검사 안 됨 7 / 빨간불 2 (**앞 판과 같다**)
```

기본값이 바뀐 상징 둘(`king`·`queen`) — **둘 다 있던 값으로** 얼렸다.

## 67. 지금 사전

| | 배치 29 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,023 | **1,039** |
| 의미 | 2,982 | **3,025** |
| 인용 | 2,996 | **3,039** |
| 밀러 표제어 | 696 | **714** / 2,264 (남은 **1,550**) |

**다음은 밀러 `English` 부터.**

---

# 2026-09-02 (11) — 배치 30 배포 확인 · 밀러 배치 31(English~Eyeglass) · **E 절 끝**

## 68. 배포 확인

`engineer` 푸시 전 404 → 푸시 후 **200**(`658df50`).

## 69. 배치 31 — 표제어 23개 · 항목 55개. 밀러 **E 절이 끝났다**

```
⓪ 이름·별칭 전수 grep   임자가 이미 있는 이름 여섯을 찾았다(아래 §70)
③ verify-dream-cite     항목 55개 · 위반 0건
④ 커버리지              입력 23 ↔ 결과 23 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현             해석·이름 0건
```

새 상징 열일곱 · 임자에게 붙인 것 여섯.

## 70. ⓪단계가 **여섯을 붙이게 했다** — 이름을 이미 남이 쓰고 있었다

| 밀러 표제어 | 어디에 붙였나 | 이미 누가 쥐고 있었나 |
| --- | --- | --- |
| `Envelope` | `letter`(편지) | **「봉투」가 편지의 별칭**이었다 |
| `Exile` | `banishment`(추방) | **「귀양」이 추방의 별칭**이었다 |
| `Escape` | `fleeing`(달아남) | **「탈출」이 달아남의 별칭**이었다 |
| `Eve` | `adam-and-eve`(아담과 이브) | 상징이 이미 있었다 |
| `Eyebrows` | `eyebrows`(눈썹) | 〃 |
| `Estate` | `bequest`(유산) | **이름이 문제였다** — 「재산」은 `wealth` 의 별칭이고 「너른 땅」은 아무도 안 치는 말이다 |

**`Estate` 가 새 갈래다.** 앞의 다섯은 「이 이름을 누가 쥐고 있나」로 갈렸는데, 이것은
**쓸 수 있는 이름이 아예 없어서** 붙였다. 「재산」을 쓰면 남의 것을 뺏고, 「너른 땅」을
쓰면 이용자가 그 말로 쓰지 않아 **아무에게도 안 걸리는 상징**이 된다.

## 71. 「눈」이 겹쳤다 — 조립기가 멈춰 세웠고 `LABEL_KO` 에 적었다

밀러 `Eye` 를 넣자 **`snow`(눈)과 한국어 이름이 같아졌다.** 조립기가 exit 1로 멈췄다.

```
「눈」 ← id=eye  (의미 6개)
「눈」 ← id=snow (의미 5개)
```

**배(선박·과일·복부)·산(山·산성)과 같은 자리다.** `LABEL_KO` 에 「눈(眼)」·「눈(雪)」을
적었다. **`term_ko` 는 괄호 없이 「눈」 그대로 둔다** — 매칭 키가 그것을 쓰기 때문이다.
곧 「눈이 아팠다」는 두 상징에 다 걸리고, 화면에서만 갈린다. 한국어 동음이의어는
엔진이 원리적으로 못 가른다.

## 72. ⑨단계 프로브 — **13건**, 이번 세션에서 가장 컸다

**아홉이 낱말 경계**다. 어제까지의 판과 같은 병이고, 이번에는 **한 판에 아홉**이 나왔다.

```
「시샘」  + 했다   ✗    「물려받」 + 았다  ✗    「맞바꾸」 + 었다 ✗
「처형」  + 되는   ✗    「벗어나려」+ 다   ✗    「처형당하려」+ 다 ✗
```

**「~려」로 끝낸 별칭도 「다」에 걸린다** — 「벗어나려」·「처형당하려」를 올렸는데 여전히
안 걸려서 「벗어나려다」·「처형당하려다」로 다시 고쳤다. **뒤에 오는 것이 조사도 공백도
아니면 죽는다**는 규칙에 예외가 없다.

**`bequest` 의 「물려받」은 이미 나 있던 상처다** — 「물려받았다」가 한 번도 안 걸리고
있었다. 어제 `adversary` 의 「적을 이겼다」와 같은 자리다. **프로브가 아니면 아무도 못 본다.**

셋은 **문장 쪽이 틀렸다** — 「얼굴이 검게 그을렸다」·「연기와 잔해가 자욱했다」·「불길에
휩싸였다」에는 `explosion` 의 이름이 한 낱말도 없다. 「폭발로 …」를 앞에 붙였다.

하나는 **동점**이었다 — 「처녀가 제가 이브가 된 꿈」이 「이브를 봄」에 졌다. 「이브가」를
두 의미가 나눠 갖고 있었기 때문이다(§30 곁가지). 넓은 쪽에서 뺐다.

## 73. 관문

```
verify-dream-cite   3,111건 위반 0
verify-dream-km     1,056개 위반 0 (「판별어에 상징 이름 그대로」 1건을 잡았다)
audit-km-dead-words 죽은 낱말 0 (넷을 잡아 갈았다 — bequest·legacy·your·being)
audit-cross-symbol-aliases  맞바꿔 쥔 짝 0쌍
verify-dream-match / conception-pick / context-parity / guide-numbers /
check-guide-examples / probe-water-fallback   전부 통과
probe-miller-batch  57건 · 안 걸림 0 · 다른 뜻 0
tsc  exit 0
전수 스윕  통과 119 / 검사 안 됨 7 / 빨간불 2 (**앞 판과 같다**)
```

기본값이 바뀐 상징 다섯(`letter`·`fleeing`·`bequest`·`banishment`·`eyebrows`) —
**전부 있던 값으로** 얼렸다. `FALLBACK_FIRST` 가 열아홉이 됐다.

## 74. 지금 사전

| | 배치 30 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,039 | **1,056** |
| 의미 | 3,025 | **3,080** |
| 인용 | 3,039 | **3,094** |
| 밀러 표제어 | 714 | **737** / 2,264 (남은 **1,527**) |

**다음은 밀러 `Fables` 부터.** `English`~`Eyeglass` 로 E 절이 끝났다.

---

# 2026-09-02 (12) — 배치 31 배포 확인 · 밀러 배치 32(Fables~Father-in-law)

## 75. 배포 확인

`eyeglass`·`eye`·`evening` 푸시 전 404 → 푸시 후 **200**(`24a5dee`).

## 76. 배치 32 — 표제어 22개 · 항목 53개 · **새 의미는 51개**

```
⓪ 이름·별칭 전수 grep   임자가 있는 이름 넷 + 「내가 잘못 갖고 있던 것」 하나(아래 §77)
③ verify-dream-cite     항목 53개 · 위반 0건
④ 커버리지              입력 22 ↔ 결과 22 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현             해석·이름 0건
```

**항목 53개인데 의미는 51개만 늘었다** — 둘은 새 의미가 아니라 **인용만 덧붙었다**(§78).

## 77. `Fat` 을 **인용만 덧붙여** 넣었다 — 새 기법

밀러 `Fat` 은 본문이 「See Corpulent」이고, 두 문장이 **이미 있는 의미와 같은 사건**이다.

```
Fat        To dream that you are getting fat …   ↔  corpulence 「제 몸이 뚱뚱해짐」
           To see others fat …                   ↔  corpulence 「남이 뚱뚱한 것을 봄」
```

**버리면 커버리지가 빠지고**(`Embarrassment` 처럼 「원문에 내용이 없다」가 아니다 — 내용은
있다), **새 의미로 넣으면 사전에 같은 말이 둘이 된다.**

조립기를 다시 읽어 보니 답이 이미 있었다 — **`context` 와 `work` 가 같으면 인용을
덧붙인다**(`build-dream-dictionary-v2.mjs` 의 `same` 갈래). 그래서 `m32.json` 에
**기존 문맥 이름 그대로** 적었고, 의미는 안 늘고 **인용이 둘 늘었다.** 화면은 한 의미 아래
밀러 인용 둘(`Corpulence`·`Fat`)을 보여 준다.

→ **「같은 사건인데 원문이 둘」이면 셋째 길이 있다** — 버리지도, 새로 만들지도 않고
**같은 문맥 이름으로 적어 인용을 포갠다.** 두 원문이 다를 때(주공해몽 vs 밀러)는 `work` 가
달라 안 포개진다 — 그것도 조립기가 이미 그렇게 막아 두었다.

**`Face` 의 첫 문장은 아예 안 넣었다** — 「밝은 얼굴 / 일그러진 얼굴」은 밀러 `Countenance`
로 이미 들어와 있다(`countenance`(얼굴 생김새)의 두 의미). 문맥 이름이 달라 포갤 수 없고,
넣으면 같은 말이 둘이 된다.

## 78. 「송골매」를 **잘못 갖고 있던 것**을 돌려줬다

`sparrowhawk`(새매)가 별칭으로 **「송골매」**를 쥐고 있었다. 송골매는 falcon 이지 새매가
아니다. 밀러 `Falcon` 이 들어오면서 드러났고, `falcon`(송골매)에게 넘겼다.

**이것은 「새 상징이 남의 이름을 가로챈다」의 거울상이다** — 기존 상징이 **아직 없던 상징의
이름**을 갖고 있었다. 그 이름의 임자가 사전에 들어오는 날에만 보인다.

## 79. ⑨단계 프로브 — 10건. **동점을 「낱말 수」로 이기는 자리**를 처음 만났다

여섯은 늘 그렇듯 낱말 경계였다(「마음이 변」+했다 · 「유명해」+졌다 · 「작별」+하면서 ·
「기절」+해). **「작별하」를 올려도 「작별하면서」에 안 걸려** 어미까지 붙였다 — 여섯 번째다.

**둘은 넓은 의미가 좁은 의미를 눌렀는데, 동점이 아니라 점수로 이겼다.**

```
「아가씨가 장이 열린 곳에 갔다」
   장이 선 곳에 감      「장이 열린 구경」 → 장이 + 열린 = 2점
   처녀가 장이 선 곳에 감 「처녀 아가씨 여자가」 → 아가씨 = 1점   ← 져 버렸다
```

**차례로는 못 푼다** — 차례는 동점일 때만 듣는다. **넓은 쪽의 판별어를 하나로 줄여
동점을 만든 뒤** 좁은 쪽을 앞에 두었다(「장이」 하나). 지금까지의 「좁은 쪽을 앞으로」는
**동점을 만든 다음에야** 쓸 수 있는 수라는 것을 이번에 알았다.

나머지 둘(`fable`·`falcon`)은 넓은 쪽에서 **좁은 문장에도 있는 낱말**을 뺐다
(「들려주」·「보았」) — 그것이 더 나은 수다. 넓은 의미는 **0점으로 떨어져 기본값 자리를
차지하는 것**이 제 일이기 때문이다. 위 `marketplace` 에서 그 수를 못 쓴 이유는 기본값이
주공해몽 의미로 얼려 있어서다.

## 80. 관문

```
verify-dream-cite   3,164건 위반 0
verify-dream-km     1,074개 위반 0 (동점 위험 둘을 잡았다 — sad ⊂ sadness · man ⊂ woman)
audit-km-dead-words 죽은 낱말 0 (셋을 잡아 갈았다 — without · being · famous)
audit-cross-symbol-aliases  맞바꿔 쥔 짝 0쌍
verify-dream-match / conception-pick / context-parity / guide-numbers /
check-guide-examples / probe-water-fallback   전부 통과
probe-miller-batch  54건 · 안 걸림 0 · 다른 뜻 0
tsc  exit 0
전수 스윕  통과 119 / 검사 안 됨 7 / 빨간불 2 (**앞 판과 같다**)
```

기본값이 바뀐 상징 둘(`face`·`marketplace`) — 둘 다 있던 값으로 얼렸다.

**기록만 — 「추락」이 이제 사전에 있다.** 조립기의 `POPULAR_SYMBOL_IDS` 주석은 「이빨 빠짐·
추락·쫓김은 두 원문 어디에도 표제어가 없어 뺐다」고 적고 있는데, 밀러 `Fall` 이 들어오면서
**추락이 생겼다**(`fall`, 의미 둘). 목록은 건드리지 않았다.

## 81. 지금 사전

| | 배치 31 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,056 | **1,074** |
| 의미 | 3,080 | **3,131** |
| 인용 | 3,094 | **3,147** |
| 밀러 표제어 | 737 | **759** / 2,264 (남은 **1,505**) |

**다음은 밀러 `Fatigue` 부터.**

---

# 2026-09-02 (13) — 배치 32 배포 확인 · 밀러 배치 33(Fatigue~Fever)

## 82. 배포 확인

`fairy`·`fall`·`father` 푸시 전 404 → 푸시 후 **200**(`4b4af88`).

## 83. 배치 33 — **표제어를 14개로 줄였다**

원래 22개를 잡았다가 **14개로 다시 잘랐다.** `Fence` 하나가 의미 아홉이고 `Feet`·`Feather`
가 각각 다섯·여섯이라, 22개면 항목이 예순을 넘는다. 절차가 「20개 이하」라고 적어 둔 이유가
**표제어 수가 아니라 일의 양**이기 때문이다.

```
③ verify-dream-cite   항목 41개 · 위반 0건
④ 커버리지            입력 14 ↔ 결과 14 · 빠뜨림 0 · 배치 밖 0
⑤ 민감 표현           해석·이름 0건
```

새 상징 아홉 · 임자에게 붙인 것 일곱.

## 84. `Fawn` 한 표제어가 **두 상징으로 갈렸다**

밀러의 `fawn` 은 **「새끼 사슴」이면서 「알랑거리다」**다. 한국어로는 아무 관계가 없는 두
낱말이라 한 상징에 담을 수 없다.

```
새끼 사슴 둘  → deer(사슴)
남이 나에게 아첨함 → adulation(아첨)
```

지금까지의 갈래는 「밀러 표제어 하나 → 상징 하나」였고, 배치 30의 `Engagement`(약혼 /
사업상 약속)에서 **상징을 새로 둘 만들어** 풀었다. 이번은 **둘 다 이미 있어서** 나눠 붙였다.

임자가 이미 있던 것 넷 — 「울타리」는 `wall`(담)이 쥐고 있었고(**돌려줬다**), 「열병」은
`ague`(학질)가, 「축제」는 `carnival`이 이미 갖고 있었다. `Feast`·`Fears` 는 상징 자체가
있었다.

## 85. 「발」이 겹쳤다 — **두 번째 `LABEL_KO`**

밀러 `Feet` 을 넣자 `bamboo-blind`(발, 대나무 발)과 한국어 이름이 같아져 조립기가 멈췄다.
어제 「눈(眼)/눈(雪)」에 이어 두 번째다 → **「발(足)」·「발(가리개)」**.

## 86. 1글자가 판별어를 막은 자리 둘

```
「닭 깃털」  → 「닭」이 1글자라 못 센다. 「닭의·닭털·병아리」로 돌렸고
              **「닭 깃털을 보았다」는 여전히 안 갈린다**(기본값으로 떨어진다)
「우리 안」  → 「안」이 1글자. 「안으로·우리로」로 돌렸다
```

`audit-km-dead-words` 가 둘 다 잡았다. **못 가르는 자리는 못 가른다고 적어 둔다**
(§29 곁가지 — 「흰 산호」와 같은 자리다).

## 87. ⑨단계 프로브 — 3건. **셋 다 낱말 경계**

```
「지쳤다」 ≠ 「지친」   ·   「아첨」+했다   ·   「기운이 없」+었다
```

일곱 번째 재발이다. 이번엔 프로브가 세 건만 낸 것이 오히려 눈에 띈다 — 앞 판들에서
배운 대로 별칭을 **처음부터 「다」까지** 적어서 그렇다.

## 88. 관문

```
verify-dream-cite   3,205건 위반 0
verify-dream-km     1,083개 위반 0 (friends ⊂ friendships 하나를 잡았다)
audit-km-dead-words 죽은 낱말 0 (여섯을 잡아 갈았다 — fawn·any·festival·out·안·down)
audit-cross-symbol-aliases  맞바꿔 쥔 짝 0쌍
verify-dream-match / conception-pick / context-parity / guide-numbers /
check-guide-examples / probe-water-fallback   전부 통과
probe-miller-batch  41건 · 안 걸림 0 · 다른 뜻 0
tsc  exit 0
전수 스윕  통과 119 / 검사 안 됨 7 / 빨간불 2 (**앞 판과 같다**)
```

기본값이 바뀐 상징 하나(`feast`) — 있던 값으로 얼렸다.

## 89. 지금 사전

| | 배치 32 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,074 | **1,083** |
| 의미 | 3,131 | **3,172** |
| 인용 | 3,147 | **3,188** |
| 밀러 표제어 | 759 | **773** / 2,264 (남은 **1,491**) |

**다음은 밀러 `Fiddle` 부터.**

**기록만 — 다음 배치의 `Fight` 에 인종 비하 대목이 둘 있다.** 「negroes attack you with
razors」·「fighting negroes」다. **⑤단계에서 통째로 뺀다** — 인용문에만 두는 것으로도 안
되는 자리다. 그 표제어는 나머지 일곱 문장으로도 커버리지가 선다.

---

# 2026-09-02 (14) — 배치 33 배포 확인 · 밀러 배치 34(Fiddle~Fireman)

## 90. 배포 확인

`fence`·`feet`·`ferry` 푸시 전 404 → 푸시 후 **200**(`15d8726`).

## 91. 배치 34 — 표제어 16개 · 항목 46개

**밀러 `Fight` 의 두 문장은 넣지 않았다.** 흑인을 겨냥한 비하 대목이다
(「negroes attack you with razors」·「fighting negroes」). 앞 판에서 미리 적어 둔 대로
⑤단계에서 **통째로 뺐다.**

**인용문에만 두는 것으로도 안 되는 자리다** — 다른 판에서는 `crippled`·`maimed` 처럼
**해석 문장에서만 빼고 인용문에는 남겼다.** 그것이 가능했던 이유는 비하가 **낱말**에
있었기 때문이다. 이번은 **꿈의 그림 자체**가 비하다. 그 그림을 문맥 이름으로 적어 23개
언어로 화면에 띄우는 순간, 인용 안에 있든 밖에 있든 이 서비스가 그것을 보여 주는 것이다.

```
③ verify-dream-cite   항목 46개 · 위반 0건
④ 커버리지            입력 16 ↔ 결과 16 · 빠뜨림 0
⑤ 민감 표현           해석·이름 0건 · **결과 파일에 negro 0건**(grep 으로 확인)
```

나머지 일곱 문장으로 `Fight` 의 커버리지는 그대로 선다.

## 92. 「불」이 「불타는」에 안 걸리고 있었다

`fire`(불)는 주공해몽 열아홉 개 의미를 가진 오래된 상징인데, 별칭이 「화재·화염·불꽃」뿐이라
**「우리 집이 불타는 것을 보았다」가 한 번도 안 걸렸다.** 「불」 뒤의 「타」가 조사가 아니라
`isStandalone` 이 낱말 조각으로 버린다.

**「대화재」도 안 걸렸다** — 「화재」 앞의 「대」가 한글이라 막힌다.

→ 「불타·불탄·불이 났·불길·대화재」를 올렸다. **`adversary`(적을 이겼다)·`bequest`(물려받았다)
에 이어 세 번째로 프로브가 찾아낸 「오래된 상징의 안 걸리는 자리」**다.

## 93. ⑨단계 프로브 — 9건

여섯은 위와 같은 낱말 경계이고, **셋은 넓은 의미가 좁은 의미를 눌렀다**.

```
「고장 난 소방차를 보았다」  넓은 쪽이 「보았」을 갖고 있었다   → 「달려·달려오」로
「불탄 가게의 잔해를 보았다」 넓은 쪽이 「가게」를 갖고 있었다   → 「점포·상점·지켜보」로
「불을 끄면서 데지 않았다」   넓은 쪽이 「데지」를 갖고 있었다   → 「멀쩡」 하나로
```

**세 번 다 넓은 쪽에서 뺐다** — 배치 32에서 배운 대로다(넓은 의미는 0점으로 떨어져
기본값 자리를 지키는 것이 제 일이다).

넷은 **문장 쪽이 틀렸다** — 「덤빈 자를 물리쳤다」·「고운 손을 보았다」에는 상징 이름이
한 낱말도 없다.

## 94. 관문

```
verify-dream-cite   3,251건 위반 0
verify-dream-km     1,095개 위반 0 (겹침 여덟을 잡아 갈았다)
audit-km-dead-words 죽은 낱말 0
audit-cross-symbol-aliases  맞바꿔 쥔 짝 0쌍
verify-dream-match / conception-pick / context-parity / guide-numbers /
check-guide-examples / probe-water-fallback   전부 통과
probe-miller-batch  46건 · 안 걸림 0 · 다른 뜻 0
tsc  exit 0
전수 스윕  통과 119 / 검사 안 됨 7 / 빨간불 2 (**앞 판과 같다**)
```

기본값이 바뀐 상징 넷(`field`·`flute`·`fight`·`fire`) — 전부 있던 값으로 얼렸다.
`FALLBACK_FIRST` 가 스물여섯이 됐다.

## 95. 지금 사전

| | 배치 33 뒤 | 지금 |
| --- | --- | --- |
| 상징 | 1,083 | **1,095** |
| 의미 | 3,172 | **3,218** |
| 인용 | 3,188 | **3,234** |
| 밀러 표제어 | 773 | **789** / 2,264 (남은 **1,475**) |

**다음은 밀러 `Fireworks` 부터.**
