/**
 * 리포트 PDF의 장수. **고시에 적는 값이자 실제로 나가는 값이다.**
 *
 * ## 왜 상수로 두는가
 *
 * 이 숫자는 세 곳이 함께 봐야 한다 — 문서를 그리는 곳(`pdf/saju-report.tsx`가 선언한 `<Page>`
 * 수), 장수를 재는 곳(`scripts/render-saju-sample.tsx`), 그리고 **고시하는 곳**(약관·요금안내·
 * 안내 문서). 세 곳이 각자 숫자를 적고 있으면 문서를 한 장 늘렸을 때 나머지 둘이 조용히 옛
 * 값으로 남는다 — 그리고 그건 화면에서 보이지 않는다.
 *
 * 예전에 실제로 그랬다. 고시의 5장·7장이 **선언된 `Page` 수가 아니라 글이 넘쳐서** 나오던
 * 값이었고, 폴백 문안에서 중복 문장 하나를 빼자 4장·6장으로 떨어졌다. 고시가 조용히 거짓이 됐다.
 *
 * ## 바꾸는 순서
 *
 * 1. `saju-report.tsx`에서 `<Page>`를 더하거나 뺀다
 * 2. 이 값을 그에 맞게 고친다
 * 3. `render-saju-sample`을 **번역이 있는 로케일 전부**로 돌려 실제로 그 수가 나오는지 본다
 * 4. 약관·안내 문서의 장수 표기를 고친다(`verify-legal-*`가 대조한다)
 *
 * 3번을 건너뛰지 말 것 — 지면이 깨지는 것은 글자가 다른 언어에서다.
 */
export const REPORT_PAGE_COUNT = 9;
