/**
 * 태몽 리포트의 장수. **이 값 하나가 정한다.**
 *
 * 렌더러·검사기·약관·안내 문서가 모두 여기를 본다. 사주링크에서 배운 것이라 그대로 옮긴다 —
 * 고시된 장수가 **글이 넘쳐서** 나오는 값이면 문장 하나에 무너진다. 바닥은 **선언된 `Page`**가
 * 보장하고, 천장은 글자 수 상한이 잡는다.
 *
 * ## 왜 넷인가
 *
 * 사주는 아홉 장이지만 여기서는 만들 수 없다. 상징 사전이 상징당 의미 1.1개로 얕아, 아홉 장을
 * 채우려면 사전에 없는 전통 의미를 지어내야 한다 — 이 서비스가 금지하는 바로 그 일이다.
 *
 *   1  표지와 요약        무엇을 꿨고 무엇이 걸렸는가
 *   2  걸린 상징          상징별 전통 의미와 그 근거(사전에 있는 것만)
 *   3  태몽으로 보는 이유  태몽 태그가 붙은 상징과 전통 배경
 *   4  간직하는 장         날짜·기록과 고지
 *
 * ## 바꿀 때
 *
 * 1. 이 값을 고친다
 * 2. 렌더러의 `Page`를 그 수만큼 맞춘다
 * 3. 견본을 로케일마다 실제로 렌더해 장을 센다
 * 4. 약관·안내 문서의 표기가 이 값을 참조하는지 확인한다
 *
 * **넘침에 기대지 말 것.** 장이 모자라면 `Page`를 선언하고, 남으면 내용을 옮긴다.
 */
export const CONCEPTION_PAGE_COUNT = 4;
