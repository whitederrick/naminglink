# CLAUDE.md — 이 저장소에서 내가 반복해서 틀린 것

이 파일은 기능 설명서가 아니다. **Claude(나)가 이 저장소에서 같은 실수를 되풀이해서, 그
목록을 적어 둔 것이다.** 새 세션은 작업을 시작하기 전에 여기부터 읽는다.

각 항목은 「하지 말 것 → 대신 이렇게」다. 근거는 실제로 그렇게 틀린 날짜다.

---

## 0. 이 파일을 갱신하는 규칙 (사용자 지시, 2026-08-20)

> **실수하면 그때그때 이 파일에 넣는다.** 세션이 끝날 때 몰아서가 아니라, **알아차린 그 자리에서.**

- 사용자가 지적했든 내가 스스로 발견했든, **되풀이될 수 있는 실수면 적는다.** 그 세션 한정
  사고(오타 하나)는 적지 않는다.
- 적는 형식은 아래와 같다 — **하지 말 것 → 대신 이렇게 → 실제로 틀린 날짜.**
  날짜와 구체적 자리가 없으면 다음 사람이 「그럴듯한 원칙」으로 읽고 넘긴다.
- 이미 있는 항목과 같은 병이면 **새 항목을 만들지 말고 그 항목에 사례를 붙인다.**
- 같은 것을 `~/.claude/projects/.../memory/`에도 남긴다. **CLAUDE.md는 이 저장소에서 자동으로
  읽히고, 메모리는 세션을 넘어 따라온다** — 둘은 대체재가 아니다.
- 커밋 메시지에 **무엇을 틀렸는지 그대로** 적는다. 「개선」이라고 뭉개지 않는다.

---

## 1. 초록불을 잘못 읽는다

**`exit 0`을 「끝났다」로 읽지 말 것.** 그리고 파이프 뒤의 종료 코드는 **마지막 명령의 것**이다.

```bash
node scripts/audit-verifiers.mjs | tail -60   # ← $? 는 tail 의 것(0). 실패가 성공으로 보인다
node scripts/audit-verifiers.mjs > /tmp/s.txt 2>&1; echo $?   # 이렇게
```

- **출력을 `tail`로 자르지 말 것.** 스윕은 `빨간불 → 못 돎 → 감싸짐 → 통과` 순으로 찍는다.
  꼬리만 남기면 **정작 빨간불 절이 잘린다.** (2026-08-20에 이것 때문에 스윕을 세 번 돌렸다)
- **`exit 1`은 「잡았다」가 아니다.** 죽어도 1이고 파일이 없어도 1이다. 판정은 **출력 본문**으로.
- **실패는 셋으로 가른다** — 진짜 결함 / 검사기 결함 / 환경 없음. 「못 돎」은 통과가 아니다.

## 2. 관문을 커밋 뒤나 여러 번에 나눠 돌린다

**관문은 커밋 앞에 한 번.** 같은 빌드·스윕을 여러 번 돌리고 있다면 **순서가 틀린 것이다.**

- **무거운 것을 뒤로 돌리면서 앞에서 파일을 편집하지 말 것.** 2026-08-20에 263MB 격리
  복사본에서 `next build`를 돌리며 편집하다 **세션이 통째로 죽었다.**
- 스윕이 도는 중에 「종합」을 내지 말 것. 끝난 뒤에 보고한다.
- **커밋한 트리를 검증한다.** 편집 중인 트리에 스윕을 걸면 움직이는 것을 잰 것이다.

## 3. 검증 안 된 것을 검증된 것처럼 말한다

- 「고쳤습니다」와 「고쳤고 돌려 봤습니다」는 다른 문장이다. **안 돌렸으면 안 돌렸다고 적는다.**
- **대조군 없는 초록불은 아무것도 증명하지 않는다.** 고치기 **전** 트리에서 그 검사가 실제로
  빨간불을 내는지 먼저 본다. (오늘 두 번 다 이 순서로 해서 시험이 진짜임을 보였다)
- **검사 0건은 실패로 처리한다.** 훑은 파일 0건, 실행 0회, 매칭 0건 — 전부 「통과」가 아니라
  「검사 안 됨」이다. 이 규칙은 **검사기 자신에게도** 적용된다.

## 4. 「없다」를 안 찾고 단정한다

**「지금은 비어 있는 것이 옳은 상태다」라고 적지 말 것.** 안 찾은 것을 없다고 적는 것이다.

- 2026-08-20 `ORIGIN_DOCS_EN = []`. 반박 근거가 **바로 그 파일 머리말**에 있었다.
  두 검토자가 독립적으로 같은 P0을 냈고 재검증 왕복이 한 번 늘었다.
- **막는 쪽으로 틀린 것도 결함이다.** 그 방향은 초록불로 위장돼 더 오래 산다.
- 생성기가 흔적을 안 남기면(`--fill-en` 등) **git 이력으로 가른다** — 파일이 처음 생긴 커밋과
  **그 부모의 소스**를 대조한다.

## 5. 한 앱만 고치고 형제 셋을 남긴다

앱이 넷이다(`naminglink` · `inyeonlink` · `sajulink` · `dreamslink`). 복제로 자란 파일이 많다.

- 결함을 찾으면 **먼저 네 앱에 같은 모양이 있는지 센다.** 2026-08-20에 `stamp-back.ts`의
  결함이 `guide-back.ts` 넷에, `audit-bundle-secrets.mjs`의 결함이 넷 모두에 있었다.
- **앱 목록을 손으로 적지 말 것.** `scripts/app-keys.mjs`의 `APP_KEYS`를 쓴다. 목록에서 빠진
  앱은 통과한 것이 아니라 **검사받지 않은 것**이다.
- **고침은 강제돼야 한다.** 고친 뒤 그것을 지키는 검사기를 함께 남긴다.

## 6. 같은 규칙을 두 벌로 적는다

판정이 두 곳에 적히면 **하나만 고쳐지는 날이 온다.**

- 부르는 자리마다 막지 말고 **갈래 자체를 없앤다.** 허용 목록 표는 객체가 아니라 `Map`으로
  (객체는 `toString`·`__proto__`까지 「아는 값」으로 통과시킨다 — 실제로 HTTP 500이었다).
- 임계값을 옮기거나 「나중에」로 미루지 말 것. **근본 원인만 고친다.**

## 7. 셸에서 헛발질한다 (Windows · Git Bash)

- **`cd`가 다음 명령까지 남는다.** 경로는 절대경로로 쓰거나 매번 `cd /c/myProjects/naminglink`.
- **셸이 백슬래시를 먹는다.** 정규식이 든 스크립트를 `node -e`나 히어독으로 넘기지 말 것 —
  **파일로 쓰고 `node <파일>`**로 돌린다.
- **파일이 CRLF다.** 문자열 치환 전에 줄끝을 확인한다. `\n`으로만 매칭하면 조용히 실패한다.
- `MSYS_NO_PATHCONV=1` — `/`로 시작하는 환경변수 값이 윈도 경로로 바뀐다.

## 8. 격리 복사본을 늘린다

**새 워크트리를 만들지 말 것.** 사용자가 2026-08-20에 거부했다. 그날 저장소에 검증용 복사본이
약 400MB 쌓여 있었고, 거부된 그 호출조차 깨진 151MB를 남겼다.

- 이미 있는 워크트리에서 `git switch`. 저장소 **안**의 워크트리는 상위 `node_modules`가 해석된다.
- 다 쓰면 `git worktree remove`로 걷는다.

## 9. 물어보지 않아도 될 것을 묻고, 물어야 할 것을 안 묻는다

- **dev 서버는 사용자가 띄운다**(:3001). 내가 띄우지 않는다.
- **운영 DB 쓰기는 인라인 `curl`/`node -e`로 하지 않는다.** 프로젝트 스크립트를 쓴다.
- 사용자를 **「사장님」이라고 부르지 않는다.** 호칭 없이 존댓말.

---

## 10. 실수 대장 — 날짜별로 쌓는다

§0 규칙에 따라 **여기에 계속 붙인다.** 위 1~9는 이 대장에서 갈래로 묶은 것이다.

내가 확실히 아는 범위는 **이 대장에 적힌 것 + 메모리 + 커밋 메시지**까지다. 기억나지 않는
과거 실수를 지어내지 않는다. 새 실수를 알아차리면 그날 줄에 붙인다.

### 2026-08-20 (하루에 낸 것 전부)

**일을 통째로 날린 것**

| # | 무엇 | 대가 |
| --- | --- | --- |
| 1 | 263MB 격리 복사본에서 `next build`를 뒤로 돌리며 **앞에서 파일을 편집** | **세션이 22:04:55에 죽었다.** 빌드는 `BUILD_ID` 없이 중단 |
| 2 | 스윕 출력을 `\| tail -60`으로 자름 | 빨간불 절이 잘려 통째로 날림. 스윕을 **세 번**(~21분) |
| 3 | 그 `tail` 때문에 종료 코드가 `tail`의 것(0) | 실패한 스윕을 **「exit 0」이라고 보고**했다 |
| 4 | 이미 400MB가 쌓였는데 **워크트리를 또** 만들려 함 | 사용자가 거부. 끊긴 호출이 **깨진 151MB**를 잠근 채 남김 |

**판정을 틀린 것**

| # | 무엇 |
| --- | --- |
| 5 | `ORIGIN_DOCS_EN`을 비우고 **「비어 있는 것이 옳은 상태다」**라고 적음. 반박 근거가 그 파일 머리말에 있었다 → 재검증 왕복 1회 추가 |
| 6 | 내가 쓴 공격 파일이 제품 검사기(`verify-legal-source`)를 **스윕에서 지웠다**. 「부르면 감싼 것」 규칙에 걸린 것 |
| 7 | 공격 ④에 `tsx` 경로를 `process.cwd()`로 **박아 둠** → 격리 복사본에서 `MODULE_NOT_FOUND`. 크래시를 결함으로 셀 뻔했다 |
| 8 | `WRAP_CONTROL` 첫 판에서 「감싼 쪽이 못 돎」을 **빨간불**로 분류. 없는 결함을 신고하는 갈래였고 스스로 고침 |
| 9 | 공격 묶음을 여섯으로 늘려 놓고 마무리 문구는 **「다섯 우회가 전부 막혔다」**로 남아 있었다 |

**증거를 잘못 잰 것**

| # | 무엇 |
| --- | --- |
| 10 | origin 대조용 정규식이 **줄을 넘어가** 코드 조각을 문장으로 셌다. **틀린 수치를 한 번 보고**한 뒤 고침 |
| 11 | 파일이 CRLF인 것을 **안 보고** 치환 → 실패 |
| 12 | 셸이 백슬래시를 먹어 `node -e`·히어독의 정규식이 **두 번** 깨짐 |
| 13 | `cd`가 다음 명령까지 남아 **경로를 두 번** 틀림 |

**같은 날 앞 세션(커밋 메시지에 기록된 것)**

| # | 무엇 |
| --- | --- |
| 14 | 1차 대조군을 **내 구현을 보고** 만들어 같은 사각지대가 양쪽에 들어감 → 검사기는 초록불인데 우회 다섯이 남아 있었다 |
| 15 | 공격 ④를 **소스 문자열 grep**으로 판정해 초록불을 냄. 실제로 자식 프로세스를 띄우게 고침 |
| 16 | `server-only`를 클라이언트도 읽는 `goods-products.ts`에 끌어들임 → **tsc 0건인데 런타임 500** |
| 17 | **「eslint 0건」이라고 잘못 적음**(`3d21920` 커밋 제목이 그 정정이다) |

### 그 전 — 메모리에 남아 있는 되풀이 병

날짜별 근거는 `~/.claude/.../memory/`에 있다. 갈래만 옮긴다.

- **초록불이 검증이 아니다** — `ALL PASS`를 찍고 안 끝나는 검사기(성공 경로에 `process.exit`
  없음), 「못 돎」을 통과로 세기, 검사 0건을 통과로 세기.
- **검사기가 다른 것을 보고 있었다** — 다른 사이트·다른 화면·없는 것을 검사하며 초록불.
  헤드리스 크롬이 남의 브라우저에 붙어 exit 13.
- **예측하고 관측하지 않았다** — `googlefc`가 있다고 오퍼월이 뜬다고 단정 → 광고 0개와
  2개가 함께 났다.
- **옮겨 적을 때 한정어가 떨어졌다** — 기록은 옳았고 전파에서 깨졌다.
- **앱 목록을 손으로 적었다** — 두 앱만 세고 `ALL PASS`.
- **환경 탓을 못 알아봤다** — Git Bash가 `/`로 시작하는 환경변수를 윈도 경로로 바꿔
  광고 단위가 안 켜진 채 `ALL PASS`.
- **확인 전에 틀부터 그렸다** — 운영 알림 채널 등.

---

## 시작 전 체크리스트

```
□ 이 작업의 관문이 무엇인지 정했다 (그리고 커밋 앞에 한 번만 돈다)
□ 고치기 전에 그 검사가 빨간불을 내는지 봤다 (대조군)
□ 같은 모양이 형제 셋에 있는지 셌다
□ 「없다」고 적으려는 것을 실제로 찾아봤다
□ 보고에서 「돌려 본 것」과 「안 돌려 본 것」을 갈라 적었다
```

자세한 그날의 기록은 `docs/WORKLOG_2026-08-20.md` §5(「내가 버린 시간 — 원인」)에 있다.
