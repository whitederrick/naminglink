# CLAUDE.md — 이 저장소에서 내가 반복해서 틀린 것

이 파일은 기능 설명서가 아니다. **Claude(나)가 이 저장소에서 같은 실수를 되풀이해서, 그
목록을 적어 둔 것이다.** 새 세션은 작업을 시작하기 전에 여기부터 읽는다.

각 항목은 「하지 말 것 → 대신 이렇게」다. 근거는 실제로 그렇게 틀린 날짜다.

---

## 0. 이 파일을 갱신하는 규칙 (사용자 지시, 2026-08-20)

> **실수하면 그때그때 이 파일에 넣는다.** 세션이 끝날 때 몰아서가 아니라, **알아차린 그 자리에서.**

- 사용자가 지적했든 내가 스스로 발견했든, **되풀이될 수 있는 실수면 적는다.** 그 세션 한정
  사고(오타 하나)는 적지 않는다.
- 적는 형식은 아래와 같다 — **하지 말 것 → 대신 이렇게 → 실제로 틀린 날짜.**
  날짜와 구체적 자리가 없으면 다음 사람이 「그럴듯한 원칙」으로 읽고 넘긴다.
- 이미 있는 항목과 같은 병이면 **새 항목을 만들지 말고 그 항목에 사례를 붙인다.**
- 같은 것을 `~/.claude/projects/.../memory/`에도 남긴다. **CLAUDE.md는 이 저장소에서 자동으로
  읽히고, 메모리는 세션을 넘어 따라온다** — 둘은 대체재가 아니다.
- 커밋 메시지에 **무엇을 틀렸는지 그대로** 적는다. 「개선」이라고 뭉개지 않는다.
- **`docs/WORKLOG_YYYY-MM-DD.md`는 작업이 있었던 날마다 무조건 쓴다**(사용자 지시,
  2026-08-27). CLAUDE.md·memory는 세션마다 강제로 읽히지만 WORKLOG는 그렇지 않다 —
  08-21-2 이후 한 주 가까이 하나도 안 쓰여서 사용자가 직접 짚어야 했다. 「오늘은 짧으니
  건너뛰자」로 미루지 말 것 — 그 판단이 쌓여서 일주일이 됐다.

---

## 1. 초록불을 잘못 읽는다

**`exit 0`을 「끝났다」로 읽지 말 것.** 그리고 파이프 뒤의 종료 코드는 **마지막 명령의 것**이다.

```bash
node scripts/audit-verifiers.mjs | tail -60   # ← $? 는 tail 의 것(0). 실패가 성공으로 보인다
node scripts/audit-verifiers.mjs > /tmp/s.txt 2>&1; echo $?   # 이렇게
```

- **출력을 `tail`로 자르지 말 것.** 스윕은 `빨간불 → 검사 안 됨 → 통과` 순으로 찍는다.
  꼬리만 남기면 **정작 빨간불 절이 잘린다.** (2026-08-20에 이것 때문에 스윕을 세 번 돌렸다)
  (「감싸짐」 칸은 §13에서 없앴다 — 옛 순서를 적어 두면 없는 절을 찾게 된다)
- **`exit 1`은 「잡았다」가 아니다.** 죽어도 1이고 파일이 없어도 1이다. 판정은 **출력 본문**으로.
- **실패는 셋으로 가른다** — 진짜 결함 / 검사기 결함 / 환경 없음. 「못 돎」은 통과가 아니다.
**2026-09-02 — 백그라운드 작업이 아무 일도 안 하고 「완료(exit 0)」로 돌아온다.** 전수 스윕이
배치 44에서 두 번, 배포 대기가 배치 48에서 한 번 그랬다. 로그에는 **한 줄만** 있었고 대기
쪽은 **빈 파일**이었는데 도구는 「completed」라고 알렸다. 배포 대기를 그대로 믿었으면
**안 올라간 것을 「배포 확인됐다」고 보고**할 뻔했다 — 다시 재니 404였다.

→ **알림의 「완료」가 아니라 로그 본문으로 판정한다**(이 §의 첫 줄과 같다). 스윕은 정상이면
140줄이고 죽으면 1줄이다. **오래 걸리는 것은 `timeout 600000` 으로 앞에서 돌린다** —
스윕은 6~7분, 배포 대기는 `until` 에 횟수 상한을 붙여 돌린다.


## 2. 관문을 커밋 뒤나 여러 번에 나눠 돌린다

**관문은 커밋 앞에 한 번.** 같은 빌드·스윕을 여러 번 돌리고 있다면 **순서가 틀린 것이다.**

- **무거운 것을 뒤로 돌리면서 앞에서 파일을 편집하지 말 것.** 2026-08-20에 263MB 격리
  복사본에서 `next build`를 돌리며 편집하다 **세션이 통째로 죽었다.**
- 스윕이 도는 중에 「종합」을 내지 말 것. 끝난 뒤에 보고한다.
- **커밋한 트리를 검증한다.** 편집 중인 트리에 스윕을 걸면 움직이는 것을 잰 것이다.

## 3. 검증 안 된 것을 검증된 것처럼 말한다

- 「고쳤습니다」와 「고쳤고 돌려 봤습니다」는 다른 문장이다. **안 돌렸으면 안 돌렸다고 적는다.**
- **대조군 없는 초록불은 아무것도 증명하지 않는다.** 고치기 **전** 트리에서 그 검사가 실제로
  빨간불을 내는지 먼저 본다. (오늘 두 번 다 이 순서로 해서 시험이 진짜임을 보였다)
- **검사 0건은 실패로 처리한다.** 훑은 파일 0건, 실행 0회, 매칭 0건 — 전부 「통과」가 아니라
  「검사 안 됨」이다. 이 규칙은 **검사기 자신에게도** 적용된다.

## 4. 「없다」를 안 찾고 단정한다

**「지금은 비어 있는 것이 옳은 상태다」라고 적지 말 것.** 안 찾은 것을 없다고 적는 것이다.

- 2026-08-20 `ORIGIN_DOCS_EN = []`. 반박 근거가 **바로 그 파일 머리말**에 있었다.
  두 검토자가 독립적으로 같은 P0을 냈고 재검증 왕복이 한 번 늘었다.
- **막는 쪽으로 틀린 것도 결함이다.** 그 방향은 초록불로 위장돼 더 오래 산다.
- 생성기가 흔적을 안 남기면(`--fill-en` 등) **git 이력으로 가른다** — 파일이 처음 생긴 커밋과
  **그 부모의 소스**를 대조한다.

## 5. 한 앱만 고치고 형제 셋을 남긴다

앱이 넷이다(`naminglink` · `inyeonlink` · `sajulink` · `dreamslink`). 복제로 자란 파일이 많다.

- 결함을 찾으면 **먼저 네 앱에 같은 모양이 있는지 센다.** 2026-08-20에 `stamp-back.ts`의
  결함이 `guide-back.ts` 넷에, `audit-bundle-secrets.mjs`의 결함이 넷 모두에 있었다.
- **앱 목록을 손으로 적지 말 것.** `scripts/app-keys.mjs`의 `APP_KEYS`를 쓴다. 목록에서 빠진
  앱은 통과한 것이 아니라 **검사받지 않은 것**이다.
- **고침은 강제돼야 한다.** 고친 뒤 그것을 지키는 검사기를 함께 남긴다.

**2026-09-01 — 같은 병을 km 파일 목록에서 또 봤다.** 스크립트 셋이
`km1~9`·`kmm1~7`을 **수를 세어** 적어 두고 있었다. `kmm8.json`을 더하자 새 상징 18개가
통째로 안 읽혔다. 가장 나쁜 것은 `audit-km-dead-words.mts`다 — **그 18개의 판별어를 한
낱말도 검사하지 않은 채 「죽은 낱말 0개」를 냈다.** 디렉터리를 읽게 고치자 곧바로 18개가
나왔다(§24와 같은 자리). **`build-dream-match-tables-v2.mjs`의 주석이 바로 그 위험을
경고하고 있었는데도 그랬다** — 경고를 적어 두는 것과 목록을 없애는 것은 다른 일이다.

## 6. 같은 규칙을 두 벌로 적는다

판정이 두 곳에 적히면 **하나만 고쳐지는 날이 온다.**

- 부르는 자리마다 막지 말고 **갈래 자체를 없앤다.** 허용 목록 표는 객체가 아니라 `Map`으로
  (객체는 `toString`·`__proto__`까지 「아는 값」으로 통과시킨다 — 실제로 HTTP 500이었다).
- 임계값을 옮기거나 「나중에」로 미루지 말 것. **근본 원인만 고친다.**

## 7. 셸에서 헛발질한다 (Windows · Git Bash)

- **`cd`가 다음 명령까지 남는다.** 경로는 절대경로로 쓰거나 매번 `cd /c/myProjects/naminglink`.
- **셸이 백슬래시를 먹는다.** 정규식이 든 스크립트를 `node -e`나 히어독으로 넘기지 말 것 —
  **파일로 쓰고 `node <파일>`**로 돌린다.
- **파일이 CRLF다.** 문자열 치환 전에 줄끝을 확인한다. `\n`으로만 매칭하면 조용히 실패한다.
- `MSYS_NO_PATHCONV=1` — `/`로 시작하는 환경변수 값이 윈도 경로로 바뀐다.
- **`/c/...` 경로를 스크립트 **안에** 문자열로 적지 말 것**(2026-09-02). 셸이 **인자로** 넘길
  때는 `C:\...`로 바꿔 주지만, `readFileSync("/c/Users/...")`처럼 **코드 안에 적은 문자열**은
  안 바뀐다 — Node가 `C:\c\Users\...`로 풀어 `ENOENT`가 난다. 같은 실행에서 두 번 밟았다.
  스크립트 안에서는 **`C:/...` 꼴로 적거나 저장소 기준 상대경로**를 쓴다.

## 8. 격리 복사본을 늘린다

**새 워크트리를 만들지 말 것.** 사용자가 2026-08-20에 거부했다. 그날 저장소에 검증용 복사본이
약 400MB 쌓여 있었고, 거부된 그 호출조차 깨진 151MB를 남겼다.

- 이미 있는 워크트리에서 `git switch`. 저장소 **안**의 워크트리는 상위 `node_modules`가 해석된다.
- 다 쓰면 `git worktree remove`로 걷는다.

## 9. 물어보지 않아도 될 것을 묻고, 물어야 할 것을 안 묻는다

- **dev 서버는 사용자가 띄운다**(:3001). 내가 띄우지 않는다.
- **운영 DB 쓰기는 인라인 `curl`/`node -e`로 하지 않는다.** 프로젝트 스크립트를 쓴다.
- 사용자를 **「사장님」이라고 부르지 않는다.** 호칭 없이 존댓말.
  **2026-09-02에 어겼다.** 이 줄이 §9에 적혀 있고 메모리에도 있는데, 마무리 문장에서
  「사장님이 하실 일은 없습니다」라고 썼다. 사용자가 다시 짚었다. **규칙을 아는 것과
  지키는 것은 다른 일이다**(§10 #43과 같다) — 호칭은 문장을 끝맺을 때 손이 먼저 나가는
  자리다. 「~하실 일은 없습니다」로 **주어 없이** 쓴다.

---

## 10. 실수 대장 — 날짜별로 쌓는다

§0 규칙에 따라 **여기에 계속 붙인다.** 위 1~9는 이 대장에서 갈래로 묶은 것이다.

내가 확실히 아는 범위는 **이 대장에 적힌 것 + 메모리 + 커밋 메시지**까지다. 기억나지 않는
과거 실수를 지어내지 않는다. 새 실수를 알아차리면 그날 줄에 붙인다.

### 2026-08-20 (하루에 낸 것 전부)

**일을 통째로 날린 것**

| # | 무엇 | 대가 |
| --- | --- | --- |
| 1 | 263MB 격리 복사본에서 `next build`를 뒤로 돌리며 **앞에서 파일을 편집** | **세션이 22:04:55에 죽었다.** 빌드는 `BUILD_ID` 없이 중단 |
| 2 | 스윕 출력을 `\| tail -60`으로 자름 | 빨간불 절이 잘려 통째로 날림. 스윕을 **세 번**(~21분) |
| 3 | 그 `tail` 때문에 종료 코드가 `tail`의 것(0) | 실패한 스윕을 **「exit 0」이라고 보고**했다 |
| 4 | 이미 400MB가 쌓였는데 **워크트리를 또** 만들려 함 | 사용자가 거부. 끊긴 호출이 **깨진 151MB**를 잠근 채 남김 |

**판정을 틀린 것**

| # | 무엇 |
| --- | --- |
| 5 | `ORIGIN_DOCS_EN`을 비우고 **「비어 있는 것이 옳은 상태다」**라고 적음. 반박 근거가 그 파일 머리말에 있었다 → 재검증 왕복 1회 추가 |
| 6 | 내가 쓴 공격 파일이 제품 검사기(`verify-legal-source`)를 **스윕에서 지웠다**. 「부르면 감싼 것」 규칙에 걸린 것 |
| 7 | 공격 ④에 `tsx` 경로를 `process.cwd()`로 **박아 둠** → 격리 복사본에서 `MODULE_NOT_FOUND`. 크래시를 결함으로 셀 뻔했다 |
| 8 | `WRAP_CONTROL` 첫 판에서 「감싼 쪽이 못 돎」을 **빨간불**로 분류. 없는 결함을 신고하는 갈래였고 스스로 고침 |
| 9 | 공격 묶음을 여섯으로 늘려 놓고 마무리 문구는 **「다섯 우회가 전부 막혔다」**로 남아 있었다 |

**증거를 잘못 잰 것**

| # | 무엇 |
| --- | --- |
| 10 | origin 대조용 정규식이 **줄을 넘어가** 코드 조각을 문장으로 셌다. **틀린 수치를 한 번 보고**한 뒤 고침 |
| 11 | 파일이 CRLF인 것을 **안 보고** 치환 → 실패 |
| 12 | 셸이 백슬래시를 먹어 `node -e`·히어독의 정규식이 **두 번** 깨짐 |
| 13 | `cd`가 다음 명령까지 남아 **경로를 두 번** 틀림 |

**같은 날 앞 세션(커밋 메시지에 기록된 것)**

| # | 무엇 |
| --- | --- |
| 14 | 1차 대조군을 **내 구현을 보고** 만들어 같은 사각지대가 양쪽에 들어감 → 검사기는 초록불인데 우회 다섯이 남아 있었다 |
| 15 | 공격 ④를 **소스 문자열 grep**으로 판정해 초록불을 냄. 실제로 자식 프로세스를 띄우게 고침 |
| 16 | `server-only`를 클라이언트도 읽는 `goods-products.ts`에 끌어들임 → **tsc 0건인데 런타임 500** |
| 17 | **「eslint 0건」이라고 잘못 적음**(`3d21920` 커밋 제목이 그 정정이다) |

### 2026-08-21 — 「내일 할 일」에 적은 것을 오늘 재 보니 셋이 틀렸다

**계획서에 적은 수치·전제도 검증 대상이다.** 어제 `WORKLOG_2026-08-20.md` §7에 열두 개를
적었다. 오늘 그 열두 개를 하나씩 실제로 확인했더니 셋이 사실과 달랐다.

| # | 무엇 | 실제 |
| --- | --- | --- |
| 18 | 디스크 정리를 **「약 400MB」**라고 적음. 하루 전 어림을 그대로 옮겼다 | **1.67GB**였다(`.verify-pr3` 혼자 1.1GB). `du`를 다시 돌리는 데 3초 걸린다 |
| 19 | `audit-bundle-secrets`를 **「빌드하면 돈다」**(=지금은 못 돎)고 적음 | **이미 돌 수 있었다.** dev 서버가 `.next/static`을 채워 둬서 그냥 돌리니 exit 0. 「못 돎」이라 적기 전에 **한 번 돌려 보지 않았다** |
| 20 | **「이 컴퓨터에 `.env.local`이 없다」**고 적음 | **있다**(`apps/naminglink/.env.local` 46줄). 진짜 원인은 **`verify-legal-source.ts`가 그 파일을 안 읽는 것**이다 — 같은 폴더 `verify-premium-ai-usage.ts:13`은 읽는다. **빠진 배선을 「환경이 없다」로 적었다** |

20번이 §4와 같은 병이다. 「없다」를 안 찾고 단정하면 **원인이 통째로 바뀐다** — 「자격증명을
받아 와야 한다」와 「여섯 줄을 베껴 넣으면 된다」는 다른 일이다.

### 2026-08-21 (2) — 4차 재검증. 내가 물어 놓고 열어 둔 것이 또 결함이었다

3차 의뢰서 §3에 「이게 새 구멍이 아닌가」로 셋을 적어 두고 **막지 않은 채 의뢰했다.**
두 검토자가 독립적으로 **셋 다 결함**이라고 냈다. 그리고 4차 의뢰서에서 **같은 짓을 또 했다.**

| # | 무엇 | 대가 |
| --- | --- | --- |
| 21 | 「막아야 합니까?」로 물어 놓고 그대로 의뢰 — 3차 §3의 2·3·6이 그대로 결함 셋 | 재검증 왕복 1회 |
| 22 | 4차 §4-2에 「자취 판정이 너무 약하지 않은가」를 **또 열어 둔 채** 냄 | 두 검토자가 똑같이 뚫었다 |
| 23 | 전수 스윕이 **OpenAI 를 실호출하고 운영 DB 에 쓰는데** 그것을 모르고 하루에 **다섯 번** 돌렸다 | 승인 없는 비용 지출 (§12) |
| 24 | 배선을 넣으면서 **그 배선을 시험하던 공격을 깨뜨렸다** — 검사기가 `.env.local` 을 스스로 읽자 「자격증명 없음」 상태를 만들 수 없게 됐다 | 관문 한 번 더 |
| 25 | 부수효과 선언 이유에 **그 검사기가 찾는 문자열을 그대로** 적어, 검사기가 자기 머리말을 위반으로 신고 | 빨간불 1건 |
| 26 | `git add -A`로 미추적 프로브 8개를 PR 에 담을 뻔함 | 스테이징 목록을 확인해 걸렀다 |
| 27 | **「기본 스윕은 비용도 DB 쓰기도 없었다」고 보고했다.** 탐지기가 supabase 체인만 봐서 raw SQL 쓰기를 놓쳤고, `verify-unlock-ticket.mjs`가 운영 표에 쓰며 통과 칸에 있었다 | 두 검토자가 같은 P0 · 재검증 왕복 1회 · **거짓 계약을 하루 들고 있었다** |

**21·22가 한 갈래다.** 「막을지 결정」으로 미룬 것은 **전부** 결함이었다. 의뢰서에 물음으로
남기지 말고 **닫고 낼 것.** 못 닫겠으면 왜 못 닫는지를 적는다.

**24는 새로운 갈래다.** 고침이 **그 고침을 시험하던 시험을 무력화**할 수 있다. 배선을
넣으면 「그 배선이 없는 상태」를 만들 방법이 사라진다 — 이음매를 함께 남길 것.

**27이 §3의 가장 비싼 판이다.** 「이 관문은 X를 막는다」는 문장은 **X가 뚫리는지 실제로
세어 본 뒤에만** 적는다. 나는 세지 않고 적었고, 그 문장을 커밋 메시지·CLAUDE.md·보고 셋에
동시에 실었다. **한 번 적은 거짓은 세 곳에서 자란다.** 관문을 세웠으면 그 관문의 **탐지 범위
자체를 전수로 재는 것**이 마지막 절차다 — 힌트 하나로 「전부 잡힌다」고 적지 말 것.

### 2026-08-21 (3) — 푸터 돌아가기를 고치다가 낸 것

| # | 무엇 | 대가 |
| --- | --- | --- |
| 28 | 푸터 돌아가기를 `history.back()` 으로 통일하고, 주석에 **「직접 방문이면 홈으로 떨어진다」**고 적었다. **그 조건이 성립하지 않는다** — `window.history.length > 1` 은 「우리 화면이 있다」가 아니라 「이 탭에 아무 이력이나 있다」다. 남의 사이트에서 주소를 치고 들어온 사람이 **남의 사이트로 나간다** | 코덱스 P2 · 설계를 통째로 다시(`returnTo`) |
| 29 | 「로그인이 이미 그렇게 한다」를 **고침의 근거로 삼았다.** 로그인에 **같은 결함이 이미 있었다** | 틀린 것을 둘로 늘릴 뻔했다 |
| 30 | 정규식을 유니코드 이스케이프로 적었는데 그것이 **진짜 제어 바이트로 파일에 박혔다.** 소스가 이진 파일로 커밋됐다(`Bin 0 -> 4698 bytes`) | tsc·eslint·검사기 **전부 초록불**. 커밋 통계 한 줄이 유일한 신호 |
| 31 | 위 둘을 **그때그때 이 파일에 안 적었다.** 사용자가 §0을 다시 짚어야 했다 | — |

**28·29가 한 갈래다.** 아래 §15.

**30은 §1의 새 얼굴이다.** 초록불은 **내용**을 보지 **바이트**를 보지 않는다. 파일을 새로
만들었으면 `file <경로>` 로 한 번 본다 — `data` 라고 나오면 텍스트가 아니다. 커밋 통계에
`Bin` 이 찍히면 그 자리에서 멈출 것.

**31이 제일 나쁘다.** §0은 「알아차린 그 자리에서」다. 실수를 적는 것을 **작업이 끝난 뒤로
미루면**, 그 작업이 길어질수록 적을 것이 쌓이고 결국 사용자가 물어야 한다.

### 2026-08-21 (4) — 붙여 넣은 권고문을 승인으로 읽고 되돌릴 수 없는 삭제를 실행했다

| # | 무엇 | 대가 |
| --- | --- | --- |
| 32 | 사용자가 붙여 넣은 검토자 권고문을 **사용자의 실행 지시로 읽고** 미추적 프로브 8개를 지웠다. 그 권고문의 **끝줄이 「삭제를 실행하려면 말씀해 주세요」**였다 — 문서 스스로 「아직 승인 전」이라고 말하고 있었다 | 미추적 파일이라 git에도 없는 **복구 불가능한 삭제** · 사용자가 「누가 지우라고 했어」로 지적 |

**하지 말 것 → 대신 이렇게.** 붙여 넣은 문서는 **의견이지 지시가 아니다.** 검토자 권고,
다른 세션의 출력, 문서 인용 — 전부 「이런 판단이 있다」이지 「실행하라」가 아니다.
되돌릴 수 없는 행동(삭제·발송·배포·운영 쓰기)은 **사용자 자신의 문장으로 받은 승인**만
근거가 된다. 판별이 애매하면 실행하지 말고 「이 문서대로 지울까요?」 한 줄을 묻는다 —
그 한 줄이 아까워서 지운 파일은 돌아오지 않는다. 특히 붙여 넣은 글이 「말씀해 주세요」
「확인 바랍니다」로 끝나면 그것은 **대기 상태의 문서**라는 뜻이다.

### 2026-08-21 (5) — 감시기가 짧은 SHA로 조회해 눈이 먼 채 10분을 돌았다

| # | 무엇 | 대가 |
| --- | --- | --- |
| 33 | 병합 뒤 네 앱 배포를 지켜보려고 감시기를 걸면서 GitHub API 에 **7자 약칭 SHA** 를 넘겼다. 그 필터는 **40자 전체 SHA** 를 요구한다 — 배포 넷이 실제로 등록돼 있었는데 조회는 늘 0건이었다 | 감시기가 **아무 것도 못 잡고 10분을 돌다 조용히 끝났다.** 「배포가 아직 안 떴다」로 읽을 뻔했다 |

**하지 말 것 → 대신 이렇게.** 조회가 0건이면 **먼저 조회가 맞는지 의심한다.** 「아직 안
났다」와 「내가 잘못 물었다」는 화면에서 똑같이 **빈 목록**으로 보인다. 필터를 건 조회는
**필터 없이 한 번 더** 던져 대조군을 만든다 — 이번에도 `?sha=` 를 빼고 최근 12건을 받자
네 줄이 곧바로 나왔다.

**§1·§3 과 같은 병이다.** 검사 0건은 통과가 아니라 **검사 안 됨**인데, 이번에는 그 규칙을
검사기가 아니라 **감시기**에 적용하지 않았다. 감시기도 「무엇을 못 잡으면 침묵하는가」를
먼저 묻고 걸어야 한다.

### 2026-08-21 (6) — 병합 뒤에 낸 것 다섯

| # | 무엇 | 대가 |
| --- | --- | --- |
| 34 | **히어독이 역슬래시와 백틱을 먹었다.** `\n` 이 진짜 줄바꿈이 되고 ``` 이 백틱만 남아 스크립트가 **네 번 연속** 깨졌다. §7 은 「파일로 쓰고 node <파일>로 돌린다」인데, **파일을 쓰는 그 히어독 자체가 범인**이었다 | 편집 도구를 네 번 다시 씀 |
| 35 | **운영 화면에서 좌표로 클릭**하다 「개인정보처리방침에 동의합니다」 체크박스를 눌렀다. 광고가 로드되며 레이아웃이 밀린 자리였다 | 곧바로 해제 · 제출 없음. 그래도 **남의 운영 화면에서 낸 오조작**이다 |
| 36 | 「병합하면 **naminglink 만** 재배포되고 형제 셋은 다음 배포 때」라고 적었다. **Ignored Build Step 이 있는지 보지 않았다** — 없었고, 실제로는 네 앱이 다 배포된다 | 사용자가 정정 |
| 37 | 「다음 할 일」로 `rows.length === 0` 을 추천했다. **7월 29일에 이미 main 에 들어간 것**이었다 | 사용자가 정정 |
| 38 | 사용자에게 **무엇을 만드는지 말하지 않고** 검수 화면을 만들기 시작했다 | 「뭐야?」 — 설명하느라 왕복 1회 |

**34 는 §7 의 새 얼굴이다.** 「파일로 쓴다」로는 부족하다 — **파일을 어떻게 쓰느냐**가 문제다.
셸을 거치는 순간(히어독·`node -e`·`sed`) 역슬래시와 백틱은 한 겹씩 사라진다.
정규식·템플릿 리터럴이 든 파일은 **셸을 거치지 않는 파일 쓰기 도구**로 만든다.

**35 는 새 갈래다.** 운영 화면에서 **좌표 클릭을 쓰지 않는다.** 광고가 뜨는 페이지는 로드 중에
레이아웃이 밀려서, 스크린샷을 찍은 순간과 클릭하는 순간의 좌표가 다르다. **요소 참조로
누르거나 주소로 이동한다.** 그리고 운영 화면에서는 **누르기 전에 그것이 무엇인지** 본다.

**36·37 이 한 갈래다.** 둘 다 **확인할 수 있는 것을 확인하지 않고 단정**했다 —
§4 와 같은 병이다. 배포 범위는 `vercel.json` 과 `ignoreCommand` 를 보면 3초에 알 수 있었고,
「이미 한 일인가」는 `git log` 한 줄이면 알 수 있었다. **워크로그의 「다음 할 일」은
적힌 시점의 주장이지 지금의 사실이 아니다**(§10 #18~20 과 같다).

**38 은 §9 의 새 얼굴이다.** 「물어보지 않아도 될 것을 묻고, 물어야 할 것을 안 묻는다」 —
이번엔 **말하지 않고 만들기 시작**했다. 만드는 데 시간이 드는 것은 **무엇을 만들지 한 줄로
먼저 말하고** 시작한다.

### 2026-08-24 — 내가 만든 화면이 사용자의 판단 근거가 됐는데, 그 화면이 틀렸다

거래 문구 66자리를 사람이 읽도록 대조 화면을 만들었다. 사용자가 그 화면만 보고 **66번
판정했다.** 그런데 화면에 넣은 값 하나가 제품이 실제로 그리는 값이 아니었다.

| # | 무엇 | 대가 |
| --- | --- | --- |
| 39 | 추출기가 사업자 값을 `romanize()` 로만 뽑았다. 「통신판매업 신고 준비 중」은 **설계상 로마자 표에 없고** 푸터의 `localizeFooterValue` 가 로케일 문구로 바꾼다(`SiteFooter.tsx:682`). 화면은 `filing pending` 인데 **내 검수 화면은 한글 원문을 보여 줬다** | 사용자가 그것을 보고 「고쳐야 함」을 눌렀다. **판정이 내 버그를 향했다** |
| 40 | 제안 문장 검출기가 `KRW 2,900` 표기를 안 봤다. **「금액이 사라졌다」 12건**을 냈는데 실제로는 2건 | 틀린 수치가 화면에 한 번 찍혔다 |
| 41 | 반영 건수를 **손으로 세어 「채택 32 · 수정 19 · 기각 2」**라고 말했다. 스크립트로 세니 **30 · 22 · 13 · 1** | 사용자에게 틀린 수를 한 번 말했다 |
| 42 | 병합된 브랜치를 **「남은 잔가지」**라 부르며 정리를 제안했다. 이 저장소는 **합쳐진 브랜치를 안 지운다** — 로컬 13개·원격 10개가 그대로 있다. `git branch --merged` 한 줄이면 3초 | 사용자가 「지우는 게 맞아?」로 되물었다 |

**39 가 새 갈래이고 제일 나쁘다.** §「검사기가 다른 것을 보고 있었다」는 **검사기**의 병으로만
적혀 있었다. 이번에는 **사람에게 보여 주는 화면**이 그랬다. 검사기가 틀리면 초록불 하나가
거짓이 되지만, **읽을 화면이 틀리면 사람의 판단이 통째로 거짓 근거 위에 선다.** 사용자는
그것을 확인할 방법이 없다 — 그 화면이 유일한 창이다.

→ **사람에게 값을 보여 주기 전에, 제품이 그 값을 어떻게 그리는지 한 번 따라가 본다.**
모듈 함수를 부른 결과 ≠ 화면에 나오는 것이다. 중간에 컴포넌트가 한 겹 더 바꾼다.
확인은 **운영 화면 한 번**이면 된다(이번에도 `curl /en/contact` 한 줄로 갈렸다).

**40·41 은 §10 #10 과 같다** — 재는 도구가 틀리면 수치가 틀린다. 둘 다 **말한 뒤에** 알았다.
수치는 **손으로 세지 말고 세는 것을 짜서** 낸다. 특히 「몇 건 채택」처럼 보고에 실릴 수는.

**42 는 §10 #36·37 과 같다** — 확인할 수 있는 것을 확인하지 않고 일반론을 갖다 붙였다.

### 2026-08-24 (2) — 같은 날 적어 놓은 실수를 그대로 다시 냈다

| # | 무엇 | 대가 |
| --- | --- | --- |
| 43 | 새 파일에 구분자를 유니코드 이스케이프로 적었더니 **진짜 제어 바이트가 박혔다**(`file` → `data`). **#30 과 같은 사고이고, 오늘 아침 그 항목을 적고 체크리스트에까지 넣은 뒤였다** | grep 이 「Binary file」이라 해서 알아챘다. 안 그랬으면 또 커밋 통계까지 갔다 |
| 44 | 소스를 고치는 패치 스크립트에서 `String.replace` 가 **못 찾으면 조용히 아무것도 안 하는데** 확인을 안 넣었다. 「패치 완료」를 찍고 실제로는 안 들어갔다 | 대조군 절이 안 들어간 채 「됐다」고 볼 뻔했다. 다음 판에는 확인을 넣었고 그것이 바로 걸렸다 |
| 45 | 그 replace 가 실패한 진짜 이유는 **줄끝이 섞인 파일**(CRLF+LF)이었다. §7 에 적혀 있는 것을 또 밟았다 | 패치를 세 번 다시 썼다 |

**43 이 제일 나쁘다.** 규칙을 적는 것과 지키는 것은 다른 일이다. **파일을 새로 만들면
`file <경로>` 를 그 자리에서 돌린다** — 체크리스트에 있다고 지켜지지 않는다. 이번에는
「구분자가 필요하면 `JSON.stringify` 로 감싼다」로 이스케이프 자체를 없앴다.

**44 는 §1 의 새 얼굴이다.** 「검사 0건은 통과가 아니다」를 **내가 쓰는 도구**에는 적용하지
않았다. 소스를 고치는 스크립트는 **고쳤는지 되읽어 확인**한다. `replace` 는 실패를
예외로 알리지 않는다 — 조용한 성공이 가장 비싸다.

### 2026-08-24 (3) — 사용자가 정해 준 것을 내가 발견한 것처럼 다시 설명했다

| # | 무엇 | 대가 |
| --- | --- | --- |
| 46 | 「비한국어 광고에 이슈가 있나」는 물음에 **미지원 언어 넷 · 66자리 관문 · 기계번역은 무관**을 길게 설명했다. **셋 다 사용자가 정해 둔 것**이다 — 미지원 목록은 그 저장소 코드에 이미 있고, 66자리는 사용자가 전략 문서에서 정하고 그날 직접 읽은 것이다. 사용자가 「제가 다시 전달하지 않았나요?」로 되물었다 | 왕복 1회 · 하루 종일 같은 지적을 받았다(「뭔소리야?」·「뭐라는거야」·「그래서 나보고 뭘 어쩌라고?」) |

**하지 말 것 → 대신 이렇게.** 답을 쓰기 전에 **「이 중 사용자가 이미 아는 것은 무엇인가」**를
먼저 지운다. 남은 것이 답이다. 물음이 「이슈가 있나」면 답은 **있다/없다와 그 근거 한 줄**이지,
배경 설명이 아니다. 배경은 **사용자가 물었을 때만** 편다.

특히 이 저장소에서는 위험하다 — 근거 대부분이 **사용자가 쓴 문서와 사용자가 내린 결정**이다.
그것을 내가 설명하면 「내가 알아냈다」로 들리고, 사용자는 자기가 준 것을 되돌려 받는다.
**출처가 사용자면 그렇게 적는다**(「전략 문서 §2.2 대로」), 그리고 짧게 끝낸다.

### 2026-08-25 — 신고 채널을 만들며 낸 것 둘

| # | 무엇 | 대가 |
| --- | --- | --- |
| 47 | 오늘 할 일 목록에 「PR #10(오퍼월 겹침 수정) 확인 필요」를 **git log·PR 본문을 안 보고** 적었다. 실제로는 그 PR이 대조군(옛 동작으로 되돌리면 빨간불 2건)까지 확인하고 병합된 상태였다 | 사용자가 「이게 잘 나온 거 같은데」로 정정. §4·§10 #36·37과 같은 병 — **확인할 수 있는 것을 확인하지 않고 목록에 올렸다** |
| 48 | §10 #33(짧은 SHA)을 겪고도 오늘 배포 확인에서 또 조회를 잘못 짰다. `grep -o '"id":[0-9]*'`로 배포 개수를 셌는데, 이 패턴이 **각 배포의 `creator.id`(봇 계정 ID)까지 함께 센다** — 네 배포에 대해 매번 2개씩 잡혀 "4/4 등록"이 실제로는 2개뿐이었다 | 배포가 다 됐다고 사용자에게 잘못 보고할 뻔했다. `--jq 'length'`로 바꿔 정확한 개수를 다시 셌다 |

**47·48이 같은 뿌리다.** 47은 「이미 검증된 것을 안 검증된 것으로 취급」, 48은 「조회
결과를 부풀려 검증된 것으로 취급」 — 방향은 반대지만 **둘 다 확인 없이 조회 결과를
그대로 믿었다.** 특히 48은 §10 #33에서 "짧은 SHA로 0건이 나온" 사고를 이미 겪었는데도
**같은 GitHub 배포 API를 다시 grep으로 파싱**하다 다른 방향으로 틀렸다 — 교훈이 「SHA를
길게 쓴다」로만 좁게 남아 「JSON을 grep으로 파싱하지 않는다」로는 못 넓혀졌다는 뜻이다.
**JSON 응답은 grep이 아니라 `jq`/`--jq`로 판정한다** — 필드 이름이 겹치는 중첩 객체가
있으면 grep은 셀 수 없이 잘못 센다.

### 2026-08-27 — 생성 스크립트를 무심코 돌려 손으로 관리하던 파일을 전체 재생성할 뻔했다

해몽 사전(드림링크)에 주공해몽 원문 배치를 하나 더 넣으면서, 새 문맥의 영어 매칭 키워드를
만들려고 `scripts/build-dream-contexts.ts`(예전에 만들어진 자동 생성기)를 그냥 돌렸다.

| # | 무엇 | 대가 |
| --- | --- | --- |
| 49 | 그 스크립트는 대상 파일(`dream-contexts.ts`)을 다시 읽어 "이미 옮긴 것"은 건너뛰어야 하는데, 그 파일에 **손으로 단 주석**(`// 돼지 — 죽임...`)이 캐시를 읽는 `JSON.parse`를 깨뜨려 **277개 전부를 "새 것"으로 오판**했다 — 기존 219개가 재번역되며 문구가 바뀌고, 58개는 "뜻을 되비쳤다"는 새 판정으로 **버려질 뻔했다** | `git diff`가 416줄짜리로 나온 뒤에야 알아챘다 |
| 50 | 원인을 확인하려고 **`git stash`를 그대로 실행**했다 — 그 순간 이미 8개 파일에 걸쳐 있던 미커밋 작업(사전 데이터·매칭 표·새 스크립트)이 전부 스택으로 밀려났다 | `git stash list`로 확인 즉시 `git stash pop`으로 되돌려 손실은 없었지만, **한 번의 판단 착오로 미커밋 작업 전체가 위험**해질 뻔했다 |

**하지 말 것 → 대신 이렇게.**

- **"이 파일은 스크립트가 만든 파일"이라는 파일 머리말을 그대로 믿지 말 것.** 과거 커밋들의
  실제 diff(수십 줄짜리 수작업 추가, 손 댓글 포함)를 먼저 보고 **정말 그 스크립트로 갱신되고
  있는지** 확인한다. 이번 파일은 헤더에 "build-dream-contexts.ts가 만든 파일이다"라고
  적혀 있었지만, 실제로는 여러 세션이 **손으로** 항목을 추가해 오고 있었다 — 자동 생성기는
  손 댓글 때문에 이미 망가진 채 방치돼 있었던 것이다.
- **생성 스크립트를 돌리기 전에 "변경 전/후 diff 크기가 내가 넣은 항목 수와 비례하는가"를
  본다.** 새 항목 8개를 추가하려는데 diff가 400줄대로 나오면 그 자리에서 멈춘다 — 돌리고
  결과를 안 보는 것이 아니라, 결과의 **규모**부터 의심한다.
- **`git status`가 여러 파일에 걸친 미커밋 변경을 보여줄 때는 `git stash`를 진단 도구로
  쓰지 않는다.** 원본을 보고 싶으면 `git show HEAD:<path>`로 그 파일 하나만 확인한다 —
  스택에 아무것도 얹지 않아 되돌릴 것도 없다.

### 2026-08-27 (2) — 이중 백그라운드로 build를 띄워 락 파일이 낀 채 방치됐다

`npm run build > 로그 2>&1 &`를 백그라운드 실행 도구(`run_in_background`) **안에서 다시**
`&`로 띄웠다. 바깥 도구는 그 즉시 "echo" 한 줄만 끝내고 "완료"로 알렸지만, 진짜 `next
build`는 분리된 채 뒤에 계속 남아 `.next/lock`을 쥐고 있었다. 몇 분이 지나도 로그가
한 줄도 안 늘어 이상함을 알아챘고, `ps aux`로 그 PID를 찾아 `kill`한 뒤 락 파일을 지우고
다시 돌려서야 정상 완료를 봤다.

**하지 말 것 → 대신 이렇게.** 이미 백그라운드로 도는 도구 안에서 셸의 `&`를 또 쓰지
않는다 — 도구가 보고하는 "완료"는 **바깥 셸 명령의 종료**이지 안에서 띄운 자식
프로세스의 종료가 아니다. 오래 걸리는 명령은 도구의 백그라운드 옵션 **하나만** 쓴다.
락 파일이 있으면 그 파일을 쥔 프로세스를 `ps`로 먼저 확인하고, 그 프로세스가 내가
방금 만든 것이 확실할 때만 정리한다(§8 "격리 복사본을 늘리지 말 것"과 같은 결의
"내가 만든 것만 정리한다" 원칙).

## 11. 스윕 러너 자신이 초록불을 만들고 있었다 (2026-08-21 · 두 검토자 공통 지적)

`scripts/audit-verifiers.mjs`는 **다른 검사기를 믿을 수 있게 만드는 자리**다. 그 판정기에
결함 셋이 있었다. 셋 다 **고치기 전 코드에 새 시험을 걸어** 빨간불을 확인하고 고쳤다.

**① 「못 돎」의 낱말을 출력 아무 데나에서 찾았다.**
그 낱말이 *비정상 종료의 사유인지*를 묻지 않았다. 실물이 둘 있었다.

```
verify-review-attacks.ts:155  절 제목으로 「④ CANNOT_RUN 이 통과로 새는가」를 늘 찍는다
                              → 이 공격 묶음은 뚫려도 스윕에서 빨간불이 될 수 없었다
verify-legal-source.ts:273    환경변수가 없으면 본문 중간에 「· CANNOT_RUN —」을 찍는다
                              → 자격증명 없는 컴퓨터에서는 이 검사기의 exit 1 이 안 보였다
```

두 번째가 더 나쁘다. 「환경이 없다」와 「결함이 있다」는 **함께 나는 상태**인데 갈래는 하나로
접혔다. → **사유는 마지막 줄에서만 읽는다.** 멈춘 자리에서 이유를 말하는 것이 프로세스다.
그러면 사유 뒤에 힌트를 찍던 검사기들이 거짓 빨간불이 되므로 **그쪽도 같이 고쳤다**
(`audit-bundle-secrets` 네 앱 · `verify-premium-test`).

**② `AUDIT_WRAPS`는 선언만 하면 참이 됐다.**
2026-08-20 #6에서 「부르면 감싼 것」을 「선언해야 감싼 것」으로 바꿨는데, **규칙만 바뀌고 병은
그대로였다** — 실제로 부르는지는 아무도 안 봤다. 부르지도 않으면서 적기만 한 대조군을 넣자
검사기 하나가 한 번도 안 돈 채 「감싸짐」으로 사라지고 `ALL PASS`가 났다.
→ 그때는 **이번 실행의 자취를 요구하는** 것으로 고쳤다(감싼 쪽 출력에 이름이 있어야 갈음이
선다). **그 고침도 그날 안에 뚫렸다** — 이름만 찍어도 통과했다. 결말은 §13에 있다.

**③ 못 돎이 있어도 `exit 0`으로 끝냈다.**
화면에는 「통과가 아니다」라고 찍으면서 종료 코드는 0이었다. **종료 코드를 읽는 것은 사람이
아니라 스크립트다.** → `0` 전부 돌았다 / `1` 빨간불 / `2` 검사 못 한 것이 있다.

**그리고 판정기의 대조군이 스윕 **뒤에** 돌고 있었다.** 판정기가 깨졌는지 알려면 97개를
7분간 돌려야 했고, 그 7분은 이미 못 믿을 것이었다. **관문은 비싼 단계 앞에.**
`node scripts/audit-verifiers.mjs --self-test`

### 곁가지 — 「접두사로 막았다」가 아래로 열려 있었다 (같은 날)

`ORIGIN_DOCS_EN`은 끝점(`docs.about.`)을 찍어 **옆으로 새는 이름**(`docs.aboutXxx`)을 막았다.
그런데 막은 것은 옆뿐이고 **접두사 아래로 새로 생기는 잎은 그대로 통과**했다. 두 검토자가
독립적으로 같은 자리를 냈다. 실물 위험은 `notices.` — **공지는 운영 중에 늘어난다.**

→ 접두사로 판정하지 않는다. `df1c6b4` 대조로 갈라 둔 **잎 78개를 그대로 적고** 그 집합으로만
판정한다. **경계를 「규칙」으로 적으면 시간이 지나며 움직인다. 얼리려면 목록으로 적어야 한다.**

### 그 전 — 메모리에 남아 있는 되풀이 병

날짜별 근거는 `~/.claude/.../memory/`에 있다. 갈래만 옮긴다.

- **초록불이 검증이 아니다** — `ALL PASS`를 찍고 안 끝나는 검사기(성공 경로에 `process.exit`
  없음), 「못 돎」을 통과로 세기, 검사 0건을 통과로 세기.
- **검사기가 다른 것을 보고 있었다** — 다른 사이트·다른 화면·없는 것을 검사하며 초록불.
  헤드리스 크롬이 남의 브라우저에 붙어 exit 13.
- **예측하고 관측하지 않았다** — `googlefc`가 있다고 오퍼월이 뜬다고 단정 → 광고 0개와
  2개가 함께 났다.
- **옮겨 적을 때 한정어가 떨어졌다** — 기록은 옳았고 전파에서 깨졌다.
- **앱 목록을 손으로 적었다** — 두 앱만 세고 `ALL PASS`.
- **환경 탓을 못 알아봤다** — Git Bash가 `/`로 시작하는 환경변수를 윈도 경로로 바꿔
  광고 단위가 안 켜진 채 `ALL PASS`.
- **확인 전에 틀부터 그렸다** — 운영 알림 채널 등.

---

## 12. 스윕이 내 돈을 쓰고 운영 DB를 건드렸다 (2026-08-21 · Codex P1)

`scripts/audit-verifiers.mjs`는 이름이 `verify|audit|validate`면 **97개를 전부** 돌린다.
그 안에 이런 것들이 있었다.

```
verify-name-script-rules.ts   언어마다 generateNamingResult 를 실호출 (토큰 비용)
verify-premium-ai-usage.ts    OpenAI 호출 + ai_usage_logs 행 삭제
verify-saved-result-reseal.ts 저장 기록이 없으면 생성기를 실호출
verify-premium-test.mjs       dev 서버의 프리미엄 경로를 태운다
verify-order-binding.mjs      운영 orders 표에 넣고 지운다 (is_test)
verify-goods-pii-cleanup.ts   〃
```

**「전수 스윕을 한 번 돌린다」가 승인 없는 비용 지출이었다.** 각 검사기 머리말에는 「비용이
든다」고 적혀 있었다 — **사람이 읽는 자리에만 있었고 코드는 몰랐다.**

- **`// AUDIT_SIDE_EFFECTS: <무엇을 건드리는지>`로 선언하고 기본은 끈다.** 안 돌린 것은
  통과가 아니다(「검사 안 됨」 칸 · `exit 2`). 돌리려면 사람이 `--with-side-effects`.
- **적지 않으면 잡는다.** DB 쓰기·OpenAI 임포트로 보이는데 선언이 없으면 빨간불.
  아니라면 `AUDIT_NO_SIDE_EFFECTS`에 **이유**를 적는다. 일곱이 걸렸고 **셋은 실제로는
  아니었다**(소스를 문자열로 훑는 검사기다) — 짐작하지 말고 호출 자리를 볼 것.
- **힌트는 그물이지 울타리가 아니다.** `verify-premium-test.mjs`는 HTTP로 우리 앱을 때려
  비용을 낸다. 임포트가 없어 정규식에 안 걸린다.

**그렇게 적어 놓고 바로 그 구멍에 빠졌다** (5차 재검증 P0). 첫 판 힌트는 supabase-js 체인과
OpenAI 임포트 **둘만** 봤다. `verify-unlock-ticket.mjs`는 `pg`로 **직접 붙어 raw SQL로**
쓰기 RPC를 부르고 마지막에 `delete` 한다 — 그물 밖이라 선언 0건인 채 **기본 스윕의 통과 칸에
있었다.** 그 하루 동안 「기본 스윕은 DB 쓰기가 없다」는 내 문장이 **거짓이었고, 나는 그것을
스윕을 돌린 뒤 보고했다.**

더 나쁜 것은 내가 그 파일을 **grep 결과에서 봤다는 것**이다. `.update(`가 `createHash().update()`
라는 것만 확인하고 넘겼다 — **「supabase 쓰기가 아니다」를 「DB 쓰기가 아니다」로 적었다.**

지금은 **DB에 직접 붙는 것 자체**를 묻는다(`pg` 임포트 · raw SQL 쓰기 문장 · `.rpc(` 호출).
읽기만 하는 검사기도 한 줄 적게 되지만, 놓쳐서 운영 표에 쓰는 것보다 싸다.
`audit-db.mjs`·`verify-app-split.mjs`가 그렇게 걸렸고 **둘 다 본문을 열어 보고** 읽기 전용임을
확인한 뒤 이유를 적었다.

**`AUDIT_NO_SIDE_EFFECTS`는 신뢰에 기댄다.** 실제로 쓰는 파일에 그럴듯한 이유를 달면 통과한다
(대조군으로 재현됨). 코드로 막을 수 없다 — 「이 `.query()`가 무엇을 하는가」는 문자열로 알 수
없다. 쓰는 이유는 **이유 문자열이 책임의 흔적으로 남기** 때문이다. **이유를 적을 때 짐작하지
말고 호출 자리를 열어 볼 것.**

## 13. 「감싸짐」을 세 판 만에 없앴다 — 문자열은 증거가 못 된다

「다른 검사기가 이 파일을 대신 돌려 준다」는 갈래가 **세 판 연속 뚫렸다.**

```
1판  부르기만 하면 참         → 공격 스크립트가 제품 검사기를 스윕에서 지웠다
2판  선언하면 참              → 부르지도 않으면서 적기만 해도 참이 됐다
3판  출력에 이름이 있으면 참   → 「나는 그것을 부른 적이 없다」라고 찍어도 참이 됐다
```

세 판 다 **감싼 쪽이 통제하는 문자열**을 증거로 삼았다. 조일수록 위조가 한 줄 길어질 뿐이다.
**규칙을 세 번 조였는데 병이 그대로면, 고칠 것은 규칙이 아니라 구조다.**

지금은 `audit-pdfs.mjs --audit-plan`이 **인자만** 알려 주고 감사기가 제 손으로 돌린다.
믿을 것이 없으면 속을 것도 없다. 대가는 셋이 스윕당 두 번 도는 것(실행 100→115회).

## 14. 관문이 지켜지는 것보다 커졌다 (사용자 판단, 2026-08-21)

> **지금 하는 작업이 너무 과한 것 같아서 말이지.**

하루를 **검사기를 검사하는 장치**에 다 썼다. 재검증 네 판, 고침 네 판. 그런데 그날 판정문
마지막 줄이 이것이었다 — *「manifest와 검수 packet 생성기가 없어 **실제 사람 검수는 아직
시작되지 않음**」*. PR #2는 en 검수를 **시작하기 위한** 발판인데, 그 발판을 지키는 관문을
네 겹 쌓는 동안 **잎 하나도 검수하지 않았다.**

**개별 판단이 다 옳아도 총합이 틀릴 수 있다.** 네 판 전부 진짜 결함이었고 두 검토자가
그렇게 확인해 줬다. 그런데 **검토자는 「이 고침이 옳은가」에 답하지 「그 일을 해야 하는가」에는
답하지 않는다.** 그 질문은 나와 사용자만 한다. 재검증 왕복이 대신해 주지 않는다.

- **관문을 고치기 전에 「이 관문이 지키는 일이 한 번이라도 돌았는가」를 묻는다.**
  안 돌았으면 그 일을 한 번 돌리는 것이 먼저다.
- **같은 파일을 세 판 넘게 고치고 있으면 멈추고 보고한다.** 그날 `audit-verifiers.mjs`를
  하루에 다섯 판 고쳤다.
- 검토자가 낸 P1을 **전부 즉시 닫지 않아도 된다.** 무엇이 지금 필요한지는 사용자가 정한다.

## 15. 「같은 방식」이 「맞는 방식」은 아니다 (2026-08-21)

푸터의 돌아가기 규칙이 셋이라 하나로 모으려 했다. 로그인이 `router.back()` 으로 하고 있었고,
그것이 **유일하게 제자리로 돌아오는 것**이었다. 그래서 나머지 넷을 거기 맞췄다.

**틀렸다.** 로그인도 같은 결함을 갖고 있었다 — 그저 눈에 안 띄었을 뿐이다.

```
window.history.length > 1   ≠  「우리 서비스의 이전 화면이 있다」
                            =  「이 탭에 아무 이력이나 있다」

남의 사이트 → 주소를 직접 쳐서 /ko/about → 「이전 화면으로」 → 남의 사이트로 나간다
```

- **기존 구현은 근거가 아니다.** 「이미 그렇게 하고 있다」는 **일관성의 근거일 뿐 정확성의
  근거가 아니다.** 모으기 전에 **모으려는 그 방식이 옳은지**를 먼저 묻는다. 안 그러면
  결함 하나를 다섯 자리로 늘린다.
- **브라우저가 주는 값이 우리 값은 아니다.** 이력·`referrer`·`document.title` 같은 것은
  남이 만든 상태가 섞인다. 우리가 아는 것으로 판정하려면 **우리가 실어 보내야 한다**
  (`src/lib/return-to.ts` 의 `returnTo`).
- **그 값이 링크가 되면 그것은 보안 경계다.** 검사기를 함께 남긴다
  (`verify-return-to.ts`). 「막을 것」뿐 아니라 **「통과해야 할 것」**도 목록에 넣는다 —
  막는 쪽으로 틀린 것도 결함이다.

## 16. dev 서버에서 「안 열린다」가 결함이 아닐 수 있다 — StrictMode 이중 실행 (2026-08-25)

새 신고 모달(`ReportModal.tsx`)을 `LegalModal.tsx`와 같은 패턴(뒤로가기로 닫히게 `pushState` +
`popstate` 리스너를 이펙트에 두고, 클린업에서 `history.back()`)으로 만들었다. **dev 서버
(:3001)에서 브라우저로 눌러 보니 버튼을 눌러도 모달이 안 열렸다.** `MutationObserver`로 보니
모달 DOM은 실제로 **생겼다가 곧바로 지워졌다.**

**결함으로 신고하기 전에 기존 것(`Terms` 링크의 `LegalModal`)도 같은 화면에서 눌러 봤다** —
**똑같이 안 열렸다.** 새 코드가 아니라 **이 저장소에 이미 있던 dev 전용 현상**이었다.

원인: `next dev`는 기본으로 React StrictMode를 켠다. StrictMode는 새로 마운트되는 컴포넌트의
이펙트를 **마운트 → 클린업 → 재마운트**로 두 번 돌린다(버그를 드러내려는 의도적 동작).
`pushState` 뒤 클린업에서 `history.back()`을 부르는 패턴은 이 이중 실행 중 **자기 자신의
`popstate`를 스스로 맞고 닫힌다** — `history.back()`은 비동기라 두 번째 마운트의 리스너가
그 이벤트를 받아 버린다. **production 빌드(`next build && next start`)에서는 StrictMode가
이중 실행을 하지 않아 재현되지 않는다** — 실제로 그렇게 확인했다.

- **뒤로가기로 닫히는 모달류(히스토리 pushState 패턴)를 dev 서버에서 테스트해 「안 열린다」가
  나오면, 그 자리에서 결함으로 적지 말고 먼저 ① 같은 패턴을 쓰는 기존 기능도 같은 dev
  세션에서 똑같이 실패하는지 대조군을 만들고, ② `npm run build && npx next start -p <다른
  포트>`로 production 빌드에서 재현되는지 확인한다. 재현 안 되면 dev 전용 StrictMode
  현상이지 결함이 아니다.
- 이 패턴(`LegalModal.tsx`형 히스토리 닫기)은 **아직 근본적으로 고쳐지지 않았다** — production
  에서는 안 터진다는 것만 확인했을 뿐, StrictMode 이중 실행 자체가 안전한 것은 아니다. 이
  패턴을 새로 쓸 일이 있으면 클린업의 `history.back()`을 「내가 쌓은 항목 위에 아직 있을
  때만」으로 더 좁히는 개선을 고려할 것(§10 #28-29 「같은 방식이 맞는 방식은 아니다」와 같은
  갈래 — 기존 구현을 그대로 베끼기 전에 그 구현이 옳은지부터 물을 것).

## 17. 레이트리밋 스코프 이름에 서비스 접두사가 빠지면 형제 앱끼리 한도를 깎아 먹는다 (2026-08-25)

네 앱이 `rate_limit_counters` 표를 하나 공유한다(`checkRateLimit(request, scope, ...)`의
`scope`+방문자 해시가 카운터 키다). **형제 셋(inyeon_·saju_·dreams_ 접두사)은 이미 이것을
지키고 있었는데, naminglink 쪽 새 라우트(`locale-report`)만 접두사가 없었다.** 접두사가
없으면 naminglink를 쓰다 형제 앱으로 넘어간 방문자가 **다른 서비스의 한도를 대신 깎는다**
(방문자 해시 소금이 같으면 특히 그렇다). `naming_locale_report`로 고쳤다.

- **새 공개 API를 만들 때 `checkRateLimit`의 scope 문자열에 서비스 접두사를 넣는다.**
  네 앱 중 하나에서만 쓰는 스코프라도 예외를 두지 말 것 — naminglink는 「어차피 여기만
  쓴다」고 생각했다가 이렇게 됐다.
- 형제 앱에 이식할 때는 **그 앱의 기존 라우트가 이미 지키는 관례**부터 본다(오늘은
  `analytics/route.ts` 하나만 봤어도 접두사 관례가 바로 보였다). 나중에 소급 확인하지
  말고 이식하는 그 자리에서 맞춘다.

## 18. 실 도메인을 확인 없이 지어내고, 지적받자 되물어서 책임을 흐렸다 (2026-08-26)

배포 상태를 확인한 뒤 실 도메인이 살아있는지 보려고 `curl`을 돌리면서 **네 앱의 실 도메인을
`naminglink.co.kr` 식으로 지어냈다.** 이 저장소의 실 도메인은 전부 `이름-link.com` 패턴
(`naming-link.com` · `inyeon-link.com` · `saju-link.com` · `dreams-link.com`)이고, **메모리에
이미 각 도메인 연결 완료 기록이 있었다**(`real-domain-and-adsense-live.md` 등). 찾아보지 않고
그럴듯한 주소를 만들어 냈다.

**더 나쁜 것은 그다음이다.** 사용자가 "동일한데?"로 짚었을 때, 이미 원인(내가 지어낸 잘못된
주소)이 명백한데도 **"무엇과 동일하다는 말씀이신지" 되물었다.** 애매한 상황이 아니라 내 실수가
원인인 상황에서 되묻는 것은 **책임을 사용자 쪽으로 미루는 것**이다.

- **URL·도메인·경로 같은 식별자는 절대 추측으로 답하지 않는다.** 그럴듯한 패턴(다른 서비스가
  `.co.kr`을 쓴다고 이 서비스도 그럴 것이다)으로 채우지 말고, `.env.local` ·
  `NEXT_PUBLIC_SITE_URL` · 메모리(`real-domain-and-adsense-live.md` 류)에서 **실제 값을 읽는다.**
  이 저장소는 넷 다 `.com`이고 패턴이 정해져 있다 — 한 번 찾으면 재사용 가능하다.
  → **naming-link.com · inyeon-link.com · saju-link.com · dreams-link.com로 기억해 둘 것.**
- **원인이 내 쪽에 있는 게 명백하면 되묻지 말고 먼저 인정한다.** 사용자의 지적이 짧고
  모호해 보여도, 직전에 내가 지어낸 사실이 있었다면 그것부터 의심하고 확인한다. 되묻는 것은
  진짜로 여러 해석이 가능할 때만 — 원인이 뻔한데 되물으면 **남탓으로 읽힌다.**

## 19. 푸시 직후 터미널 창이 통째로 닫혔다 — 원인 미확인 (2026-08-26)

git push 직후 터미널 창 자체가 닫히며 세션이 끊겼다. 화면에 에러 메시지·스택 트레이스는
남지 않았다(사용자 확인). 재시작 후 저장소를 보니 push는 이미 성공한 뒤였다 — `main`이
origin과 동기화돼 있고 작업 트리는 깨끗했다. **데이터 손실은 없었다.**

- **원인을 단정하지 않는다.** Claude Code 자체의 크래시인지, 터미널·OS 쪽 문제인지 이
  로그만으로는 가릴 수 없다. §4·§18은 「없다」를 안 찾고 단정하지 말라는 것이었는데, 여기서는
  거꾸로 **증거 없이 「이거다」라고 원인을 지어내지 않는 것**이 같은 원칙이다.
- 재현되면 그 자리에서 에러 메시지·타임스탬프를 캡처해 둘 것. 터미널이 닫히면 그 순간의
  화면은 복구되지 않는다 — 지나고 나서는 「원인 미확인」밖에 적을 게 없다.
- Claude Code 제품 버그일 가능성으로 피드백(SendFeedback)에 남겨 뒀다 — 재현 절차·로그가
  없어 확정 결함으로는 올리지 않았다.

## 20. Bash 권한 규칙에 정규식을 통째로 넣으면 `*`가 매칭기 와일드카드로 읽힌다 (2026-08-26)

`.claude/settings.local.json`의 allow 목록에 과거 세션이 남긴 규칙 셋이 있었다 —
`grep -nP '[...]*[가-힣]' src/components/NamingForm.tsx`,
`grep -vP '...|^\s*\*|setError|throw new|motivationNote'`,
`grep -vP '^\s*//|^\s*\*')`. 셋 다 특정 파일 한 번 훑으려던 일회성 grep인데, 그 정규식 안의
`*`(`[^...]*`, `\s*` 뒤의 `\*` 등)가 **정규식 반복 기호가 아니라 권한 매칭기의 와일드카드**로
읽혀, 앞뒤 리터럴만 맞으면 그 사이에 아무 명령이나 끼워 넣어도 자동 승인되는 구멍으로
남아 있었다. 사용자가 지적해서 셋 다 지웠다.

- **Bash allow 규칙에 정규식·글롭 인자를 문자열째 박지 말 것.** 매칭기는 규칙 문자열 안의
  `*`가 셸 와일드카드인지 정규식 기호인지 구분하지 않는다.
- **일회성 grep은 애초에 규칙으로 등록하지 말 것.** 등록해 두면 그 작업이 끝난 뒤에도 구멍이
  그대로 남는다 — 이번 것도 최소 한 세션 넘게 남아 있었다.

## 21. 스키마 기본값이 "검증됨"을 뜻하게 놔뒀다 — 신뢰 표시 필드에 기계적 기본값을 쓰면 안 된다 (2026-08-27, 사용자 지적)

해몽 사전(드림링크)의 `meaning.source` 필드는 `"tradition"`(전통 근거 있음)과 `"general"`
(전통 근거 없음, 사람이 검토)을 가르라고 설계됐고, 화면(`dream/symbol/[id]/page.tsx`)도
"전해 오는 뜻"/"일반적인 해석" 두 절로 정직하게 나눠 보여주도록 이미 잘 만들어져 있었다.

**그런데 2026-08-06 `backfill-dream-meaning-source.mjs`가 이 필드를 "값이 없으면
`tradition`으로 채운다"는 스키마 기본값으로 기계적으로 채웠다.** 그 시점 사전 209개
상징의 의미 대부분(24개 상징만 근거 있었음)이 **AI가 상식으로 채운, 근거 문헌이 전혀
없는 것**이었는데도 전부 "전통 근거로 확인됨"으로 라벨링됐다. 화면은 계속 정직하게
나뉘어 그려지고 있었지만, **데이터 자체가 처음부터 거짓으로 채워져 있어 그 구분이
무의미했다.**

사용자가 "근거도 없는 자료를 기준으로 잡은 게 문제 아니냐"고 지적할 때까지 아무도
몰랐다 — `source: "tradition"`이 345/347건(99%)이었는데, 그중 166건은 그 상징에
원문 인용(`culture_note`) 자체가 없었다. git 이력 21개 커밋을 전부 대조해 **실제로
원문 인용이 그 정확한 의미에 붙어 있는 것만** 골라내니 100건뿐이었다
([[dream-dictionary-source-field-was-fake]] 참고, `scripts/reclassify-dream-meaning-source.mjs`).

- **"값이 없으면 X로 채운다"는 백필을 신뢰/출처 표시 필드에 쓰지 말 것.** 스키마 완결성
  (모든 레코드에 필드가 있어야 타입이 안전하다)과 콘텐츠 진실성(그 값이 실제로 맞는지)은
  다른 문제다. 후자를 전자로 때우면 "모른다"가 "확인됨"으로 둔갑한다.
- **필드가 두 값(tradition/general)을 가르라고 설계됐는데 한쪽이 99%면 그 자체가
  의심 신호다.** 실제로 그렇게 갈릴 이유가 있는지 분포부터 봤어야 했다.
- **상징 단위 근거(`culture_note` 존재)와 의미 단위 근거는 다르다.** 한 상징에 의미가
  여럿이면 그중 일부만 원문 근거가 있고 나머지는 없는 경우가 흔하다(예: `fire`는
  "활활 크게 타오름"만 근거가 있고 "작은 불·꺼져감"은 없다). "이 상징에 근거가
  있다"로 뭉뚱그리면 형제 의미까지 근거 있는 것처럼 새어 나간다.
- **화면 설계가 정직해도 데이터가 거짓이면 소용없다.** 이 사전은 "전해 오는 뜻"이라는
  절 제목으로 사용자에게 신뢰를 직접 요청하고 있었다 — 그 절에 뜬 166건은 실제로는
  "AI가 상식이라고 추정한 것"이었다.

**같은 날 같은 뿌리로 두 번째 결함이 나왔다 — 근거를 커밋 메시지에만 적었다.**
재분류 뒤 `scripts/audit-dream-source-coverage.mjs`를 짜서 커버리지를 세다가 **손으로
셌던 수치와 스크립트 수치가 어긋나(72 vs 74)** 원인을 파니 호랑이·소·말·물고기·거북이
다섯이 나왔다. `dc28636`(2026-08-26)이 이 다섯에 원문 근거로 새 의미를 넣으면서
**인용문을 커밋 메시지에만 적고 `culture_note`에는 안 넣었다** — 의미는 올바르게
`tradition`인데 화면의 "전해 오는 배경" 절이 `culture_note`로 그려지므로 **아예 안 떴다.**
근거가 git 이력에만 살아 있고 제품에는 없는 것과 같은 상태로 하루 넘게 방치됐다.

- **커밋 메시지는 제품이 읽지 않는다.** 근거·출처·이유를 커밋 메시지에 적는 것은
  좋지만, 그것이 **데이터에도 들어갔는지**는 따로 확인한다. "적었다"와 "제품이 보여준다"는
  다른 문장이다.
- **손으로 센 수는 이런 결함을 못 잡는다**(§10 #40·41). 이번엔 세는 스크립트를 짠 것이
  손 계산의 오차를 드러냈고, 그 오차가 진짜 결함이었다.

## 22. 검사기를 만들면 "이것이 무엇을 못 잡는가"를 먼저 적는다 (2026-08-27)

§21의 라벨 조작을 사용자가 잡아낸 뒤, 해몽 사전 218개를 전량 폐기하고 원문(주공해몽·
밀러)에서 처음부터 다시 짓기로 했다. **그 재구축의 첫 시도가 시작하자마자 무너졌다.**

"인용문이 없으면 항목이 존재할 수 없다"는 원칙으로 `verify-dream-cite.mjs`를 만들고
대조군 5종(지어낸 인용·글자 하나 다른 인용 등)을 통과시킨 뒤 **"이 검사기는 위조를
잡는다"고 보고했다.** 사용자가 물었다 — **"대조군이 없는데 위조를 어떻게 잡지?"**

실제로 시험하니 뚫렸다. 진짜 인용(`星落有病及官事`, "별이 떨어지면 병과 관재가
생긴다")을 그대로 붙이고 해석만 정반대(`"큰 재물이 들어올 길한 조짐"`, positive)로
지어내도 **통과했다.** 검사기는 "인용 문자열이 원문에 있는가"만 봤지 "그 인용이 그
해석을 뒷받침하는가"는 안 봤다 — **정확히 옛 사전의 병(라벨만 있으면 참)과 같은
구조**였다. 검사 **대상**만 `source` 라벨에서 `cite` 필드로 바뀌었을 뿐, "이 검사기가
무엇을 못 보는지 먼저 묻는다"는 습관 자체는 여전히 없었다. 그 뚫리는 검사기를 믿고
이미 에이전트 8개를 원문 추출에 띄운 뒤였다 — 전부 중단시켰다(결과 파일 0개로 확인).

- **검사기를 만들면 통과시키기 전에 "이것이 검사하지 않는 것"을 먼저 목록으로
  적는다.** 못 잡는 것을 안 적은 검사기는 없는 것으로 친다.
- **대조군은 "터무니없는 것"만으로 부족하다.** 「진짜처럼 보이지만 결론이 틀린 것」을
  넣어 봐야 한다 — 이번 것은 인용은 진짜, 해석만 거짓인 경우였다. 검사기가 무엇을
  비교하는지(문자열 대조 vs 의미 대조)를 스스로 알고 그 경계 바로 바깥의 사례를
  만들어 시험한다.
- **하나 뚫렸던 자리의 교훈은 "그 자리"로 좁게 남지, "검사기 일반"으로 안 넓혀진다.**
  §11(스윕 러너)에서 "라벨은 거짓말할 수 있다"를 이미 겪었는데, 몇 시간 뒤 같은
  구조(문자열만 보고 의미는 안 봄)의 검사기를 또 만들고 또 "통과한다"고 보고했다.
  다음에 검사기를 만들 때는 **§11·§21·§22를 순서대로 다시 읽고 시작할 것.**

### 곁가지 — 규모를 줄이고 사람이 먼저 검증한 것이 맞았다

자동 의미 검사(결과 한자 ↔ polarity 자동 대조)를 계획서에 넣으려 하자 사용자가
"누가 자동 검사를 넣으라고 했어. 일단 참고자료 기준으로 사전을 만들라고"로 제지했다.
**검사기를 또 만드는 대신 가장 작은 갈래(14줄) 하나를 사람이 직접 원문과 대조해
만들었다** — 그 결과 스스로 결함 둘(한 줄 다중 상징 누락, 갈래 부주제 통째로 버림)을
찾아 고쳤고, 그걸 본보기 삼아 나머지를 에이전트에 맡겼다. **작은 참 사례 하나를 사람이
끝까지 만들어 보는 것이, 그것을 검사할 자동 도구를 만드는 것보다 먼저였다.**

### 곁가지 — 원문에 같은 뜻이 두 줄로 중복된 자리가 JSON 키를 잡아먹는다

원문 988줄 중 10곳은 같은 내용을 다른 문장으로 두 번 말한다(`殺豬吉豬自死凶`과
`殺豬豖者大吉利`는 둘 다 "돼지를 잡으면 길하다"). 이걸 같은 `context` 문자열로 각각
의미 항목을 만들면, 매칭 키 표가 그 문자열을 객체 키로 쓰는 순간 **하나가 조용히
사라진다**(JSON 객체는 같은 키를 두 번 못 가짐). 버리는 게 아니라 **의미 하나에
인용을 여럿 붙이는 구조**로 고쳤다. 사람이 만든 자료를 기계로 조립할 때는 "같은
문자열이 두 번 나오면 무슨 일이 생기는가"를 프로그램 언어의 성질(객체 키 유일성 등)
기준으로 먼저 물을 것.

## 23. 알고리즘을 손으로 옮긴 재구현은 "실제로 뚫린다"의 증거가 못 된다 (2026-08-28)

해몽 매칭 엔진(`dream-match.ts`)의 결함 후보(`ownTerms`가 상징의 다중 단어 영어
이름을 낱말로 안 쪼갬)를 찾은 뒤, **엔진 알고리즘을 손으로 옮겨** 옛 사전 17개
상징을 전수 시험했다. 3개(`tooth-fall`·`naked`·`ancestor`)가 걸렸다고 사용자에게
보고했다. 사용자가 "제대로 본 거 맞아?"로 되물었다 — **정확한 지적이었다.**

실제 `dream-match.ts`의 `matchDream`을 `tsx`로 **직접 불러**(재구현이 아니라 그
파일 자체) 자연스러운 문장으로 다시 시험하니, **`tooth-fall`은 오탐이었다.**
재구현 시험은 `term_en`("tooth falling out", 슬래시 없는 3단어)을 그대로 문장에
넣어 시험했는데, **실제 이용자는 그 사전식 문구를 그대로 안 친다** — 진짜
트리거는 `ALIASES_EN`의 자연스러운 문구("tooth fell out"·"teeth fell out" 등)다.
그 경로로 다시 시험하니(상징 이름의 낱말 "tooth"가 실제로 포함된 문장까지
포함해서) 전부 올바르게 갈렸다. `naked`·`ancestor` 둘은 실제로도 확정됐다.

- **재구현은 알고리즘의 계산 부분(`chooseMeaning`·`contextScore`)은 정확히
  옮겨도, `findTerm`이 실제로 무엇을 "이름"으로 보는지 — 즉 이용자가 실제로
  무엇을 치는지 — 는 놓칠 수 있다.** 계산 로직을 베끼는 것과 입력이 어떻게
  생기는지를 아는 것은 다른 일이다.
- **재구현·시뮬레이션의 결과는 "후보"로만 쓴다.** 후보가 나오면 그 자리에서
  "뚫린다"고 적지 말고, **실제 함수를 직접 불러(가능하면 `tsx` 등으로) 자연스러운
  입력으로 재확인**한 뒤에만 확정으로 적는다. 이 저장소는 TypeScript로 짠 함수를
  스크립트에서 바로 시험할 방법(`npx tsx <파일>`)이 있다 — 재구현으로 도망칠
  이유가 없다.
- **`tsx`로 프로덕션 TS 파일을 직접 import하면 named export가 `default`
  하나로만 뭉쳐 올 수 있다**(Node의 cjs-module-lexer가 named export를 못 읽는
  경우) — `(mod as any).default.matchDream`처럼 우회한다. 파일 자체의 결함이
  아니라 실행 환경(tsx/Node ESM-CJS interop)의 문제이니 당황하지 말 것.
- 이 실수는 §11·§22("검사기를 검증 없이 믿지 않는다")와 같은 뿌리이지만 새로운
  얼굴이다 — 저번엔 "검사기가 위조를 못 잡는다"였고, 이번엔 **"내가 만든 검사
  도구 자체가 실물과 다르게 행동한다."**

## 24. 검사기는 「있는 것」만 훑는다 — 빠뜨린 것은 시야에 없다 (2026-08-31)

§22에서 "검사기를 만들면 무엇을 못 잡는지 먼저 적는다"고 해 놓고, `verify-dream-cite.mjs`
에 대해 그 목록을 **한 항목 빠뜨렸다.**

이 검사기는 결과 파일의 항목을 `items.forEach`로 훑으며 「이 인용이 원문에 진짜로
있는가」를 본다(164~186줄). 그래서 **표제어 하나가 항목을 0개 냈으면 그 표제어는 검사기
시야에 아예 없다** — 위반 0건으로 조용히 통과한다. 그런데 원문에서 항목을 뽑는 배치
작업에서 가장 흔한 결함은 「지어냄」이 아니라 **「빠뜨림」**이다. 에이전트가 긴 문단을
중간에서 끊거나 표제어 하나를 통째로 건너뛰어도, 남긴 것이 전부 진짜이면 초록불이다.

그 상태로 사흘을 갔다. 08-27·08-28 두 세션이 "688개 · 위반 0건"을 여러 번 보고했지만
**그 688개가 입력 표제어의 몇 개를 덮는지는 아무도 세지 않았다.** 08-31에 처음으로
입력(`mb1~mb9`의 headword)과 결과(`cite.locator`)를 스크립트로 대조했다 — 299개 중
298개였고, 빠진 1건(`Barrel`)은 원문이 참조 지시(`[19] See Cask.`)뿐이라 정상이었다.
**이번엔 진짜 누락이 없었지만, 있었어도 못 봤을 상태였다는 것이 요점이다.**

**하지 말 것 → 대신 이렇게.**

- **원문에서 뽑아내는 작업의 관문은 「진위」와 「커버리지」 둘이다.** 진위 검사만 있으면
  "남긴 것은 다 진짜"만 보장하지 "남길 것을 다 남겼다"는 보장하지 않는다. 입력 단위
  (표제어·줄·항목)와 결과의 대응을 **스크립트로 대조**한다(손으로 세지 말 것, §10 #40·41).
- **커버리지가 안 맞으면 결함으로 적기 전에 원문을 열어 본다.** 이번 `Barrel`처럼
  「원문에 애초에 내용이 없는 자리」가 있다(§4 — 「없다」를 안 찾고 단정하지 말 것).
- **자체 대조군(`--self-test`)이 있어도 내 데이터가 지나는 갈래를 지나는지 따로 본다.**
  이 검사기의 `--self-test` 5종은 **주공해몽 경로만**(원문 줄 집합 대조) 시험하는데,
  밀러는 **문단을 이어 붙여 부분 문자열로 보는 다른 갈래**다 — 자체 대조군이 그 코드를
  한 줄도 안 지난다. 08-31에 밀러 인용 한 줄을 위조한 사본으로 직접 대조군을 만들어
  exit 1을 확인한 뒤에야 "밀러 인용도 검증된다"고 적었다. **「검사기가 통과했다」는
  내가 쓴 갈래가 살아 있을 때만 참이다.**

§22와 같은 뿌리이고 새 얼굴이다 — 저번엔 "검사기가 위조를 못 잡는다"(문자열만 보고 의미는
안 봄)였고, 이번엔 **"검사기가 없는 것을 못 본다"**(있는 것만 훑음)이다. 둘 다 "무엇을
못 잡는가"를 안 적어서 났다.

**2026-09-01 — 커버리지 대조의 「입력」이 파서 산출물이면 그 대조도 눈이 먼다.**

위에서 세운 ④(커버리지 대조)를 세 세션이 지켰는데도, 밀러 원문 파서가 **표제어 일곱을
통째로 안 만들고 있던 것**을 아무도 못 봤다(`Canary Birds`·`Cistern`·`Gale`·`Jailer`·
`Katydids`·`Quoits`·`Yule Log` — 표제어 줄이 앞뒤 문장과 한 줄에 붙어 있어 정규식이
안 셌고, **앞 표제어의 본문 속에 묻혀** 있었다. 책 자신의 색인에는 일곱 다 있다).

관문 둘이 원리적으로 못 본다 — ③은 **남긴 인용이 진짜인가**만 보고, ④는 **입력 표제어
목록 ↔ 결과**를 맞추는데 **그 입력 목록 자체가 파서가 만든 것**이다. 파서가 안 만든
표제어는 「빠뜨린 것」이 아니라 「애초에 없던 것」이 되어 **양쪽 다 초록불**이다.
발견은 관문이 아니라 **원문을 눈으로 읽다가** 나왔다.

→ **커버리지를 잴 때 「무엇과 무엇을 맞추는가」를 적고, 그 둘이 같은 도구에서 나오지
않는지 본다.** 같은 도구가 양쪽을 만들면 그 도구의 결함은 대조에 안 잡힌다. 이번에는
**원본 텍스트**(`miller-1901-raw.txt`)를 세 번째 자로 삼아야 했다 — 실제로 거기서
`_…_.` 꼴을 세자 11개가 나왔고, 그중 넷은 성경 인용, 일곱이 놓친 표제어였다.

→ **고칠 때는 「지금 그대로 돌려 diff가 0인가」를 먼저 본다**(§26 곁가지). 그 대조군을
세운 뒤에 고쳐야 「내 고침이 만든 차이」와 「원래 있던 차이」가 갈린다. 이번 diff는
정확히 일곱 자리의 분리뿐이었고 기존 추출 2,023건이 그대로 재검증됐다.

## 26. 「커밋했다」는 「배포됐다」가 아니다 — 그리고 데이터를 갈면 **안내 문서도 함께 거짓이 된다** (2026-09-01)

어제 워크로그가 이렇게 시작한다 — *「해몽 사전 재구축이 **프로덕션에 올라갔다.**」*
오늘 아침 `git status -sb` 한 줄이 이렇게 답했다 — `## main...origin/main [ahead 4]`.

**커밋 넷이 로컬에만 있었다.** 운영에는 옛 218개 사전이 그대로 떠 있었다. 사용자가 그
문장을 읽고 「끝났다」고 판단했다면 그것은 거짓 근거 위에 선 판단이다(§10 #39와 같은 자리 —
이번엔 화면이 아니라 **내가 쓴 보고서**가 창이었다).

**하지 말 것 → 대신 이렇게.** 「배포됐다」·「운영에 올라갔다」·「라이브다」를 적기 전에
`git status -sb`를 본다. 커밋·푸시·배포는 셋 다 다른 사건이다. 세션이 끝나는 자리에서
특히 위험하다 — 커밋까지 하고 「끝」이라고 느끼는 순간이 푸시를 빠뜨리는 자리다.

### 그리고 §25가 같은 교체에서 **두 번 더** 났다

어제 §25를 적으면서 잡은 것은 태몽 하나였다. 오늘 같은 교체에서 둘이 더 나왔다.

| 무엇 | 어떻게 죽었나 |
| --- | --- |
| **「자주 찾는 상징」 목록** | 결과 화면이 `weight >= 3`으로 고르는데 **v2는 678개 전부가 `weight: 1`**이다. 목록이 빈 배열이 되어 그 절이 통째로 사라졌다. tsc·검사기·하니스 전부 초록불 |
| **안내 문서의 사실 문장** | 「아홉 갈래」(여덟이다) · 「좋은 쪽이 절반을 넘는다」(29.6%다) · 「뜻이 하나뿐인 경우가 대부분」(61%가 여럿이다) · 「뱀을 품는 것과 물리는 것은 정반대」(**v2에선 둘 다 길하다**) · 「높은 곳에서 떨어졌다」(**안 걸린다**, `추락` 표제어가 없다) |

**숫자는 자리표시자라 저절로 따라갔다. 글로 적힌 사실은 따라가지 않았다.** 이것이 §25의
새 얼굴이다 — 갈아 끼운 데이터를 읽는 것은 **코드만이 아니다.** 안내 문서·예문·비유가
전부 옛 데이터를 보고 쓰인 것이고, 그중 **틀려도 아무 관문에서 빨간불이 안 나는 것**이
대부분이다(숫자만 `verify-guide-numbers`가 본다).

→ **데이터를 갈면 그 데이터를 설명하는 글도 함께 재검한다.** 특히 **예문**은 제품 엔진에
실제로 넣어 본다(`apps/dreamslink/scripts/check-guide-examples.ts` — 오늘 만들었고, 고치기
전 트리에서 실제로 2건 빨간불을 냈다). 「이렇게 적으면 걸립니다」는 **이용자에게 한 약속**이다.

→ **「정반대입니다」라고 적은 짝은 극성이 실제로 반대인지 잰다.** 옛 뱀 예시는 뜻이
**갈리기는 했다** — `verify-dream-context-parity`가 「갈린다」로 통과시켰다. 그런데 둘 다
길했다. **「다르다」와 「반대다」는 다른 주장인데 관문은 앞엣것만 재고 있었다.**

### 곁가지 — 제어 바이트가 **세 번째로** 박혔고, 이번엔 기능까지 죽였다

`scripts/build-dream-dictionary-v2.mjs`가 `file`에 `data`(=텍스트가 아니다)로 나왔다.
어제 커밋된 것이고 §10 #30·#43과 **정확히 같은 사고**다 — 구분자를 유니코드 이스케이프로
적어 진짜 NUL 바이트가 박혔다.

이번엔 여기서 그치지 않았다. 같은 파일의 `EXPLICIT_IDS` 표는 구분자를 **공백**으로 적어
두었다(`"iron 다리미"`). 만드는 쪽은 NUL, 찾는 쪽은 공백 — **그 표는 한 번도 맞은 적이
없다.** id가 지금 맞는 것은 slugify 중복 접미사가 우연히 같은 순서를 냈기 때문이고,
추출 파일이 하나 늘면 `/dream/symbol/iron`이 조용히 다른 상징을 가리킨다.

→ **눈에 안 보이는 바이트를 구분자로 쓰지 않는다.** 필요하면 `JSON.stringify([a, b])`로
감싼다(§22 곁가지에 이미 적혀 있던 것이다). 그리고 **표를 만들면 그 표가 실제로 쓰이는지
세는 관문을 함께 남긴다** — 검사 0건은 통과가 아니라 검사 안 됨이다(§3).

### 곁가지 — 생성 스크립트가 **두 단계**인 것을 모르고 한 단계만 돌렸다

`build-dream-dictionary-v2.mjs`는 `aliases: []`로 비워 쓰고, `build-dream-match-tables-v2.mjs`
가 km 파일에서 별칭 **2,072개**를 채운다. 앞엣것만 돌리자 diff가 **3,360줄 삭제**로 나왔다.
§10 #49와 같은 자리이고, 그때 적어 둔 「diff 크기가 내가 넣은 항목 수와 비례하는가」 규칙이
이번에 실제로 나를 멈춰 세웠다(멈추고 `git checkout`으로 되돌렸다).

→ **생성 파이프라인은 「다 돌리고 diff가 0인가」로 확인한다.** 둘을 이어 돌리자 바이트까지
같았다. 그 대조군을 먼저 세운 뒤에야 스크립트를 고쳤다.

**2026-09-03(배치 64) — 두 번째. 이번엔 「수치만 보려고」 1단계를 돌렸다.** 상징·의미 수를
찍어 보려고 `build-dream-dictionary-v2.mjs` 만 다시 돌렸는데, 그것이 `aliases` 를 비워 놓는다.
바로 다음에 돌린 극성 하니스가 **실패 16건**을 냈고(상징 열둘이 통째로 안 걸림) 하마터면
없는 결함을 쫓을 뻔했다 — 2단계를 이어 돌리니 진짜는 **2건**이었다.

→ **조립기는 「고칠 때」만이 아니라 「볼 때」도 끝까지 돌린다.** 수치만 필요하면 조립기를
돌리지 말고 **이미 만들어진 `dream-symbols.v2.data.json` 을 읽어서 센다.**

### 곁가지 — **처음 쓰는 `category` 하나가 안내 문서 23로케일을 거짓으로 만든다** (2026-09-03 배치 68)

상징 하나(`january`)의 갈래를 `number` 로 적었더니 `verify-guide-numbers` 가 빨간불 다섯을 냈다 —
*「갈래 수(한글): 문서 여덟(8) · 실제 9」*. 타입에는 아홉이 허용돼 있지만 **실제로 쓰인 것은
여덟뿐**이었고, 안내 문서 열두 편이 「여덟」이라고 **글로** 적어 23개 언어로 번역돼 있다.

→ **새 갈래를 쓰기 전에 그 갈래가 이미 쓰이고 있는지 센다.** 안 쓰이던 갈래를 처음 쓰면
**안내 문서 23로케일을 함께 고쳐야 한다** — 상징 하나 때문에 할 일이 아니다. 이번에는
`autumn`(가을)·`day`(대낮)가 `nature` 인 것을 보고 한 달도 거기로 돌렸다.

이것이 §26의 새 얼굴이다 — 지금까지 안내 문서의 **숫자**는 자리표시자라 저절로 따라갔는데,
**글로 적힌 「여덟」**은 안 따라간다. 그것을 지키는 관문이 `verify-guide-numbers` 다.

### 곁가지 — 명령 끝에 붙인 `echo`가 종료 코드를 삼켰다

백그라운드로 돌린 번역 묶음 하나가 **네트워크 타임아웃으로 죽었는데 도구는 「exit code 0」
이라고 알렸다.** 내가 이렇게 적었기 때문이다.

```bash
tsx ... translate-doc-content.ts tr ar km > 로그 2>&1; echo "g6 exit=$?"
#                                                       ^^^^ 셸의 종료 코드는 이 echo 의 것(0)
```

**§10 #3(`| tail`이 종료 코드를 가로챈 것)과 같은 병이고 새 얼굴이다.** 파이프만이 아니라
**뒤에 오는 아무 명령**이나 종료 코드를 가져간다. 로그 본문을 열어 보고서야
`TypeError: fetch failed`를 봤다 — 그때까지 「일곱 묶음 다 성공」으로 세고 있었다.

→ 종료 코드를 알고 싶으면 **그 명령을 마지막에 둔다.** 굳이 찍어야 하면 그 값을 **로그
파일 안에** 남긴다(도구가 보고하는 것은 바깥 셸의 코드다). 그리고 **성공 판정은 언제나
출력 본문으로** 한다 — 「끝났다」 같은 마지막 줄이 실제로 찍혔는지 본다.

### 곁가지 — 백틱이 든 문자열을 또 셸로 넘겼다

메모리 파일을 고치려고 `node -e`에 백틱이 든 한국어 문장을 넣었다. 셸이 `\`dream-symbols.ts\``
를 **명령 치환으로 실행**해 `command not found`가 났다. §7·§10 #34에 이미 적혀 있는 것을
그대로 밟았다 — 그날 안에 두 번째다(앞엣것은 CRLF).

→ **문장이 든 편집은 셸을 거치지 않는 도구로 한다.** `node -e`·히어독은 코드가 짧고
백틱·역슬래시가 **하나도 없을 때만** 쓴다. 오늘 파일로 쓴 패치 스크립트들은 전부 무사했고,
셸로 넘긴 것만 깨졌다.

### 곁가지 — 번역기가 만든 영어가 **어투를 바꿔 놓았다**

`--fill-en`이 새로 만든 영어가 서비스를 **1인칭 「I」**로 가리켰다(「I did not create…」).
지우기 전 en에는 그런 자리가 **0건**이었고 전부 「we」였다 — 한국어 원문은 주어가 없는
존댓말이라 모델이 매번 다르게 고른다.

**en은 21개 언어의 본이다.** 그대로 21개를 돌렸으면 어투가 **스물한 번 복제**됐을 것이다.
`docs/I18N_DOC_CONTENT.md`가 「en을 한 번 읽어 보고 나서 21개를 돌린다」고 적어 둔 이유가
이것이고, 오늘 그 줄이 값을 했다. 검사기는 이것을 못 잡는다 — 문법도 맞고 한글도 안 남는다.

---

## 25. 데이터를 갈아 끼울 때는 「그 기능이 무엇에 기대고 있는가」를 먼저 본다 (2026-08-31)

해몽 사전을 옛 218개에서 v2(678개)로 교체했다. 타입을 맞추고 tsc가 exit 0을 냈다.
**그런데 유료 태몽 리포트가 조용히 0건이 될 뻔했다.**

`isConceptionDream`은 태그가 아니라 **풀이 문구에 「태몽」이라는 낱말이 있는지**로
판정하고 있었다(`meaning.interpretation_ko.includes("태몽")`). 옛 사전에는 그 낱말이
적혀 있었고 — **그 태몽 의미들이 바로 AI가 지어낸 것이었다**(§21) — v2는 원문에서
지었으므로 「태몽」이라는 낱말이 **한 번도 안 나온다.** 타입은 맞고, 화면도 뜨고,
tsc도 초록불인데 **태몽만 사라진다.**

찾은 것은 회귀 하니스였다. 안 돌렸으면 유료 상품이 빈 문서를 냈을 것이다.

(고침은 원문이 그 일을 말하는 방식을 목록으로 얼린 것이다 — `蛇入懷中生貴子`·
`吞日者生貴子`. v2에 그 자리가 41개 있다. **근거가 지어낸 라벨에서 인용으로 바뀐 것이지
태몽이 사라진 것이 아니었다.**)

**하지 말 것 → 대신 이렇게.**

- **데이터를 갈기 전에 그 데이터를 읽는 기능들이 「무엇을 보고 판정하는가」를 센다.**
  필드 이름만 대조하면 안 된다 — `tags`는 그대로 옮겨도 판정은 **문구**를 보고 있었다.
  `grep`으로 그 필드를 쓰는 자리를 찾고, **판정 함수 본문을 열어 볼 것.**
- **타입 검사는 「값이 있다」만 본다. 「값이 무엇인가」는 안 본다.** tsc가 초록불인 교체는
  아직 아무것도 증명하지 않았다(§1의 새 얼굴).
- **회귀 하니스를 교체 직후 반드시 돌린다.** 이번에 하니스가 잡아 준 것이 넷이다 —
  태몽 · 별칭이 너무 넓어 생긴 오탐 셋(`stool`=의자가 똥으로, `a stream`=빛줄기가 강으로,
  맨 `hill`이 산으로) · `term_en`이 밀러 복수형("bees")이라 "a bee"가 안 걸리던 12자리 ·
  자연스러운 문장("뱀을 품에 안았다")이 판별어에 없어 새던 자리.

### 곁가지 — 옛 데이터로 쓰인 하니스를 고칠 때의 기준

교체하면 하니스가 **옛 상징 id와 옛 내용**을 기대해 무더기로 빨간불이 난다. 여기서
「통과하게 고치는 것」과 「기대치를 실제에 맞추는 것」을 가르는 기준은 하나다 —
**그 기대가 무엇에 근거했는가.**

- 옛 사전이 **지어낸 내용**에 기댄 기대치는 바꾼다(용이 하늘로 오르면 태몽 → v2는
  「부인이 용을 봄」만 태몽이다. 원문이 그렇게 적혀 있다).
- 옛 사전이 **옳게 하던 것**에 기댄 기대치는 그대로 두고 **데이터를 고친다**. 이번에
  맨 `hill`을 별칭에서 빼자 「a high hill」이 산에 안 걸렸는데, 옛 사전은 `high hill`·
  `steep hill`처럼 **한정된 구**로 갖고 있었다 — 옛 쪽이 옳았다(§15의 반대 방향:
  「이미 그렇게 한다」가 근거는 아니지만, **틀렸다고 단정할 근거도 아니다**).
- **없는 것을 시험하려고 사전에 넣지 않는다.** 「밤」·「시든 꽃」은 원문에 없다. 시험을
  살리려고 항목을 지어내면 그것이 폐기된 옛 사전의 병이다.

### 곁가지 — 판별어를 적을 때 실제로 새는 두 가지

둘 다 이번에 실물로 걸렸고, 화면에는 **엉뚱한 뜻이 조용히** 뜬다.

1. **활용형이 어긋나면 0점이 되고, 0점이면 대표(첫) 의미로 떨어진다.** 「떨어졌」은
   적혀 있는데 이용자는 「떨어지는」이라고 쓴다 — 부분 문자열이 아니라 안 걸린다.
   판별어는 **이용자가 실제로 칠 활용형**으로 적는다.

   **2026-09-01 — 별칭에서 같은 것을 또 했고, 이번엔 상징이 통째로 안 걸렸다.**
   밀러 20개를 넣으며 별칭을 **어간**으로 적었다(`데었`·`파묻힘`·`암탉 소리`).
   검사기 셋(`verify-dream-km`·`audit-km-dead-words`·`verify-dream-cite`)이 전부
   초록불이었는데, 제품 엔진에 자연스러운 문장을 넣으니 **다섯 상징이 하나도 안 걸렸다** —
   「손을 데었다」·「산 채로 파묻혔다」·「암탉이 우는 소리를 들었다」·「배의 객실에 갇혀
   있었다」·「낯선 목소리가 내 이름을 불렀다」.

   원인은 `dream-match.ts`의 `PARTICLES`가 **조사만** 담기 때문이다(어미를 넣었다가
   「말았다」가 말(馬)로 걸리는 오탐이 나서 되돌린 자리다). 그 주석의 결론이 이미 이렇게
   적혀 있었다 — **「어미를 담으려면 상징별 `aliases`에 완전한 활용형을 하나씩 올린다」.**
   `데었다`·`데어서`·`산 채로 파묻혔다`처럼 **완전한 꼴**로 바꾸자 다섯 다 올바른 상황으로
   걸렸다.

   **2026-09-02(배치 41) — 「안 걸림」이 내 문장의 결함일 때가 있다.** 프로브가 넷을 냈는데
고칠 것이 데이터가 아니라 **프로브 문장**이었다 — 「메마른 묘지를 보았다」에 「무덤」이 없다.
「묘지」는 `cemetery` 의 이름이라 `grave` 를 대신 걸어 주지 않는다.

→ **가르는 물음은 「원문의 그 꿈에 그 사물이 있는가」다.** 밀러 원문에는 무덤이 들어 있다
(a person **in a grave** · return **for a corpse, to bury it**) — 옮기면서 내가 뺀 것이다.
있으면 **문장을 고치고**, 없으면 **데이터를 고친다.** 문장을 고치는 쪽을 고를 때는
**왜 그것이 옳은지 적는다** — 안 적으면 다음 사람이 「통과하도록 문장을 골랐다」로 읽는다.
**2026-09-02(배치 42·44) — 두 번째 얼굴은 띄어쓰기다.** 「제얼굴이」·「남의손을」·「남들이
잘생겨」는 **아무도 그렇게 안 쓴다.** 데이터가 아니라 내가 쓴 문장이 틀린 것이다.

→ **프로브 문장을 쓸 때 둘을 함께 본다 — ① 그 상징의 이름이 문장에 있는가
② 띄어쓰기가 이용자가 쓰는 대로인가.**

**2026-09-02(배치 49) — 세 번째 얼굴은 「안 걸린다」가 아니라 「걸리는데 딴 뜻」이다.**
`bee` 의 두 의미가 활용형을 **어긋나게 나눠 쥐고** 있었다.

```
벌에 쏘임(밀러, 흉)             "쏘인 쏘이 벌침 물렸"     ← 「쏘였다」에 안 걸린다(쏘였 ≠ 쏘이)
벌이 사람의 다리를 쏨(주공, 길)  "쏘였 쏘여 쏘았 다리를"   ← 넓은 활용형을 이쪽이 쥐고 있었다
```

상징은 걸리므로 **화면에는 답이 뜬다 — 다른 뜻으로.** 지금까지 이 병은 늘 「안 걸린다」로
나타나서 눈에 띄었는데, 이번은 **몇 주 동안 흔한 문장 하나가 통째로 남의 뜻으로 가고 있었다.**
어떤 관문도 못 본다(낱말이 겹치지도 죽지도 않았다) — 프로브에 **「옛 답이 그대로인가」를
재는 지킴 케이스**를 넣었더니 그 자리에서 드러났다.

→ **활용형을 형제끼리 나눌 때는 「넓은 꼴이 누구 것인가」를 먼저 정한다.** 한정된 의미
(「다리를 쏨」)에는 **그 한정에 매인 말만** 남기고, 넓은 활용형은 일반 의미에 준다.
→ **배치를 넣을 때 프로브에 그 상징의 옛 답을 재는 케이스를 함께 넣는다.** 새로 넣은 것만
재면 「원래 틀려 있던 것」은 영영 안 보인다.

→ **새 상징을 넣으면 검사기가 초록불이어도 끝이 아니다. 자연스러운 문장을
   `matchDream`에 직접 넣어 본다**(§23). 검사기는 「적어 둔 것이 규칙에 맞는가」를 보지
   「그 말을 이용자가 쓰는가」를 보지 않는다.
2. **일반적인 낱말이 형제 의미에 있으면 구체적인 의미를 가로챈다.** 「대청 위에 관이
   놓여 있음」의 `placed`가 「관에 시신을 넣는 것을 봄」을, 「해와 달을 지나감」의
   `sailed`가 「돛에 바람을 받아 나아감」을 가로챘다. 넓은 낱말은 **그 상황을 실제로
   말하는 쪽에만** 남긴다.
3. **대표(첫) 의미의 판별어는 비워 두는 것이 옳다.** 「무엇을 봄」처럼 가장 넓은 의미에
   `보았` 같은 흔한 말을 넣으면 구체적인 형제와 **늘 동점**이 되고, **동점이면 앞의 것이
   이기므로 구체적인 뜻이 영영 안 뽑힌다.** 대표 의미는 아무것도 안 걸렸을 때 떨어지는
   자리이므로 **점수 0으로 두어야** 제 일을 한다.

   **그리고 이것을 적어 둔 뒤에도 두 배치 만에 또 밟았다**(2026-09-01, 「부서진 샹들리에를
   보았다」가 「샹들리에를 봄」으로). 규칙을 적는 것과 지키는 것은 다른 일이다(§10 #43과 같다) —
   **판별어를 적을 때마다 「대표 의미에 흔한 말이 들어갔는가」를 그 자리에서 본다.**

   동점이 어쩔 수 없는 자리는 **차례로 푼다** — 동점이면 앞의 것이 이기므로 **구체적인
   의미를 파일에서 앞에 둔다**(「칠판에」가 「판에」를 품는 자리, 「젊은 여성이 화려한 방」이
   「화려한 방」과 겹치는 자리 — 둘 다 이렇게 풀었다).

   **2026-09-02(배치 32) — 차례는 「동점일 때만」 듣는다.** 넓은 의미가 판별어 **개수로**
   이기면 순서로는 못 푼다. 「아가씨가 장이 열린 곳에 갔다」에서 넓은 쪽이 「장이+열린」
   2점, 좁은 쪽이 「아가씨」 1점이라 그냥 졌다. → **넓은 쪽 판별어를 줄여 동점을 만든 뒤**
   좁은 쪽을 앞에 둔다. **더 나은 수는 넓은 쪽에서 「좁은 문장에도 있는 낱말」을 아예 빼는
   것**이다 — 넓은 의미는 0점으로 떨어져 **기본값 자리를 지키는 것이 제 일**이기 때문이다
   (그 상징의 기본값이 다른 의미로 얼려 있으면 이 수를 못 쓴다).

   2026-09-01에 실물로 넷 걸렸다 — 「처녀가 포탄을 보았다」·「젊은이가 포탄을 보았다」·
   「죄수가 쓴 챙모자를 보았다」·「광부가 쓴 챙모자를 보았다」가 전부 대표 의미로 갔다.
   `verify-dream-km`은 이것을 못 본다: 판별어가 **겹치지도 죽지도 않았기** 때문이다.
   잡은 것은 `matchDream`에 문장을 넣어 본 프로브뿐이다
   (`apps/dreamslink/scripts/probe-miller-batch.ts` — 같은 판에서 9건을 잡았다).

   **활용형 문제도 같은 날 또 밟았다** — `걸린`을 적어 두고 「암에 걸렸다」가 안 걸렸고,
   `탁하`를 적어 두고 「물이 탁했다」가 안 걸렸다. 판별어는 **부분 문자열**로 세므로
   어간이 아니라 **이용자가 실제로 칠 꼴**을 적는다(위 1번과 같은 병, 세 번째 재발).
4. **복수형 「~들」은 조사가 아니라 접미사다 — 이름 뒤에 붙으면 상징이 통째로 안 걸린다.**
   `dream-match.ts`의 `PARTICLES`는 조사만 담으므로 「목수**들이**」·「송아지**들을**」·
   「닭**들이**」·「아이**들이**」가 전부 `isStandalone`에서 걸러진다. **네 배치 연속으로**
   같은 자리에서 났다(2026-09-01, 배치 12~15).

   → **사람·동물처럼 여럿으로 말하는 상징은 「~들」 별칭을 처음부터 함께 올린다**
   (`목수들`·`송아지들`·`닭들`·`아이들`). 검사기는 이것을 못 본다 — 프로브만 잡는다.

### 곁가지 — 새 상징은 **남이 이미 쓰던 이름과 자리**를 가로챈다 (2026-09-01)

새 상징을 넣을 때 이름만 고르고 끝내면 **이미 그 이름을 쓰던 곳이 조용히 죽는다.**
같은 날 배치 둘에서 넷이 나왔다. 세 갈래이고, **검사기는 셋 다 못 본다.**

| 무엇 | 어떻게 죽나 |
| --- | --- |
| **별칭을 남이 갖고 있다** | `candlestick`(촛대)를 만들었는데 `candle`의 별칭에 「촛대」가 있었다. 그대로 뒀으면 「촛대를 봤다」가 촛불로 걸렸다. `carriage`(마차)도 같았다 — 「마차」가 `cart`·`cab` **둘의** 별칭이었다 |
| **id 를 다른 곳이 기다리고 있다** | 밀러 `Cars`(1901년의 철도 객차·전차)를 `term_en: "car"`로 넣자 id 가 `car`가 됐다. 그런데 회귀 하니스가 그 id 를 **자동차**로 기다리고 있었다(밀러 `Automobile`이 오면 채워질 자리) — 「건너뜀」이던 세 쌍이 **빨간불**로 바뀌었다. `railway car`로 갈랐다 |
| **낱말이 이미 다른 상징의 것이다** | 「험담」은 `verbal-abuse`·`accuse`가, 「부스럼」은 `boil`이, 「천막」은 `curtain`이 이미 쓰고 있었다 |

→ **새 이름을 정하면 그 문자열을 기존 별칭·id 전부에서 먼저 찾는다.** 겹치면 **더 정확한
쪽에 넘긴다**(「마차」는 `carriage`의 것이지 `cart`의 것이 아니다). 셋 다 `verify-dream-km`도
`audit-km-dead-words`도 못 잡는다 — 그 검사기들은 **한 상징 안**만 본다.

**2026-09-02(배치 40) — 두 번째. 이번에는 하니스의 기대가 짐작이었다.**
극성 하니스에 `god`·`gold` 를 기대하는 네 쌍이 있었는데, 그 상징이 사전에 없어
**「건너뜀」으로 몇 주를 지났다.** 새 상징이 그 id 를 차지하자 넷 다 빨간불이 됐고, 읽어 보니
**기대 자체가 틀렸다** — 「귀신에게 맞았다」의 임자는 `ghost` 이고, 「보석이 품에 가득」은
`jade` 다. 「금」이라는 상징은 아무 데도 없다(황금이다).

→ **「아직 없는 상징」으로 건너뛰는 기대치는 검증된 것이 아니다**(§24와 같은 뿌리 — 검사기는
「있는 것」만 훑는다). 그 상징이 들어오는 날 **한 번도 안 재 본 주장**이 깨어난다.
새 상징이 건너뛰던 id 를 채우면 **그 쌍을 지우지 말고 문장이 실제로 가리키는 상징으로 고친다.**
→ **id 는 주소이고 다른 코드가 그것을 기다린다.** 회귀 하니스가 「아직 없는 상징」으로
건너뛰고 있던 자리를 다른 뜻으로 채우면, 하니스는 **없던 결함을 신고**한다.

**다음 배치에서 그 grep 을 먼저 돌렸더니 다섯이 나왔다**(애벌레→`insect` · 묘지→`grave` ·
사슬→`shackles` · 걸상→`bench` · cattle→`ox`). **그리고 이미 나 있던 상처도 하나 찾았다** —
`ox`의 별칭에 「송아지」와 `calf`가 있었는데, `calf`는 **두 배치 앞에서 내가 만든 상징**이다.
그때 이 grep 을 안 해서 「송아지를 봤다」가 소로도 걸린 채 배포돼 있었다.
**미리 도는 데 10초, 지나가면 배포된 채로 남는다.**

**2026-09-02 — 네 번째 갈래를 찾았다. 새 이름이 남의 이름을 「품으면」 둘 다 뜬다.**
`findTerm`은 **상징마다 따로** 돈다 — 긴 이름이 짧은 이름을 이기는 것은 **한 상징 안**에서
뿐이고, 전역 중재는 없다. 그래서 「지하 감옥」을 새 상징으로 세우면 「지하 감옥에 갇혔다」가
`prison`(감옥)에도 걸려 **화면에 상징이 둘 뜬다**(「거름더미」와 `manure`(거름)도 같다).

→ **새 이름이 기존 이름을 부분 문자열로 품으면 새 상징을 세우지 말고 그 임자에게 의미를
붙인다.** 밀러 `Dungeon`은 `prison`에, `Dunghill`은 `manure`에 붙였다. 이것은 ⓪의 전수
grep 으로 **찾을 수는 있지만**(문자열이 겹치니까) 「가로챈다」가 아니라 **「같이 걸린다」**라서
판단이 다르다 — 앞의 셋은 이름을 넘기면 되고, 이것은 **상징을 세우지 않는 것**이 답이다.

**그리고 뒤에 붙는 것이 조사가 아니면 짧은 이름은 안 걸린다.** 「거름」은 「거름더미」에
안 걸린다(뒤의 「더」가 조사가 아니라 합성어로 읽힌다 — 「문」과 「문간」도 같았다).
**합성어는 별칭으로 따로 올린다.**
**2026-09-02(배치 46·47) — 앞 글자만 막는 것이 아니다.** 「산울타리」는 **앞**의 「산」이,
「비탈길」은 **뒤**의 「길」이 막는다. 「역사책」의 「책」, 「두드러기」 앞의 것도 같다.
→ **합성어를 만나면 어느 쪽이 막는지 따지지 말고 그 합성어를 통째로 별칭에 올린다.**


**2026-09-02(배치 29) — 「품는다」가 곧 결함은 아니다. 띄어쓰기가 갈림길이다.** 다음 배치에서
이 grep 을 먼저 돌리자 여섯이 나왔는데 **실제로 위험한 것은 하나뿐**이었다.

```
선거   ⊂ 「선거 운동」(campaign)   위험 — 뒤가 공백이라 isStandalone 을 지난다 → 둘 다 걸린다
귀     ⊂ 당나귀·까마귀·귀뚜라미    안전 — 앞 글자가 한글이라 막힌다
전기   ⊂ 발전기·전기톱             안전 — 앞이 한글이거나 뒤가 조사가 아니다
집게벌레 ⊃ 벌레 / 흑단나무 ⊃ 나무   안전 — 같은 이유
음식을 먹음 ⊃ 「음식」(food)        둘 다 걸리지만 **옳다** — 음식이 실제로 그 꿈에 있다
```

→ **품음 관계를 찾았으면 거기서 멈추지 말고 `isStandalone` 을 손으로 한 번 돌려 본다** —
앞 글자가 한글이면 안 걸리고, 뒤가 공백이거나 조사면 걸린다. 걸리는 것 가운데 **뜻이 겹치는
것**만 고친다(이번엔 「선거」를 **「투표」**로 돌렸다).

## 27. 「빌드가 된다」는 「배포가 된다」가 아니다 — 그리고 가설을 배포로 시험하면 한 판에 10분이다 (2026-09-01)

§26에서 「커밋했다 ≠ 배포됐다」를 적고 그날 바로 **한 겹 더 있는 것**을 만났다.

푸시하자 **드림링크만 배포가 실패했다**(형제 셋은 같은 커밋에서 성공). 운영 화면을
직접 찍어 보니 v2 에만 있는 상징이 404 였다 — **운영은 아직 옛 218개 사전이었다.**

```
https://dreams-link.com/ko/dream/symbol/apparel  404   ← v2 에만 있다
https://dreams-link.com/ko/dream/symbol/pig      200   ← 옛 사전에도 있다
```

**이 두 줄이 「배포됐는가」를 재는 가장 싼 자입니다.** 배포 상태가 success 라고 나와도
그것은 「빌드가 끝났다」이지 「내 변경이 살아 있다」가 아니다 — **새 데이터에만 있는
주소 하나를 골라 두면** 그 뒤로는 curl 한 줄로 갈린다.

### 원인을 한 번 틀렸고, 그 대가가 10분이었다

「미리 그리는 장이 5,014장 → 15,594장으로 늘어 빌드가 메모리를 넘겼다」고 보고
`experimental.cpus: 2`로 일꾼을 묶었다. **이 컴퓨터의 빌드 크래시는 실제로 사라졌다**
— 그래서 그럴듯했다. 그런데 **배포는 그대로 실패했다.**

다시 재니 진짜 수치는 이것이었다.

```
미리 그린 장 16,153  →  산출물 파일 193,463개 · 2.9GB
```

미리 그린 장 하나가 파일 여럿(`.html`·`.rsc`·`.meta`·`.segments`)을 낳는다.
**장수가 아니라 파일 수가 자리다.** 옛 사전 때는 그 3분의 1이었고 그때는 배포가 섰다.

→ **로컬에서 고쳐졌다는 것은 가설의 증거가 아니다.** 「이 컴퓨터에서 안 죽는다」와
「저기서 선다」는 다른 명제다. 로컬에서만 확인하고 배포로 시험하면 **한 판에 10분**이고,
틀린 가설은 그 10분을 통째로 먹는다.

→ **재는 것을 먼저 늘린다.** 이번에 `find .next -type f | wc -l` 한 줄이 가설을
바꿔 놓았다. 크기·장수·파일 수·시간을 **다 재 놓고** 그중 무엇이 임계인지를 고른다 —
하나만 재고 그것으로 설명하면 그럴듯한 오답이 나온다.

→ **틀린 가설을 코드에 설명으로 남기지 않는다.** `cpus: 2`는 배포 결과로 반증된
순간 되돌렸다. 남겨 두면 다음 사람이 그 주석을 근거로 읽는다(§15 「기존 구현은 근거가
아니다」의 뿌리와 같다).

### 곁가지 — 라우트 설정은 **위에서 아래를 지배한다**

미리 그리는 범위를 줄이려고 상징 페이지에 `dynamicParams = true`를 적었다. **안 먹었다** —
`[locale]/layout.tsx`의 `dynamicParams = false`가 그 갈래 전체를 지배해서, 미리 그리지
않은 조합이 **21개 언어에서 전부 404**가 됐다. 서버를 띄워 재고서야 알았다.

→ **잎에서 여는 것으로 부모의 금지를 풀 수 없다.** 열려면 그 설정이 적힌 자리를 연다.
그리고 그 설정이 **막고 있던 것을 무엇이 대신 막는지** 먼저 확인한다 — 이번에는
`routeLocale()`의 `notFound()`와 미들웨어 308이 이미 그 일을 하고 있었다.

→ **범위를 줄이는 고침은 「무엇이 사라지는가」를 실제로 세고 낸다.** production 빌드를
띄워 23개 언어를 전부 두드려 보는 데 1분이 걸린다. 그 1분이 없었으면 21개 언어가
404인 채로 나갔다.

## 28. 별칭이 겹치면 **어긋난 짝도 초록불이 된다** — 그리고 늘 빨간불인 관문은 안 보게 된다 (2026-09-01)

밀러 배치를 열아홉 판 넣는 동안, 배치마다 ⓪ 절차로 **새 상징의 이름이 기존 별칭에 있는지**
grep 했다. 그런데도 이미 나 있던 상처가 셋 나왔고 — `ox`가 「송아지」를, `money`가 「현금」을,
`prisoner`가 「포로」를 쥐고 있었다 — **셋 다 찾으려던 것이 아니라 곁에서** 걸렸다.
**⓪는 새로 넣는 이름만 본다.** 지난 배치가 남긴 것은 아무도 안 훑는다.

전수로 대조하니(`scripts/audit-cross-symbol-aliases.mjs`) **335건**이 나왔다.

**여기서 두 가지를 배웠다.**

**① 겹친 별칭은 다른 관문의 눈도 가린다.** 12쌍을 고치자 회귀 하니스가 세 자리에서
빨간불을 냈는데, 그중 하나가 이것이다.

```
ko "바위 위에 누워 있었다"   ↔   en "I was lying atop a stone"
```

**한국어와 영어가 다른 상징을 말하고 있었다.** 그런데 `stone`이 「바위」를 별칭으로 쥐고
있어서 **관문이 몇 주째 초록불이었다.** 겹침을 없앤 그 순간 드러났다.

→ **별칭 겹침은 「둘 다 걸려서 편하다」가 아니라 「틀린 것도 통과시킨다」다.** 이름이
겹치는 자리를 없애는 것은 정확도 문제이기 전에 **관문을 다시 눈 뜨게 하는 일**이다.

**2026-09-02(배치 49) — 별칭이 아니라 판별어에서 같은 일이 났다.** `bee` 의 「벌에 쏘였다」를
제 뜻으로 돌려놓자 극성 하니스가 **빨간불**을 냈다 — en `"a bee stung me"` 는 여전히 주공해몽
쪽으로 갔다. **겹침이 있는 동안에는 ko·en 이 둘 다 같은 엉뚱한 뜻으로 가서 짝이 맞았고,
그래서 관문이 초록불이었다.** 고침이 관문을 빨갛게 만든 것이 아니라 **관문이 눈을 뜬 것**이다.

→ **한쪽 언어의 판별어를 고치면 다른 쪽도 같은 병인지 그 자리에서 본다.** 영어도 넓은
활용형("stung"·"sting")을 한정된 의미가 쥐고 있었다 — 같은 고침을 함께 냈다.

**② 335건짜리 관문은 관문이 아니다.** 읽어 보니 대부분이 결함이 아니었다 — 「단지」가
동이·항아리·그릇을 다 가리키는 것, 「독사」를 `snake`가 갖는 것은 **옳다.** 그대로 exit 1을
내게 두면 이 스크립트는 **§3-1에 이미 둘 있는 「늘 빨간불이라 아무도 안 보는 스크립트」**가
된다.

→ **빨간불은 「판단이 필요 없는 부분집합」에만 낸다.** 여기서는 **서로 이름을 맞바꿔 쥔 짝**
(A가 B의 이름을, B가 A의 이름을) — 그것은 뜻을 몰라도 결함이다. 12쌍이었고 전부 진짜였다.
나머지 291건은 **참고 목록으로 찍되 종료 코드에 넣지 않는다.** 「누가 더 정확한가」는
사람이 정할 일이고, 그 판단을 코드가 대신하면 관문이 죽는다.

## 29. 「마지막/가장 큰/하나도 없다」를 **좁혀 놓은 범위 안에서** 답한다 (2026-09-01)

사용자가 「마지막 저장한 md 파일 불러와」라고 했다. 나는 이렇게 찾았다.

```bash
ls -lt $(git ls-files '*.md' | head -400)     # ← 미추적 파일 제외 · 400개에서 자름
```

저장소 안에서 가장 새 것을 골라 냈고 **사용자가 「이거 맞아?」로 되물었다.** 진짜 마지막은
`~/.claude/.../memory/` 의 파일 셋이었다(1분 늦다). **답이 맞았던 것은 우연이다** — 저장소의
md 가 121개라 `head -400` 이 아무것도 안 잘랐을 뿐이고, 메모리 디렉터리는 애초에 시야 밖이었다.

**하지 말 것 → 대신 이렇게.** **최상급으로 답할 때는 그 최상급이 어느 집합의 것인지 먼저
적는다.** 「마지막」·「가장 큰」·「하나도 없다」는 **집합을 말하지 않으면 거짓이 되기 쉬운
문장**이다. 그리고 그 집합을 만들 때 `head`·`| head -N`·손으로 적은 확장자 목록으로
**자르지 않는다** — 자를 것이면 자른 사실을 답에 적는다.

§4(「없다」를 안 찾고 단정) · §5(목록을 손으로 적음)와 같은 뿌리이고 새 얼굴이다.
저 둘은 **검사기**의 병으로 적혀 있었는데, 이번에는 **사용자에게 하는 대답**이 그랬다.

### 곁가지 — 별칭으로 올린 낱말은 판별어에서 죽는다 (2026-09-01, 배치 20)

프로브가 잡은 「풋옥수수를 먹었다」·「짐승의 주검을 보았다」를 고치려고 그 낱말을 **별칭에
올리자**, 같은 낱말을 쓰던 **판별어가 그 자리에서 죽었다.** `scoringWordsOf` 가 상징의 제
이름(`ownTerms` — `term_ko`·`term_en`·`aliases` 전부)을 점수에서 빼기 때문이다.

→ **별칭을 더하면 그 낱말을 쓰던 판별어를 함께 갈아야 한다.** 잡아 주는 것은
`audit-km-dead-words` 뿐이다 — 별칭만 더하고 검사기를 안 돌리면 그 의미가 조용히 안 갈린다.

**2026-09-02 정정 — 이것은 영어 쪽 이야기다.** `ownTermsOf` 는 첫 줄이
`if (korean) return []` 이다(2026-08-31 `366fcdc` 부터 그렇다) — **한국어 판별어는 별칭으로
죽지 않는다.** 죽는 것은 `contexts_en` 뿐이고, 오늘 `audit-km-dead-words` 가 낸 넷도
전부 `[en]` 이었다(`prison` 의 `dungeon` · `drowning` 의 `drowned` 는 제 `aliases_en` 이라
0점, `dynamo` 의 `out of` 는 불용어). 위 09-01 기록은 **갈래를 안 적어서** 한국어에도
적용되는 것처럼 읽혔다 — 한국어에서 낱말을 피할 이유가 없다.

**영어 쪽에서는 별칭 하나가 판별어를 통째로 죽일 수 있다**(2026-09-02 배치 29). `elephant`
의 `aliases_en` 에 「white elephant」가 있어서 **「white」가 제 이름이 됐고**, 「흰 코끼리를
봄」의 영어 판별어가 그것 하나뿐이라 **살아 있는 낱말이 0개**가 됐다(`audit-km-dead-words`
가 「⚠ 살아 있는 판별어가 0개다」로 잡는다). 「elephant」 하나로 이미 걸리므로 그 별칭을
뺐다. **영어 별칭을 늘리기 전에 그 낱말이 어느 판별어에 쓰이는지 본다.**

→ **판별어는 2글자 이상이어야 한다**(`scoringWordsOf` 의 `length >= 2`). 「흰 산호」처럼
뜻을 가르는 말이 1글자면 **그 갈래는 엔진이 원리적으로 못 가른다** — 다른 말(「하얀」)로
돌리거나, 못 가른다는 것을 적어 둔다.

### 곁가지 — 판별어와 별칭을 적을 때 두 번 더 밟은 것 (2026-09-02, 배치 24~26)

**① 별칭은 「다」까지 붙여야 한다.** 「낯을 잃었」까지 올렸는데도
「낯을 잃었다」가 안 걸렸다 — 뒤에 오는 **「다」가 조사가 아니라** `isStandalone`이
낱말 조각으로 버린다. 「땅을 파는데」의 「데」, 「잠수하는」의 「하」도 같다.
§25의 「완전한 활용형」은 **「다」까지**라는 뜻이다.

**2026-09-02(배치 30) — 네 번째 재발. 규칙을 한 줄로 다시 적는다: 별칭은 「다」까지 붙이거나
**공백 앞에서 끝나야** 한다.** 같은 판에서 네 개가 이것으로 안 걸렸다.

```
「적을 이겼」  + 다   ✗  「다」는 조사가 아니다        → 「적을 이겼다」로
「말을 잘」    + 해서 ✗  「해」가 붙어 낱말 조각이 된다 → 「말을 잘해서」로
「수를 놓」    + 고   ✗                              → 「수를 놓고」로
「적에게」     + 「 졌다」 ✓  **뒤가 공백이라 산다**
```

→ 별칭을 적을 때 **그 뒤에 무엇이 오는지**를 소리 내어 붙여 본다. 조사도 공백도 아니면
그 별칭은 그 문장에서 죽는다. 활용형을 다 적을 수 없으면 **공백에서 끊기는 꼴**(「적에게」)을
고른다 — 그것이 가장 넓게 산다.

**2026-09-02(배치 31) — 다섯 번째. 한 판에 아홉이 이것이었다**(「시샘」+했다 · 「물려받」+았다 ·
「맞바꾸」+었다 · 「처형」+되는 …). **예외가 없다는 것도 그날 확인했다** — 고치면서 「벗어나려」·
「처형당하려」로 올렸는데 **여전히 안 걸려서** 「벗어나려다」·「처형당하려다」로 다시 고쳤다.
「~려」도 뒤의 「다」에 걸린다.

**2026-09-02(배치 38) — 여섯 번째. 이번 것은 내가 이번 판에 적은 것이 아니라 **옛 배치가
남긴 것**이다.** `bet`(내기)의 별칭 「돈을 걸」이 그렇다 — 「돈을 걸었다」의 「었」이 조사가
아니라 `isStandalone` 이 낱말 조각으로 버린다. **심어진 뒤로 한 번도 맞은 적이 없다.**

**어떤 관문도 이것을 못 본다.** `verify-dream-km` 은 겹침을, `audit-km-dead-words` 는 판별어를,
`audit-cross-symbol-aliases` 는 상징끼리의 겹침을 본다 — **「이 별칭이 어떤 문장에서든
걸릴 수 있는가」는 아무도 안 묻는다.** 프로브도 못 잡는다(프로브는 내가 이번에 넣은 상징만 본다).
⓪ 전수 grep 도 **새 이름만** 본다(§28과 같은 자리).

→ **이미 있는 상징에 의미를 붙일 때는 그 상징의 별칭을 한 줄씩 읽는다.** 각 별칭 뒤에
「다」·「았다」·「어서」를 붙여 소리 내 보고, 조사도 공백도 아닌 것이 온다면 그 별칭은 죽은 것이다.
붙이려고 그 상징을 여는 그 순간이 **유일하게 눈이 가는 때**다 — 지나면 다음 사람도 안 본다.
**2026-09-02 — 이 자리에서 네 번 났다.** `bet` 「돈을 걸」(배치 38) · `despair` 「음침」(42) ·
`bequest` 「물려받」(46) · `killing-someone` 이 「살인」·「죽이는」만 갖고 있어 **「죽였다」 꼴이
하나도 안 걸린 것**(48). **넷 다 내가 그 상징을 건드릴 때에야 드러났다** — 프로브는 이번
배치가 건드린 상징만 보고, 전수로 재는 검사기는 없다.

→ **넓히는 고침도 좁게 한다.** `killing-someone` 에 낱말 하나(「죽였다」)를 올리려다 말았다 —
「개가 뱀을 죽였다」까지 살인이 된다. **사람이 든 구절**로 올렸다(「사람을 죽였다」·「목숨을
끊었다」). 안 걸리는 것을 고치다 **엉뚱한 꿈에 뜨게 만드는 것**이 더 나쁘다.

→ **별칭을 적은 그 자리에서** 뒤에 「하」·「진」·「두」·「길」·「책」·「했다」를 붙여 본다.
배치 47은 프로브까지 가서 넷을 되돌렸고, 그렇게 한 배치 48은 셋을 적는 자리에서 걸렀다.

**2026-09-03(배치 63) — 열한 번째이고 새 얼굴이다. 「이름을 서술형으로 지으면 그 이름만으로는
안 걸린다.」** 한 판에 프로브가 아홉을 냈고 **아홉 다 「상징 자체가 안 걸림」**이었다.

```
검시 + 하는     ✗     심문 + 하는 ✗     실성 + 해 ✗     파산 + 했다/한 ✗
「정신이 흐려짐」 ✗   이용자는 「정신이 흐려진」이라고 쓴다
「새긴 글」       ✗   「돌에 글을 새겼다」에 그 꼴이 없다
「빚을 갚지 못함」 ✗   `bankrupt` 에 그 꼴 별칭이 아예 없었다(옛 상처)
```

지금까지 이 병은 **별칭**에서만 났는데, 이번엔 **`term_ko` 자체**가 원인이다 — 밀러의
`Inquest`·`Inquisition`·`Insane`·`Inscription` 처럼 **동작이나 상태를 가리키는 표제어**는
한국어 이름이 「~함」·「~짐」·「~시」 꼴이 되고, 이용자는 절대 그 꼴로 쓰지 않는다.

→ **이름이 사물이 아니면 그 자리에서 활용형 별칭을 함께 적는다** — 「~하는」·「~한」·「~했다」·
「~진」·「~해」. 사물 이름(오두막·고드름·아이스크림)에서는 한 번도 안 났다.

**2026-09-03(배치 67) — 열두 번째. 이 규칙은 「한 낱말」이 아니라 「구절」에도 그대로 든다.**
`prison` 에 「옥에 갇」을 별칭으로 올렸더니 뒤의 「혀」가 조사가 아니라 막혔다 —
「옥에 갇혀」·「옥에 갇혔다」로 고쳐야 걸린다. **구절 별칭도 공백 앞이나 「다」에서 끝나야 한다.**


**2026-09-02(배치 40) — 일곱 번째. 같은 세션에서 이 규칙을 두 번 적어 놓고 또 밟았다.**
`despair` 에 별칭 「음침」을 올렸더니 「음침한 곳에 둘러싸여 있었다」가 안 걸렸다 — 「한」이
조사가 아니다. **규칙을 적는 것과 지키는 것은 다른 일이다**(§10 #43과 같다). 별칭을 적는
그 자리에서 **뒤에 「한」·「했다」·「하게」를 붙여 소리 내 볼 것.**
**② 판별어에 공백을 넣으면 두 낱말로 쪼개진다.** 「텅 빈」·「못 받」·「서 있」을
적으면 「텅」·「빈」·「못」처럼 **1글자 토막**이 되어 엔진이 안 센다(`length >= 2`).
배치 26에서 `audit-km-dead-words`가 이것으로만 **15건**을 잡았다 — **한 낱말로 붙여 적을 것.**

**2026-09-02(배치 41) — 이 규칙은 `verify-dream-km` 이 이미 막고 있다.** 밀러 `Gong` 을
「징」으로 두려 하자 **「한 글자 상징(징)의 별칭이 한 글자」**로 빨간불이 났다. 글로만 적힌
규칙이 아니라 코드로 서 있는 자리다.

→ **우회법은 조사를 붙인 두 글자 꼴을 별칭으로 올리는 것이다.** 이름을 「징소리」로 하고
「징을」·「징이」·「징에」를 별칭에 넣으면 「징을 울렸다」·「징이 울렸다」가 둘 다 걸린다.
이름은 두 글자 이상으로 짓되 **이용자가 치는 한 글자 꼴은 조사째 별칭으로** 살린다.
**③ 한 글자 이름은 「형용사 어간 + 조사」에 걸린다.** `Day`를 「낮」으로 두려다 말았다 —
「낮은 목소리」의 「낮은」이 조사 「은」으로 읽혀 그대로 걸린다. `Debt`를 「빚」으로
두려다 말은 것도 같다(「송편을 빚은」). **한 글자 이름은 동형이의어 활용형을 먼저 찾아본다.**

---

## 30. 배치를 더하면 **이미 있던 상징의 기본값이 조용히 바뀐다** (2026-09-02)

배치 27b를 넣다가 사용자가 물 하나를 짚었는데, 재 보니 **다섯이었다.**

```
물     「물을 마심」(길)            →  「맑은 물을 마시려 해도 마시지 못함」(흉)
문     「문이 높고 큼」(길)          →  「문으로 들어감」(흉)
용     「용을 타고 하늘에 오름」(길) →  「용을 봄」(흉)
술     「남이 권하는 술을 마심」(길) →  「여성이 흥청거리며 술을 마심」(흉)
비둘기 「비둘기가 욺」(흉)           →  「비둘기가 짝을 짓고 둥지를 지음」(길)
```

`chooseMeaning`의 `pickByScore`는 판별어 점수가 전부 0이면 `from[0]` — **첫 의미**를
돌려준다. 곧 **첫 의미가 그 상징의 기본값**이고, 판별어에 안 걸린 꿈은 전부 거기로 떨어진다.
그런데 **누가 첫째가 되는지는 `readdirSync().sort()`가 정한다.** `m*.json`(밀러)이
`r*.json`(주공해몽)보다 먼저 정렬되므로, **밀러 배치를 하나 넣을 때마다** 이미 있던 상징의
기본값이 새 밀러 의미로 갈아치워진다.

**tsc·검사기 아홉·회귀 하니스 전부 초록불이었다.** 화면에 뜨는 답만 바뀐다.
그리고 이것은 이번에 처음 난 것이 아니다 — **밀러 배치 스물일곱 판 내내 그랬고 아무도 안 쟀다.**

**하지 말 것 → 대신 이렇게.**

- **데이터를 더할 때 「이미 있던 답이 바뀌었는가」를 잰다.** 「새로 더한 것이 옳은가」만
  보면 못 본다. 자는 **덮어쓰기 전의 산출물**이다 — 이번 물음(「이번 조립이 이미 나가 있는
  답을 바꿨는가」)의 자는 정의상 지금 나가 있는 사전이다(§24의 「두 자가 같은 도구에서
  나오면 안 된다」는 **원문을 옳게 읽었는가**를 물을 때의 이야기다. 무엇을 재는지가 먼저다).
- **순서가 정하는 것을 순서에 맡기지 않는다 — 목록으로 얼린다**(§11).
  `build-dream-dictionary-v2.mjs`의 `FALLBACK_FIRST`(상징 id → 기본값 문맥)가 그것이고,
  적어 둔 문맥이 사라지면 조립이 멈춘다. `EXPLICIT_IDS`가 id에 대해 하던 일과 같다.
- **관문은 「바뀐 것이 옳은가」를 판정하지 않는다.** 사람이 보고 목록에 적으라고 멈춰 세울
  뿐이다. 판정을 코드에 맡기면 그 관문이 죽는다(§28).

### 곁가지 — 「여성이~」 갈래는 **성별이 유일한 구분일 때만** 부딪친다 (2026-09-03 배치 55)

밀러는 표제어마다 「For a woman…」을 붙인다. 배치 50~52에서 **한 상징에 그 갈래가 둘이 되면
둘째는 넣을 수 없다**고 적고 문장 넷을 뺐다 — 「여자가·여성이·처녀가」를 형제끼리 나눠 가질
수 없고, 그 말이 없으면 어느 쪽도 안 걸리기 때문이다.

**그런데 배치 55의 `Husband` 는 열두 문장이 전부 아내가 남편을 보는 이야기인데도 안 부딪쳤다.**
가르는 것이 「여성이냐」가 아니라 **「남편이 무엇을 하느냐」**였기 때문이다(떠남·꾸짖음·죽음·
창백함·앓음·딴 여자를 마음에 둠…).

→ **규칙을 좁혀 적는다 — 부딪치는 것은 「성별이 유일한 구분일 때」이지 그 갈래가 여럿일
때가 아니다.** 뺄지 넣을지는 **그 문장에 성별 말고 다른 구분이 있는가**로 가른다.
(적어 둔 규칙이 실제보다 넓으면 넣어야 할 것을 뺀다 — 그것도 결함이다, §「막는 쪽으로 틀린
것도 결함이다」와 같은 자리)

### 곁가지 — 관문의 **안내문이 약속한 것을 코드가 안 하고 있었다** (2026-09-02 배치 54)

「기본값이 바뀐 상징」 관문은 멈추면서 이렇게 안내한다 — *「바꿀 뜻이었으면 `FALLBACK_FIRST`
에 적어 얼리고…」*. **그런데 코드는 얼렸는지를 보지 않았다.** 적어서 얼려도 그대로 멈춘다.

**스물여섯 판 동안 아무도 못 봤다.** 지금까지는 바뀐 기본값을 **늘 옛 값으로 되돌렸기**
때문이다 — 배치 54에서 처음으로 **바꾸는 것이 옳은 자리**가 나왔고(밀러가 `hunting`·`embrace`
에 **가장 일반적인 그림**을 처음 들여왔다. 막연한 문장은 기본값으로 떨어지는데 그 자리에
있던 것은 「숲속에서 사냥을 함」·「지아비를 시들하게 껴안음」이라는 **좁은 상황**이었다),
그제서야 관문이 길을 막는 것으로 드러났다.

→ **관문이 「이렇게 하면 된다」고 적어 둔 길이 실제로 열려 있는지 한 번 걸어 본다.**
안내문은 사람이 읽는 자리이고 코드는 그것을 모른다(§12의 「사람이 읽는 자리에만 있었고
코드는 몰랐다」와 같은 병이고, 이번엔 **반대 방향**이다 — 코드가 안내문보다 더 엄했다).

→ 고침은 **얼린 것과 같을 때만 넘어가게** 한 것이다. 대조군을 양쪽으로 세웠다 —
얼림을 지우면 다시 멈추고(exit 1), 얼리면 지나간다. **얼리지 않은 채 바뀐 것은 그대로 멈춘다.**

### 곁가지 — 기존 상징에 의미를 더하면 **그 상징이 배열 앞으로 뛴다** (2026-09-02 배치 50)

배치 50은 `horse` 에 의미 열다섯을 더했을 뿐인데 `dream-symbols.v2.data.json` 의 diff 가
**4,368줄**로 나왔다. §10 #49대로 그 자리에서 멈추고 규모를 확인했다.

원인은 **상징 배열의 차례가 「그 상징이 처음 나오는 추출 파일」로 정해지는 것**이다.
`horse` 의 첫 등장이 `m8.json`(밀러 `Foal`)에서 `m50.json` 으로 앞당겨지자
(`"m50.json" < "m8.json"`) **상징이 통째로 맨 앞으로 옮겨 갔고 뒤가 전부 밀렸다.**

**id 는 안 바뀌었고 상징 수도 같다 — 그것을 실제로 대조해서 확인했다**(앞 커밋의 데이터
파일을 `git show` 로 꺼내 id 목록을 맞췄다. 사라진 id 0 · 새 id 0).

→ **그래도 이 자리는 위험하다.** id 충돌 접미사(`-2`)가 **차례로** 정해지므로, 같은
슬러그를 내는 상징이 둘 있으면 **누가 맨 이름을 갖는지가 뒤바뀔 수 있다** — 주소가 조용히
바뀐다는 뜻이다. 그리고 화면에 여러 상징이 걸릴 때 **뜨는 차례**도 바뀐다.

→ **diff 가 항목 수에 안 맞으면 「데이터가 망가졌나」만 보지 말고 「차례가 바뀌었나」를 본다.**
확인은 `git show HEAD~1:<데이터 파일>` 로 꺼내 **id 목록을 맞춰 보는 것**이다(§10 #50 —
`git stash` 를 진단에 쓰지 않는다).

### 곁가지 — **기본값으로 얼린 의미가 형제를 가로챈다** (2026-09-02 배치 39)

`FALLBACK_FIRST` 는 그 의미를 **배열의 첫째**로 옮긴다. 그런데 동점이면 앞의 것이 이기므로,
**얼린 의미는 동점을 전부 가져간다.** 「마늘을 먹음」을 얼려 두고 그 판별어에 「먹었」을
남겨 두자 「마늘을 씹어 먹었다」가 1:1 동점에서 그쪽으로 갔다 — 새로 넣은 「마늘을 씹어 먹음」이
**어떤 문장으로도 안 뽑혔다.**

→ **기본값 자리에 둔 의미의 판별어는 더 좁게 적는다.** 그 자리는 **아무것도 안 걸렸을 때
떨어지는 자리**이지 낱말로 이겨서 뽑히는 자리가 아니다. 흔한 말을 쥐고 있으면 얼림이
그 상징의 다른 답을 통째로 덮는다(§25 곁가지 3과 같은 병이고, **얼림이 더한 새 갈래**다).

→ 잡은 것은 프로브뿐이다. `verify-dream-km` 은 겹침을 보지 **차례가 만든 동점**은 안 본다.

### 곁가지 — 붙이는 쪽만 채우면 안 된다. **기존 판별어도 함께 좁힌다** (2026-09-02 배치 39)

이미 있는 상징에 의미를 더하면 **기존 의미가 쥔 넓은 낱말**이 새 의미를 가로챈다.

```
attic 「다락방에 있음」            이 「올라갔」을 쥐어  → Garret 의 「다락방으로 올라감」이 안 뽑힘
goose 「거위와 오리가 함께 헤엄침」 이 「헤엄」을 쥐어   → Geese 의 「거위가 헤엄치는 것을 봄」이 안 뽑힘
ghost 「죽은 벗의 귀신을 봄」      이 「벗의」를 쥐어   → 「살아 있는 벗의 귀신을 봄」이 안 뽑힘
```

→ **새 의미를 붙일 때 기존 의미의 판별어를 함께 읽고, 새 문장에도 들어 있는 낱말을 뺀다.**
검사기는 못 본다 — 낱말이 **겹치지도 죽지도 않았기** 때문이다(§25 곁가지와 같은 자리).
### 곁가지 — 형제끼리 판별어를 나눠 가질 수 없다. 동점은 **차례**로 푼다

「넓은 쪽에 낱말을 하나 더 얹어 이기게 한다」로 판별어를 짰다가 `verify-dream-km`이
**42건**을 냈다 — 같은 상징의 두 문맥이 낱말을 공유하거나 한쪽이 다른 쪽의 부분 문자열이면
전부 「동점 위험」이다. 그러면 남는 수는 하나다 — **좁은 쪽을 파일에서 앞에 두는 것**
(§25 곁가지 3에 이미 적혀 있다). 이번에 셋을 그렇게 풀었다(흰 비둘기 떼 · 마차의 남자·여자
갈래). **앞뒤로도 못 푸는 자리는 상징을 가른다** — 「짐수레를 몲」은 `cart`에 붙이면 더 앞에
있는 「수레를 몲」에 영영 진다. `wagon`을 세워 옮겼다.

### 곁가지 — 한 상징에 의미가 **스물을 넘으면 영어 판별어가 글자로 부딪친다** (2026-09-02 배치 43)

`hair` 가 스물여섯이 되자 `verify-dream-km` 이 그 상징에서만 겹침 **열둘**을 냈다.
뜻이 아니라 **글자**가 물린 것이다.

```
red   ⊂ cove**red**      「정인의 머리카락이 붉음」 ↔ 「머리털을 풀어 얼굴을 덮음」
less  ⊂ fear**less**     「머리카락에 꽃이…」     ↔ 「금빛 머리카락의 여성을 봄」
gray  ⊂ **gray**ing      「처녀가 흰머리…」       ↔ 「머리가 하얗게 셈」
white ⊂ **white**ness    「흰 머리카락과 검은…」   ↔ 「눈처럼 새하얀…」
```

→ **의미가 스물을 넘는 상징에는 짧고 흔한 영어 낱말(red·less·one·man·gray)을 안 쓴다.**
그리고 겹침을 고칠 때는 **새로 넣는 쪽만 갈고 기존은 그대로 둔다** — 기존을 건드리면
이미 돌던 극성 하니스가 깨진다(배치 43에서 실제로 둘이 깨졌다).
### 곁가지 — 판별어를 **빈 문자열로 두는 것**과 **안 적는 것**은 다르다

`contextFor`는 `CONTEXT_KO[key] ?? display` — 표에 없으면 **화면 문구를 그대로** 판별어로
쓴다. 물의 새 문맥을 안 적어 두자 그 문구 속 「물을」이 「물을 보았다」에 걸려 기본값이 샜다.
그렇다고 빈 문자열도 안 된다(`verify-dream-km`이 막는다). **대표 의미에는 좁은 낱말을 적는다** —
넓은 말을 주면 구체적인 형제를 동점으로 가로채고, 좁은 말이면 어차피 0점으로 제자리에 떨어진다.

### 곁가지 — 곁에서 찾은 **오래된** 문제를 오늘의 결과인 것처럼 붙여서 냈다

위 다섯을 재려고 전수로 돌린 김에 더 큰 것이 나왔다 — **기본값이 답을 정하는 상징이 669개이고,
화면은 그것을 「골라낸 답」과 똑같이 보여준다**(엔진은 `contextMatched: false`로 알고 있는데
화면이 그 값을 안 쓴다). 그중 89개는 기본값의 길흉이 그 상징의 다수와 반대다.

**이건 오늘 생긴 것이 아니다.** 사전을 v2로 갈기 전부터 그랬고, 밀러 배치 스물여덟 판 동안
내가 못 본 것이다. 그런데 보고할 때 다섯과 한 덩어리로 묶어 **「길 셋 · 추천은 C(반나절)」**
로 냈다. 받은 지시는 「27b 넣고 같이 푸시」였다. 사용자가 「지금까지 가만히 있다가 지금
문제로 제기하는 이유가 뭐야?」로 짚었다.

**하지 말 것 → 대신 이렇게.** 고치다가 곁에서 오래된 문제를 찾으면 **오늘 만든 결함과 갈라
적는다** — 「이번 변경이 만든 것」과 「원래 그랬는데 이번에 재다가 보인 것」은 다른 문장이다.
뒤엣것은 **사실만 적고 끝낸다.** 선택지와 추천을 붙이는 순간 「지금 정하실 일」이 되고,
그것은 §14다(개별 판단이 다 옳아도 총합이 틀릴 수 있다). 할 일을 늘릴지는 사용자가 정한다.

### 곁가지 — `verify-dream-cite`를 인자 없이 돌리면 **밀러를 한 줄도 안 본다**

기본 파일 목록이 `/^(r\d+|result-tomb)\.json$/`다. 인자 없이 돌리고 「전수 통과」라고 읽으면
**주공해몽 1,178건만 본 것**이다(전수는 2,937건). §24와 같은 병 — 검사기는 **훑기로 한 것**만
훑는다. 밀러 파일을 인자로 넘길 것.

---

## 31. 옛 원문의 비하 — 「낱말」이면 인용문에 남기고, 「꿈의 그림」이면 통째로 뺀다 (2026-09-02)

1901년 밀러 해몽서를 옮기는 일이라 옛 시대의 비하 표현이 섞여 나온다. 지금까지 두 갈래를
실제로 만났고, **처리가 다르다.**

```
낱말이 비하다        crippled · maimed · dwarf · dumb
                     → 해석 문장과 화면 이름에서 빼고 **인용문(cite.original)에는 그대로 둔다.**
                       인용은 원문의 증거이고, 화면 이름은 우리가 고르는 말이다
                       (`말을 못 함`·`키 작은 사람`으로 짓고 「벙어리」·「난쟁이」는 별칭에만)

꿈의 그림이 비하다   밀러 `Fight` 의 「negroes attack you with razors」·「fighting negroes」
                     → **항목을 아예 만들지 않는다.** 문맥 이름으로 적는 순간 그 그림이
                       23개 언어 화면에 뜬다 — 인용 안에 있든 밖에 있든 우리가 보여 주는 것이다
```

**2026-09-02(배치 42) — 두 번째 실물: 밀러 `Gypsy` 를 통째로 뺐다.** 표제어 다섯 문장이
전부 **한 겨레를 두고 속임·손해·경솔한 혼인을 점친다**(「trading with a gypsy, you will lose
money」). 비하를 빼면 남는 것은 「점쟁이가 점을 봐 준다」뿐이고 그것은 이미 `divination` 이
갖고 있다 — **남는 꿈이 없다.**

→ **표제어 하나가 통째로 이 갈래일 수 있다.** 문장 하나만 빼는 것이 아니라 표제어를 건너뛰는
판단이 필요하고, 그러면 **커버리지 대조에서 표제어가 비어 보인다** — 그것이 옳은 자리이니
**패치 머리말과 워크로그에 반드시 적는다**(§24). 안 적으면 다음 사람이 「빠뜨렸다」로 읽고 넣는다.
→ **가르는 물음은 「이 문장에서 비하를 빼면 남는 꿈이 있는가」다.** `crippled` 를 빼도
「다친 소방수를 봄」이 남는다. 「negroes …」에서 빼면 **남는 것이 없다** — 그 인종이 곧
그 꿈의 내용이기 때문이다.

→ 뺀 것은 **커밋 메시지·워크로그·인계에 적는다.** 안 적으면 다음 사람이 「빠뜨렸다」로 읽고
다시 넣는다. 커버리지 대조에서 표제어가 비어도 그것이 옳은 자리다(§24).
### 곁가지 — 문맥 이름이 같으면 **뒤엣것이 영영 안 뽑힌다** (2026-09-02 배치 35)

조립기는 `context` 와 `work` 가 **둘 다** 같을 때만 인용을 포갠다. **`work` 가 다르면 의미를
둘로 남긴다** — 두 원문이 같은 말을 하면 출처를 갈라 둘 다 보여 주려는 설계다.

**그런데 판별어 표는 문맥 이름을 키로 쓴다.** 이름이 같으면 판별어가 하나뿐이고, 두 의미가
**언제나 같은 점수**를 받는다. 동점은 앞의 것이 이기므로 **뒤엣것은 어떤 문장으로도 안 뽑힌다.**

```
work 가 같다   → 같은 이름으로 적어 **인용을 포갠다**(배치 32 Fat → corpulence)
work 가 다르다 → **이름을 갈라 적는다**(주공해몽 「불꽃놀이를 봄」 / 밀러 「불꽃놀이를 구경함」)
```

**그래서 표제어를 통째로 빼는 이유가 둘이다**(2026-09-02 배치 45).

```
§31        그림 자체가 비하다            Gypsy — 다섯 문장이 전부 한 겨레를 두고 속임을 점친다
같은 그림  이미 다른 표제어로 들어와 있다  Hate  — 세 문장이 전부 abhor(혐오)에 있는 그림이다
```

뒤엣것은 넣으면 **인용만 포개져 풀이가 뒤섞이거나**(같은 이름) **판별어를 가를 수 없다**
(다른 이름). 문장 단위로도 같은 판정을 한다 — `Glass` 의 거울 일곱(40) · `Hair` 의 넷(43) ·
`Hatchet`·`Hawk`·`Hat` 각 하나(45). **셋째 것은 프로브가 잡아 줬다.**

→ **어느 쪽이든 커버리지가 비는 것이 옳고, 패치 머리말·워크로그·인계 셋에 적는다.**
안 적으면 다음 사람이 「빠뜨렸다」로 읽고 다시 넣는다.
→ 이미 있는 상징에 다른 원문을 붙일 때는 **기존 문맥 이름을 먼저 읽고**, 같아지려 하면
한쪽을 다르게 적는다. `audit-unreachable-meanings` 가 뒤늦게 잡아 주기는 하나 빨간불은 아니다.

### 곁가지 — 의미가 하나뿐이던 상징은 **판별어 표가 비어 있다** (2026-09-02 배치 35)

의미가 하나면 판별어가 없어도 된다(어차피 그것으로 떨어진다). **여럿이 되는 순간 모두
있어야 한다** — 붙이는 쪽만 채우면 **기존 의미가 0점으로 남아 안 뽑힌다.**
`verify-dream-km` 이 「판별어 없음」으로 잡는다(`fly`·`flea` 가 그랬다).

### 곁가지 — 그 절을 쓰다가 **§7을 또 밟았다** (2026-09-02)

바로 위 두 절을 `node -e` 로 써 넣었다가 **셸이 백틱을 명령 치환으로 먹었다.** 코드 표기가
전부 사라지고 코드 블록 한 덩이가 통째로 날아갔는데 **스크립트는 「OK」를 찍었다** — 확인이
「글자가 들어갔는가」만 봤기 때문이다(§10 #44의 새 얼굴).

→ **글이 든 파일 편집은 `node -e`·히어독으로 하지 않는다**(§7 · §10 #34, 이번이 세 번째다).
스크립트를 **파일로 써서** 돌린다. 그리고 **확인은 「깨질 수 있는 글자」로** 한다 — 백틱이
몇 개 남았는지, 코드 블록이 있는지를 센다.
### 곁가지 — 그 grep 을 내가 잘랐다 (2026-09-02 배치 36)

⓪단계 전수 grep 을 짜면서 결과를 `.slice(0, 6)` 으로 잘랐다. 「꽃」에 별칭이 여섯 줄 나와
**`flower`(꽃) 상징이 이미 있다는 줄이 잘려 나갔고**, 새 상징인 줄 알고 새 파일에 적었다.
조립기의 「기본값이 바뀐 상징」 관문이 찍어서야 알았다(같은 id 가 두 파일에 있으면 안 된다).

**§29를 내 도구에서 그대로 밟은 것이다.** 그때는 사용자에게 하는 대답이었고 이번은 내가
쓰는 검사다 — **자른 사실을 내가 몰랐다는 점이 같다.**

→ **⓪ grep 에 `.slice()`·`head` 를 넣지 않는다.** 그리고 **EXACT(이름이 그대로 같은 것)를
맨 앞에 찍는다** — 같은 판에서 「외나무다리」가 `bridge` 의 별칭인 것은 EXACT 로 나와 바로 봤다.
## 시작 전 체크리스트

```
□ 이 작업의 관문이 무엇인지 정했다 (그리고 커밋 앞에 한 번만 돈다)
□ 고치기 전에 그 검사가 빨간불을 내는지 봤다 (대조군)
□ 같은 모양이 형제 셋에 있는지 셌다
□ 「없다」고 적으려는 것을 실제로 찾아봤다
□ 보고에서 「돌려 본 것」과 「안 돌려 본 것」을 갈라 적었다
□ 이 관문이 지키는 일이 **한 번이라도 돌았는가** (§14)
□ 같은 파일을 세 판 넘게 고치고 있지 않은가 — 그렇다면 멈추고 보고 (§14)
□ 의뢰서에 「막아야 합니까?」로 열어 둔 물음이 없는가 — 닫고 낸다 (§10 #21·22)
□ 이 스윕이 **돈을 쓰거나 운영 DB를 건드리지 않는가** (§12)
□ 기존 구현을 **근거로 삼고 있지 않은가** — 그것이 옳은지 먼저 물었다 (§15)
□ 새로 만든 파일이 텍스트인가 — `file <경로>` · 커밋 통계에 `Bin` 이 없는가 (§10 #30)
□ 정규식·템플릿이 든 파일을 **셸을 거쳐** 쓰고 있지 않은가 (§10 #34)
□ 시간이 드는 것을 만들기 전에 **무엇을 만들지 한 줄로 말했는가** (§10 #38)
□ 조회가 0건일 때 **필터를 뺀 조회로 대조군**을 만들었는가 (§10 #33)
□ JSON 응답을 **grep이 아니라 jq로** 판정했는가 — 필드 이름 겹침을 grep은 못 가른다 (§10 #48)
□ 사람에게 보여 줄 값이 **제품이 실제로 그리는 값**인가 — 모듈 함수 결과로 갈음하지 않았는가 (§10 #39)
□ 보고에 실을 수를 **손으로 세지 않았는가** — 세는 것을 짜서 냈는가 (§10 #40·41)
□ **실수를 그 자리에서 이 파일에 적었는가** — 나중으로 미루지 않았다 (§0 · §10 #31)
□ dev 서버에서 히스토리 닫기형 모달이 「안 열린다」면 **production 빌드로도 재현되는지** 봤다 (§16)
□ 새 공개 API의 레이트리밋 scope에 **서비스 접두사**를 넣었다 (§17)
□ URL·도메인·경로를 **추측하지 않고** `.env.local`·메모리에서 읽었는가 (§18)
□ 터미널이 갑자기 닫히면 원인을 단정하지 않고 재현 여부·로그부터 확인했는가 (§19)
□ 새 Bash 권한 규칙에 정규식·글롭 조각을 문자열째 넣지 않았는가 — `*`는 매칭기 와일드카드다 (§20)
□ "스크립트가 만든 파일"이라는 머리말을 믿기 전에 실제 git diff 이력으로 확인했는가 — 생성기
  결과의 diff 크기가 내가 넣은 항목 수와 비례하지 않으면 멈춘다 (§10 #49)
□ 원본을 보려고 `git stash`를 진단 도구로 쓰지 않았는가 — `git show HEAD:<path>`로 볼 것 (§10 #50)
□ 신뢰/출처 표시 필드에 "값이 없으면 X로 채운다"는 기계적 기본값을 쓰지 않았는가 — 분포가
  한쪽으로 99% 쏠리면 의심할 것 (§21)
□ 새 검사기를 "통과했다"고 적기 전에 "이것이 무엇을 못 잡는가"를 목록으로 적었는가 —
  진짜처럼 보이지만 결론이 틀린 대조군까지 시험했는가 (§22)
□ "실제로 뚫린다/안 뚫린다"를 **재구현·시뮬레이션만으로** 확정하지 않았는가 —
  실제 함수를 직접 불러(`tsx` 등) 재확인했는가 (§23)
□ 원문에서 뽑는 작업에 **진위 검사와 커버리지 대조가 둘 다** 있는가 — 검사기는 결과에
  「있는 것」만 훑으니 빠뜨린 표제어는 위반 0건으로 통과한다 (§24)
□ 자체 대조군이 **내 데이터가 지나는 갈래**를 지나는가 — 다른 갈래만 시험하는 대조군은
  내 경로에 대해 아무것도 증명하지 않는다 (§24)
□ 데이터를 갈아 끼우기 전에 **그것을 읽는 기능이 무엇을 보고 판정하는지** 함수 본문을
  열어 봤는가 — 필드가 아니라 **문구**를 보고 있을 수 있다 (§25)
□ 교체 직후 **회귀 하니스를 돌렸는가** — tsc 초록불은 「값이 있다」만 증명한다 (§25)
□ 데이터를 갈았으면 **그것을 설명하는 안내 문서**도 재검했는가 — 예문은 제품 엔진에 실제로
  넣어 보고, 「정반대입니다」라고 적은 짝은 극성이 실제로 반대인지 쟀는가 (§26)
□ 「배포됐다」·「운영에 올라갔다」를 적기 전에 `git status -sb`로 **푸시됐는지** 봤는가 (§26)
□ 구분자로 **눈에 안 보이는 바이트**를 쓰지 않았는가 — `JSON.stringify`로 감쌀 것 (§26)
□ 생성 파이프라인을 **끝까지** 돌렸는가 — 한 단계만 돌리면 뒷단계가 채우는 값이 날아간다 (§26)
□ 명령 뒤에 `echo`·`tail` 같은 것을 붙이지 않았는가 — **종료 코드는 마지막 명령의 것**이고,
  판정은 언제나 출력 본문으로 한다 (§1 · §26)
□ 「배포됐다」를 **새 데이터에만 있는 주소 하나로** 확인했는가 — 배포 success 는 「빌드가
  끝났다」이지 「내 변경이 살아 있다」가 아니다 (§27)
□ 원인을 **로컬에서 고쳐진 것만으로** 단정하지 않았는가 — 크기·장수·파일 수·시간을 다 재
  놓고 골랐는가 (§27)
□ 라우트 설정을 잎에서 바꿨다면 **부모가 그것을 지배하지 않는지** 봤는가 (§27)
□ 새 상징·별칭을 넣었으면 **자연스러운 문장을 `matchDream`에 직접 넣어** 봤는가 —
  검사기 초록불은 「규칙에 맞다」이지 「걸린다」가 아니다 (§25 · §23)
□ 파일을 새로 더했으면 그것을 읽는 스크립트의 **목록이 손으로 적혀 있지 않은지** 봤는가 (§5)
□ 새 이름을 넣었으면 **기존 별칭 전수 대조**를 돌렸는가 — ⓪는 새 이름만 본다 (§28)
□ 새 관문이 수백 건을 내면 **빨간불을 「판단이 필요 없는 부분집합」으로 좁혔는가** — 늘
  빨간불인 관문은 아무도 안 본다 (§28)
□ 커버리지를 재는 **두 자가 같은 도구에서 나오지 않는지** 봤는가 — 같은 도구가 양쪽을
  만들면 그 도구의 결함은 대조에 안 잡힌다 (§24)
□ 대표(첫) 의미의 판별어를 **비워 뒀는가** — 흔한 말을 넣으면 구체적인 형제가 영영 안
  뽑힌다. 검사기는 이것을 못 본다 (§25)
□ 새 상징의 **이름·id·별칭을 기존 전부에서 먼저 찾아봤는가** — 남이 쓰던 별칭을 뺏거나,
  하니스가 기다리던 id 를 가로챌 수 있다. 검사기는 한 상징 안만 본다 (§25 곁가지)
□ 「마지막」·「가장 큰」·「하나도 없다」를 답하면서 **그 집합을 `head` 나 손으로 적은 목록으로
  좁히지 않았는가** — 좁혔으면 좁힌 사실을 답에 적는다 (§29)
□ 별칭을 더했으면 **그 낱말을 쓰던 판별어를 함께 갈았는가** — 제 이름이 된 낱말은 점수에서
  빠진다. `audit-km-dead-words` 만 이것을 본다 (§29 곁가지)
□ 별칭을 **「다」까지 붙인 완전한 꼴로** 올렸는가 — 「잃었」까지만 올리면 「잃었다」가
  안 걸린다. 「데」·「하」도 조사가 아니다 (§29 곁가지 ①)
□ 판별어에 **공백을 넣지 않았는가** — 쪼개져 1글자가 되면 엔진이 안 센다 (§29 곁가지 ②)
□ 한 글자 이름을 쓰기 전에 **같은 글자로 시작하는 활용형**을 찾아봤는가 — 「낮은 목소리」·
  「송편을 빚은」이 조사로 읽혀 걸린다 (§29 곁가지 ③)
□ 데이터를 더했으면 **이미 있던 상징의 기본값(첫 의미)이 바뀌지 않았는지** 덮어쓰기 전
  산출물과 대조했는가 — 첫 의미는 파일 이름 정렬 순서가 정하고, 검사기는 하나도 못 본다 (§30)
□ 판별어를 형제끼리 **나눠 갖지 않았는가** — 동점은 낱말을 더 얹어서가 아니라 **차례**로
  푼다. 차례로도 못 풀면 상징을 가른다 (§30 곁가지)
□ 이미 있는 상징에 붙일 때 **기존 문맥 이름과 같아지지 않는가** —  가 다르면
  이름을 갈라야 뒤엣것이 뽑힌다. 그리고 **의미가 하나뿐이던 상징은 판별어 표가 비어 있다** (§30 곁가지)
□ 옛 원문의 비하가 **「낱말」인가 「꿈의 그림」인가** — 낱말이면 인용문에 남기고, 그림이면
  항목을 만들지 않는다. 뺐으면 적어 둔다 (§31)
□ `verify-dream-cite`를 **밀러 파일까지 인자로 넘겨** 돌렸는가 — 인자 없이 돌리면
  주공해몽만 본다 (§30 곁가지)
□ 새 이름이 기존 이름을 **부분 문자열로 품지 않는가** — 품으면 상징을 세우지 말고 임자에게
  붙인다. `findTerm`은 상징마다 따로 돌아 **둘 다 뜬다** (§25 곁가지)
□ 새 상징의 id 가 **하니스가 「아직 없는 상징」으로 건너뛰던 자리**를 채우지 않는가 — 건너뛰던
  기대치는 한 번도 안 재 본 주장이다. 깨어나면 지우지 말고 실제 임자로 고친다 (§25 곁가지)
□ `FALLBACK_FIRST` 로 얼린 의미의 판별어를 **더 좁게** 적었는가 — 얼림은 그것을 배열 첫째로
  옮기므로 **동점을 전부 가져간다**. 흔한 말을 쥐면 형제가 영영 안 뽑힌다 (§30 곁가지)
□ 새 의미를 붙이면서 **기존 의미의 판별어도 함께 좁혔는가** — 기존 것이 쥔 넓은 낱말이
  새 의미를 가로챈다. 검사기는 못 본다(겹치지도 죽지도 않았다) (§30 곁가지)
□ 오래 걸리는 것(스윕·배포 대기)을 **앞에서 돌렸는가** — 백그라운드는 아무 일도 안 하고
  「완료」로 돌아온 적이 세 번 있다. **로그 본문으로 판정한다**(스윕은 정상 140줄) (§1)
□ 프로브 문장에 ① 상징 이름이 있고 ② **띄어쓰기가 이용자가 쓰는 대로**인가 (§25 곁가지)
□ 별칭을 **넓히는** 고침도 좁게 했는가 — 낱말 하나로 넓히면 엉뚱한 꿈에 뜬다 (§29 곁가지 ①)
□ 의미가 스물을 넘는 상징에 **짧고 흔한 영어 판별어**를 넣지 않았는가 — 뜻이 아니라 글자가
  물린다. 겹침은 **새로 넣는 쪽만** 간다 (§30 곁가지)
□ 표제어를 통째로 빼는 두 이유(**그림이 비하** · **같은 그림이 이미 있다**)를 가려서 적었는가 (§31)
□ 이미 있는 상징에 붙일 때 **그 상징의 별칭을 한 줄씩 읽었는가** — 뒤에 「았다」를 붙여 보고
  조사도 공백도 아니면 그 별칭은 심어진 뒤로 한 번도 안 걸린 것이다. **어떤 관문도 못 본다** (§29 곁가지 ①)
□ 스크립트 **안에** `/c/...` 경로를 문자열로 적지 않았는가 — Node가 `C:\c\...`로 푼다 (§7)
□ 새 상징의 이름이 **사물이 아니라 동작·상태**(「~함」·「~짐」·「~시」)면 이용자가 쓰는 활용형을
  **별칭으로 함께** 올렸는가 — 이름만으로는 한 문장도 안 걸린다 (§29 곁가지 ①, 배치 63)
□ **구절 별칭**도 공백 앞이나 「다」에서 끝나는가 — 「옥에 갇」은 뒤의 「혀」에 막힌다 (배치 67)
□ 새 항목의 `category` 가 **이미 쓰이는 여덟 갈래 안**인가 — 아홉째를 처음 쓰면 안내 문서
  23로케일의 「갈래 여덟」이 거짓이 된다 (§26 곁가지, 배치 68)
□ 기존 상징에 붙일 때 **그 상징의 화면 문구를 소리 내어 읽고 그 말이 별칭에 있는지** 봤는가 —
  「갓 태어난」(62) · 「쇠로」(65) · 「옥에 갇혀」(67)가 심어진 뒤로 한 번도 안 걸리고 있었다
□ 표제어가 X라도 **그 꿈에 실제로 있는 사물**이 X인지 봤는가 — 밀러 `Jailer` 의 「무리가 옥문을
  부수려 함」은 간수가 아니라 감옥의 자리다 (배치 67)
```

자세한 그날의 기록은 `docs/WORKLOG_2026-08-20.md` §5(「내가 버린 시간 — 원인」)에 있다.
