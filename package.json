{
  "name": "naminglink-monorepo",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "pnpm --filter naminglink dev",
    "dev:inyeon": "pnpm --filter inyeonlink dev",
    "build": "pnpm --filter naminglink build",
    "build:inyeon": "pnpm --filter inyeonlink build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/fontkit": "^2.0.9",
    "fontkit": "^2.0.4"
  }
}
